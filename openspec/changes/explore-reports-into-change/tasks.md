# Срез S1: Фактура в каталоге ЗНИ

**Сценарий:** После исследования с отчётом создали ЗНИ — файлы темы лежат в каталоге задачи.
**Primary acceptance:** Given отчёт в превью чата → When `/opsx:new` по этой теме → Then тот же файл в `reports/` ЗНИ, в `temp` его нет.
**Приёмка:** открыть каталог созданной ЗНИ (kit, без ИБ).
**Связь со spec:** Requirement «Reports of this topic move into the change catalog» — Scenario «Reports of this topic move into the change catalog», Scenario «Confirm message has no file list», Scenario «Handoff file moves only if it exists»; Requirement «New without research reports succeeds» — Scenario «New without research reports succeeds»; Requirement «Parallel topics do not mix» — Scenario «Parallel topics do not mix»; Requirement «Extend from temp moves the file» — Scenario «Extend from temp moves the file»; Requirement «Continuity finds reports after move» — Scenario «Continuity finds reports after move».
**Зависимости:** нет
**Режим apply:** mechanical

## 1. Правило переезда

- [x] S1.1 Добавить в `.cursor/rules/preserve-subagent-reports.mdc` секцию переезда в каталог ЗНИ, чтобы фактура темы попала в git: сразу после появления `openspec/changes/<name>/` перенести кандидатов (сначала пути из чата, `report:`, «Источники» передачи, `@`-ссылок — если файл ещё есть; недостающее — glob 48 ч ∩ тема: slug в имени или заголовок / шапка пересекается со значимым токеном имени ЗНИ, служебные `exploration`/`trace`/`analysis`/дата не считаются); корни glob: `temp/reports/` для отчётов исследования и `temp/` (не `temp/reports/`) для `explore-handoff-*.md`; allowlist имён (не префикс `architecture-*`): `exploration-*`, `trace-analysis-*`, `explain-*`, `explore-handoff-*`, `architecture-YYYY-MM-DD.md` / `architecture-YYYY-MM-DD-<тема>.md`; не брать `architecture-new-*`, `architecture-new-selfreview-*`, `architecture-task-readiness-*`, `architecture-extend-coherence-*`, `architecture-precedent-coherence-*`, `architecture-review-*`, `architecture-loop-redesign-*`, `design-challenge-*`, `quality-control-*`, `verification-*`, `resolved-contract-*`, `handoff-acceptance-*`, `handoff-pause-*`, `code-map*`, `slice-acceptance-*`, `session-notes`, чужой slug без цитаты; только переезд, не копия; назначение уже есть и то же содержимое — удалить источник; разное — не затирать, источник → `<stem>-from-temp.md`; нет кандидатов — тихий пропуск; после успеха исходника в `temp` нет; в постановке и design `report: temp/reports/X` → `reports/X`; ingest new/extend после переезда ищет файл передачи в `reports/` ЗНИ, не только `temp/explore-handoff-*`; внутри перенесённых файлов ссылки на перенесённый `temp/reports/…` → `reports/<basename>`; для журнала пошагового разбора href `../../src/` → `../../../../src/` по канону шаблона explain; два разных slug в окне без цитаты — спорные не переносить, вопрос на этом шаге не задавать; скиллы new/extend ссылаются сюда, процедуру целиком не копируют. Секцию шапки `## Вводные` в этом шаге не писать (Decision 1, 10, 11; Scenario «Reports of this topic move into the change catalog», Scenario «Parallel topics do not mix», Scenario «Handoff file moves only if it exists»)

## 2. Создание и дополнение ЗНИ

- [x] S1.2 Добавить в `.cursor/skills/openspec-new-change/SKILL.md` шаг «Перенос отчётов исследования» сразу после появления каталога `openspec/changes/<name>/`, до записи proposal/design и до проверки постановки на design, чтобы Design Gate видел `reports/architecture-*.md` в ЗНИ: выполнить переезд по правилу сохранения; переписать `report: temp/reports/X` на `reports/X`; нет кандидатов — не останавливать и не требовать «приложите отчёт»; resume — тот же шаг идемпотентно; в финальном сообщении не печатать перечень перенесённых путей, одна фраза эффекта по стилю чата. Алгоритм отбора в скилл целиком не копировать (Decision 1; Scenario «Reports of this topic move into the change catalog», Scenario «New without research reports succeeds»). Зависимости: S1.1
- [x] S1.3 Уточнить в `.cursor/skills/openspec-new-change/templates/handoff-contract.md` примечание к полю `Architect / verify`: после переезда путь `report:` переписывается на `reports/<файл>` внутри ЗНИ, чтобы артефакты не ссылались на `temp`; новую обязательную строку таблицы полей (список отчётов) не добавлять (Decision 4)
- [x] S1.4 Добавить в `.cursor/skills/openspec-extend-change/SKILL.md` зеркало переезда для `--from-report` из `temp/reports/…`, чтобы дополнение существующей ЗНИ не оставляло фактуру в gitignore: если файл ещё в `temp` — сначала перенести в `openspec/changes/<name>/reports/` по правилу сохранения, дальше читать и ссылаться на путь в каталоге ЗНИ, оригинал в `temp` не оставлять (Decision 1; Scenario «Extend from temp moves the file»). Зависимости: S1.1

## 3. Поиск продолжения и сообщение о переносе

- [x] S1.5 Расширить в `.cursor/skills/openspec-explore/SKILL.md` шаг Continuity (glob 7 дней), чтобы «продолжи вчерашний разбор» находил файлы после переезда и не предлагал служебные отчёты проверки: смотреть и `openspec/changes/*/reports/` по тому же allowlist, что правило сохранения (`exploration-*`, `trace-analysis-*`, `explain-*`, `explore-handoff-*`, `architecture-YYYY-MM-DD.md` / `architecture-YYYY-MM-DD-<тема>.md`; не `architecture-new-*`, `architecture-task-readiness-*`, `design-challenge-*`, `quality-control-*`, `verification-*` и прочие из deny-списка design), не требуя живой копии в `temp`; фильтр по теме и подтверждение текстом — как сейчас. Слоты шапки в промпте `Task` в этом шаге не трогать (Decision 1, 11; Scenario «Continuity finds reports after move»)
- [x] S1.6 Заменить в `.cursor/docs/opsx-output-style.md` указание про путь отчётов (§5.1a таблица «Файлы» и T-CONFIRM создания ЗНИ), чтобы после `/opsx:new` разработчик не думал, что фактура ещё в `temp`: до создания ЗНИ превью explore по-прежнему указывает на `temp/reports/`; после переезда отчёты темы живут в `openspec/changes/<name>/reports/`; в коротком подтверждении new — одна фраза «материалы разбора перенесены в задачу» без перечня путей (Decision 4; Scenario «Confirm message has no file list»)

## 4. Журнал разбора и тест-кейс

- [x] S1.7 Уточнить в `.cursor/skills/openspec-explain/templates/explain-report.md` правило ссылок при переезде журнала из `temp` в каталог ЗНИ, чтобы клик по фрагменту кода не ломался: при promote поправить относительный href `../../src/` → `../../../../src/` (канон таблицы в этом шаблоне); label citation `start:end:src/...` не менять; секцию `## Вводные` в этом шаге не добавлять (Decision 1)
- [x] S1.8 Добавить в `.cursor/skills/openspec-explore/test-cases/question-to-new-handoff.md` контрольные точки переноса, чтобы регресс explore→new ловил каталог, а не только чат-блок: после `/opsx:new` exploration-файл из превью лежит в `reports/` ЗНИ, в `temp/reports/` этого файла нет; финальное сообщение new без перечня путей; шапку вводных в этом шаге не описывать (Scenario «Reports of this topic move into the change catalog», Scenario «Confirm message has no file list»)

## 5. Сверка по тексту

- [x] S1.9 Верифицировать по тексту `.cursor/rules/preserve-subagent-reports.mdc`, скиллов new/extend/explore и `.cursor/docs/opsx-output-style.md`, что переезд совпадает с дельтой без живого прогона команд: нет кандидатов — тихий пропуск, нет «приложите отчёт» (Scenario «New without research reports succeeds»); два slug в окне — в ЗНИ только тема, чужой файл остаётся в `temp` (Scenario «Parallel topics do not mix»); `explore-handoff-*` ищется в корне `temp/` (не только `temp/reports/`) и переезжает только если файл есть, иначе ЗНИ без него, отчёты обследования всё равно в наборе (Scenario «Handoff file moves only if it exists»); extend из temp ссылается на путь в каталоге ЗНИ (Scenario «Extend from temp moves the file»); Continuity glob включает `openspec/changes/*/reports/` за 7 дней по allowlist и не берёт служебные отчёты проверки постановки (Scenario «Continuity finds reports after move»); в перенесённом `explain-*` href на `src/` — `../../../../src/`, не `../../src/` (Scenario «Reports of this topic move into the change catalog»); T-CONFIRM / финал new без списка путей (Scenario «Confirm message has no file list»). Зависимости: S1.1–S1.8

## 6. Приёмка

- [x] S1.accept Принять срез S1 «Фактура в каталоге ЗНИ» — файл темы из превью лежит в каталоге задачи, в temp его нет:
  - **Primary (обязательно):** Given отчёт в превью чата → When `/opsx:new` по этой теме → Then тот же файл в `reports/` ЗНИ, в `temp` его нет
  - Scenario «Confirm message has no file list» (опционально): в том же ходе создания ЗНИ — фраза эффекта «материалы разбора в задаче» и нет перечня перенесённых путей
  - Scenario «New without research reports succeeds» (опционально): создание ЗНИ только по блоку постановки, без отчётов обследования — ЗНИ создана, нет «приложите отчёт» и нет остановки из‑за пустого `temp`
  - Scenario «Parallel topics do not mix» (опционально): в `temp` два отчёта разных тем, ЗНИ по второй — в каталоге новой ЗНИ только файлы второй темы, файлы первой остаются в `temp`
  - Scenario «Extend from temp moves the file» (опционально): дополнение существующей ЗНИ из `temp/reports/exploration-….md` — файл в `reports/` этой ЗНИ, новые ссылки не указывают на `temp/reports/` для этого файла
  - Scenario «Continuity finds reports after move» (опционально): поиск «продолжи вчерашний разбор» находит следы в `openspec/changes/<name>/reports/` за 7 дней, без требования живой копии в `temp`; среди предложений нет служебных отчётов проверки постановки соседних задач
  - Scenario «Handoff file moves only if it exists» (опционально): если словами просили сохранить постановку — файл `temp/explore-handoff-*.md` (корень `temp/`) тоже в `reports/` ЗНИ; если не просили — ЗНИ без этого файла, отчёты обследования всё равно переезжают

<!-- slice-gate: файл из превью лежит в reports/ ЗНИ, в temp его нет -->

# Срез S2: Вводные в отчёте

**Сценарий:** Открыл отчёт — видно объект и понятный исходный запрос.
**Primary acceptance:** Given исследование с названным объектом → When открыть отчёт обследования → Then после заголовка шапка: объект (или «не назван») и понятная формулировка исходного запроса без дословной цитаты чата; секция «Для заказчика» в конце.
**Приёмка:** открыть файл отчёта (kit, без ИБ; шапка ценна уже в `temp`).
**Связь со spec:** Requirement «Intake header names the object» — Scenario «Intake header names the object»; Requirement «Original request is a clear restatement» — Scenario «Original request is a clear restatement»; Requirement «Missing header is filled on save» — Scenario «Missing header is filled on save»; Requirement «Trace customer section stays distinct» — Scenario «Trace customer section stays distinct»; Requirement «Explain meta is not duplicated» — Scenario «Explain meta is not duplicated»; Requirement «Brief is not saved as a file» — Scenario «Brief is not saved as a file»; Requirement «Chat постановка has no reports list» — Scenario «Chat постановка has no reports list».
**Зависимости:** нет
**Режим apply:** mechanical

## 1. Правило шапки

- [x] S2.1 Добавить в `.cursor/rules/preserve-subagent-reports.mdc` шаблон `## Вводные` и страховку prepend-if-missing, чтобы по файлу было видно, с чем пришли: сразу после заголовка (у архитектурного — после YAML и H1), до тела находок — поля **Исходный запрос** (понятная формулировка смысла, 1–2 предложения; MUST NOT дословная цитата чата; MUST NOT пересказ находок), **Область** (названный объект или «не назван»), **Объекты / пути из постановки** (из слота Контекст или «не указаны»), **Симптом**, **Вопрос**; писать названное пользователем, не выведенное из кода; не копировать маршрут, варианты, ожидание, «решено когда», KB, имена агентов; секция «Для заказчика» в конце и не подменяет поле объектов; агент пишет по шаблону, оркестратор при сохранении: если после H1 нет `## Вводные` — вставить блок; «Исходный запрос» = подтверждённый слот «Вопрос» или сжатие сырой реплики в 1–2 предложения, MUST NOT вставлять реплику чата целиком; объект не выдумывать; пятипольную шапку требовать у обследования, трассы и архитектурного отчёта исследования (`report:` / запись без ЗНИ), не у служебных отчётов проверки постановки; при переезде MAY вставить минимум из постановки текущего чата, если шапки нет; уже лежащие файлы без живого чата задним числом не дописывать. Секцию переезда S1 в этом шаге не переписывать (Decision 3, 5, 12; Scenario «Intake header names the object», Scenario «Original request is a clear restatement», Scenario «Missing header is filled on save»)

## 2. Агенты и промпт исследования

- [x] S2.2 Добавить в `.cursor/agents/onec-code-explorer.md` в OUTPUT GUIDANCE секцию `## Вводные` сразу после H1 по SSOT правила сохранения, чтобы отчёт обследования открывался с объектом и понятным запросом; секцию «Для заказчика» в конце не сливать с шапкой и не подменять ею поле «Объекты / пути»; таблицу полей целиком не копировать — ссылка на правило сохранения (Decision 3; Scenario «Intake header names the object»). Зависимости: S2.1
- [x] S2.3 Добавить в `.cursor/agents/onec-trace-analyst.md` в OUTPUT FORMAT секцию `## Вводные` сразу после H1 по SSOT правила сохранения, чтобы объект постановки не терялся в разборе трассы; четырёхстрочный блок «Для заказчика» / «Что наблюдаешь у себя» оставить формулировкой симптома, не списком объектов из шапки (Decision 3; Scenario «Trace customer section stays distinct»). Зависимости: S2.1
- [x] S2.4 Добавить в `.cursor/agents/onec-code-architect.md` в OUTPUT FORMAT секцию `## Вводные` после YAML и H1, до тела Task / KB, чтобы архитектурный **отчёт исследования** (mode записи в каталог без ЗНИ / `report:` в постановке) нёс объект и понятный запрос; ссылаться на SSOT в правиле сохранения, таблицу полей не копировать; `scope.files` YAML не считать заменой шапки; ту же пятипольную шапку не требовать у готовности задач, независимого разбора постановки и оценки срезов (Decision 3, 12; Scenario «Intake header names the object»). Зависимости: S2.1
- [x] S2.5 Добавить в `.cursor/skills/openspec-explore/SKILL.md` в блок «Промпт `Task` обязан содержать» слоты вводных, чтобы агент формулировал шапку из постановки, а не из находок: вопрос исследования, исходный запрос (для понятной формулировки, не для копипаста чата), симптом, объекты/пути из Контекст или маркер «не указаны». Шаг Continuity в этом задании не трогать (Decision 5; Scenario «Original request is a clear restatement»)
- [x] S2.6 Уточнить в `.cursor/skills/openspec-explore/profiles/bug.md` одной строкой границу шаблонов, чтобы шапка вводных не подменила Symptom Lock: `## Вводные` ≠ четырёхстрочный «Для заказчика»; объект — в шапке отчёта, не вместо «Что наблюдаешь у себя» (Decision 3; Scenario «Trace customer section stays distinct»)

## 3. Журнал разбора и чат-постановка

- [x] S2.7 Уточнить в `.cursor/skills/openspec-explain/templates/explain-report.md` каркас «Мета», чтобы журнал пошагового разбора не требовал вторую шапку: отдельную `## Вводные` не требовать, если «Мета» заполнена сценарием и вопросом; при отсутствии объекта — одна строка «Объекты / пути из постановки» в «Мета», не вторая шапка. Правило href при переезде в этом шаге не дублировать (Scenario «Explain meta is not duplicated»)
- [x] S2.8 Закрепить в `.cursor/skills/openspec-explore/templates/handoff-block.md` правило сборки чат-блока: обязательное поле со списком файлов отчётов в `## Постановка ЗНИ` не добавлять; `report:` до создания ЗНИ может указывать на `temp`; перечень фактуры — файлы, не чат (Decision 4, ADR-0001; Scenario «Chat постановка has no reports list»)

## 4. Тест-кейс

- [x] S2.9 Добавить в `.cursor/skills/openspec-explore/test-cases/question-to-new-handoff.md` контрольную точку шапки, чтобы регресс ловил вводные в exploration-файле: после заголовка есть `## Вводные` с объектом (или «не назван») и понятным исходным запросом без дословной цитаты чата; секция «Для заказчика» в конце; в шапке нет слотов маршрута и вариантов. Точки переноса каталога в этом шаге не дублировать (Decision 5; Scenario «Intake header names the object», Scenario «Original request is a clear restatement», Scenario «Brief is not saved as a file»)

## 5. Сверка по тексту

- [x] S2.10 Верифицировать по тексту `.cursor/rules/preserve-subagent-reports.mdc`, трёх агентов, промпта explore, `profiles/bug.md`, шаблона explain и `handoff-block.md`, что шапка совпадает с дельтой без живого прогона команд: поле «Исходный запрос» требует понятную формулировку и запрещает дословную цитату чата и пересказ находок; при дописывании оркестратором источник — слот «Вопрос» или сжатие 1–2 предложения, не полная реплика (Decision 5, 12; Scenario «Original request is a clear restatement», Scenario «Missing header is filled on save»); пятипольная шапка обязательна у обследования / трассы / архитектурного отчёта исследования и не требуется у служебных отчётов проверки постановки (Scenario «Intake header names the object»); при отсутствии `## Вводные` оркестратор вставляет блок, объект не выдумывается (Scenario «Missing header is filled on save»); «Для заказчика» не слита с шапкой, у трассы «Что наблюдаешь» — симптом (Scenario «Trace customer section stays distinct»); у explain при заполненной «Мета» второй `## Вводные` нет (Scenario «Explain meta is not duplicated»); запрет `temp/briefs/*.md` на месте, в шаблоне шапки нет маршрута и вариантов (Scenario «Brief is not saved as a file»); в `handoff-block.md` и таблице `handoff-contract.md` нет обязательного поля «Отчёты исследования» (Scenario «Chat постановка has no reports list»). Зависимости: S2.1–S2.9

## 6. Приёмка

- [x] S2.accept Принять срез S2 «Вводные в отчёте» — по файлу отчёта видно объект и понятный исходный запрос, не цитату чата:
  - **Primary (обязательно):** Given исследование с названным объектом → When открыть отчёт обследования → Then после заголовка шапка: объект (или «не назван») и понятная формулировка исходного запроса без дословной цитаты чата; секция «Для заказчика» в конце
  - Scenario «Missing header is filled on save» (опционально): тело без `## Вводные` при слотах в промпте — в сохранённом файле после заголовка шапка; «Исходный запрос» из слота «Вопрос» или сжатие 1–2 предложения, не полная реплика чата; пустой контекст → «не назван» / «не указаны»
  - Scenario «Trace customer section stays distinct» (опционально): в отчёте разбора трассы шапка содержит объект или пути (или «не указаны»), а «Что наблюдаешь у себя» формулирует симптом, не список объектов
  - Scenario «Explain meta is not duplicated» (опционально, покрыто S2.7): журнал с заполненной «Мета» — отдельной `## Вводные` нет, либо в «Мета» только строка объектов, если её не было
  - Scenario «Brief is not saved as a file» (опционально): файла `temp/briefs/*.md` нет, в шапке нет слотов маршрута и вариантов
  - Scenario «Chat постановка has no reports list» (опционально): в блоке постановки нет обязательного перечня отчётов (наличие файлов в каталоге ЗНИ после new не проверять — это S1)

<!-- slice-gate: в шапке отчёта видны объект (или «не назван») и понятная формулировка запроса, не цитата чата -->
