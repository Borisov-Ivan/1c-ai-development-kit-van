---
report_type: task-readiness
generated_at: 2026-09-24
agent: onec-code-architect
mode: task-readiness
scope:
  change: tz-tech-project
  slices: [S1]
  files:
    - .cursor/commands/opsx-techproject.md
    - .cursor/skills/openspec-techproject/SKILL.md
    - .cursor/skills/openspec-techproject/**
    - .cursor/skills/openspec-explore/SKILL.md
  modules: []
  capabilities: [tz-tech-project]
related_reports:
  - reports/architecture-task-readiness-2026-09-23.md
confidence: high
open_questions_count: 0
superseded_by: null
---

# Task Readiness — tz-tech-project (delta 2026-09-24)

## Вердикт

**ГОТОВО С ЗАМЕЧАНИЯМИ**

**Overall status (orchestrator map):** warning-level-gaps — блокирующего пробела нет; изменившиеся S1.2, S1.4, S1.5, S1.6, S1.8, S1.10 исполнимы as-is по design + spec + тексту задач.

**Scope:** только `changed_tasks` (S1.2, S1.4, S1.5, S1.6, S1.8, S1.10) + чеклист S1.accept; S1.1/S1.3/S1.7/S1.9 — только на противоречие с новым текстом (противоречий нет). Матрица сценариев не повторялась.

**Контекст реализации:** конфигурация 1С не меняется; цели — команда и навык в `.cursor`. Writer BSL / метаданные / формы — N/A. `form_mode: n/a`.

**Closed contracts (не reopen):** EC-1, EC-2, EC-3; **affected:** EC-4 (сумма надбавок), EC-5 (протокол исследования + постановка).

**Orchestrator pre-screen (принято как факт):** маркеров ручной конфигурации нет; Executability / механические замечания — нет; `reused_evidence`: `reports/architecture-task-readiness-2026-09-23.md` (warning-level, без blocking).

**Trigger:** new-cross-module — навык берёт дыры протоколом исследования (не собственным чеклистом) + изменившийся текст задач.

## Оценка по изменившимся задачам

| ID | Артефакт | Исполнимо as-is | Forward-ref | Data / contract | User spike | Замечание |
|----|----------|-----------------|-------------|-----------------|------------|-----------|
| S1.2 | Entry Protocol в `SKILL.md` | да | нет (каталог навыка есть; S1.10 уточняет содержимое постановки) | EC-1 + EC-5: два файла сразу; техпроект+постановка только после подтверждения и повторного запуска; дыры/архив — протокол исследования | нет | WARNING G3: путь к `.cursor/skills/openspec-explore/SKILL.md` не назван в тексте задачи (см. design Existing Mechanisms) |
| S1.4 | лист вопросов | да | нет | ≥2 варианта + место ответа или «вопросов нет»; строка подтверждения в обоих случаях | нет | Ок; закрывает прежний G1 по наличию строки при «вопросов нет» |
| S1.5 | запрет/фильтр/нормативы + протокол дыр | да | нет | чеклист шаблона не источник; runtime-дыры = протокол исследования; `template/Техпроект` не читать/не удалять | нет | WARNING G2 (имена файлов внутри навыка) + G3 (якорь explore) |
| S1.6 | формула часов | да | нет | EC-2/EC-3/EC-4: (нижняя×коэф) + сумма нижних границ названных факторов; дробь вверх; две строки справочника → вопрос | нет | Ок; заменяет ед. число «фактор» на сумму |
| S1.8 | убрать отдельную сверку имён | да | нет | EC-5 / D5: нет второго источника `src/КАСК/cf/`; имя, не найденное исследованием → дыра; команда не стопится | нет | Ок; текущий раздел «Сверка с выгрузкой» в навыке — цель снятия |
| S1.10 | `<имя>-постановка.md` | да | нет (gate уже в S1.2) | EC-5 / D1: вместе с техпроектом после подтверждения; каталог ЗНИ не создавать; модули не править | нет | WARNING G4: каркас полей постановки не enumerated (достаточно «текст для `/opsx:new`» + связь с архивом из D5) |
| S1.accept | приёмка среза | N/A (приёмка) | — | Primary + optional scenarios, включая постановку / сумму надбавок / explore-дыры | runtime-прогон — зона приёмки, не structural spike в рабочих задачах | Вне оценки исполнимости кодовых задач (по промпту) |

**Неизменённые [x] без противоречия новому тексту:** S1.1 (обёртка), S1.3 (каркас согласования / обзор не трогать), S1.7 (карта разделов ЧЗ), S1.9 (таблицы техпроекта + gate подтверждения) — остаются валидными рядом с S1.10.

## Оценка по критериям

| # | Критерий | Вердикт | Обоснование |
|---|----------|---------|-------------|
| 1 | Реализуемость кодовых задач | OK | Каждая изменившаяся задача указывает каталог/файл навыка и наблюдаемое правило (D1–D5, EC-4/EC-5). Writer BSL не нужен. Межмодульная связь с протоколом исследования закрыта design: брать дыры и архив, не копировать чат-диалог / ожидание ответа / создание каталога ЗНИ. |
| 2 | Реализуемость форм и метаданных | OK (N/A) | `form_mode: n/a`; метаданные 1С вне scope; маркеров ручной конфигурации нет. |
| 3 | Разрешённость решений | OK | Open Questions: нет. Option A Chosen. EC-1…EC-5 и «Решения verify» закрывают фазы вывода, формулу часов, сумму надбавок, источник дыр и постановку. Неразрешённых «или» в тексте изменившихся задач нет. |
| 4 | Полнота покрытия | OK | S1.2/S1.4/S1.5/S1.8/S1.10 покрывают MUST spec про протокол исследования, отказ от чеклиста/отдельной сверки и файл постановки; S1.6 — сумму факторов и округление; S1.accept — Primary + optional scenarios, включая новые. |
| 5 | Согласованность tasks ↔ design ↔ spec | OK | D1/D3/D4/D5, Behavior Contract, spec и задачи согласованы после переписи среза (EC-4/EC-5). Готовые S1.1/S1.3/S1.7/S1.9 не противоречат новому тексту. |
| 6 | Связность и порядок задач | OK | Порядок: Entry Protocol (S1.2) → лист (S1.4) → источник дыр (S1.5) → часы (S1.6) → снятие сверки (S1.8) → постановка (S1.10) → одна приёмка `S1.accept`. Маркер `<!-- slice-gate: … -->` на месте. Нет ссылки на ещё не создаваемый объект. |
| 7 | Архитектурная эстетика | OK | Подход не пересматривался: Preference Hierarchy уровень 3, Option A; explore ограничен дырами/архивом внутри команды, без поглощения полного UX explore. |
| 8 | User Task Contract | OK | В S1.2/S1.4/S1.5/S1.6/S1.8/S1.10 нет user runtime-spike (ИБ, консоль, отладчик, эмуляция API). Прогон команды — только S1.accept. |
| 8e / Precedent Coherence | OK | Capability новая; EC-4/EC-5 зафиксированы заказчиком и отражены в design/spec/tasks. Blast на архивные контракты объектов 1С нет. `## Blast Radius` не требуется для kit-only change. |

## Пробелы (WARNING only — не GAP критериев)

### G2 — имена файлов внутри навыка после переноса (повтор; не блокирует)

- **Задачи:** S1.5 (и контекст уже сделанного S1.7)
- **Что неоднозначно:** «оставить в навыке» запрет/фильтр/нормативы без enumeration имён файлов.
- **Почему не CRITICAL:** каталог `.cursor/skills/openspec-techproject/` задан; `labor_standards.md` уже есть; runtime-правило «не читать `template/Техпроект`» однозначно.
- **Рекомендация:** при S1.5 явно сослаться на существующие секции/`labor_standards.md` в SKILL.

### G3 — якорь протокола исследования (новый; не блокирует)

- **Задачи:** S1.2, S1.5, S1.8
- **Что неоднозначно:** фраза «тот же протокол исследования, что перед созданием задачи на разработку» без пути к `.cursor/skills/openspec-explore/SKILL.md` и без списка «что адаптировать / что не копировать» в тексте задачи (есть в design D4/Risks).
- **Почему не CRITICAL:** design Existing Mechanisms + D4/Risks однозначно: дыры и связь с архивом → в лист и постановку; чат-диалог, ожидание ответа в чате, создание каталога ЗНИ — не копировать. Исполнитель kit находит `openspec-explore` без возврата постановки.
- **Рекомендация (сниппет для S1.5 / S1.2):**

```markdown
Источник протокола дыр и архива: адаптировать из `.cursor/skills/openspec-explore/SKILL.md`
(поиск по конфигурации / архиву changes; результат — в лист и `<имя>-постановку.md`).
Не копировать: вопросы по одному в чат, ожидание ответа в чате до записи файлов,
создание каталога ЗНИ, вызов `/opsx:new`.
```

### G4 — каркас полей `<имя>-постановка.md` (новый; не блокирует)

- **Задачи:** S1.10
- **Что неоднозначно:** «текст для создания задачи на разработку» без списка полей; D5 требует связь с прошлыми изменениями в этом файле.
- **Почему не CRITICAL:** spec/D1/EC-5 задают роль файла и запрет создавать каталог/править модули; исполнитель может опереться на каркас `openspec-explore/templates/handoff-block.md` как образец полей без вызова `/opsx:new`.
- **Рекомендация (сниппет для S1.10):**

```markdown
Каркас `<имя>-постановка.md`: поля по образцу
`.cursor/skills/openspec-explore/templates/handoff-block.md`
(включая связь с архивом); файл — только текст рядом с заданием.
```

### G1 (2026-09-23) — закрыт текстом

Прежняя неоднозначность «строка подтверждения при „вопросов нет“» закрыта S1.4 / design D1 / Primary acceptance. Точная фраза маркера по-прежнему может остаться как в текущем SKILL (`Подтверждение согласования:`) — уточнение постановки не требуется.

## Closed decisions / не reopen

- **EC-1** — S1.2, S1.10 (и готовый S1.9): техпроект+постановка после подтверждения в листе.
- **EC-2 / EC-3** — S1.6: надбавка после коэффициента; дробь вверх.
- **EC-4** — S1.6 + S1.accept «Несколько факторов надбавки»: сумма нижних границ.
- **EC-5** — S1.2, S1.5, S1.8, S1.10: протокол исследования вместо чеклиста/отдельной сверки; файл постановки без создания каталога ЗНИ.

## Manual configuration / DENY

- Маркеры ручной конфигурации: **не найдены** (orchestrator evidence).
- User Task Contract DENY / runtime-spike в рабочих задачах S1.2–S1.10: **не найдены**.

## Итог для оркестратора

| Поле | Значение |
|------|----------|
| overall_status | `warning-level-gaps` |
| blocking_tasks | _(нет)_ |
| warning_tasks | S1.2/S1.5/S1.8 (G3), S1.5 (G2), S1.10 (G4) |
| ready_tasks | S1.2, S1.4, S1.5, S1.6, S1.8, S1.10 — все исполнимы as-is |
| unchanged_ok | S1.1, S1.3, S1.7, S1.9 — без противоречия новому тексту |
| layer_5_hint | WARNING (не NO-GO) |

## Источники

- `openspec/changes/tz-tech-project/tasks.md` — changed S1.2, S1.4, S1.5, S1.6, S1.8, S1.10, S1.accept
- `openspec/changes/tz-tech-project/design.md` — D1–D5, Behavior Contract, Решения verify, Existing Mechanisms
- `openspec/changes/tz-tech-project/proposal.md` — Why / What Changes / Acceptance Criteria
- `openspec/changes/tz-tech-project/specs/tz-tech-project/spec.md` — Requirements + Scenarios
- `openspec/changes/tz-tech-project/debug.md` — EC-4, EC-5, closed_decisions
- `openspec/changes/tz-tech-project/reports/architecture-task-readiness-2026-09-23.md` — reused_evidence
- Контекст кода навыка (состояние до доработки задач): `.cursor/skills/openspec-techproject/SKILL.md` (чеклист дыр + «Сверка с выгрузкой» — цели S1.5/S1.8)
