# Приёмка среза S2 — Режимы форм (per-form)

**Change:** sequential-ui-mode-questions  
**Дата:** 2026-08-01  
**Решение:** принят (manual shortcut)

## Primary

Учебный `/opsx:new`: после маркера и создания proposal/design — отдельный вопрос про поставку конкретной формы (`БизнесПроцесс.Исполнение.Форма.ФормаЗадачиИсполнителя`), без склейки с макетом — принято.

## Follow-up вне среза

- `F1` / `F2` в `tasks.md` остаются открытыми (не блокируют приёмку срезов).
