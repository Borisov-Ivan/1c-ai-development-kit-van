# Quality Control — scenario-map-readability-meaning

Date: 2026-08-29  
Report: `quality-control-2026-08-29-4.md`  
Mode: **slice** (`# Срез S1` in `tasks.md`)  
Scope: criteria 1–6, 8, 8b, 9–11 + task readability  
Prior reports: `quality-control-2026-08-29.md` (proposed-slice, без `tasks.md`); `quality-control-2026-08-29-2.md` (10 Scenario); `quality-control-2026-08-29-3.md` (13 Scenario, иная нумерация задач до перестановки групп) — **не перезаписывались**  
Out of scope: исполнимость приёмки «прямо сейчас» на живой панели / в проекте Документооборота; тестовые данные; эталоны ИБ

Context: kit-only change (скилл, шаблон панели, эталоны, роль сборщика, дельта spec). Продуктовый BSL/XML не требуется. `form_mode: n/a`. **Режим apply:** mechanical. В spec **13** `#### Scenario:`. Все чекбоксы `[ ]`. Приёмка живой панели — на границе среза (`S1.accept`), не внутри `S1.1`–`S1.20`.

Sources: `tasks.md`, `design.md` (`## Slices`), `proposal.md`, `specs/scenario-map-canvas/spec.md` (13 `#### Scenario:`), `.cursor/rules/vertical-slices.mdc`, `.cursor/rules/task-readability.mdc`.

Mechanical pre-check (prompt): чекбоксы на месте; ровно один `S1.accept`; закрывающий `<!-- slice-gate -->` есть; `<!-- phase-gate -->` нет; пустых буллетов нет; ID задач с префиксом среза. User Task Contract DENY-grep по строкам `^- \[[ x]\] S\d+\.\d+` (не accept): none (`тестовой ИБ`, `на ИБ вериф`, `на стенде`, `runtime-verify`, `спайк`, `в консоли`, `отладчик`, `эмулировать вызов`, `вызвать API`, `При успешном verify S`, `после verify S`, `после стенда`). Маркеров ручной конфигурации нет.

Repository: скилл, шаблон панели, хороший причинный эталон, эталон списка без рёбер, роль и бриф сборщика — на месте и непусты; `map-bad-no-insight.md` отсутствует (явный результат `S1.13`); прикладного `src/` и `openspec/project.md` нет и не требуется (kit).

Отличие от `quality-control-2026-08-29-3.md`: группы внутри среза переставлены. Сейчас: шаблон `S1.1`–`S1.4` → скилл `S1.5`–`S1.10` → роль `S1.11`–`S1.12` → эталон «граф без смысла» `S1.13`–`S1.14` → аннотации `S1.15`–`S1.19` → сверка текстов `S1.20` → `S1.accept`. Развилка «кто считает координаты» в design **закрыта** (панель); в `S1.1` записано то же. Привязки задач в этом отчёте — к **текущему** `tasks.md`, не к нумерации `-3.md`.

## Verdict

`OK`

Один срез, одна приёмка, один маркер границы. Все тринадцать сценариев спецификации покрыты (одиннадцать имён в чеклисте приёмки, два — задачами агента). Обязательный осмотр панели — один взгляд после открытия штатной кнопкой: направление, легенда, честные полосы, выбранный виновник и ловушка одной подписи. Второй срез не требуется: один самостоятельный пользовательский исход.

## Slice Summary

| Slice | Scenario | Tasks | Acceptance | Dependencies | Gate |
|---|---|---|---|---|---|
| S1: Читаемая карта со смыслом | Разработчик просит карту по теме с несколькими отчётами и открывает панель штатной кнопкой среды | 20 агентских (`S1.1`–`S1.20`) + `S1.accept`; группы `## 1`–`## 6` внутри среза | `S1.accept` (11/13 имён в чеклисте; 2/13 только в `S1.8`, `S1.14`) | нет | да (`<!-- slice-gate -->`) |

Notes:

- Порог: 20 кодовых задач — Full (≥16). Второй срез **не** требуется: один самостоятельный пользовательский исход (открытая панель читается и не теряет смысл). Группы внутри среза — не отдельные срезы. Согласовано с `design.md` `## Slices` (черновик из трёх срезов отклонён: отдельный «шаблон» был бы подготовкой без самостоятельной приёмки).
- `map-bad-no-insight.md` в репозитории ещё нет — это результат `S1.13`, не дыра слоя и не structural unreachability Primary.
- В поле `**Связь со spec:**` имя «Аннотации не добирают порог» повторено дважды (у требования про контракт узла и у требования про слои). В spec сценарий один, под требованием про слои и ветвление; MUST «аннотации не в пороге» живёт также в тексте контракта узла. На покрытие это не влияет: уникальных имён — 13.
- Координаты: D1 и `S1.1` согласованы («считает панель»). QC-алерт не эмитируется.

## Scenario Coverage

13 `#### Scenario:` в `openspec/changes/scenario-map-readability-meaning/specs/scenario-map-canvas/spec.md`. Поле `**Связь со spec:**` перечисляет все тринадцать уникальных имён.

| Scenario | Covered by | Status |
|---|---|---|
| Направление связей и легенда видны без клика | Primary `S1.accept` + `S1.2` | OK |
| Главный вывод виден без клика по узлу | Primary `S1.accept` + `S1.3`, `S1.10`, `S1.11` | OK |
| Находка, меняющая действие, на полотне | Primary `S1.accept` + `S1.5`, `S1.9`, `S1.11` | OK |
| Ловушка одной подписи видна на полотне | Primary `S1.accept` + `S1.15`, `S1.17`, `S1.19` | OK |
| Слои или ветвление видны на панели | optional `S1.accept` + `S1.1` | OK |
| С узла открывается доказательство, новый чат с панели не запускается | optional `S1.accept` + `S1.3`, `S1.7` | OK |
| Вывод только в шапке не публикуется | optional `S1.accept` + `S1.5`, `S1.13` | OK |
| Плашка без доказательства не публикуется | optional `S1.accept` + `S1.13`, `S1.17` | OK |
| Аннотации не добирают порог | optional `S1.accept` + `S1.16`, `S1.20` | OK |
| Режим не прячет рёбра | optional `S1.accept` + `S1.2`, `S1.10` | OK |
| Связь без видимого отношения не выдумывается | optional `S1.accept` + `S1.12`, `S1.18` | OK |
| Провал смысла не уходит в текстовый резерв | `S1.8` (запись правила в скилл; имя Scenario в тексте задачи); смежно `S1.5`, `S1.20` | OK — покрытие задачей агента (implementation-only path; не blocking) |
| Эталон карты в скилле — граф, не список | `S1.13` (создаёт третий эталон), `S1.14` (ссылки в скилле; имя Scenario в тексте задачи); смежно `S1.20` | OK — покрытие задачей агента |

Покрытие только в `S<N>.<M>` для двух последних — **допустимо** (критерий 5b / правило среза 6). `accept-bullets-missing-scenario` **не** эмитируется.

Смежный optional «Вывод только в шапке не публикуется» наблюдает отказ публикации, но не фиксирует «резерв в журнал не пишется» — отличительный THEN сценария провала смысла закрыт текстом скилла в `S1.8`, этого достаточно.

Optional «Связь без видимого отношения не выдумывается» допускает исход «отсутствие зафиксировано аннотацией» рядом с отказом публикации — согласовано с D4 / `S1.18`; имя Scenario в spec совпадает буквально.

Имена в ёлочках чеклиста и задач совпадают с заголовками `#### Scenario:` spec (без перефразирования).

## Dependency Graph

```mermaid
flowchart LR
  S1[S1 Читаемая карта со смыслом]
```

- Cycles: none.
- Forward acceptance dependencies: none (единственный срез).
- Undeclared slice predecessors: none (`**Зависимости:** нет`).
- Intra-slice (порядок файла): шаблон (`S1.1`–`S1.4`) → скилл (`S1.5`–`S1.10`; `S1.10` явно откладывает аннотации) → роль/бриф (`S1.11`–`S1.12`) → эталон «граф без смысла» (`S1.13` создаёт файл, `S1.14` ссылается) → аннотации на полотне (`S1.15`–`S1.19`: шаблон → контракт скилла → self-check → роль → хороший эталон) → сверка текстов (`S1.20`) → `S1.accept`. Цикла нет. Явная межсрезовая зависимость не нужна.

## Checklist Results

| # | Criterion | Result |
|---|---|---|
| 1 | Scenario Coverage | **Pass** — 13/13. Primary / optional / агентская задача. User runtime только на границе `S1.accept` |
| 2 | Slice Independence | **Pass** — принимать S1 можно без несуществующего следующего среза |
| 3 | Slice Completeness | **Pass** — слои kit, нужные для Primary «открыть панель → видно направление, легенду, полосы по слоям, виновника и ловушку»: шаблон (`S1.1`–`S1.4`, `S1.15`), скилл смысла и читаемости (`S1.5`–`S1.10`, `S1.16`–`S1.17`), роль + prompt сборщика (`S1.11`–`S1.12`, `S1.18`), эталоны (`S1.13`–`S1.14`, `S1.19`), сверка контрактов (`S1.20`). Метаданных/форм 1С нет и не требуется |
| 4 | Slice Dependency Graph | **Pass** — S1 → нет |
| 5 | Slice Gate Integrity | **Pass** — ровно один `S1.accept`, ровно один `<!-- slice-gate -->`. Legacy `S1.T<M>` нет. Фаз / `phase-gate` нет |
| 5b | Acceptance Checklist Coverage | **Pass** — есть `**Primary acceptance:**` в metadata и `**Primary (обязательно):**` в теле accept. Чеклист не пуст. Чужих сценариев нет (один срез). Все Scenario spec где-то покрыты |
| 6 | Rework Risk | **Pass** — нет опоры на непринятый предыдущий срез; сценарии не дублируются между срезами. Плотность Primary (четыре имени в одном Then) намеренна: один взгляд на ту же панель, не второй срез. `S1.10` / `S1.16` (контракт карты: сначала фокус и режимы, аннотации отдельной задачей) и `S1.5` / `S1.9` / `S1.17` (смысл vs self-check `6a`–`6d`) — дополнение внутри среза, не ложная граница |
| 8 | Slice Verticality | **Pass** — mandatory Primary: попросить карту → открыть панель штатной кнопкой → **взглядом** увидеть направление, легенду, честные полосы, выбранного виновника и ловушку. Black-box (панель рядом с чатом), не вызов API и не код-ревью. Programmatic (сверка скилла, эталон markdown) вынесены из Primary |
| 8b | Self-Achievable Acceptance | **Pass** — нет пары S1/S2. Все включающие слои Primary (шаблон со стрелками/полосами/аннотациями, проверки скилла, бриф сборщика с несколькими отчётами, `focus_node`, возврат `annotations`, ловушка на хорошем эталоне) лежат в задачах этого же среза. Наблюдаемый исход не заимствован у более позднего среза. Отсутствие `map-bad-no-insight.md` до `S1.13` — результат задачи, не forward-зависимость приёмки |
| 9 | Foundation slice with gate | **Pass** — нет foundation-среза с programmatic accept и зависимого UX-среза. Подготовка шаблона и аннотаций внутри того же S1, не отдельный gate |
| 10 | Acceptance Simplicity | **Pass** — **один** mandatory black-box journey (одна открытая панель, один взгляд). Четыре Scenario указаны в скобках одного Then, не четырьмя обязательными буллетами. Остальные семь — «(опционально)» |
| 11 | User Task Contract | **Pass** — DENY-grep по `S1.<M>` пуст. Семантика: `S1.1`–`S1.20` — правки markdown / сверка по тексту (агент). Осмотр живой панели, клик, смена режима, негативные «панель не появилась» — только в `S1.accept`. Условных цепочек «после verify / после стенда» нет. Assumptions design п.4 (пересборка в Документообороте) = граница приёмки, не mid-slice user-spike. Фраза «после чистой проверки панели» в `S1.6` — место вставки чек-листа в скилл, не обязанность пользователя на стенде |

## Task Readability

Применяется ко всем `S1.1`–`S1.20`. `S1.accept` — по исключению (бизнес-результат в заголовке + чеклист).

| Check | Result |
|---|---|
| Глагол + файл/объект + зачем + опора (D/ADR/Scenario) | **Pass** — зачины «В `<путь>`: …» / «Создать `<файл>`» / «Сверить по тексту …»; опоры D1–D9, ADR-0008, Behavior Contract, имена Scenario |
| Антипаттерн «голый идентификатор решения» | нет |
| Антипаттерн «глагол без объекта» | нет |
| Описание < 8 слов | нет |
| `S1.accept` заголовок с бизнес-результатом | **Pass** — «карта читается с первого взгляда и не теряет дорогое знание» |
| Имена Scenario в ёлочках | **Pass** — буквальное совпадение с `#### Scenario:` spec; два имени без буллета accept закрыты `S1.8` и `S1.14` |
| Recipe vs outcome | **Pass** — детали раскладки, двух чек-листов и аннотаций — принятые решения design (D1, D2, D8, D9), не самовольный рецепт вместо наблюдаемого Then |

Алерты читаемости не эмитируются. `S1.7`, `S1.12` и `S1.18` трогают два файла одной строкой — формулировки самодостаточны, дробить не требуется.

## Alerts

Нет.

Предыдущий WARNING `accept-bullets-missing-scenario` (отчёт создания ЗНИ) остаётся снятым: два имени без буллета accept по-прежнему закрыты `S1.8` и `S1.14`.

Профилактические SUGGESTION отчёта создания (`slice-gate-pending-tasks`, `acceptance-simplicity-guard`, `user-task-contract-guard`, явность слоёв) **исполнены** в `tasks.md` и повторно не поднимаются.

**Не эмитировано:** `no-slices`; `deprecated-phase-gate`; `legacy-acceptance-format`; `primary-acceptance-missing`; `accept-checklist-empty`; `accept-bullets-missing-scenario`; `accept-bullet-foreign-scenario`; `slice-not-vertical`; `slice-accept-not-self-achievable`; `slice-foundation-with-gate`; `acceptance-simplicity-overload`; `user-task-contract-violation`; `task-opaque-title`; `task-too-short`; `task-opaque-acceptance`.

## Recommendations

**Automatic fix**

Нет.

**Decision required**

Нет. Объединение срезов / переписывание Primary не требуется.

**Do not**

- Не вводить второй срез «шаблон», «аннотации» или «смысл отдельно».
- Не разворачивать четыре имени в Primary в четыре обязательных буллета.
- Не переносить осмотр живой панели / клик / смену режима / негатив «панель не появилась» в `S1.<M>`.
- Не откладывать подпись `S1.accept` «пока не будет следующего среза».
- Не требовать тестовых данных / эталона ИБ / уже созданного `map-bad-no-insight.md` как условие когерентности среза (out of scope QC / результат задачи).
