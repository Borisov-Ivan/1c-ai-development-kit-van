# Срез S1: Живой мэппинг моделей

**Сценарий:** оркестратор делегирует работу субагентам на моделях, которые реально существуют в текущей сборке Cursor, и не начинает вызов с ошибки выбора модели; при будущем обновлении списка моделей самосверка не даёт таблице тихо устареть.
**Primary acceptance:** в сессии Cursor запустить архитектора по новой таблице ролей (`Task(subagent_type="onec-code-architect", model="claude-opus-5-thinking-high")`) → вызов проходит без ошибки выбора модели и без молчаливого перехода на запасную модель; в правилах независимый разбор постановки указывает Fable только при наличии слага в enum, иначе Opus 5.
**Приёмка:** ручная проверка в сессии Cursor (тестовое делегирование + поиск по репозиторию), без информационной базы 1С.
**Связь со spec:** Requirement «Мэппинг ролей на актуальные модели» — Scenario «Вызов архитектора без ошибки enum», Scenario «Команда на Grok 4 без смены чата», Scenario «Рантайм свободен от мёртвых слагов»; Requirement «Самосверка enum перед вызовом с model=» — Scenario «Слаг таблицы отсутствует в enum сборки»; Requirement «Двухшаговая цепочка вызова» — Scenario «Сбой Primary», Scenario «Согласованность описаний цепочки»; Requirement «Fable только как закрытая эскалация архитектора» — Scenario «Декомпозиция срезов не идёт на Fable», Scenario «Независимый разбор постановки идёт на Fable», Scenario «Сбой Opus не включает Fable».
**Зависимости:** нет.

## 1. Таблица ролей и цепочки

- [x] S1.1 Заменить в `.cursor/rules/model-selection.mdc` таблицу «Роли и `Task.model`» на актуальные Primary: обычный architect → `claude-opus-5-thinking-high`, reviewer → `gemini-3.1-pro`, simplifier → `composer-2.5-fast`, writer / explorer / trace-analyst / quality-controller → без `model=`; Fable не является Primary ни одной роли; записать рекомендацию: чат оркестратора — семейство Grok 4 (слаг сборки `cursor-grok-4.6-xhigh`) (D1, D1a)
- [x] S1.2 Сократить в `.cursor/rules/model-selection.mdc` цепочки вызова до двух шагов «Primary → без `model=`»: удалить колонки Fallback 1 / Fallback 2 из таблицы и переписать разделы «Цепочка для архитектора», «Цепочка для writer / explorer», «Цепочка для reviewer / simplifier», чтобы лишние failed-вызовы исчезли (D2)
- [x] S1.3 Привести раздел «Целостность цепочки Task» в `.cursor/rules/model-selection.mdc` к двухшаговой модели: сбой на последнем шаге = стоп и сообщение пользователю, а не подмена отчёта субагента текстом оркестратора (D2)

## 2. Самосверка списка моделей

- [x] S1.4 Удалить в `.cursor/rules/model-selection.mdc` секцию «Ограничение enum параметра `Task.model`» с захардкоженным перечнем слагов и заменить её правилом самосверки: актуальный список — в описании инструмента `Task` текущей сборки, сверка выполняется перед первым в сессии вызовом с `model=` (D3)
- [x] S1.5 Записать в `.cursor/rules/model-selection.mdc` поведение при расхождении: вызов без `model=`, одна строка предупреждения пользователю без жаргона, предложение обновить таблицу; подстановка «похожей» модели без явного выбора пользователя запрещена (D3)
- [x] S1.6 Удалить в `.cursor/rules/model-selection.mdc` (раздел «Запреты в параметре `Task`») ложное утверждение «`model="inherit"` — в enum параметра обычно нет», чтобы правило не противоречило реальному списку сборки (D3)

## 3. Согласование дублей

- [x] S1.7 Заменить в `.cursor/rules/architect-gate.mdc` дублирующий слаг модели архитектора ссылкой на SSOT `model-selection.mdc` (обычный вызов — Opus 5; Fable — только по таблице эскалации), чтобы слаг не приходилось править в двух местах (D1, D1a)
- [x] S1.8 Привести в `.cursor/rules/tool-name-guard.mdc` все упоминания цепочки вызова (чеклист `Task`, разделы про `model`) к двухшаговой формуле и убрать утверждение «в enum параметра `inherit` обычно нет»; анти-примеры слагов оставить как анти-примеры (D2, D3)
- [x] S1.9 Проверить поиском по `.cursor/rules/`, `.cursor/skills/`, `.cursor/commands/`, `.cursor/agents/` рантайм-употребления `model=`: слагов вне актуального списка сборки нет; исключения: исторические записи `CHANGELOG.md`; анти-примеры `tool-name-guard.mdc`; закрытая таблица эскалации Fable — слаг `claude-fable-5-thinking-high` MAY быть записан как цель политики вместе с fallback D1a (нет в enum → Opus 5 + одна строка) и не считается мёртвым Primary (Scenario «Рантайм свободен от мёртвых слагов»)
- [x] S1.10 Проверить поиском упоминания цепочки моделей в `.cursor/skills/openspec-verify-change/SKILL.md` и прочих скиллах и привести найденные описания к двухшаговой цепочке плюс эскалации Fable (независимый разбор постановки — Fable **при наличии слага в enum**, иначе Opus 5 + одна строка; декомпозиция срезов — Opus), чтобы двух конкурирующих описаний не осталось (Scenario «Согласованность описаний цепочки», D1a)
- [x] S1.11 Записать в `.cursor/rules/model-selection.mdc` закрытую таблицу эскалации Fable: разрешённые режимы и триггеры, запрет Fable как запасной модели, лимит «один вызов на гейт», одна строка в чат перед запуском Fable (без слага и без имени агента); если слаг отсутствует в описании `Task` текущей сборки, таблица всё равно фиксирует режимы, фактический вызов — Opus 5 + предупреждение; живой вызов Fable не входит в обязательную приёмку первого среза (D1a)

## 4. Приёмка

- [ ] S1.accept Принять срез S1 — делегирование идёт на живые модели без ошибки выбора, Fable не сжигает бюджет на обычных вызовах:
  - **Primary (обязательно):** запустить обычного архитектора по таблице (`model="claude-opus-5-thinking-high"`) → вызов проходит без ошибки enum; в правилах записано, что декомпозиция срезов идёт на Opus, а независимый разбор постановки — на Fable при наличии слага в enum, иначе на Opus 5
  - Scenario «Декомпозиция срезов не идёт на Fable» (опционально): по тексту `model-selection.mdc` / `architect-gate.mdc` режим декомпозиции срезов не содержит Fable как Primary
  - Scenario «Независимый разбор постановки идёт на Fable» (опционально): по тексту правил независимый разбор указывает Fable и одну строку в чат; если слага нет в enum — Opus 5 + предупреждение, без вызова с отсутствующим слагом
  - Scenario «Сбой Opus не включает Fable» (опционально): цепочка обычного архитектора — Opus → без `model=`, без шага Fable
  - Scenario «Команда на Grok 4 без смены чата» (опционально): команда workflow в чате Grok 4 extra high не требует переключения на Opus или Fable
  - Scenario «Слаг таблицы отсутствует в enum сборки» (опционально): подставить в запрос заведомо отсутствующий слаг → оркестратор вызывает субагента без `model=`, выдаёт одну строку предупреждения и не подставляет «похожую» модель
  - Scenario «Сбой Primary» (опционально): при ошибке вызова с Primary следующий вызов идёт без `model=`; при сбое и этого шага — стоп и сообщение пользователю

<!-- slice-gate: обычный архитектор на Opus 5; независимый разбор — Fable при наличии слага в enum, иначе Opus 5; мёртвых Primary-слагов в рантайм-файлах нет -->

# Срез S2: Диета always-apply

**Сценарий:** каждый запрос к оркестратору несёт заметно меньше постоянного контекста, при этом ни один гейт не перестаёт срабатывать: команда по-прежнему начинается с чтения скилла, попытка править XML метаданных останавливается, массовое чтение уходит в стратегию контекста, правка кода 1С — в делегирование.
**Primary acceptance:** в чистом окне Cursor (Reload Window) выполнить четыре запроса подряд → команда из `.cursor/commands/` даёт первым действием чтение `SKILL.md`; запрос правки XML в `src/` даёт стоп с блокером; запрос анализа 3+ файлов даёт загрузку стратегии контекста; запрос правки `.bsl` даёт делегирование writer.
**Приёмка:** ручная проверка в чистом окне Cursor после Reload Window, без информационной базы 1С; замеры и таблицы — в отчётах change.
**Связь со spec:** Requirement «Бюджет постоянного контекста» — Scenario «Замер после диеты»; Requirement «Разжалование без потери обязательств» — Scenario «Обязательство-diff без непокрытых строк», Scenario «Поведенческий smoke в чистом окне», Scenario «Якорь apply-reviewer после выноса процедуры», Scenario «Якорь поверхности после выноса процедуры»; Requirement «Передача SSOT writer pipeline» — Scenario «Потребители ссылаются на нового владельца»; Requirement «Актуализация потребителей бюджетного порога» — Scenario «Порог целостности поставки актуален».
**Зависимости:** нет.

## 1. Перенос обязательств в якоря

- [ ] S2.1 Перенести в `.cursor/rules/session-discipline.mdc` дословно переносимый минимум трёх разжалуемых правил: первый tool call командной сессии — только Read `SKILL.md`; запрет читать файлы пользователя до Entry Protocol; страховка при обрезке большого `SKILL.md` (дочитать `templates/*.md`); TodoWrite checkpoint; persistence «протокол команды на каждом ходе»; на каждом ходе — проверка активной команды, ограничений скилла и СТОП при нарушении (Gate check из persistence); TRIGGER/ACTION/BYPASS стратегии контекста (3+ файлов / XML|CSV|JSON / код 500+ строк / «береги контекст»); таблица антипаттернов follow-up (explore → «создай ЗНИ», explore → самостоятельный Grep по `.bsl`, apply → «а может лучше») (D6)
- [ ] S2.2 Принять в `.cursor/rules/1c-agent-delegation.mdc` содержание `bsl-write-guard.mdc`: абсолютный запрет прямой правки `.bsl` оркестратором, минимальный поток и три carve-out (JSDoc / шапка метода, контекст apply/review, Mechanical Mode), чтобы слияние не потеряло исключений (D6)
- [ ] S2.3 Принять в `.cursor/rules/chat-output-budget.mdc` содержание стабов `conversational-discipline.mdc` и `orchestrator-as-navigator.mdc` (роль навигатора, правило тишины, пять принципов диалога), чтобы контракт чата остался в одном always-apply файле (D6)

## 2. Разжалование и удаление

- [ ] S2.4 Перевести `.cursor/rules/1c-xml-write-guard.mdc` в on-demand (`alwaysApply: false` + триггер в `description`), оставив сам запрет правки XML в always-apply секции delegation, чтобы стоп срабатывал до чтения файла (D6)
- [ ] S2.5 Перевести `.cursor/rules/command-skill-gate.mdc`, `.cursor/rules/command-session-persistence.mdc`, `.cursor/rules/context-strategy-gate.mdc` в on-demand (`alwaysApply: false`), сохранив триггер загрузки в frontmatter каждого файла (D6)
- [ ] S2.6 Удалить `.cursor/rules/conversational-discipline.mdc` и `.cursor/rules/orchestrator-as-navigator.mdc` после переноса содержания в `chat-output-budget.mdc`, чтобы дубли не тратили постоянный контекст (D6)
- [ ] S2.7 Удалить `.cursor/rules/bsl-write-guard.mdc` после переноса запрета и carve-out в delegation, чтобы у запрета остался один владелец (D6)
- [ ] S2.8 Заменить в `.cursor/rules/command-skill-gate.mdc` висячую ссылку на `conversational-discipline.mdc` (принцип Compact Brief) ссылкой на `chat-output-budget.mdc`, чтобы правило не указывало на удалённый файл

## 3. Сжатие delegation и передача SSOT

- [ ] S2.9 Вынести подробности секции § KB CONTEXT из `.cursor/rules/1c-agent-delegation.mdc` в `.cursor/rules/knowledge-format.mdc`, оставив в delegation однострочный якорь: при делегировании explorer / architect / trace — блок `## Existing Knowledge` по правилу в `knowledge-format.mdc` (D6)
- [ ] S2.10 Вынести полную процедуру § АВТО-ИСПРАВЛЕНИЕ РЕВЬЮ (включая DISPROPORTIONATE_SURFACE и disposition `/review`) из `1c-agent-delegation.mdc` в `.cursor/skills/review/SKILL.md`, оставив в delegation: (1) лимит итераций writer↔reviewer = 2; (2) дословный минимум D6(в): авто-fix только functional MUST_FIX без QualityFlag=weak, tag design-prescribed и agreement-override; weak / design-prescribed / agreement-override — не авто-fix, не авто-waive, не AskQuestion в apply, одна строка-след «на `/review` потребуется подтверждение качества»; (3) дословный MUST поверхности: полное ревью нового/переписанного модуля с REFACTOR по поверхности → не закрывать apply/`/review` без simplifier или явного waive. Вынос не удаляет три carve-out слияния `bsl-write-guard` из S2.2 (JSDoc/шапка метода, контекст apply/review, Mechanical Mode) и правило «post-reviewer fixes только через writer». `1c-utility-agents.mdc` и sidecar не заменяют якорь (D6)
- [ ] S2.11 Вынести таблицу шагов § WRITER PIPELINE из `1c-agent-delegation.mdc` в `.cursor/rules/1c-writer-pipeline.mdc`, оставив в delegation однострочный поток `writer → ReadLints → … → reviewer`, который нужен до появления `.bsl` в контексте (D6)
- [ ] S2.12 Передать статус «единственный эталон сквозного потока» файлу `.cursor/rules/1c-writer-pipeline.mdc` и обновить обратные ссылки в `1c-writer-pipeline.mdc`, `.cursor/skills/review/SKILL.md`, `.cursor/skills/openspec-apply-change/SKILL.md` (указатель «SSOT: 1c-agent-delegation.mdc § АВТО-ИСПРАВЛЕНИЕ» заменить на «якорь минимума — 1c-agent-delegation.mdc; полная процедура — review/SKILL.md шаг 4.5»; apply-скилл ссылается на якорь, но не является единственным носителем), чтобы двух конкурирующих описаний порядка не было (Scenario «Потребители ссылаются на нового владельца», D6)

## 4. Индексы, ссылки и потребители порога

- [ ] S2.13 Добавить в `.cursor/rules/gate-dispatcher.mdc` строки-cue для разжалованных правил (`1c-xml-write-guard`, `command-skill-gate`, `command-session-persistence`, `context-strategy-gate`), чтобы оркестратор знал об их существовании и триггере
- [ ] S2.14 Обновить карту SSOT в `AGENTS.md` под новый состав правил (удалённые стабы, разжалованные гейты, новый владелец writer pipeline), чтобы навигация не вела на несуществующие якоря
- [ ] S2.15 Перевести в `AGENTS.md` ссылку на термины workflow с отсутствующего `openspec/project.md` на существующий `openspec/glossary.md`, чтобы ссылка перестала быть битой (D12)
- [ ] S2.16 Сопроводить в `AGENTS.md` и `1c-agent-delegation.mdc` § PROJECT PATHS ссылки на `openspec/project.md` пометкой «создаётся `/init-project`; в kit-репозитории отсутствует — блок путей в промптах агентов опускается», чтобы правило было выполнимым в самом kit (D12)
- [ ] S2.17 Заменить в `.cursor/docs/delivery-integrity.md` §7 устаревший ориентир «< ~50 KB» на порог ≤ 34 КБ, чтобы проверка целостности поставки ловила деградацию бюджета (Scenario «Порог целостности поставки актуален»)

## 5. Контроль результата

- [ ] S2.18 Составить в `reports/` таблицу «обязательство разжалованного файла → якорь в always-apply» по каждому разжалованному и удалённому правилу и убедиться, что непокрытых строк нет (Scenario «Обязательство-diff без непокрытых строк», D6)
- [ ] S2.19 Замерить фактический бюджет — сумму байт файлов `.cursor/rules/*.mdc` с `alwaysApply: true` плюс `AGENTS.md` — после всех правок среза, зафиксировать результат в `reports/`; заявленные дельты мер не суммировать (Scenario «Замер после диеты», методика замера D6)

## 6. Приёмка

- [ ] S2.accept Принять срез S2 — постоянный контекст дешевле, все гейты срабатывают как раньше:
  - **Primary (обязательно):** в чистом окне Cursor (Reload Window) выполнить четыре запроса подряд → команда из `.cursor/commands/` даёт первым действием чтение `SKILL.md`; запрос правки XML в `src/` даёт стоп с блокером; запрос анализа 3+ файлов даёт загрузку стратегии контекста; запрос правки `.bsl` даёт делегирование writer
  - Scenario «Замер после диеты» (опционально): сверить отчёт замера — фактическая сумма байт always-apply набора и `AGENTS.md` ≤ 34 КБ
  - Scenario «Обязательство-diff без непокрытых строк» (опционально): просмотреть таблицу обязательств — у каждой строки есть якорь в always-apply
  - Scenario «Потребители ссылаются на нового владельца» (опционально): поиск ссылок на эталон writer pipeline возвращает `1c-writer-pipeline.mdc` во всех трёх файлах-потребителях

<!-- slice-gate: в чистом окне все четыре сценария-триггера срабатывают, фактический бюджет always-apply ≤ 34 КБ -->

# Срез S3: Диета промпта reviewer

**Сценарий:** каждый вызов ревьювера обходится дешевле по контексту, а глубина проверок не падает: чек-листы не удалены, а вынесены в файлы, которые агент читает по типу задачи и отчитывается о прочитанном. Диета промпта архитектора (sidecar per-mode) — вне этой ЗНИ.
**Primary acceptance:** прогнать ревьювера на фрагментах BSL из `.cursor/docs/standard/std-06-code-modules.md` (тот же файл, что и до диеты) → отчёт содержит строку `Checklists read:` с перечнем прочитанных чек-листов, а состав находок сопоставим с сохранённым базовым прогоном.
**Приёмка:** ручной прогон ревьювера в сессии Cursor на фрагментах BSL из `.cursor/docs/standard/std-06-code-modules.md` + сверка с базовым отчётом; информационная база не требуется.
**Связь со spec:** Requirement «Бюджет промптов агентов» — Scenario «Полнота чек-листов после диеты», Scenario «Evidence-строка в отчёте».
**Зависимости:** S2.

## 1. Базовая линия

- [ ] S3.1 Прогнать `onec-code-reviewer` на фрагментах BSL из существующего файла поставки `.cursor/docs/standard/std-06-code-modules.md` (не создавать новый `.bsl`, не использовать `temp/`). Сохранить полный отчёт в `reports/` как базовую линию для сравнения находок (D7)
- [ ] S3.2 Составить в `reports/` инвентарь чек-листов и справочников промпта `.cursor/agents/onec-code-reviewer.md` до диеты (перечень пунктов по секциям), чтобы после переноса было с чем сверять полноту (D7)

## 2. Вынос чек-листов

- [ ] S3.3 Вынести чек-листы и справочные перечни из `.cursor/agents/onec-code-reviewer.md` в on-demand файлы `.cursor/docs/standard/reviewer-checks.md` и соседние, разложив их по типам задач, чтобы промпт перестал нести весь справочник в каждом вызове (D7)
- [ ] S3.4 Сократить `.cursor/agents/onec-code-reviewer.md` до ядра протокола (роль, входной контракт, DESIGN AUTHORITY & QUALITY DISPOSITION, фазы, формат отчёта с QualityFlag/Disposition, `prompt_contract_version`) и добавить правило самостоятельного чтения нужного чек-листа по типу задачи; DESIGN AUTHORITY не выносить в on-demand (D7, ADR-0003)

## 3. Проверяемость соблюдения

- [ ] S3.5 Добавить в секцию REPORT FORMAT промпта `.cursor/agents/onec-code-reviewer.md` обязательную evidence-строку `Checklists read: <перечень>`, чтобы соблюдение переноса читалось из отчёта (D7)
- [ ] S3.6 Записать в `.cursor/skills/review/SKILL.md`, что отсутствие строки `Checklists read:` — дефект отчёта того же класса, что отсутствие блока Linter Signals, чтобы пропуск чек-листов не проходил молча (D7)
- [ ] S3.7 Сверить инвентарь чек-листов до и после диеты и зафиксировать в `reports/` результат: ни один пункт не исчез, только переехал в on-demand файл (Scenario «Полнота чек-листов после диеты»)

## 4. Приёмка

- [ ] S3.accept Принять срез S3 — ревью стоит меньше на каждом вызове при неизменном покрытии:
  - **Primary (обязательно):** прогнать ревьювера на фрагментах BSL из `.cursor/docs/standard/std-06-code-modules.md` (тот же файл, что в базовом прогоне) → отчёт содержит строку `Checklists read:` с перечнем прочитанных чек-листов, состав находок сопоставим с базовым отчётом
  - Scenario «Полнота чек-листов после диеты» (опционально): просмотреть сверку инвентарей — каждый пункт исходного промпта найден в on-demand файле, удалённых пунктов нет

<!-- slice-gate: прогон ревьювера после диеты даёт строку `Checklists read:` и находки, сопоставимые с базовым прогоном -->

# Срез S4: Профили моделей

**Сценарий:** чат команд ведёт Grok 4; Fable 5 / GPT-5.6 / Opus 5 используются как модели субагентов, без переключения чата; ни один защитный гейт не смягчается.
**Primary acceptance:** в сессии Grok 4 extra high запустить команду workflow → чат остаётся на Grok; три конфликтных запроса («отвечай подробнее лимита», «не перепроверяй себя», «не читай стаб и полное тело») получают ответ в пользу базового свода.
**Приёмка:** ручная проверка в сессии Cursor (три конфликтных запроса + чтение созданных файлов), без информационной базы 1С.
**Связь со spec:** Requirement «Трёхуровневая пирамида профилей» — Scenario «Профиль активен для известной модели», Scenario «Бриф субагента учитывает профиль его модели», Scenario «Неизвестная модель»; Requirement «Граница MAY / MUST NOT» — Scenario «Конфликт „длина ответа против лимитов“», Scenario «Конфликт „не перепроверяй себя против предписанных проверок“», Scenario «Конфликт „lean context против стаб-полное тело“»; Requirement «Precedence» — Scenario «Профиль против overlay».
**Зависимости:** S2.

## 1. Роутер

- [ ] S4.1 Создать `.cursor/rules/model-adaptation.mdc` — роутер слоя профилей: селектор по self-knowledge модели чата, нормализация имени по семейству и мажорной версии (`cursor-grok-4.6-xhigh` → `grok4`, `claude-opus-5-thinking-high` → `opus5`), правило «незнакомая модель = профиля нет, вопрос пользователю не задаётся»; Grok 4.6→4.7 остаётся `grok4`, Grok 5 и чужое семейство не угадываются (D4)
- [ ] S4.2 Записать в `.cursor/rules/model-adaptation.mdc` precedence: указания пользователя → project overlay → активный профиль → базовый свод, с оговоркой, что список MUST NOT выигрывает при любом порядке чтения (D5)
- [ ] S4.3 Записать в `.cursor/rules/model-adaptation.mdc` формулу misread и область действия: профиль чата — у оркестратора; свой профиль оркестратор в брифы не копирует; при `Task` с Primary Fable/GPT/Opus MAY той модели можно учесть в intent-брифе без микрошагов (D5)
- [ ] S4.4 Записать в `.cursor/rules/model-adaptation.mdc`, что предписанные протоколом повторные проходы (независимый проход verify, spot-check отчётов субагентов, проверка расширений, повторный ревьювер после writer) профилем не снимаются — гасятся только самопридуманные перепроверки (D5)

## 2. Файлы профилей

- [ ] S4.5 Создать `.cursor/rules/model-grok4.mdc` — основной профиль оркестратора: MAY для семейства Grok 4 (прямая речь в пределах лимитов чата, thinking не выключать на extra high, не раздувать план); MUST NOT как у остальных (D1, D4, D5)
- [ ] S4.6 Создать `.cursor/rules/model-fable5.mdc` с секцией MAY (длина и плотность ответа в пределах лимитов, нарратив, глубина планирования, охота к делегированию, evidence-аудит) для модели семейства Fable 5 (D4, D5)
- [ ] S4.7 Создать `.cursor/rules/model-gpt56.mdc` с секцией MAY для модели семейства GPT-5.6 и явным carve-out: принцип «не читать индекс и детальный файл вместе» не распространяется на конструкцию kit «стаб → полное тело» (`chat-output-budget` → `chat-output-budget-full`, `gate-dispatcher` → файл гейта, командный вход → `templates/*.md` при обрезке) (D4)
- [ ] S4.8 Создать `.cursor/rules/model-opus5.mdc` с секцией MAY для модели семейства Opus 5: короткий отчёт с исхода в первом предложении, один преамбульный абзац перед инструментами, запрет субагента «проверить мой дифф», thinking не выключать (D4, D5)
- [ ] S4.9 Добавить в четыре файла профилей секцию MUST NOT с перечнем неослабляемых гейтов: запрет прямой правки BSL и XML оркестратором, LINT GATE, обязательность ревьювера, HALT-триггеры, гейты метаданных / форм / дизайна / архитектуры, лимиты итераций, лимиты и HALT-список контракта чата, граница chat-facing (в чат не копировать имена субагентов, скиллов, гейтов, Schema) (D5, ADR-0001)
- [ ] S4.10 Записать в четырёх профилях разграничение «длина против лимита»: профиль настраивает длину и плотность внутри лимитов таблицы `chat-output-budget.mdc`; сами лимиты, HALT-список жаргона и обязательные блоки вне охвата профиля (D5)

## 3. Точка входа и видимость

- [ ] S4.11 Добавить в `AGENTS.md` стаб-строку: слой адаптации под модель; рекомендуемый чат оркестратора — Grok 4; Fable/GPT/Opus — Primary субагентов, не требование к чату команд; ссылка на роутер `model-adaptation.mdc` (D1, D4)
- [ ] S4.12 Добавить в `.cursor/skills/openspec-status/SKILL.md` строку о текущем активном профиле модели в снимок состояния, чтобы пользователь видел режим без отдельной команды (черновое решение Open Question, D4)

## 4. Приёмка

- [ ] S4.accept Принять срез S4 — чат команд на Grok 4, дорогие модели у субагентов, гейты не ослабляются:
  - **Primary (обязательно):** в сессии Grok 4 extra high запустить команду workflow → чат остаётся на Grok; три конфликтных запроса («отвечай подробнее лимита», «не перепроверяй себя», «не читай стаб и полное тело») получают ответ в пользу базового свода
  - Scenario «Бриф субагента учитывает профиль его модели» (опционально): бриф архитектора с Primary Opus 5 содержит MAY opus5 (короткий отчёт, без «проверь мой дифф»), без микрошагов
  - Scenario «Профиль активен для известной модели» (опционально): в сессии Grok 4 оркестратор применяет `grok4`, определив модель самостоятельно
  - Scenario «Неизвестная модель» (опционально): в сессии модели без файла профиля работает базовый свод, вопрос пользователю не задаётся, «похожий» профиль не применяется
  - Scenario «Профиль против overlay» (опционально): при расхождении указания профиля и project overlay действует overlay

<!-- slice-gate: чат команд на Grok 4, четыре профиля не ослабляют гейты -->

# Срез S5: Усиление делегирования

**Сценарий:** обследование кода 1С всегда уходит проектному агенту-обследователю, а не встроенному; брифы субагентам описывают цель и критерий готовности вместо микрошагов; после двух неудач подряд оркестратор перестаёт крутить одно и то же делегирование.
**Primary acceptance:** в чистом окне Cursor попросить обследовать поведение по путям `src/**/*.bsl` → делегирование уходит агенту `onec-code-explorer`, встроенный explore не вызывается, а бриф содержит цель, ограничения, scope и критерий готовности.
**Приёмка:** ручная проверка в чистом окне Cursor (запрос на обследование + чтение шаблонов) и замер бюджета по файлам; без информационной базы 1С.
**Связь со spec:** Requirement «Запрет built-in Explore для 1С-кода» — Scenario «Обследование BSL при недоступном кастомном агенте», Scenario «Не-1С файлы»; Requirement «Intent-брифы субагентам» — Scenario «Бриф writer в intent-формате»; Requirement «Эскалация после двух неудач» — Scenario «Два неудачных прохода»; Requirement «Охота к делегированию» — Scenario «Профиль не запускает проверяющего субагента»; Requirement «Coverage-first брифы reviewer» — Scenario «Полное ревью с фильтром в отчёте»; Requirement «Бюджет постоянного контекста» — Scenario «Контрольный замер после дописывания delegation».
**Зависимости:** S2.

## 1. Маршрутизация обследования

- [ ] S5.1 Записать в `.cursor/rules/1c-agent-delegation.mdc` запрет встроенного `subagent_type: "explore"` и `generalPurpose` для обследования 1С-контента (`src/**`, `cfe/**`, `.bsl`, XML метаданных) и правило «молчаливый переход на встроенный агент — дефект, оркестратор сообщает пользователю» (D8)
- [ ] S5.2 Записать в `.cursor/skills/context-strategy/SKILL.md` маршрутизацию: 1С-контент — только `onec-code-explorer`; markdown, отчёты и файлы данных — generic explore допустим, чтобы не-1С сценарии не попали под запрет (D8)

## 2. Формат брифов

- [ ] S5.3 Добавить в `.cursor/skills/1c-agent-patterns/SKILL.md` и ролевые файлы шаблон intent-брифа с полями «цель / ограничения / scope / критерий готовности» вместо перечня микрошагов реализации (D9)
- [ ] S5.4 Записать в шаблонах промптов `.cursor/skills/1c-agent-patterns/` явный запрет просить субагента пересказать ход внутренних рассуждений и добавлять указания вида «не думай долго», чтобы шаблоны не провоцировали извлечение рассуждений (D9)
- [ ] S5.5 Переписать шаблон брифа ревьювера в `.cursor/skills/1c-agent-patterns/` под coverage-first: бриф требует полного покрытия проверок, приоритизация делается в отчёте, а не сужением охвата (Scenario «Полное ревью с фильтром в отчёте», D9)

## 3. Эскалация

- [ ] S5.6 Записать в `.cursor/rules/1c-agent-delegation.mdc` правило эскалации: после двух подряд непригодных результатов субагента на ясной постановке третье делегирование той же задачи не выполняется — оркестратор решает сам в пределах своих guard-ограничений или ставит вопрос пользователю; существующие лимиты итераций не меняются (Scenario «Два неудачных прохода», D9)
- [ ] S5.7 Записать в `.cursor/rules/1c-agent-delegation.mdc`, что профиль модели может менять охоту к параллельным независимым трекам, но не уполномочивает субагента «проверить работу оркестратора»; критерии каталога агентов и запрет built-in Explore профилем не меняются (Scenario «Профиль не запускает проверяющего субагента», D5)

## 4. Контроль бюджета

- [ ] S5.8 Повторить замер бюджета — сумму байт файлов `alwaysApply: true` плюс `AGENTS.md` — после дописывания текста в `1c-agent-delegation.mdc` и зафиксировать в `reports/`, что порог ≤ 34 КБ не превышен (Scenario «Контрольный замер после дописывания delegation», методика замера D6)

## 5. Приёмка

- [ ] S5.accept Принять срез S5 — обследование 1С не утекает во встроенного агента, брифы дают цель и критерий готовности:
  - **Primary (обязательно):** в чистом окне попросить обследовать поведение по путям `src/**/*.bsl` → делегирование уходит агенту `onec-code-explorer`, встроенный explore не вызывается, бриф содержит цель, ограничения, scope и критерий готовности
  - Scenario «Обследование BSL при недоступном кастомном агенте» (опционально): при недоступности `onec-code-explorer` оркестратор останавливается и сообщает пользователю, встроенный explore не вызывается
  - Scenario «Не-1С файлы» (опционально): запрос анализа markdown-отчёта допускает generic explore согласно стратегии контекста
  - Scenario «Бриф writer в intent-формате» (опционально): промпт writer по шаблону содержит цель, ограничения, scope и критерий готовности без микрошагов реализации
  - Scenario «Профиль не запускает проверяющего субагента» (опционально): в правилах делегирования записан запрет субагента «проверить мой дифф»

<!-- slice-gate: запрос обследования 1С-кода уходит проектному агенту-обследователю с intent-брифом, бюджет после правок delegation ≤ 34 КБ -->

# Срез S6: Гигиена свода

**Сценарий:** on-demand правило само сообщает, когда его надо загрузить; выбор маршрута для типовой задачи не требует чтения полного справочника; опасные правки (контракт публичной процедуры, транзакции, права) не проскакивают через облегчённый режим; в поставке нет мёртвых файлов и ссылок на них.
**Primary acceptance:** запросить у оркестратора быструю правку, меняющую контракт публичной `Экспорт`-процедуры → задача маршрутизируется в полный цикл независимо от облегчённого режима, с явной ссылкой на promotion trigger.
**Приёмка:** ручная проверка в сессии Cursor (сценарный запрос + поиск ссылок по репозиторию), без информационной базы 1С.
**Связь со spec:** Requirement «Самоописание триггера on-demand правил» — Scenario «Правило читается изолированно»; Requirement «Decision shortcut в triage» — Scenario «Типовая задача»; Requirement «Safety floor и promotion triggers» — Scenario «Правка контракта Экспорт-процедуры в Light Mode»; Requirement «Удаление рудиментов» — Scenario «Ссылки после удаления».
**Зависимости:** S2, S4, S5.

## 1. Самоописание правил

- [ ] S6.1 Добавить шапку «Когда загружать» первой содержательной строкой в топ-10 on-demand правил `.cursor/rules/` по размеру и частоте использования (включая разжалованные в S2 `1c-xml-write-guard`, `command-skill-gate`, `command-session-persistence`, `context-strategy-gate` и созданные в S4 профили), чтобы правило отвечало на вопрос о своём триггере без индекса (D10)
- [ ] S6.2 Записать в `.cursor/rules/gate-dispatcher.mdc` и карте SSOT `AGENTS.md` принцип «строка индекса — routing cue, единственный источник триггера — frontmatter самого файла», чтобы индекс не считался авторитетным (D10)

## 2. Ускорение маршрутизации

- [ ] S6.3 Добавить в начало `.cursor/rules/task-triage.mdc` decision shortcut до четырёх строк, маршрутизирующий типовые задачи, оставив полный справочник для спорных случаев (D10)

## 3. Пол безопасности

- [ ] S6.4 Добавить в `.cursor/rules/1c-halt-triggers.mdc` секцию safety floor — перечень проверок, которые не ослабляет ни один режим: обязательный ревьювер даже в исключениях, ReadLints после каждой правки, синтаксический контроль (D10)
- [ ] S6.5 Добавить в `.cursor/rules/1c-halt-triggers.mdc` формализованные promotion triggers (транзакционные пути, изменение контракта публичной `Экспорт`-процедуры, RLS, заимствованные объекты расширений, подписки и регламентные задания) и формулу «quick-fix снижает накладные расходы, не глубину проверки» (D10)

## 4. Удаление рудиментов

- [ ] S6.6 Перенести `.cursor/agents/CHANGELOG.md` в `.cursor/docs/`, чтобы каталог агентов содержал только промпты агентов (D10)
- [ ] S6.7 Удалить alias-стабы `.cursor/commands/opsx-ff.md` и `.cursor/commands/opsx-continue.md` и убрать их из списка команд в `AGENTS.md`, чтобы устаревшие входы не предлагались пользователю (D10)
- [ ] S6.8 Удалить `.cursor/rules/openspec-sessions.mdc` и снять упоминание legacy-сессий в `.cursor/rules/session-discipline.mdc`; read-only fallback `openspec/sessions/` в скиллах и командах оставить как есть — его отрезание вне scope (D10, Non-Goals)
- [ ] S6.9 Проверить поиском по `.cursor/**`, `AGENTS.md`, `openspec/**`, что ссылок на удалённые в этом change пути (`opsx-ff`, `opsx-continue`, `openspec-sessions`, `bsl-write-guard`, `conversational-discipline`, `orchestrator-as-navigator`, `.cursor/agents/CHANGELOG.md`) не осталось (Scenario «Ссылки после удаления»)
- [ ] S6.10 Финализировать карту SSOT и список команд в `AGENTS.md` на итоговом составе файлов после всех срезов, чтобы поставка описывала себя корректно

## 5. Приёмка

- [ ] S6.accept Принять срез S6 — правила самоописывают триггер, опасные правки защищены явным полом:
  - **Primary (обязательно):** запросить быструю правку, меняющую контракт публичной `Экспорт`-процедуры → задача маршрутизируется в полный цикл независимо от облегчённого режима, с явной ссылкой на promotion trigger
  - Scenario «Правило читается изолированно» (опционально): открыть любое из топ-10 on-demand правил без индекса — первая содержательная строка отвечает, когда файл должен быть загружен
  - Scenario «Типовая задача» (опционально): для очевидной задачи маршрут выбирается по короткому классификатору без чтения полного справочника triage
  - Scenario «Ссылки после удаления» (опционально): поиск по репозиторию не находит ссылок на удалённые в этом change пути, список команд в `AGENTS.md` актуален

<!-- slice-gate: правка контракта публичной `Экспорт`-процедуры уходит в полный цикл, ссылок на удалённые пути не осталось -->
