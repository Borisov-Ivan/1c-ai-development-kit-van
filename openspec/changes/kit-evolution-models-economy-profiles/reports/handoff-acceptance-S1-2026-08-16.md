## Срез S1 — передача на приёмку: kit-evolution-models-economy-profiles

**Change:** kit-evolution-models-economy-profiles
**Прогресс:** 11/12 задач среза S1 [x] (приёмка S1.accept: открыта)

### 1. Что реализовано

Таблица ролей субагентов переведена на живые модели текущей сборки: обычный архитектор — Opus 5, ревьюер — Gemini, упрощение кода — Composer Fast; writer и explorer идут на модели чата. Цепочка вызова сокращена до двух шагов (Primary, затем без указания модели). Список допустимых моделей больше не зашит в файл: перед первым вызовом оркестратор сверяет слаг с описанием инструмента Task. Независимый разбор постановки идёт на самой дорогой модели только если её слаг есть в списке сборки; иначе — на Opus 5 с одной строкой в чат. Дублирующий слаг убран из правила архитектора.

- [x] S1.1 — таблица Primary; файл: `.cursor/rules/model-selection.mdc`
- [x] S1.2 — двухшаговые цепочки; файл: `.cursor/rules/model-selection.mdc`
- [x] S1.3 — целостность цепочки: стоп после второго сбоя; файл: `.cursor/rules/model-selection.mdc`
- [x] S1.4 — самосверка списка моделей; файл: `.cursor/rules/model-selection.mdc`
- [x] S1.5 — поведение при расхождении без угадывания; файл: `.cursor/rules/model-selection.mdc`
- [x] S1.6 — убрано ложное утверждение про inherit; файл: `.cursor/rules/model-selection.mdc`
- [x] S1.7 — слаг не дублируется в правиле архитектора; файл: `.cursor/rules/architect-gate.mdc`
- [x] S1.8 — чеклист вызова субагента; файл: `.cursor/rules/tool-name-guard.mdc`
- [x] S1.9 — поиск мёртвых слагов в рантайм-файлах
- [x] S1.10 — описания цепочки в скиллах; файлы: `openspec-verify-change/SKILL.md`, `1c-agent-patterns`
- [x] S1.11 — закрытая таблица эскалации; файл: `.cursor/rules/model-selection.mdc`

### Карта правок (перед тестом)

Полная карта: `openspec/changes/kit-evolution-models-economy-profiles/reports/code-map.md`

- Таблица ролей и самосверка — `.cursor/rules/model-selection.mdc`:16-83
- Правило архитектора больше не хранит свой слаг — `.cursor/rules/architect-gate.mdc`:95-99
- Чеклист вызова субагента — два шага — `.cursor/rules/tool-name-guard.mdc`:22-88
- Независимый разбор постановки в скилле проверки — `.cursor/skills/openspec-verify-change/SKILL.md`:199-242

### 2. Что проверить СЕЙЧАС

**Primary acceptance**

1. В этой или новой сессии Cursor запустите делегирование архитектору с моделью `claude-opus-5-thinking-high` — вызов должен пройти без ошибки выбора модели и без молчаливого перехода на другую модель.
2. В тексте правил независимый разбор постановки указывает самую дорогую модель только если её слаг есть в списке сборки; иначе — Opus 5 и одна строка, без вызова с отсутствующим слагом. Декомпозиция срезов — на Opus, не на самой дорогой модели.

Опционально: остальные сценарии — см. `tasks.md` у S1.accept.

### 3. Следующие задачи

| Задача | Действие | Тип | Исполнитель | Зависит от | Статус |
|--------|----------|-----|-------------|------------|--------|
| `S1.accept` | Принять срез — делегирование на живые модели | Ручной тест | пользователь | S1.1–S1.11 | [ ] |
| `S2.1` | Перенести минимум трёх session-правил в session-discipline | Проверка | оркестратор | S1.accept | [ ] |
| `S2.2` | Принять запрет прямой правки BSL в delegation | Проверка | оркестратор | S1.accept | [ ] |

### 4. Как вернуться

`/opsx:apply kit-evolution-models-economy-profiles` — новая сессия начнётся с запроса вердикта по срезу S1.

### 5. Blockers

Нет.

### 7. Short-cut

Если уже проверено и принято — напишите обычной фразой («принято», «срез S1 принят»), отмечу без полного handoff.
