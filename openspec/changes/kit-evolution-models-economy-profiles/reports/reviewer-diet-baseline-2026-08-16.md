# Базовая линия ревьювера — до диеты промпта (S3.1)

**Change:** kit-evolution-models-economy-profiles
**Дата:** 2026-08-16
**Источник:** прогон ревьювера на фикстурах std-06 + три модуля PavDO (разрешение заказчика).
**Примечание:** прогон стартовал до записи диеты промпта v4.1; строка Checklists read в отчёте есть, но это не заменяет обязательный повторный прогон после диеты (S3.accept).

## === MAIN REPORT ===

```yaml
File(s):
  - .cursor/docs/standard/std-06-code-modules.md (фрагменты ```bsl)
  - C:\GitHub\PavDO\src\ДО2\cfe\ДО21_Павлик\CommonModules\pavОбщийМодуль\Ext\Module.bsl
  - C:\GitHub\PavDO\src\ДО2\cfe\ДО21_Павлик\CommonModules\РаботаСБизнесПроцессами\Ext\Module.bsl
  - C:\GitHub\PavDO\src\ДО2\cfe\ДО21_Павлик\CommonModules\pavДополнительныеФункции\Ext\Module.bsl
Status: FAIL
prompt_contract_version: 4
Checklists read: bsl-antipatterns.mdc (индекс AP); marker-canon.md; 1c-coding-standards.md; reviewer-checks.md (кат. 1–15, Phase 0/1b/1c/1d); 1c-vendor-standards/SKILL.md; std-06-code-modules.md; std-08-security.md; std-03-event-handlers.md; 1c-extensions/SKILL.md; 1c-bsp/SKILL.md
Linter Signals: unavailable (модули PavDO вне workspace kit; фрагменты std-06 — markdown)
Naming Signals: skipped (фикстура); Phase 1c выполнен доменным тестом по символам в scope
Comment Hygiene Signals: skipped; Phase 1d — семантический проход Category 9 + AP-054
Phase 0: 4 findings (2 HIGH, 2 MEDIUM)
Попытка Audit: 0 blocks checked, 0 findings
Identity / Hardcode Audit: 0 literals, 0 findings; completeness 0=0
AP Registry: 22 findings (CRITICAL: std-08-Выполнить, integrity-write-on-cancel, contract-signature; HIGH: privileged, AP-016, AP-014, lock, AP-047, AP-012, AP-024, AP-038, AP-004, AP-007, unused-export; MEDIUM: AP-036, AP-037, AP-025, AP-031, AP-006, mixed-responsibilities)
Release-hygiene: 6 findings (AP-040×3 файла, AP-043, AP-044, AP-054)
Top-risk items:
  1. std-08 Выполнить без безопасного режима — pav_ФормаЗадачиПередЗаписьюНаСервере (risk 7.41)
  2. Запись предмета при Отказ=Истина — та же процедура (risk 6.08)
  3. Привилегированный режим без снятия — та же процедура (risk 5.56)
Overall: Эталон std-06 даёт ожидаемые находки на «неправильном» фрагменте; модуль задач — сломанный контракт вызова плюс выполнение произвольного кода и запись чужого объекта; остальные два модуля — гигиена, отладочный вывод и мёртвый экспорт.
```

### Findings

[std-08-Выполнить] CRITICAL · functional · risk=7.41 (public-api · security · normal · 0.95)
File: `C:\GitHub\PavDO\src\ДО2\cfe\ДО21_Павлик\CommonModules\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 52 (на момент ревью)
Procedure: pav_ФормаЗадачиПередЗаписьюНаСервере
Anchor: `Выполнить СтрНЗ.Скрипт;`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: На сервере исполняется произвольный текст из поля `Скрипт` без безопасного режима и без `ОбщегоНазначения.ВыполнитьВБезопасномРежиме`. Вызов доступен с формы задачи (`&НаСервере`).
Root cause: Обход ограничения std-08 «Ограничения на использование Выполнить и Вычислить на сервере»; скрипт читается из данных, а не из проверенного справочника администратора.
Counterfactual: Не вызывать `Выполнить` на сервере. Выполнить заранее проверенный метод объекта/модуля или `ОбщегоНазначения.ВыполнитьВБезопасномРежиме` после аудита источника.
Remediation: Удалить `Выполнить`; заменить на явный метод конфигурации либо безопасный API БСП; источник скрипта закрыть правами записи только для администратора безопасности.
Evidence: std-08-security.md § «Ограничения на использование Выполнить и Вычислить на сервере»; caller `BusinessProcesses\Согласование\Forms\ФормаЗадачиИсполнителя\Ext\Form\Module.bsl:14`.

[integrity-write-on-cancel] CRITICAL · functional · risk=6.08 (cross-module · data-corruption · normal · 0.95)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 97 (на момент ревью)
Procedure: pav_ФормаЗадачиПередЗаписьюНаСервере
Anchor: `Если ЕстьИзм тогда` / `ОбПредмет.Записать();`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: После цикла при `Отказ = Истина` (незаполненный обязательный доп. реквизит) предмет всё равно записывается, если ранее выставлялся `ЕстьИзм`. Задача откатывается, предмет — нет.
Root cause: Запись вынесена из-под условия `Не Отказ`; `Прервать` выходит из цикла, но не блокирует `Записать`.
Counterfactual: `Если ЕстьИзм И Не Отказ Тогда ОбПредмет.Записать();` либо не писать предмет в обработчике записи задачи.
Remediation: Связать запись предмета с успешным прохождением проверок; запись связанных данных — в транзакции записи задачи / после неё.

[std-08-privileged] HIGH · functional · risk=5.56 (public-api · security · normal · 0.95)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 11 (на момент ревью)
Procedure: pav_ФормаЗадачиПередЗаписьюНаСервере
Anchor: `УстановитьПривилегированныйРежим(Истина);`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Привилегированный режим включается безусловно и не выключается. Под ним — `ПолучитьОбъект`, `Записать`, `Выполнить`. До конца серверного вызова RLS не действует.
Root cause: Нарушение std-08 § «Использование привилегированного режима» п. 3.3 (включить точечно, выключить сразу после действия).
Counterfactual: Включить режим только вокруг чтения служебных настроек; запись предмета и выполнение скрипта — без привилегированного режима либо с `УстановитьПривилегированныйРежим(Ложь)` сразу после узкого блока.
Remediation: Сузить охват; гарантировать парный сброс (в т.ч. при раннем `Прервать`).

[contract-signature] CRITICAL · functional · risk=4.94 (public-api · user-feedback · normal · 0.95)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 2–3 (на момент ревью)
Procedure: pav_ФормаЗадачиПередЗаписьюНаСервере
Anchor: `//&Перед("ФормаЗадачиПередЗаписьюНаСервере")` / `Процедура pav_ФормаЗадачиПередЗаписьюНаСервере(Форма, Отказ, ТекущийОбъект, ПараметрыЗаписи)`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Аннотация `&Перед` закомментирована — перехват типового метода не работает. Процедура без `Экспорт`, но форма вызывает её как экспорт модуля. Сигнатура 4 параметра (`ТекущийОбъект`, `ПараметрыЗаписи`), вызов с формы — 5 аргументов (`ТекстСообщения`, `ИмяДопРеквизита`, `ИзмЗадача`). Параметры тела не используются; `ИзмЗадача` присваивается и никуда не возвращается.
Root cause: Смешение перехвата `&Перед` и ручного вызова; копия сигнатуры не согласована с caller.
Counterfactual: Либо рабочий `&Перед("ФормаЗадачиПередЗаписьюНаСервере")` с типовой сигнатурой, либо экспорт с сигнатурой caller (как в `УправлениеСвойствами.pav_ФормаЗадачиПередЗаписьюНаСервере`) и один владелец логики.
Remediation: Восстановить один контракт; удалить мёртвый путь; синхронизировать форму и общий модуль.
Evidence: caller `ФормаЗадачиИсполнителя\Ext\Form\Module.bsl:14`; экспорт-двойник `УправлениеСвойствами\Ext\Module.bsl:649`. [BOUNDARY_EXCEPTION]: контракт читался по caller вне scope, finding на объявлении в scope.

[std-03-side-write] HIGH · functional · risk=4.79 (cross-module · data-corruption · normal · 0.95)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 20, 99 (на момент ревью)
Procedure: pav_ФормаЗадачиПередЗаписьюНаСервере
Anchor: `ОбПредмет = Форма.Объект.Предметы.Получить(0).Предмет.ПолучитьОбъект();` / `ОбПредмет.Записать();`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: ARCHITECTURE
Issue: В точке «перед записью задачи» загружается и пишется другой объект (предмет). Нет проверки `ОбменДанными.Загрузка`, нет блокировки, вложенная запись.
Root cause: Побочный эффект вынесен в обработчик чужого объекта (std-03: связанные данные — `ПриЗаписи` своего объекта / отдельный сервис, не «чужой» ПередЗаписью).
Counterfactual: Копирование доп. реквизитов — в модуле предмета или в явном прикладном методе после успешной записи задачи, в одной транзакции.
Remediation: Убрать `Записать` чужого объекта из этого обработчика.

[AP-016] HIGH · functional · risk=3.93 (cross-module · data-write · normal · 0.95)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 3 (на момент ревью)
Procedure: pav_ФормаЗадачиПередЗаписьюНаСервере
Anchor: `Если pavДополнительныеФункцииВызовСервера.ДополнительнаяФункцияВключена("ИспЗаполнениеРеквПредметаВЗадаче")`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Тело почти дословно повторяет `УправлениеСвойствами.pav_ФормаЗадачиПередЗаписьюНаСервере`, но с другой сигнатурой и без экспорта. Два владельца одной фичи.
Root cause: Копипаст вместо делегирования (AP-016 / AP-047).
Counterfactual: Один экспорт в модуле свойств; этот модуль либо перехват `&Перед` с вызовом того метода, либо пустой.
Remediation: Удалить дубль; оставить один API.

[AP-014] HIGH · functional · risk=3.42 (module-local · data-write · normal · 0.95)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 43–46 (на момент ревью)
Procedure: pav_ФормаЗадачиПередЗаписьюНаСервере
Anchor: `Форма.Объект.Ссылка.Исполнитель` / `Форма.Объект.Ссылка.РольИсполнителя.Владелец`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Реквизиты читаются через точку от ссылки (полная загрузка задачи + второй hop `Владелец`). На форме уже есть `Форма.Объект.Исполнитель` / `РольИсполнителя`; данные в форме и в БД могут различаться до записи.
Root cause: Обход данных формы и БСП `ОбщегоНазначения.ЗначениеРеквизитаОбъекта` / `ЗначенияРеквизитовОбъекта`.
Counterfactual: `Форма.Объект.Исполнитель`; для владельца роли — `ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Форма.Объект.РольИсполнителя, "Владелец")`.
Remediation: Убрать разыменование `Ссылка.`; один запрос/БСП на нужные поля.
Также: `Свойство.Заголовок` (стр. 76) — ещё одно разыменование ссылки доп. реквизита.

[cat-13-lock] HIGH · functional · risk=3.38 (cross-module · data-corruption · normal · 0.70)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 20–99 (на момент ревью)
Procedure: pav_ФормаЗадачиПередЗаписьюНаСервере
Anchor: `Предмет.ПолучитьОбъект();` … `ОбПредмет.Записать();`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Чтение объекта и запись без `БлокировкаДанных` / ответственного чтения. Параллельная запись предмета даст потерянные обновления или исключение.
Root cause: reviewer-checks §13: read-then-write без блокировки.
Counterfactual: Управляемая блокировка по ссылке предмета до чтения, затем запись.
Remediation: `БлокировкаДанных` по `Предмет` либо отказ от записи здесь.

[AP-047] HIGH · functional · risk=3.15 (module-local · security · normal · 0.70)
File: `...\pavОбщийМодуль\Ext\Module.bsl`
Line: 138 (на момент ревью)
Procedure: ДобавитьСсылкиИнтегрированныхСистемНаФорму
Anchor: `Если РольДоступна("ПолныеПрава") Тогда`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Проверка полной роли платформенным `РольДоступна("ПолныеПрава")` вместо БСП `Пользователи.ЭтоПолноправныйПользователь()` / `ПравоДоступа` (std-08 п. 3–4).
Root cause: Локальная подмена владельца проверки прав (БСП «Пользователи»).
Counterfactual: `Пользователи.ЭтоПолноправныйПользователь()` либо право на конкретный регистр связей ИС.
Remediation: Заменить вызов; не завязывать UI на имя роли.

[AP-012] HIGH · functional · risk=3.14 (public-api · user-feedback · rare · 0.95)
File: `...\pavДополнительныеФункции\Ext\Module.bsl`
Line: 72–73, 87, 99, 101 (на момент ревью)
Procedure: pavВыводНастроекФормыНаСервере
Anchor: `Сообщить("КлючСохраненияПоложенияОкна=" + КлючСохраненияПоложенияОкна);`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Пять вызовов `Сообщить()` с сырыми строками (опечатка «настоек»). Команда формы реально вызывает процедуру. Нет `НСтр`, нет БСП.
Root cause: Отладочный дамп оставлен как пользовательский вывод (AP-012 + AP-025; поддерживающий AP-024).
Counterfactual: `ОбщегоНазначения.СообщитьПользователю(НСтр("ru = '…'"))` либо запись в журнал; для диагностики — отдельная обработка, не кнопка рабочих форм.
Remediation: Убрать `Сообщить`; заменить API и локализацию либо удалить команду с рабочих форм.

[AP-024] HIGH · functional · risk=3.14 (public-api · user-feedback · rare · 0.95)
File: `...\pavДополнительныеФункции\Ext\Module.bsl`
Line: 60–102 (на момент ревью)
Procedure: pavВыводНастроекФормыНаСервере
Anchor: `//Ключ объекта=Справочник.ВнутренниеДокументы.Форма.ФормаЭлемента/...`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Процедура — дамп хранилища настроек с закомментированным выводом консоли. Это отладочный код в поставке.
Root cause: AP-024 stub/debug в релизе; std-06 п. 3.
Counterfactual: Удалить процедуру и вызовы с форм либо вынести во внешнюю обработку администратора.
Remediation: Удалить дамп и закомментированные образцы вывода.

[AP-024] HIGH · functional · risk=2.14 (module-local · cosmetic · one-off · 0.95)
File: `.cursor/docs/standard/std-06-code-modules.md`
Line: 21–23 (на момент ревью)
Procedure: ПередУдалением
Anchor: `// Если Истина Тогда Сообщение("Для отладки"); КонецЕсли;`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Design ref: эталон S3.1 (блок «Неправильно»)
design-endorsed: false
Type: CODE
Issue: В эталонном «неправильном» фрагменте — отладка `Если Истина`, `Сообщение`, закомментированный код. Для базовой линии состава находок фиксируется как AP-024 (и пересечение с AP-043, Category 9).
Root cause: Учебный антипример std-06 п. 3.
Counterfactual: Пустое тело обработчика не оставлять; отладочный код удалить (как пишет сам стандарт в «Правильно»).
Remediation: Для сравнения диеты: эта находка должна сохраниться на том же фрагменте.

[unused-export] HIGH · functional · risk=2.78 (public-api · cosmetic · one-off · 0.95)
File: `...\pavДополнительныеФункции\Ext\Module.bsl`
Line: 6 (на момент ревью)
Procedure: ПереходКДокументам21_ПриСозданииНаСервере
Anchor: `Процедура ПереходКДокументам21_ПриСозданииНаСервере(Форма) Экспорт`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Экспорт нигде в расширении не вызывается (grep по `*.bsl`). Мёртвый публичный API. Страж повторного создания проверяет элемент `КнопкаПереход21`, а создаются реквизит `КнопкаПереход21` и кнопка `ОткрытьДокумент21` — повторный вызов упадёт на `ИзменитьРеквизиты`.
Root cause: Category 15 unused export + AP-016 «на всякий случай» с неверным именем.
Counterfactual: Вызвать с нужных `ПриСозданииНаСервере` либо удалить; страж — `ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Форма, "КнопкаПереход21")` или `Элементы.Найти("ОткрытьДокумент21")`.
Remediation: Подключить или удалить; исправить страж до любого вызова.

[AP-038] HIGH · functional · risk=2.10 (module-local · user-feedback · normal · 0.70)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 25–32 (на момент ревью)
Procedure: pav_ФормаЗадачиПередЗаписьюНаСервере
Anchor: `НЗ = ТабНЗ.НайтиСтроки(Новый Структура("Свойство", СтрСвойство.Свойство));` / `СвойстваПредмета.Найти(...)`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: В цикле по доп. реквизитам формы — линейный `НайтиСтроки` по ТЗ без индекса и `Найти` по массиву свойств предмета.
Root cause: Поиск в коллекции внутри цикла (AP-038).
Counterfactual: `Соответствие` свойство→строки настройки; индекс ТЗ или карта свойств предмета до цикла.
Remediation: Построить соответствие один раз, в цикле — O(1).

[AP-025] MEDIUM · style · risk=1.90 (module-local · user-feedback · normal · 0.95)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 76 (на момент ревью)
Procedure: pav_ФормаЗадачиПередЗаписьюНаСервере
Anchor: `ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Не заполнен доп.реквизит: " + Свойство.Заголовок, , СтрСвойство.ИмяРеквизитаЗначение);`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Пользовательская строка без `НСтр`; конкатенация; на сервере — `ОбщегоНазначенияКлиентСервер` вместо `ОбщегоНазначения.СообщитьПользователю`.
Root cause: AP-025; отклонение от БСП-модуля сообщений.
Counterfactual: `ОбщегоНазначения.СообщитьПользователю(НСтр("ru = 'Не заполнен дополнительный реквизит: %1'"), …)` с подстановкой заголовка, уже прочитанного через БСП, не через `.Заголовок` ссылки.
Remediation: `НСтр` + серверный метод БСП.

[AP-025] MEDIUM · style · risk=1.90 (module-local · user-feedback · normal · 0.95)
File: `...\pavОбщийМодуль\Ext\Module.bsl`
Line: 87–91, 144 (на момент ревью)
Procedure: ДобавитьСсылкиИнтегрированныхСистемНаФорму
Anchor: `Новый РеквизитФормы(..., "Узел ИС")` / `Кнопка.Заголовок = "Открыть Связи ИС";`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Заголовки колонок и кнопки без `НСтр` (у таблицы заголовок с `Нстр` есть).
Root cause: AP-025.
Counterfactual: Все синонимы UI через `НСтр("ru = '…'")`.
Remediation: Обернуть литералы.

[AP-025] MEDIUM · style · risk=1.90 (module-local · user-feedback · one-off · 0.95)
File: `...\pavДополнительныеФункции\Ext\Module.bsl`
Line: 20 (на момент ревью)
Procedure: ПереходКДокументам21_ПриСозданииНаСервере
Anchor: `НоваяКоманда.Заголовок = "2.1";`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Заголовок команды без `НСтр`.
Root cause: AP-025.
Counterfactual: `НоваяКоманда.Заголовок = НСтр("ru = '2.1'");`
Remediation: Обернуть литерал.

[AP-004] HIGH · functional · risk=1.68 (module-local · cosmetic · normal · 0.70)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 7 (на момент ревью)
Procedure: pav_ФормаЗадачиПередЗаписьюНаСервере
Anchor: `ТипЗнч(Форма.Объект.Ссылка) = Тип("ЗадачаСсылка.ЗадачаИсполнителя")`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Имя и `&Перед("ФормаЗадачиПередЗаписьюНаСервере")` фиксируют форму задачи исполнителя. Проверка типа — защита на известном контракте.
Root cause: AP-004; Knowledge: имя точки расширения, не Form.xml.
Counterfactual: Убрать `ТипЗнч`; оставить `ЗначениеЗаполнено` для новой ссылки, если это предметное условие.
Remediation: Удалить проверку типа либо явно документировать вызов с форм других типов (тогда контракт dynamic — тогда OK).

[AP-007] HIGH · functional · risk=1.68 (module-local · data-write · normal · 0.70)
File: `...\pavОбщийМодуль\Ext\Module.bsl`
Line: 6–7 (на момент ревью)
Procedure: НавигационныеСсылкиИнтегрированногоОбъекта
Anchor: `Узел = Новый Массив;`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Параметр `Узел` перезаписывается массивом всех узлов плана обмена. Без шапки не видно, что `Неопределено` = «все системы». Выборка плана без отбора по пометке удаления.
Root cause: AP-007 перезапись параметра + скрытое расширение охвата.
Counterfactual: Локальная `УзлыОтбора`; в шапке описать значение по умолчанию; не мутировать параметр.
Remediation: Локальная переменная; JSDoc; при необходимости отбор живых узлов.

[Phase0-KNOWLEDGE_DEFICIT] HIGH · functional · risk=1.68 (module-local · user-feedback · normal · 0.70)
File: `...\pavОбщийМодуль\Ext\Module.bsl`
Line: 38–41 (на момент ревью)
Procedure: НавигационныеСсылкиИнтегрированногоОбъекта
Anchor: `ЧастиИдентификатора[3], ЧастиИдентификатора[4], ЧастиИдентификатора[2], ЧастиИдентификатора[1], ЧастиИдентификатора[0]`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Формат `ИДВнешнегоОбъекта` как UUID с 5 сегментами не проверяется. Иной формат — исключение по индексу. Контракт поля регистра не зафиксирован в коде.
Root cause: Q3 PARTIAL: поле запроса есть, канон UUID — нет.
Counterfactual: Если `ЧастиИдентификатора.Количество() <> 5` — `ВызватьИсключение` с понятным текстом или пропуск строки с сообщением; лучше API навигационной ссылки внешней ИБ, если есть.
Remediation: Проверка длины/формата до индексов.

[AP-040] MEDIUM · release-hygiene · risk=1.52 (module-local · cosmetic · normal · 0.95)
File: `...\pavОбщийМодуль\Ext\Module.bsl`
Line: 1, 79, 104, 121, 150 (на момент ревью)
Procedure: [module-level] / ДобавитьСсылкиИнтегрированныхСистемНаФорму
Anchor: `// + Листов А.С. 28.03.2025 Вывод ссылок … [ID#65270] - разработка [ID#65585]`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Маркер `// +` / `// -` не из kit-whitelist (таблицы пусты → строгая гигиена). В комментарии номера заявок, слово «разработка», закомментированный код с GUID узла и отключёнными свойствами формы.
Root cause: AP-040 changelog + commented-out code; whitelist пуст.
Counterfactual: Удалить процессные комментарии и мёртвый код; при необходимости — каноническая пара `// +++` / `// ---` с доменной фразой (и тогда AP-053 на suffix).
Remediation: Вычистить строки 1, 79, 104, 121, 150.

[AP-040] MEDIUM · release-hygiene · risk=1.52 (module-local · cosmetic · normal · 0.95)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 2, 28, 64–67 (на момент ревью)
Procedure: pav_ФормаЗадачиПередЗаписьюНаСервере
Anchor: `//&Перед("ФормаЗадачиПередЗаписьюНаСервере")` / закомментированный `НайтиСтроки`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Закомментированная аннотация расширения и мёртвые фрагменты (видимость поля, повторный поиск имени реквизита).
Root cause: AP-040 / std-06 п. 3.
Counterfactual: Аннотацию либо включить, либо удалить; мёртвый код удалить.
Remediation: Удалить комментарии-заготовки.

[AP-040] MEDIUM · release-hygiene · risk=1.52 (module-local · cosmetic · normal · 0.95)
File: `...\pavДополнительныеФункции\Ext\Module.bsl`
Line: 14–16, 24, 28, 37, 63, 86–94 (на момент ревью)
Procedure: ПереходКДокументам21_ПриСозданииНаСервере / pavВыводНастроекФормыНаСервере
Anchor: `/////   КОМАНДЫ   //////////` / `// ******   Команда формы "2.1"    ***********`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Декоративные разделители (3+ `/` и `*`) — AI-typography AP-040; закомментированный код и дамп ключей настроек.
Root cause: Category 9 + AP-040.
Counterfactual: Удалить орнамент и commented-out; смысл — в имени процедуры.
Remediation: Вычистить.

[AP-044] LOW · release-hygiene · risk=0.57 (module-local · cosmetic · normal · 0.70)
File: `...\pavДополнительныеФункции\Ext\Module.bsl`
Line: 16, 28 (на момент ревью)
Procedure: ПереходКДокументам21_ПриСозданииНаСервере
Anchor: `// ******   Команда формы "2.1"` / `// ******   Реквизит формы "КнопкаПереход21"`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Комментарий пересказывает следующий оператор («команда», «реквизит») без «зачем».
Root cause: AP-044.
Counterfactual: Удалить; при необходимости пояснить, почему кнопка называется «2.1».
Remediation: Удалить нарратив (вместе с AP-040).

[AP-054] MEDIUM · style · risk=1.52 (module-local · cosmetic · normal · 0.95)
File: `...\pavОбщийМодуль\Ext\Module.bsl`
Line: 1 (на момент ревью)
Procedure: [module-level]
Anchor: `[ID#65270]` / `[ID#65585]`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Латиница `ID` в прозе комментария вне allow-list (не protocol HTTP/JSON, не backtick-идентификатор).
Root cause: Phase 1d семантический проход; AP-054. Пересекается с AP-040 на той же строке — править удалением строки, не «переводом ID».
Counterfactual: Нет процессных меток в модуле.
Remediation: Удалить строку (не переименовывать ticket в кириллицу).

[AP-043] MEDIUM · style · risk=1.14 (module-local · cosmetic · one-off · 0.95)
File: `.cursor/docs/standard/std-06-code-modules.md`
Line: 21–23 (на момент ревью)
Procedure: ПередУдалением
Anchor: `Процедура ПередУдалением(Отказ)` … тело только комментарий
Action: MUST_FIX
QualityFlag: none
Disposition: open
Design ref: эталон S3.1
design-endorsed: false
Type: CODE
Issue: После снятия комментариев исполняемых операторов нет. Обработчик пустой (AP-043 empty-body). Параметр `Отказ` не используется.
Root cause: std-06 п. 3 «Неправильно».
Counterfactual: Удалить обработчик из модуля (текст стандарта).
Remediation: Для диеты — находка должна остаться на этом блоке.

[AP-006] HIGH · functional · risk=1.79 (module-local · cosmetic · normal · 0.70)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 27 и 41 (на момент ревью)
Procedure: pav_ФормаЗадачиПередЗаписьюНаСервере
Anchor: `Если НЕ НЗ.Количество() = 0 тогда` … `Если НЗ.Количество() > 0 тогда`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: После `Иначе Продолжить` пустой `НЗ` в цикл не попадает; вторая проверка количества избыточна. Ветвь `Тогда` первой проверки пустая (закомментированное тело).
Root cause: AP-006 cake + leftover after comment-out.
Counterfactual: Один `Если НЗ.Количество() = 0 Тогда Продолжить; КонецЕсли;`
Remediation: Схлопнуть проверки; удалить пустую ветку.

[Phase0-DISPROPORTIONATE_COMPLEXITY] HIGH · functional · risk=2.07 (module-local · data-write · normal · 0.70)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 3–105 (на момент ревью)
Procedure: pav_ФормаЗадачиПередЗаписьюНаСервере
Anchor: `Процедура pav_ФормаЗадачиПередЗаписьюНаСервере`
Action: REFACTOR
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Q1: копирование доп. реквизитов в предмет размазано на ~100 строк и 6+ уровней `Если`/`Цикл`. Доменная сложность — фильтр настройки + перенос значений, не такая вложенность.
Root cause: Нет раннего выхода, нет вынесенных функций «настройка для свойства», «записать значение в предмет».
Counterfactual: Guard-клаузы; 3–4 именованных шага; вложенность ≤ 2.
Remediation: Передать simplifier после исправления MUST_FIX по безопасности и записи.

[AP-036] MEDIUM · style · risk=0.80 (module-local · cosmetic · normal · 0.50)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 5–103 (на момент ревью)
Procedure: pav_ФормаЗадачиПередЗаписьюНаСервере
Anchor: цепочка `Если` feature → предметы → ТабНЗ → цикл → НЗ → исполнитель → обязательность
Action: REFACTOR
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Вложенность логики > 3 (AP-036).
Root cause: Q1; отсутствие guard clauses.
Counterfactual: Инвертировать условия, выйти рано; тело одного уровня.
Remediation: Вместе с Phase0-DISPROPORTIONATE_COMPLEXITY; не дублировать правку дважды — сначала MUST_FIX, затем упрощение.

[AP-037] MEDIUM · style · risk=0.80 (module-local · cosmetic · normal · 0.50)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 43–48 (на момент ревью)
Procedure: pav_ФормаЗадачиПередЗаписьюНаСервере
Anchor: `(ЗначениеЗаполнено(Форма.Объект.Ссылка.Исполнитель) И …) ИЛИ (ЗначениеЗаполнено(Форма.Объект.Ссылка.РольИсполнителя) И …) ИЛИ НЕ ЗначениеЗаполнено(СтрНЗ.Исполнитель)`
Action: REFACTOR
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Составное условие > 3 операторов без именованных частей (AP-037).
Root cause: Смешение трёх правил сопоставления исполнителя в одном выражении.
Counterfactual: `ИсполнительЗадачиСовпадает` / `РольСовпадает` / `НастройкаБезИсполнителя`.
Remediation: Вынести булевы переменные (после замены AP-014).

[Phase0-DISPROPORTIONATE_SURFACE] MEDIUM · style · risk=1.36 (module-local · cosmetic · normal · 0.70)
File: `...\pavДополнительныеФункции\Ext\Module.bsl`
Line: 14–56 (на момент ревью)
Procedure: ПереходКДокументам21_ПриСозданииНаСервере / [module-level]
Anchor: пустая `#Область СлужебныйПрограммныйИнтерфейс` / орнамент `/////`
Action: REFACTOR
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Q1b: пустая область (std-06 п. 1.8), декоративные комментарии, реквизит-флаг `КнопкаПереход21`, который не читается, экспорт служебной процедуры в `#Область СлужебныеПроцедурыИФункции`.
Root cause: Шум поверхности без вклада в поведение.
Counterfactual: Две области без пустых; без орнамента; экспорт дампа не в Private либо не экспорт.
Remediation: Упростить разметку; убрать неиспользуемый реквизит после исправления стража.

[AP-031] MEDIUM · style · risk=0.80 (module-local · cosmetic · normal · 0.50)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 18–72 (на момент ревью)
Procedure: pav_ФормаЗадачиПередЗаписьюНаСервере
Anchor: `ТабНЗ` / `НЗ` / `ОбПредмет` / `ЕстьИзм` / `СтрДР` / `ЗначениеДР` / `ИзмЗадача`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Q6: сокращения и роль «контейнер/флаг» без домена. Коллега без ЗНИ не расшифрует `НЗ`/`ТабНЗ`.
Root cause: AP-031 доменный тест (не латиница).
Counterfactual: `НастройкиЗаполнения`, `СтрокиНастройки`, `ПредметОбъект`, `ПредметИзменён`, `СтрокаДопРеквизита`, `ЗначениеДопРеквизита`. `ИзмЗадача` удалить, если не часть контракта.
Remediation: Переименовать в границах процедуры; не использовать жаргон постановки.

[AP-031] MEDIUM · style · risk=0.80 (module-local · cosmetic · normal · 0.50)
File: `...\pavДополнительныеФункции\Ext\Module.bsl`
Line: 60, 75 (на момент ревью)
Procedure: pavВыводНастроекФормыНаСервере
Anchor: `Процедура pavВыводНастроекФормыНаСервере(ЭтаФорма)` / `КолвоНастроек`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Параметр `ЭтаФорма` — устаревший синоним платформы, в общем модуле читается как форма модуля формы. `КолвоНастроек` — аббревиатура (std-06 имена).
Root cause: AP-031.
Counterfactual: Параметр `Форма`; `КоличествоНастроек`.
Remediation: Переименовать (совместимость вызовов — только этот модуль + тонкие обёртки форм).

[AP-031] MEDIUM · style · risk=0.80 (module-local · cosmetic · normal · 0.50)
File: `...\pavОбщийМодуль\Ext\Module.bsl`
Line: 35, 76, 83 (на момент ревью)
Procedure: НавигационныеСсылкиИнтегрированногоОбъекта / ДобавитьСсылкиИнтегрированныхСистемНаФорму
Anchor: `МассивСсылок` / `ГруппаДобавленияСтрока` / `МассивРеквизитов`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: В имени тип коллекции (`Массив`) и «Строка» как суффикс параметра (std-06: не описывать тип в имени).
Root cause: AP-031 третичный тест уровня абстракции.
Counterfactual: `НавигационныеСсылки`; `ИмяГруппыФормы`; `ДобавляемыеРеквизиты`.
Remediation: Переименовать локальные/параметр.

[cat-4-mixed] MEDIUM · style · risk=1.36 (module-local · user-feedback · normal · 0.70)
File: `...\pavОбщийМодуль\Ext\Module.bsl`
Line: 76–149 (на момент ревью)
Procedure: ДобавитьСсылкиИнтегрированныхСистемНаФорму
Anchor: `Процедура ДобавитьСсылкиИнтегрированныхСистемНаФорму`
Action: REFACTOR
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: >40 строк, три ответственности: чтение связей, создание реквизитов/элементов, кнопка по правам.
Root cause: reviewer-checks §4 mixed responsibilities.
Counterfactual: `СоздатьТаблицуСсылокНаФорме`, `ЗаполнитьТаблицуСсылок`, `ДобавитьКомандуСвязейИС`.
Remediation: Разрезать после MUST_FIX по НСтр/маркерам.

[cat-6-structure] MEDIUM · style · risk=1.36 (module-local · cosmetic · normal · 0.95)
File: `...\pavОбщийМодуль\Ext\Module.bsl` и `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 1 (на момент ревью)
Procedure: [module-level]
Anchor: нет `#Область` при объёме > 100 строк; нет заголовка модуля
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: std-06 структура: нет областей ПрограммныйИнтерфейс; нет описания модуля. Экспорты `pavОбщийМодуль` без шапок «Параметры» / «Возвращаемое значение».
Root cause: reviewer-checks §6–7; Phase 1b вручную при Linter unavailable.
Counterfactual: Области + JSDoc на каждый экспорт.
Remediation: Разметить модули; добавить шапки трём экспортам `pavОбщийМодуль` и экспортам `pavДополнительныеФункции`.

[cat-6-empty-region] MEDIUM · style · risk=1.36 (module-local · cosmetic · normal · 0.95)
File: `...\pavДополнительныеФункции\Ext\Module.bsl`
Line: 54–56, 58–60 (на момент ревью)
Procedure: [module-level]
Anchor: `#Область СлужебныйПрограммныйИнтерфейс` / `#КонецОбласти` без тел; `pavВыводНастроекФормыНаСервере` Экспорт в Private
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Пустая область; экспорт в служебном разделе.
Root cause: std-06 п. 1.8; reviewer-checks §6 export in private.
Counterfactual: Удалить пустую область; дамп не экспортировать либо перенести в ПрограммныйИнтерфейс, если команда форм — публичный API (после решения по AP-024).
Remediation: Почистить разметку.

[cat-15-dead-locals] MEDIUM · style · risk=1.36 (module-local · cosmetic · normal · 0.95)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 13–16, 72 (на момент ревью)
Procedure: pav_ФормаЗадачиПередЗаписьюНаСервере
Anchor: `СвойстваЗадачи = Новый Массив;` … цикл заполнения без последующего чтения; `ИзмЗадача = Истина;`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Массив свойств собирается и не используется. `ИзмЗадача` не объявлен в сигнатуре и не читается (мёртвое присваивание; у caller как раз был out-параметр).
Root cause: Category 15 leftover после копипаста.
Counterfactual: Удалить массив и присваивание либо вернуть флаг через согласованный `[out]`.
Remediation: Удалить мёртвые символы.

[cat-10-ternary] MEDIUM · style · risk=1.14 (module-local · cosmetic · rare · 0.95)
File: `...\pavДополнительныеФункции\Ext\Module.bsl`
Line: 87 (на момент ревью)
Procedure: pavВыводНастроекФормыНаСервере
Anchor: `?(Настройка = Неопределено,"; - Неопределено", ";")`
Action: MUST_FIX
QualityFlag: none
Disposition: open
design-endorsed: false
Type: CODE
Issue: Тернарный `?()` (reviewer-checks §10 remain inline MEDIUM), к тому же внутри отладочного `Сообщить`.
Root cause: Сжатие ветвления в дампе.
Counterfactual: Обычный `Если` либо удаление всей процедуры (AP-024).
Remediation: Удалить вместе с дампом.

Фрагменты std-06 «Правильно» (присваивания трёх переменных; `ТипЗнч(...) = Тип("ДокументСсылка.ПоступлениеТоваровУслуг")`): замечаний нет — это позитивные примеры стандарта.

---

## Investigation Request

| # | Метод | Контекст вызова | Что нужно определить |
|---|-------|-----------------|----------------------|
| 1 | pav_ФормаЗадачиПередЗаписьюНаСервере | `СтрНЗ.Скрипт` / `УправлениеСвойствами.pav_ДопРеквизитыПредметаЗадачи` | Тип и происхождение поля `Скрипт`: кто пишет, фиксированный контракт колонок ТЗ, допустимо ли исполнение на сервере |
| 2 | НавигационныеСсылкиИнтегрированногоОбъекта | РС `СвязиОбъектовИнтегрированныхСистем.ИДВнешнегоОбъекта` | Канон значения (UUID с дефисами / 32 hex / иное); есть ли платформенный/продуктовый API внешней навигационной ссылки |

## Unverified API

| # | File:Line | Call | Note |
|---|-----------|------|------|
| 1 | pavДополнительныеФункции\Ext\Module.bsl:61 | `Обработки.НастройкиПользователей.ИмяПользователяИБ` | Модуль менеджера обработки в этом прогоне не читался |
| 2 | РаботаСБизнесПроцессами\Ext\Module.bsl:18 | `УправлениеСвойствами.pav_ДопРеквизитыПредметаЗадачи` | Метод расширения существует; состав колонок ТЗ (в т.ч. `Скрипт`) не верифицирован по телу |

## Elegance Score

- Читаемость: 2/5 (модуль задач — стрелка и сокращения; `pavДополнительныеФункции` — орнамент и дамп; `pavОбщийМодуль` — связный, но без областей и с процессными маркерами)
- Когнитивная нагрузка: Высокая в `pav_ФормаЗадачиПередЗаписьюНаСервере`; Средняя в создании элементов формы; Низкая в эталонных «правильных» фрагментах std-06
- Вердикт: Рефакторинг модуля задач обязателен после устранения безопасности и рассогласования записи; поверхность `pavДополнительныеФункции` резать вместе с удалением дампа (Q1b → REFACTOR уже в findings)

---

## === REASONING APPENDIX ===

### 1. Intent Map

| Procedure / block | Intent | Expected | Actual |
|---|---|---|---|
| std-06 `ПередУдалением` | Учебный антипример пустого обработчика с отладкой | 0 исполняемых строк | Пусто + commented debug |
| std-06 присваивания | Показать «один оператор — одна строка» | 3 присваивания | 3 присваивания |
| std-06 `ТипЗнч` | Правильное сравнение с `Тип("…")` | 1 условие | 1 условие (фрагмент без `КонецЕсли`) |
| `НавигационныеСсылкиИнтегрированногоОбъекта` | Собрать внутренние/внешние e1c-ссылки по РС связей | Запрос + цикл сборки URL | То же + скрытый «все узлы» + хрупкий разбор UUID |
| `ЗаписатьАдресИБИнтегрированнойСистемы` | Записать кластер и имя ИБ в РС | 1 менеджер записи | Соответствует |
| `ДобавитьСсылкиИнтегрированныхСистемНаФорму` | Вывести таблицу ссылок и кнопку связей на форме объекта | Данные + элементы формы | Смешение 3 забот, мёртвые комментарии |
| `pav_ФормаЗадачиПередЗаписьюНаСервере` | Перенести доп. реквизиты задачи в предмет при записи | Несколько шагов, вложенность 2 | ~100 строк, 6+ уровней, запись чужого объекта, `Выполнить` |
| feature-флаг / предметы / ТабНЗ | Ограничить сценарий | 2–3 guard | Вложенные `Если` вместо раннего выхода |
| цикл свойств + `Выполнить` | Применить правило настройки | Таблица соответствия + присвоение | Поиск в цикле + произвольный код |
| `ПереходКДокументам21_ПриСозданииНаСервере` | Кнопка перехода в ДО 2.1 | Команда + элемент | Страж с неверным именем, нет вызовов |
| `pavВыводНастроекФормыНаСервере` | Показать ключи настроек формы | Диагностика админа | `Сообщить` + commented dump |

### 2. Contract Map

| source | origin | field_accesses (field · access · line) |
|---|---|---|
| `Узел` | параметр | Неопределено GUARDED 3; перезапись DIRECT 6–11; в запросе DIRECT 30 |
| `Ссылка` | параметр | параметр запроса DIRECT 31 |
| `Выборка` | запрос (явный список полей) | ИДВнешнегоОбъекта DIRECT 38–41; ТипВнешнегоОбъекта DIRECT 43; КластерСерверов / ИмяИнформационнойБазы DEFENSIVE 46–47; Узел DIRECT 56 |
| `Форма.Параметры.Ключ` | форма объекта | DIRECT 78 |
| `Форма.Элементы[ГруппаДобавленияСтрока]` | имя с caller | DIRECT 98, 143 |
| `Форма` (задача) | параметр перехвата/вызова | Объект.Ссылка GUARDED 6–7; Предметы DIRECT 8–9; Свойства_ОписаниеДополнительныхРеквизитов DIRECT 14, 25; динамические реквизиты DIRECT 39 |
| `ТабНЗ` | `УправлениеСвойствами.pav_ДопРеквизитыПредметаЗадачи` | НайтиСтроки / колонки Исполнитель, Обязателен, Скрипт DIRECT 26–52 — контракт колонок не доказан |
| `СтрНЗ.Скрипт` | строка ТЗ | DIRECT 51–52 |
| `ОбПредмет.ДополнительныеРеквизиты` | объект предмета | Найти/Добавить DIRECT 69–90 |
| `ЭлементыФормы` | `Форма.Элементы` | ЕстьРеквизит…("КнопкаПереход21") DEFENSIVE 9; ГруппаОсновнаяКомПанель / Зарегистрировать DIRECT 41–42 |
| `ЭтаФорма` (дамп) | параметр | ИмяФормы, ключи окна DIRECT 64–67; Выборка хранилища DIRECT 76–87 |

### 3. Knowledge Assessment

| source | evidence_of_knowledge | evidence_of_ignorance | verdict | explanation |
|---|---|---|---|---|
| Выборка запроса ссылок | текст запроса в том же методе | формат ИДВнешнегоОбъекта | PARTIAL | Поля известны; канон UUID — нет |
| Узел = все планы | нет JSDoc | семантика Неопределено | PARTIAL | Поведение угадано по коду |
| Форма.Параметры.Ключ | вызовы с форм элементов справочников | нет Form.xml в scope | PARTIAL | Практика объектных форм |
| ТабНЗ / Скрипт | имя метода расширения | тело/колонки не читались полностью | ABSENT/PARTIAL | Нужен резолв (Investigation 1) |
| Форма.Свойства_ОписаниеДополнительныхРеквизитов | соглашение БСП | Form.xml задачи не читался | PARTIAL | Guard нет, доступ DIRECT |
| Тип задачи | имя `&Перед` / формы задачи | аннотация закомментирована, caller другой | PARTIAL | AP-004 под вопросом |
| КнопкаПереход21 vs ОткрытьДокумент21 | комментарий «повторное создание» | имена не совпадают | ABSENT | Страж не доказывает контракт (антицикл) |
| Элементы.Зарегистрировать | нет | только этот метод | ABSENT | Хардкод структуры чужой формы |

### 4. Evaluation Checklist

| # | Вопрос | Ответ | Обоснование | Finding |
|---|---|---|---|---|
| 1 | Complexity justified? (нет → yes) | **yes** | Intent Map: перенос реквизитов vs 6+ уровней в `pav_ФормаЗадачиПередЗаписьюНаСервере` | DISPROPORTIONATE_COMPLEXITY |
| 1b | Signal density / поверхность? | **yes** | Орнамент, пустая область, мёртвый реквизит в `pavДополнительныеФункции` | DISPROPORTIONATE_SURFACE |
| 2 | Contract consistency? | **no** | LEFT JOIN + `ЗначениеЗаполнено` на кластере согласовано с необязательными полями; остальное не смешивает DIRECT/DEFENSIVE на одном фиксированном поле без причины | — |
| 3 | Knowledge deficit? | **yes** | ИД UUID; Скрипт/колонки ТЗ; имена элементов формы перехода | KNOWLEDGE_DEFICIT |
| 4 | Contract inference / EXPLORATORY? | **no** | Исполнитель vs роль — предметные альтернативы задачи, не два пути к одному полю контракта | — |
| 5 | Попытка as contract compensation? | **no** | Блоков `Попытка` в scope нет | — |
| 6 | Naming / AP-031? | **yes** | Таблицы New / Touched: `ТабНЗ`, `НЗ`, `ОбПредмет`, `ЭтаФорма`, `КолвоНастроек`, `МассивСсылок` | CLARITY_DEFICIT + AP-031 |

### 5. Audit Table (Попытка)

A. Enumerate: нет блоков `Попытка`/`Исключение` в четырёх источниках scope.

B. Audit Table: 0 строк. Completeness 0=0.

C. Defensive Checks Table:

| # | Procedure | Line | Source | Field | Contract verified? | Verdict | Evidence |
|---|---|---|---|---|---|---|---|
| 1 | НавигационныеСсылкиИнтегрированногоОбъекта | 3–7 | Узел | Неопределено→массив | needs-resolution (семантика default) | AP-007 | нет шапки; не Свойство() |
| 2 | НавигационныеСсылкиИнтегрированногоОбъекта | 46–47 | Выборка | КластерСерверов, ИмяИнформационнойБазы | verified optional (LEFT JOIN) | OK | текст запроса ЛЕВОЕ СОЕДИНЕНИЕ |
| 3 | НавигационныеСсылкиИнтегрированногоОбъекта | 40–41 | Выборка | ИДВнешнегоОбъекта сегменты | needs-resolution | KNOWLEDGE_DEFICIT | нет проверки количества частей |
| 4 | pav_ФормаЗадачиПередЗаписьюНаСервере | 6–7 | Форма.Объект.Ссылка | ТипЗнч | needs-verification | AP-004 | имя точки = форма задачи; Form.xml не читался |
| 5 | pav_ФормаЗадачиПередЗаписьюНаСервере | 6 | Форма.Объект.Ссылка | ЗначениеЗаполнено | verified dynamic (новая задача) | OK | пустая ссылка до записи |
| 6 | pav_ФормаЗадачиПередЗаписьюНаСервере | 27, 41 | НЗ | Количество | resolved-fixed после Continue | AP-006 | вторая проверка поглощается первой |
| 7 | ПереходКДокументам21_ПриСозданииНаСервере | 9 | ЭлементыФормы | КнопкаПереход21 | phantom (элемент с этим именем не создаётся) | AP-016 / unused-export | создаётся ОткрытьДокумент21 + реквизит КнопкаПереход21 |
| 8 | СтрНЗ.Скрипт | 51–52 | ТабНЗ | Скрипт | unverified | needs-resolution | Investigation 1; не Попытка |

Completeness defensive: строки покрывают access ≠ DIRECT и обращения с неподтверждённым контрактом.

### 6. Identity / Hardcode Audit (Phase 2.6)

A. Кандидаты AP-055 (ИмяФормы / ОткрытьФорму / allow-list метаданных в хуке): не найдены.

Прямые `ЭлементыФормы.ГруппаОсновнаяКомПанель` / `Зарегистрировать` — связь с конкретной формой, но не detector 1–3 (нет строкового фильтра охвата хука). Out of class.

B. Таблица: 0 строк. Completeness 0=0.

C. Summary: Identity / Hardcode Audit: 0 literals, 0 findings (AP-055); completeness 0=0

### 7. Boundary coverage note

Режим **full** (секции Review Boundaries нет).

| Метод | Покрытие |
|---|---|
| std-06 ПередУдалением | AP-024, AP-043 |
| std-06 присваивания | no issues (позитивный пример) |
| std-06 ТипЗнч | no issues (позитивный пример) |
| НавигационныеСсылкиИнтегрированногоОбъекта | AP-007, KNOWLEDGE_DEFICIT, JSDoc в cat-6 |
| ЗаписатьАдресИБИнтегрированнойСистемы | no functional issues; JSDoc в cat-6 |
| ДобавитьСсылкиИнтегрированныхСистемНаФорму | AP-047, AP-025, AP-040, cat-4-mixed, AP-031 |
| pav_ФормаЗадачиПередЗаписьюНаСервере | все CRITICAL/HIGH кроме модуля ссылок/дампа |
| ПереходКДокументам21_ПриСозданииНаСервере | unused-export, AP-025, AP-040/044, Q1b |
| pavВыводНастроекФормыНаСервере | AP-012, AP-024, AP-031, ternary |
| [module-level] три модуля | структура, маркеры, пустая область |

### New identifiers (domain test)

Полный обзор символов scope (не diff ЗНИ). Префикс `pav` / `pav_` — dismiss `code-prefix`. Метаданные `pavПланыОбменаИнтегрированныеСистемы` — `metadata-name`.

| Символ | Доменное значение одной фразой | Verdict |
|---|---|---|
| НавигационныеСсылкиИнтегрированногоОбъекта | навигационные ссылки объекта в связанных ИС | OK |
| ЗаписатьАдресИБИнтегрированнойСистемы | записать адрес ИБ узла интеграции | OK |
| ДобавитьСсылкиИнтегрированныхСистемНаФорму | вывести ссылки ИС на форму | OK |
| ГруппаДобавленияСтрока | имя группы формы для вставки | AP-031 (суффикс типа) |
| МассивСсылок / МассивРеквизитов | контейнер без домена после снятия «Массив» | AP-031 |
| ЧастиИдентификатора | сегменты UUID | OK |
| pav_ФормаЗадачиПередЗаписьюНаСервере | перехват записи формы задачи | OK (префикс) |
| ТабНЗ, НЗ, ОбПредмет, ЕстьИзм, СтрДР, ЗначениеДР, ИзмЗадача, СтрНЗ, СтрСвойство | не расшифровываются без ЗНИ | AP-031 |
| ПереходКДокументам21_ПриСозданииНаСервере | кнопка перехода в ДО 2.1 при создании формы | OK (номер продукта) |
| КнопкаПереход21 | задуман как маркер «кнопка уже есть» | OK домен; phantom имя элемента |
| pavВыводНастроекФормыНаСервере | вывести настройки формы | OK (префикс) |
| ЭтаФорма (параметр) | форма | AP-031 |
| КолвоНастроек | количество настроек | AP-031 |
| ПередУдалением / Отказ / НачальныйИндекс / Индекс / Результат / Ссылка | стандартные/учебные | OK |

### Touched-scope identifiers (domain test)

Full-review: все тела процедур = touched.

| Символ | Процедура | Scope | Доменное значение | Verdict |
|---|---|---|---|---|
| Узел | НавигационныеСсылкиИнтегрированногоОбъекта | touched-procedure | отбор узлов ИС | OK; мутация параметра — AP-007 |
| ИзмЗадача | pav_ФормаЗадачиПередЗаписьюНаСервере | touched-procedure | флаг изменения задачи | AP-031 + dead |
| Результат | pav_ФормаЗадачиПередЗаписьюНаСервере | touched-procedure | «правило настройки сработало» | AP-031 (слишком общий) |
| ТипКлюча | pavВыводНастроекФормыНаСервере | touched-procedure | вид ключа настроек формы | OK |

Phase 1c dismiss: `pav*` = `code-prefix`; не `design term`.

Phase 1d: confirm AP-054 на `[ID#…]`; dismiss не применялся (не protocol/product). Остальной латинский текст в строках `e1cib` / `e1c://` — литералы протокола навигации, не комментарии.

### Phase 1b

Linter unavailable — findings по шапкам экспорта вынесены вручную (cat-6-structure). Нет confirm/dismiss строк LSP.

`&ИзменениеИКонтроль`: нет в scope. Base-файл: не применимо.
