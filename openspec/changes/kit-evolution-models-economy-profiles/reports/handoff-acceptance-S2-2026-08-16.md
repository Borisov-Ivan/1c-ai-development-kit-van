## Срез S2 — передача на приёмку: kit-evolution-models-economy-profiles

**Change:** kit-evolution-models-economy-profiles
**Прогресс:** 19/20 задач среза S2 [x] (приёмка S2.accept: открыта)

### 1. Что реализовано

Постоянный набор правил оркестратора упакован: три гейта сессии больше не висят в каждом запросе, их обязательства живут в дисциплине сессии; запрет править `.bsl` и XML метаданных остаётся в диспетчере 1С; подробности пайплайна writer и авто-исправления ревью уехали в файлы, которые подгружаются по задаче. Стабы навигатора и дисциплины диалога слиты в бюджет чата. Фактический размер always-apply набора вместе с `AGENTS.md` — 33,17 КБ (порог 34 КБ).

- [x] S2.1 — переносимый минимум в дисциплине сессии
- [x] S2.2 / S2.7 — запрет BSL и три исключения в диспетчере; отдельный файл удалён
- [x] S2.3 / S2.6 — навигатор и принципы диалога в бюджете чата; стабы удалены
- [x] S2.4–S2.5 / S2.8 — разжалование XML-guard и трёх session-гейтов; ссылка Compact Brief обновлена
- [x] S2.9–S2.12 — якоря KB / авто-исправления / поток writer; эталон порядка в writer-pipeline
- [x] S2.13–S2.17 — cue в диспетчере гейтов, карта SSOT, glossary, порог 34 КБ
- [x] S2.18–S2.19 — таблица обязательств и замер бюджета

### Карта правок (перед тестом)

Полная карта: `openspec/changes/kit-evolution-models-economy-profiles/reports/code-map.md`

- Дисциплина сессии несёт минимум трёх гейтов — `.cursor/rules/session-discipline.mdc`:11-51
- Запрет прямой правки модулей 1С и исключения — `.cursor/rules/1c-agent-delegation.mdc`:22-36
- Навигатор и тишина — `.cursor/rules/chat-output-budget.mdc`:10-20
- Эталон порядка writer — `.cursor/rules/1c-writer-pipeline.mdc`:8-12
- Замер бюджета — `reports/always-apply-budget-S2-2026-08-16.md`

### 2. Что проверить СЕЙЧАС

**Primary acceptance**

1. Reload Window в Cursor.
2. Команда из `.cursor/commands/` — первым действием чтение `SKILL.md`.
3. Запрос правки XML в `src/` — стоп с блокером.
4. Запрос анализа 3+ файлов — загрузка стратегии контекста.
5. Запрос правки `.bsl` — делегирование writer, без прямой правки оркестратором.

Опционально: сверка замера ≤ 34 КБ и таблица обязательств — см. `tasks.md` у S2.accept.

### 3. Следующие задачи

| Задача | Действие | Тип | Исполнитель | Зависит от | Статус |
|--------|----------|-----|-------------|------------|--------|
| `S2.accept` | Принять срез — гейты срабатывают в чистом окне | Ручной тест | пользователь | S2.1–S2.19 | `[ ]` |
| `S3.1` | Прогнать ревьювера на фрагментах из `std-06-code-modules.md` | Проверка | агент + ревью | S2 | `[ ]` |
| `S3.2` | Составить инвентарь чек-листов промпта ревьювера | Проверка | оркестратор | S3.1 | `[ ]` |

### 4. Как вернуться

`/opsx:apply kit-evolution-models-economy-profiles` — новая сессия начнётся с запроса вердикта по срезу S2.

### 5. Blockers

Нет.

### 7. Short-cut

Если уже проверено и принято — напишите обычной фразой («принято», «срез S2 принят»), отмечу без полного handoff.
