# Прогон ревьювера после диеты промпта (S3.accept Primary)

**Change:** kit-evolution-models-economy-profiles
**Дата:** 2026-08-16
**Источник:** повторный прогон на тех же фикстурах, что базовая линия (std-06 + три модуля PavDO).
**Промпт агента:** v4.1 (чек-листы в reviewer-checks.md).

## === MAIN REPORT ===

### Summary

```yaml
File(s):
  - .cursor/docs/standard/std-06-code-modules.md (```bsl ×3)
  - C:\GitHub\PavDO\src\ДО2\cfe\ДО21_Павлик\CommonModules\pavОбщийМодуль\Ext\Module.bsl
  - C:\GitHub\PavDO\src\ДО2\cfe\ДО21_Павлик\CommonModules\РаботаСБизнесПроцессами\Ext\Module.bsl
  - C:\GitHub\PavDO\src\ДО2\cfe\ДО21_Павлик\CommonModules\pavДополнительныеФункции\Ext\Module.bsl
Status: FAIL
Checklists read: §1–4, §5, §6, §7, §8, §9, §10, §11, §13, §15; Phase 0, Phase 1, Phase 1b, Phase 1c, Phase 1d, Phase 2, Phase 2.5, Phase 2.6; vendor std-03, std-05, std-06
Phase 0: 5 findings (4 HIGH, 1 MEDIUM)
Linter Signals (Phase 1b): 0 confirmed, 0 dismissed, unavailable (PavDO вне workspace; std-06 — markdown). Warning: Incomplete review (автопроверка синтаксиса недоступна). Для экспортных/перехватывающих методов без шапки — ручной MUST_FIX (документация).
Identifier Hygiene (Phase 1c): 28 символов в domain-test, 6 confirmed AP-031, 8 dismissed (code-prefix | protocol)
Comment Hygiene Signals (Phase 1d): skipped (scan skipped); семантический проход Category 9: 0 AP-054, AP-040/AP-044 — ниже
Попытка Audit: 0 blocks checked, 0 findings
Identity / Hardcode Audit: 0 literals, 0 findings; completeness 0=0
AP Registry: 22 findings (CRITICAL: security-eval, broken-hook-contract; HIGH: AP-007, AP-012, AP-014, AP-004, AP-006, AP-038, AP-047, unused-export, wrong-guard, unrestored-privileged, nested-write, lost-out-param, write-on-cancel; MEDIUM: AP-025, AP-031, AP-040, AP-043, AP-044, structure/docs; LOW: unused params, module header)
Release-hygiene: 6 findings (AP-040 ×4 файла/фрагмента, AP-044 ×1)
Top-risk items: security-eval (7.87); unrestored privileged (5.90); broken hook contract (5.67)
Overall: Фикстуры после диеты дают тот же класс дефектов, что ожидается на полном прогоне: произвольный Выполнить под привилегированным режимом, мёртвый/рассинхронизированный перехват записи задачи, побочная запись предмета и отладочный Сообщить в пользовательском контуре.
```

HIDDEN_PARTIAL_RESULT_GATE: N/A (блоков Попытка нет).

### Findings

[security-eval] CRITICAL · functional · risk=7.87 (extension-wide · security · hot-path · conf=0.95)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 52
Procedure: `pav_ФормаЗадачиПередЗаписьюНаСервере`
Anchor: `Выполнить СтрНЗ.Скрипт;`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: На записи задачи выполняется произвольный текст из `СтрНЗ.Скрипт` после `УстановитьПривилегированныйРежим(Истина)`. Скрипт из регистра/настройки работает с отключённым RLS и может менять данные/права.
Root cause: Точка расширения сделана через `Выполнить` вместо ограниченного API (имя процедуры, обработчик, безопасное выражение).
Counterfactual: Если скрипт — доверенный идентификатор обработчика, вызывается экспорт общего модуля по белому списку, без привилегированного режима или с узким повышением только на чтение настроек.
Remediation: Убрать `Выполнить`. Хранить имя разрешённого обработчика; вызывать его явно. Привилегированный режим — только на чтение таблицы настроек, затем сразу сбросить.
Evidence: строки 11, 51–53; тот же приём в `УправлениеСвойствами` (~703) вне scope, как образец копирования.

[security-privileged] HIGH · functional · risk=5.90 (extension-wide · security · hot-path · conf=0.95)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 11
Procedure: `pav_ФормаЗадачиПередЗаписьюНаСервере`
Anchor: `УстановитьПривилегированныйРежим(Истина);`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: Привилегированный режим включается и не сбрасывается. В 8.3 он держится до конца серверного вызова. Дальше в том же вызове (запись задачи, другие обработчики формы) RLS обойден. Под ним же `ОбПредмет.Записать()`.
Root cause: Нет парного `УстановитьПривилегированныйРежим(Ложь)` и нет сохранения/восстановления предыдущего флага (в этом же расширении такой паттерн есть в модуле объекта Согласование).
Counterfactual: Чтение настроек в коротком privileged-блоке; запись предмета — без привилегии или через штатный API с правами пользователя.
Remediation: Сохранить `ПривилегированныйРежим()`, включить только вокруг чтения `pav_ДопРеквизитыПредметаЗадачи` / `СвойстваОбъекта`, восстановить. Запись предмета вынести из privileged.
Evidence: строка 11; нет `Ложь` до `КонецПроцедуры`.

[broken-hook-contract] CRITICAL · functional · risk=5.67 (public-api · user-feedback · hot-path · conf=0.95)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 2–3
Procedure: `pav_ФормаЗадачиПередЗаписьюНаСервере`
Anchor: `//&Перед("ФормаЗадачиПередЗаписьюНаСервере")` / `Процедура pav_ФормаЗадачиПередЗаписьюНаСервере(Форма, Отказ, ТекущийОбъект, ПараметрыЗаписи)`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: ARCHITECTURE
Issue: Три несовместимых контракта сразу: (1) аннотация `&Перед` закомментирована — платформа хук не вызывает; (2) нет `Экспорт`, но форма `Согласование.ФормаЗадачиИсполнителя` вызывает `РаботаСБизнесПроцессами.pav_ФормаЗадачиПередЗаписьюНаСервере(...)`; (3) в модуле 4 параметра `(Форма, Отказ, ТекущийОбъект, ПараметрыЗаписи)`, вызов формы — 5 аргументов `(ЭтаФорма, Отказ, ТекстСообщения, ИмяДопРеквизита, ИзмЗадача)`, как у экспортной копии в `УправлениеСвойствами`.
Root cause: Метод оставили «как перехват» и одновременно дергают как сервис формы, не сверив сигнатуру.
Counterfactual: Либо живой `&Перед` с 4 параметрами типового `ФормаЗадачиПередЗаписьюНаСервере`, либо один экспортный метод с контрактом формы; второй путь — делегирование в `УправлениеСвойствами.pav_ФормаЗадачиПередЗаписьюНаСервере`.
Remediation: Выбрать один контракт, включить аннотацию или `Экспорт`, выровнять параметры с единственным вызывающим кодом. Параметры `ТекущийОбъект` и `ПараметрыЗаписи` сейчас не используются.
Evidence: форма `ФормаЗадачиИсполнителя` строки 11–14; `УправлениеСвойствами` строка 649 (`Экспорт`, 5 параметров).

[Phase0-AUTHORITY_MISPLACEMENT | AP-047] HIGH · functional · risk=4.83 (cross-module · data-write · hot-path · conf=0.7)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 5
Procedure: `pav_ФормаЗадачиПередЗаписьюНаСервере`
Anchor: `Если pavДополнительныеФункцииВызовСервера.ДополнительнаяФункцияВключена("ИспЗаполнениеРеквПредметаВЗадаче")`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: ARCHITECTURE
Issue: Локальная копия алгоритма `УправлениеСвойствами.pav_ФормаЗадачиПередЗаписьюНаСервере` (feature-flag, привилегия, `НайтиСтроки`, `Выполнить`, запись предмета). Копия беднее: нет `ПроверятьПриВыполнении`, нет ветки булева значения, `ИзмЗадача` не является параметром.
Root cause: Вместо делегирования владельцу поведения размножили тело.
Counterfactual: Форма/перехват вызывает один экспорт `УправлениеСвойствами.pav_ФормаЗадачиПередЗаписьюНаСервере`.
Remediation: Удалить дубль; оставить делегирование. Не чинить две копии параллельно.
Evidence: совпадение структуры со строками 649–775 `УправлениеСвойствами`; Phase 3 similar-code.
Intent: перенос доп. реквизитов задачи в предмет при записи.
Expected: вызов существующего экспорта.
Actual: ~100 строк локальной копии.
Supporting: AP-047

[vendor-std-03-nested-write] HIGH · functional · risk=4.51 (cross-module · data-write · hot-path · conf=0.95)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 97–99
Procedure: `pav_ФормаЗадачиПередЗаписьюНаСервере`
Anchor: `ОбПредмет.Записать();`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: ARCHITECTURE
Issue: В обработчике записи задачи пишется другой объект (предмет). std-03: побочная запись связанных данных — `ПриЗаписи`, не подготовка `ПередЗаписью`. Неявная транзакция записи задачи поглощает `Записать()` предмета: ошибка дальше даёт «в этой транзакции уже происходили ошибки»; успех предмета при `Отказ` по задаче — рассогласование.
Root cause: Синхронизация доп. реквизитов смешана с валидацией записи задачи.
Counterfactual: Проверка заполнения без записи предмета здесь; запись предмета — в `ПриЗаписи` задачи / отдельной процедуре после успешной записи.
Remediation: Разделить validate vs persist. Не вызывать `ОбПредмет.Записать()` из `ПередЗаписьюНаСервере` формы задачи.
Evidence: std-03 #std464/#std465; строки 20, 97–99.

[lost-out-param] HIGH · functional · risk=3.93 (module-local · data-write · hot-path · conf=0.95)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 72
Procedure: `pav_ФормаЗадачиПередЗаписьюНаСервере`
Anchor: `ИзмЗадача = Истина;`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: Присваивание необъявленному `ИзмЗадача`. В сигнатуре параметра нет. Форма передаёт `ИзмЗадача` пятым аргументом (и ожидает `[out]`), модуль его не принимает. Флаг «задача изменена из предмета» теряется.
Root cause: Сигнатура не доведена до контракта формы/`УправлениеСвойствами`.
Counterfactual: `ИзмЗадача` — параметр `[out]`, как в `УправлениеСвойствами`.
Remediation: Добавить параметр или убрать присваивание. Не полагаться на неявную локальную переменную.
Evidence: строка 72; объявление строки 3 без `ИзмЗадача`.

[AP-007] HIGH · functional · risk=3.71 (public-api · user-feedback · normal · conf=0.95)
File: `...\pavОбщийМодуль\Ext\Module.bsl`
Line: 7
Procedure: `НавигационныеСсылкиИнтегрированногоОбъекта`
Anchor: `Узел = Новый Массив;`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: Входной `Узел` перезаписывается массивом всех узлов плана обмена. Контракт «узел или Неопределено» на выходе становится «массив ссылок»; значение caller потеряно.
Root cause: Параметр использован как рабочая переменная.
Counterfactual: Локальные `УзлыИнтегрированныхСистем`; параметр только читать.
Remediation: `УзлыДляОтбора = Новый Массив` (или копия переданного); в запрос — локальная переменная. Заголовок: при `Неопределено` отбираются все узлы.
Evidence: строки 2–12, 30.

[integrity-write-on-cancel] HIGH · functional · risk=3.47 (cross-module · data-corruption · hot-path · conf=0.7)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 97
Procedure: `pav_ФормаЗадачиПередЗаписьюНаСервере`
Anchor: `Если ЕстьИзм тогда`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: `ОбПредмет.Записать()` выполняется при `ЕстьИзм` без `НЕ Отказ`. Если по позднему свойству `Отказ = Истина` после изменений предыдущих, предмет всё равно пишется, запись задачи отменяется.
Root cause: Флаг изменений не связан с отказом.
Counterfactual: `Если ЕстьИзм И Не Отказ Тогда ОбПредмет.Записать();`
Remediation: Не писать предмет при отказе; откатить объект в памяти или не менять его до финального решения.
Evidence: строки 75–76, 93–99.

[lock-read-then-write] HIGH · functional · risk=3.38 (cross-module · data-corruption · normal · conf=0.7)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 20
Procedure: `pav_ФормаЗадачиПередЗаписьюНаСервере`
Anchor: `ОбПредмет = Форма.Объект.Предметы.Получить(0).Предмет.ПолучитьОбъект();`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: `ПолучитьОбъект` → правка ТЧ → `Записать` без `БлокировкаДанных` / ответственного чтения. Параллельная запись предмета даёт lost update доп. реквизитов.
Root cause: Нет блокировки перед изменением связанного объекта.
Counterfactual: `БлокировкаДанных` по ссылке предмета до `ПолучитьОбъект`, либо запись в том же контуре, что штатная запись документа.
Remediation: Управляемая блокировка на `Предмет` перед чтением объекта.
Evidence: строки 20, 99; reviewer-checks §13.

[Phase0-CONTRACT_INCONSISTENCY | AP-014] HIGH · functional · risk=3.28 (module-local · user-feedback · hot-path · conf=0.95)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 43–46
Procedure: `pav_ФормаЗадачиПередЗаписьюНаСервере`
Anchor: `Форма.Объект.Ссылка.Исполнитель` / `Форма.Объект.Ссылка.РольИсполнителя.Владелец`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: Поля задачи с формы читаются через `Объект.*` (предметы, доп. реквизиты), исполнитель — через `Объект.Ссылка.*` (загрузка объекта из БД, в т.ч. устаревшие значения vs форма). `РольИсполнителя.Владелец` — второй загрузкой.
Root cause: Смешение in-memory формы и ссылочного разыменования.
Counterfactual: `Форма.Объект.Исполнитель`, `Форма.Объект.РольИсполнителя`; владелец роли — `ОбщегоНазначения.ЗначениеРеквизитаОбъекта`.
Remediation: Не использовать `Ссылка.Реквизит` в цикле. Пакетно прочитать нужные реквизиты роли.
Evidence: строки 8–9 vs 43–46; `Свойство.Заголовок` на строке 76 — тот же AP-014 на плане видов характеристик.
Intent: сравнить исполнителя строки настройки с исполнителем задачи.
Expected: поля формы + один запрос реквизитов.
Actual: N загрузок объектов в цикле.
Supporting: AP-014

[AP-012] HIGH · functional · risk=3.15 (public-api · user-feedback · rare · conf=0.95)
File: `...\pavДополнительныеФункции\Ext\Module.bsl`
Line: 72–73, 87, 99, 101
Procedure: `pavВыводНастроекФормыНаСервере`
Anchor: `Сообщить("КлючСохраненияПоложенияОкна=" + КлючСохраненияПоложенияОкна);`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: Пять `Сообщить()` с сырыми ключами настроек. Команда формы дергается из карточек документов/контрагентов — пользователь видит служебный дамп.
Root cause: Отладочный вывод оставлен как «сервис».
Counterfactual: `ОбщегоНазначения.СообщитьПользователю(НСтр(...))` либо запись в журнал; для диагностики — обработка, не карточка объекта.
Remediation: Заменить `Сообщить`; строки через `НСтр`. Исправить опечатку «настоек».
Evidence: строки 72, 73, 87, 99, 101; вызовы из форм Внутренние/Входящие/Исходящие/Контрагенты.

[AP-025] MEDIUM · style · risk=2.47 (public-api · user-feedback · normal · conf=0.95)
File: `...\pavОбщийМодуль\Ext\Module.bsl`; `...\РаботаСБизнесПроцессами\Ext\Module.bsl`; `...\pavДополнительныеФункции\Ext\Module.bsl`
Line: 87–90, 144; 76; 20
Procedure: `ДобавитьСсылкиИнтегрированныхСистемНаФорму` / `pav_ФормаЗадачиПередЗаписьюНаСервере` / `ПереходКДокументам21_ПриСозданииНаСервере`
Anchor: `"Узел ИС"` / `"Не заполнен доп.реквизит: "` / `НоваяКоманда.Заголовок = "2.1"`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: Пользовательские строки без `НСтр("ru = '...'")`. Заголовок таблицы — `Нстр(...)` (нестандартный регистр, но локализация есть); колонки и кнопка — нет.
Root cause: Заголовки и сообщения собраны как литералы разработки.
Counterfactual: Все видимые строки — `НСтр`; сообщение о реквизите — шаблон с именем свойства.
Remediation: Обернуть литералы; на строке 76 не конкатенировать `Свойство.Заголовок` без `НСтр`.
Evidence: pavОбщийМодуль 84 vs 87–90, 144; РаботаСБизнесПроцессами 76; pavДополнительныеФункции 20.

[AP-038] HIGH · functional · risk=2.42 (module-local · user-feedback · hot-path · conf=0.7)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 26–27
Procedure: `pav_ФормаЗадачиПередЗаписьюНаСервере`
Anchor: `НЗ = ТабНЗ.НайтиСтроки(Новый Структура("Свойство", СтрСвойство.Свойство));`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: Для каждого доп. реквизита формы — линейный `НайтиСтроки` по `ТабНЗ` и `Найти` по ТЧ предмета.
Root cause: Нет индекса/соответствия по `Свойство`.
Counterfactual: `Соответствие` свойство → строки настроек один раз до цикла.
Remediation: Построить соответствие из `ТабНЗ`; поиск в ТЧ — тоже карта, либо один проход.
Evidence: циклы 14–16, 25–27, 69, 82.

[wrong-idempotency-guard | AP-016] HIGH · functional · risk=2.42 (module-local · user-feedback · rare · conf=0.95)
File: `...\pavДополнительныеФункции\Ext\Module.bsl`
Line: 9–12
Procedure: `ПереходКДокументам21_ПриСозданииНаСервере`
Anchor: `Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ЭлементыФормы, "КнопкаПереход21") Тогда`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: Страховка «на всякий случай» ищет элемент `КнопкаПереход21`. Создаются реквизит `КнопкаПереход21` (Булево) и кнопка `ОткрытьДокумент21`. Повторный вызов не находит элемент и падает на `ИзменитьРеквизиты` (реквизит уже есть).
Root cause: Имя проверки не совпадает с создаваемым элементом; проверка идёт по `Элементы`, не по реквизитам формы.
Counterfactual: `ЕстьРеквизитИлиСвойствоОбъекта(Форма, "КнопкаПереход21")` или `ЭлементыФормы.Найти("ОткрытьДокумент21")`.
Remediation: Проверять реквизит формы или имя кнопки; не создавать реквизит, который нигде не читается, если он только сторожок.
Evidence: строки 9–12 vs 30–45; комментарий «На всякий случай».
Supporting: AP-016

[unused-export] HIGH · functional · risk=2.11 (public-api · cosmetic · one-off · conf=0.9)
File: `...\pavДополнительныеФункции\Ext\Module.bsl`
Line: 6
Procedure: `ПереходКДокументам21_ПриСозданииНаСервере`
Anchor: `Процедура ПереходКДокументам21_ПриСозданииНаСервере(Форма) Экспорт`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: Экспорт без вызовов в расширении `ДО21_Павлик` (Grep по `*.bsl`). Мёртвый публичный API плюс хрупкое создание кнопки.
Root cause: Обработчик не подключили к формам либо перенесли и оставили тело.
Counterfactual: Вызов из `&После("ПриСозданииНаСервере")` нужных форм либо удаление метода и пустой области.
Remediation: Подключить или удалить. Category 15, unused export.
Evidence: единственное совпадение имени — объявление; клиентский модуль содержит только область с другим API (`ПолнаяНавигационнаяСсылка21`).

[AP-004] HIGH · functional · risk=2.10 (module-local · cosmetic · normal · conf=0.7)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 7
Procedure: `pav_ФормаЗадачиПередЗаписьюНаСервере`
Anchor: `ТипЗнч(Форма.Объект.Ссылка) = Тип("ЗадачаСсылка.ЗадачаИсполнителя")`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: Единственный найденный вызывающий код — форма задачи исполнителя согласования. Тип ссылки фиксирован контрактом формы. Guard компенсирует незнание места вызова.
Root cause: Knowledge PARTIAL: нет Resolved Contracts / проверки всех вызовов как хука `&Перед` (хук выключен).
Counterfactual: Если метод только для `ЗадачаИсполнителя` — убрать `ТипЗнч`. Если общий хук всех задач — оставить и задокументировать полиморфный контракт.
Remediation: Зафиксировать контракт (см. Investigation Request), затем убрать или обосновать проверку типа.
Evidence: caller `ФормаЗадачиИсполнителя`; антициркулярное правило Phase 2.5.

[AP-006] HIGH · functional · risk=2.28 (module-local · cosmetic · normal · conf=0.95)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 27, 41
Procedure: `pav_ФормаЗадачиПередЗаписьюНаСервере`
Anchor: `Если НЕ НЗ.Количество() = 0 тогда` / `Если НЗ.Количество() > 0 тогда`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: После ненулевого `НЗ` снова проверяется `Количество() > 0` на том же массиве. Вторая проверка поглощается первой.
Root cause: Defensive cake после копирования кода.
Counterfactual: Одна проверка; внутренний цикл сразу по `НЗ`.
Remediation: Удалить блок строк 41–59 как вложенный в уже истинное условие; цикл `Для каждого СтрНЗ из НЗ` сразу после первого if.
Evidence: 27–31 vs 41–59.

[Phase0-DISPROPORTIONATE_COMPLEXITY | AP-036] HIGH · style · risk=2.07 (module-local · cosmetic · hot-path · conf=0.5)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 5–103
Procedure: `pav_ФормаЗадачиПередЗаписьюНаСервере`
Anchor: `Если pavДополнительныеФункцииВызовСервера.ДополнительнаяФункцияВключена`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: Намерение — скопировать заполненные доп. реквизиты в предмет и проверить обязательность. Ожидаемая сложность: 2–3 уровня, ~25–35 строк после ранних выходов. Факт: ~100 строк, вложенность 6–7 `Если`/`Цикл`.
Root cause: Feature-flag, тип, предметы, свойства, скрипт, обязательность и запись свалены в одну стрелу.
Counterfactual: Guard-clause по флагу/предмету; отдельные функции «настройки свойства», «применить к предмету», «записать если нужно».
Remediation: Разрезать процедуру. См. также REFACTOR AP-036 ниже.
Intent: синхронизация доп. реквизитов задачи с предметом.
Expected: ~30 строк, вложенность ≤3.
Actual: 100 строк, вложенность >3.
Supporting: AP-036, AP-037

[AP-036] MEDIUM · style · risk=0.92 (module-local · cosmetic · hot-path · conf=0.5)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 5
Procedure: `pav_ФормаЗадачиПередЗаписьюНаСервере`
Anchor: `Если НЕ Форма.Объект.Предметы.Количество() = 0`
Action: REFACTOR
QualityFlag: none
Disposition: open
Type: CODE
Issue: DISPROPORTIONATE_SURFACE: вложенность >3. Нужен simplifier, не точечный патч условий.
Root cause: Arrow code.
Counterfactual: Ранние `Возврат` по структурным условиям, затем плоский цикл.
Remediation: Вынести цикл по свойствам и применение к предмету.
Evidence: Q1b = yes.

[AP-037] MEDIUM · style · risk=0.85 (module-local · cosmetic · hot-path · conf=0.5)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 43–48
Procedure: `pav_ФормаЗадачиПередЗаписьюНаСервере`
Anchor: `Если (ЗначениеЗаполнено(Форма.Объект.Ссылка.Исполнитель)`
Action: REFACTOR
QualityFlag: none
Disposition: open
Type: CODE
Issue: Условие с несколькими `И`/`ИЛИ` без именованных частей («исполнитель совпал», «роль совпала», «настройка без исполнителя»).
Root cause: Cognitive overload.
Counterfactual: Три булевых с доменными именами, затем `Если ИсполнительПодходит Или РольПодходит Или НастройкаОбщая`.
Remediation: Self-documenting variables.
Evidence: строки 43–48.

[mixed-responsibilities] MEDIUM · style · risk=1.90 (module-local · cosmetic · normal · conf=0.95)
File: `...\pavОбщийМодуль\Ext\Module.bsl`
Line: 76
Procedure: `ДобавитьСсылкиИнтегрированныхСистемНаФорму`
Anchor: `Процедура ДобавитьСсылкиИнтегрированныхСистемНаФорму(Форма, ГруппаДобавленияСтрока) Экспорт`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: >40 строк: реквизиты формы, элементы, заполнение таблицы, кнопка по `РольДоступна("ПолныеПрава")`.
Root cause: Смешаны построение UI и наполнение данными.
Counterfactual: `СоздатьРеквизитыИЭлементыСсылокИС(Форма, ...)` + `ЗаполнитьСсылкиИС(...)` + `ДобавитьКомандуСвязейИС(...)`.
Remediation: Разрезать по заботам.
Evidence: строки 76–147; category 4.

[AP-031] MEDIUM · style · risk=1.12 (module-local · cosmetic · normal · conf=0.7)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`; `...\pavДополнительныеФункции\Ext\Module.bsl`
Line: 18, 21, 22, 35, 39, 75; 75
Procedure: `pav_ФормаЗадачиПередЗаписьюНаСервере` / `pavВыводНастроекФормыНаСервере`
Anchor: `ТабНЗ` / `ОбПредмет` / `ЕстьИзм` / `СтрДР` / `КолвоНастроек`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: Имена не проходят доменный тест: `ТабНЗ`/`НЗ`/`СтрНЗ` (что такое НЗ?), `ОбПредмет`, `ЕстьИзм`, `СтрДР`, `КолвоНастроек`.
Root cause: Аббревиатуры реализации.
Counterfactual: `НастройкиДопРеквизитовПредмета`, `ОбъектПредмета`, `ПредметИзменен`, `СтрокаДопРеквизита`, `КоличествоНастроек`.
Remediation: Переименовать по таблице domain-test. `pav_` — dismiss `code-prefix`.
Evidence: Phase 0 Q6; Naming Signals skipped ≠ OK.
NamingVerdict: confirm

[AP-040] MEDIUM · release-hygiene · risk=1.14 (module-local · cosmetic · one-off · conf=0.95)
File: `...\pavОбщийМодуль\Ext\Module.bsl`
Line: 1, 79, 104, 121, 150
Procedure: `[module-level]` / `ДобавитьСсылкиИнтегрированныхСистемНаФорму`
Anchor: `// + Листов А.С. 28.03.2025 Вывод ссылок... [ID#65270]`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: Маркер `// +` / `// -` не из whitelist kit (`// +++` / `// ---`). Внутри — номера заявок, «разработка», закомментированный `НайтиПоРеквизиту` с UUID, закомментированные свойства элемента.
Root cause: Changelog и отладочный код в комментариях; project.md нет → строгая гигиена marker-canon.
Counterfactual: Whitelist-пара `+++` с русской `domain_label` без ticket; мёртвый код удалён.
Remediation: Удалить `// +`/`// -` и commented-out; при необходимости — канонический маркер с доменной фразой.
Evidence: строки 1, 79, 104, 121, 150.

[AP-040] MEDIUM · release-hygiene · risk=1.14 (module-local · cosmetic · one-off · conf=0.95)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 2, 28, 64–67
Procedure: `pav_ФормаЗадачиПередЗаписьюНаСервере`
Anchor: `//&Перед("ФормаЗадачиПередЗаписьюНаСервере")` / `//Форма.Элементы[СтрСвойство.ИмяРеквизитаЗначение].Видимость = Ложь;`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: Закомментированная аннотация и мёртвые блоки поиска `ИмяРеквизитаЗначение`. Это не `#Вставка`.
Root cause: История правок оставлена в теле метода.
Counterfactual: Аннотация либо живая, либо удалена; мёртвый код удалён.
Remediation: Удалить commented-out; решить судьбу `&Перед` (см. broken-hook-contract).
Evidence: 2, 28, 64–67.

[AP-040 | AP-044] MEDIUM · release-hygiene · risk=1.14 (module-local · cosmetic · one-off · conf=0.95)
File: `...\pavДополнительныеФункции\Ext\Module.bsl`
Line: 10, 14–16, 28, 63, 86, 89–94
Procedure: `ПереходКДокументам21_ПриСозданииНаСервере` / `pavВыводНастроекФормыНаСервере`
Anchor: `/////   КОМАНДЫ   //////////` / `// ******   Команда формы "2.1"`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: Декораторы `/////` и `******` (AP-040 typography); комментарии пересказывают «команда/реквизит» (AP-044); закомментированный `ИмяФормы` и дамп ключей.
Root cause: Генераторный/отладочный стиль.
Counterfactual: Без декораторов; комментарий только «зачем»; дамп ключей удалён.
Remediation: Удалить декоративные и commented-out строки.
Evidence: 14–16, 28, 63, 86–94.

[AP-040 | AP-043 | AP-024] MEDIUM · design · risk=1.14 (module-local · cosmetic · one-off · conf=0.95)
File: `.cursor/docs/standard/std-06-code-modules.md`
Line: 21–23
Procedure: `ПередУдалением`
Anchor: `// Если Истина Тогда Сообщение("Для отладки"); КонецЕсли;`
Action: MUST_FIX
QualityFlag: weak
Disposition: needs-confirm
Type: CODE
Issue: Тело пустое после снятия комментария (AP-043 empty-handler). В комментарии отладка `Если Истина` и несуществующий `Сообщение(` (AP-024/AP-040). Фрагмент помечен в документе как «Неправильно» — педагогический антипример, не production-модуль.
Root cause: Пример запрещённого кода.
Counterfactual: Для живого модуля — удалить обработчик из метаданных. Для стандарта — finding не чинить как BSL репозитория.
Remediation: Оркестратору: as-designed для текста стандарта; не переносить фрагмент в конфигурацию.
Evidence: подпись «Неправильно» над блоком.
tag: agreement-override (документированный антипример)

[missing-jsdoc] MEDIUM · style · risk=1.98 (public-api · cosmetic · normal · conf=0.95)
File: все три `.bsl`
Line: 2, 65, 76; 3; 6, 60
Procedure: все `Экспорт` / перехват
Anchor: `Функция НавигационныеСсылкиИнтегрированногоОбъекта` / `Процедура pav_ФормаЗадачиПередЗаписьюНаСервере` / `Процедура ПереходКДокументам21_ПриСозданииНаСервере`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: Phase 1b fallback: экспортные и перехватывающие методы без шапки «Параметры» / «Возвращаемое значение». Linter unavailable — проверка ручная, не откладывается.
Root cause: Нет контракта в комментарии.
Counterfactual: Шаблон БСП над каждой экспортной процедурой/функцией (выше аннотации, если аннотация будет).
Remediation: Добавить шапки: назначение, параметры с типами, возврат для функции.
Evidence: reviewer-checks Phase 1b; std-06 § описание процедур.

[module-structure-regions] MEDIUM · style · risk=1.52 (module-local · cosmetic · normal · conf=0.95)
File: `...\pavОбщийМодуль\Ext\Module.bsl`; `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 1
Procedure: `[module-level]`
Anchor: `Функция НавигационныеСсылкиИнтегрированногоОбъекта` (нет `#Область`)
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: Модули >100 строк без `#Область`. Нет заголовка модуля (LOW, тем же местом).
Root cause: Наращивание методов без каркаса std-06.
Counterfactual: `ПрограммныйИнтерфейс` → `СлужебныеПроцедурыИФункции`; комментарий назначения модуля.
Remediation: Области по шаблону общих модулей; краткий заголовок.
Evidence: 150 и 105 строк; §6.

[empty-region | export-in-private] MEDIUM · style · risk=1.98 (public-api · cosmetic · normal · conf=0.95)
File: `...\pavДополнительныеФункции\Ext\Module.bsl`
Line: 54–60
Procedure: `pavВыводНастроекФормыНаСервере`
Anchor: `#Область СлужебныйПрограммныйИнтерфейс` / `#Область СлужебныеПроцедурыИФункции` + `Экспорт`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: Пустая `СлужебныйПрограммныйИнтерфейс` (std-06 п.1.8). Экспорт `pavВыводНастроекФормыНаСервере` в служебной области.
Root cause: Каркас областей без содержания; отладочный экспорт спрятан в Private.
Counterfactual: Удалить пустую область; экспорт — в `ПрограммныйИнтерфейс` (или снять `Экспорт`, если только формы-обёртки).
Remediation: Перенести экспорт; убрать пустую область.
Evidence: 54–56, 58–60.

[dead-local] MEDIUM · style · risk=1.52 (module-local · cosmetic · normal · conf=0.95)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 13–16
Procedure: `pav_ФормаЗадачиПередЗаписьюНаСервере`
Anchor: `СвойстваЗадачи = Новый Массив;`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: Массив заполняется и не читается. Category 15 analog (мёртвый код в теле).
Root cause: Остаток незавершённого фильтра свойств.
Counterfactual: Использовать для отбора или удалить цикл.
Remediation: Удалить 13–16, если отбор не нужен.
Evidence: нет чтений `СвойстваЗадачи` после наполнения.

[unused-params] LOW · style · risk=0.71 (module-local · cosmetic · normal · conf=0.95)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 3
Procedure: `pav_ФормаЗадачиПередЗаписьюНаСервере`
Anchor: `ТекущийОбъект, ПараметрыЗаписи`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: Параметры не используются (если оставить 4-параметровый `&Перед` — допустимы как контракт платформы; тогда не удалять, а задействовать `ПараметрыЗаписи.ВыполнитьЗадачу` как в закомментированном коде `УправлениеСвойствами`).
Root cause: Сигнатура хука без тела под параметры.
Counterfactual: Либо читать `ПараметрыЗаписи`, либо не объявлять лишние имена в сервисном экспорте.
Remediation: Согласовать с выбранным контрактом (broken-hook-contract).
Evidence: нет вхождений имён в теле кроме заголовка.

[ternary] MEDIUM · style · risk=1.29 (module-local · cosmetic · rare · conf=0.95)
File: `...\pavДополнительныеФункции\Ext\Module.bsl`
Line: 87
Procedure: `pavВыводНастроекФормыНаСервере`
Anchor: `?(Настройка = Неопределено,"; - Неопределено", ";")`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: Оператор `?()` (inline category 10).
Root cause: Сжатый отладочный вывод.
Counterfactual: `Если Настройка = Неопределено Тогда ...`
Remediation: Заменить ветвлением; вместе с AP-012 уйдёт сам литерал.
Evidence: строка 87.

[AP-050] MEDIUM · style · risk=1.70 (module-local · user-feedback · hot-path · conf=0.7)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 76
Procedure: `pav_ФормаЗадачиПередЗаписьюНаСервере`
Anchor: `ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Не заполнен доп.реквизит: " + Свойство.Заголовок, , СтрСвойство.ИмяРеквизитаЗначение);`
Action: REFACTOR
QualityFlag: none
Disposition: open
Type: CODE
Issue: Блокирующее сообщение рядом с `Отказ = Истина` называет реквизит, но не говорит, какое значение ожидается (обязателен при записи задачи / при выполнении) и что делать.
Root cause: Шаблон «не заполнен X».
Counterfactual: `НСтр` с фактическим именем и требованием «укажите значение до записи задачи».
Remediation: Расширить текст; `ОбщегоНазначения.СообщитьПользователю` на сервере.
Evidence: 75–76; default action каталога — REFACTOR.

[AP-017] MEDIUM · style · risk=1.43 (module-local · cosmetic · rare · conf=0.7)
File: `...\pavДополнительныеФункции\Ext\Module.bsl`
Line: 9–12
Procedure: `ПереходКДокументам21_ПриСозданииНаСервере`
Anchor: `Возврат;`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: Ранний `Возврат` по «элемент уже есть», затем единственное действие — создание кнопки. Условие привязано к выходу. Сочетается с неверным именем в guard.
Root cause: Инвертированный idempotency-check.
Counterfactual: `Если ЭлементыФормы.Найти("ОткрытьДокумент21") = Неопределено Тогда` … создание … `КонецЕсли`.
Remediation: Обернуть создание, не выходить из процедуры-хука целиком (если появятся другие шаги).
Evidence: 9–12 vs 18–47.

[AP-014-property-title] HIGH · functional · risk=3.28 (module-local · user-feedback · hot-path · conf=0.95)
File: `...\РаботаСБизнесПроцессами\Ext\Module.bsl`
Line: 76
Procedure: `pav_ФормаЗадачиПередЗаписьюНаСервере`
Anchor: `Свойство.Заголовок`
Action: MUST_FIX
QualityFlag: none
Disposition: open
Type: CODE
Issue: `Свойство` — ссылка ПВХ; `.Заголовок` грузит объект. На отказ по обязательному реквизиту — лишняя загрузка (часто после уже загруженного исполнителя).
Root cause: Разыменование ссылки для текста сообщения.
Counterfactual: Заголовок из `СтрСвойство` формы (описание доп. реквизитов БСП обычно содержит представление) или `ЗначениеРеквизитаОбъекта`.
Remediation: Не читать `.Заголовок` с ссылки в цикле.
Evidence: строка 76; частично пересекается с CONTRACT_INCONSISTENCY, источник другой.

Фрагменты std-06 строки 30–32 и 260: замечаний нет (корректные иллюстрации; у третьего блока нет `КонецЕсли` как у усечённого примера, не как у модуля).

### Elegance Score

| Процедура | Читаемость | Когнитивная нагрузка | Вердикт |
|-----------|------------|----------------------|---------|
| `pav_ФормаЗадачиПередЗаписьюНаСервере` | 1 | 5 | DISPROPORTIONATE_SURFACE |
| `ДобавитьСсылкиИнтегрированныхСистемНаФорму` | 3 | 3 | mixed responsibilities |
| `НавигационныеСсылкиИнтегрированногоОбъекта` | 3 | 2 | AP-007 |
| `ПереходКДокументам21_ПриСозданииНаСервере` | 3 | 2 | неверный сторожок |
| `pavВыводНастроекФормыНаСервере` | 2 | 2 | отладочный вывод |

### Investigation Request

1. **Источник:** `Форма.Объект.Ссылка` в `pav_ФормаЗадачиПередЗаписьюНаСервере`  
   **Почему не закрыт:** нет Resolved Contracts; хук `&Перед` выключен; живой caller — одна форма задачи.  
   **Нужно:** полный список вызовов (включая другие расширения) и подтверждение, что тип всегда `ЗадачаИсполнителя`. От этого зависит снятие AP-004.

2. **Источник:** `ЭлементыФормы` / реквизит `КнопкаПереход21`  
   **Почему не закрыт:** Form.xml целевых форм не в scope; процедура никем не вызывается.  
   **Нужно:** на каких формах планировалась кнопка «2.1» и какой идентификатор элемента каноничен.

3. **Источник:** контракт `pav_ФормаЗадачиПередЗаписьюНаСервере`  
   **Почему не закрыт:** расхождение 4 vs 5 параметров и Export.  
   **Нужно:** собирается ли конфигурация с этой формой; какой метод реально вызывается в ИБ.

---

## === REASONING APPENDIX ===

### Intent Map

| Процедура | Намерение | Ожид. сложность | Факт |
|-----------|-----------|-----------------|------|
| `НавигационныеСсылкиИнтегрированногоОбъекта` | По ссылке ДО вернуть внутренние/внешние e1c-ссылки интегрированных систем | запрос + сбор URL, 1 цикл, ~40 строк | ~60 строк; перезапись `Узел`; разбор UUID без проверки числа частей |
| `ЗаписатьАдресИБИнтегрированнойСистемы` | Записать кластер и имя ИБ в регистр | 10 строк, прямая запись | пропорционально |
| `ДобавитьСсылкиИнтегрированныхСистемНаФорму` | Показать таблицу ссылок и кнопку связей на форме объекта | создание элементов + fill, ~40 строк | ~70; 4 заботы |
| `pav_ФормаЗадачиПередЗаписьюНаСервере` | При записи задачи перенести/проверить доп. реквизиты предмета | 3 уровня, ранние выходы | 6–7 уровней, запись другого объекта, `Выполнить` |
| `ПереходКДокументам21_ПриСозданииНаСервере` | Один раз добавить кнопку перехода в 2.1 | 20 строк | сторожок по чужому имени |
| `pavВыводНастроекФормыНаСервере` | Показать ключи сохранённых настроек формы | цикл выборки хранилища | `Сообщить` + ternary + commented dump |
| `ПередУдалением` (std-06) | (антипример) пустой обработчик с отладкой | — | пустое тело |

Значимые блоки `pav_ФормаЗадачи…`: feature-flag → предметы → privileged → цикл свойств → скрипт → обязательность → запись. Каждый слой — отдельный `Если`, без раннего выхода из процедуры.

### Contract Map

| source | origin | field_accesses |
|--------|--------|----------------|
| `Узел` | параметр | GUARDED (`= Неопределено`) затем overwrite; DIRECT в параметре запроса |
| `Выборка` | запрос модуля | DIRECT: `ИДВнешнегоОбъекта`, `ТипВнешнегоОбъекта`, `КластерСерверов`, `ИмяИнформационнойБазы`, `УзелИнтегрированнойСистемы`; `ЗначениеЗаполнено` на LEFT JOIN полях — бизнес, не DEFENSIVE контракта колонок |
| `Форма.Параметры.Ключ` | параметр формы | DIRECT |
| `Форма` (ссылки ИС) | параметр | DIRECT элементы/реквизиты после `ИзменитьРеквизиты` |
| `Форма.Объект.Ссылка` | форма задачи | DIRECT `ЗначениеЗаполнено`; DEFENSIVE `ТипЗнч`; DIRECT через точку `Исполнитель`, `РольИсполнителя` |
| `Форма.Объект.Предметы` | ТЧ формы | DIRECT `Количество`, `Получить(0).Предмет` |
| `Форма.Свойства_ОписаниеДополнительныхРеквизитов` | БСП на форме | DIRECT цикл, `Свойство`, `ИмяРеквизитаЗначение` |
| `ТабНЗ` | `УправлениеСвойствами.pav_ДопРеквизитыПредметаЗадачи` | DIRECT `НайтиСтроки`; поля `Исполнитель`, `Обязателен`, `Скрипт` — контракт callee неизвестен (PARTIAL) |
| `ОбПредмет.ДополнительныеРеквизиты` | объект предмета | DIRECT `Найти`/`Добавить` (ТЧ метаданных — фиксирована) |
| `ЭлементыФормы` | `Форма.Элементы` | DEFENSIVE `ЕстьРеквизитИлиСвойствоОбъекта(..., "КнопкаПереход21")`; DIRECT `ГруппаОсновнаяКомПанель`, `Зарегистрировать` |
| `ЭтаФорма` (настройки) | параметр | DIRECT `ИмяФормы`, ключи окна |

### Knowledge Assessment

| source | evidence_of_knowledge | evidence_of_ignorance | verdict |
|--------|----------------------|----------------------|---------|
| Запрос связей ИС | текст запроса в модуле | — | FULL |
| `Узел` | сигнатура | тип то узел, то массив | PARTIAL |
| `Форма.Объект` задачи | caller — форма задачи | `ТипЗнч` как компенсация | PARTIAL |
| `ТабНЗ` / `Скрипт` | использование полей | нет тела `pav_ДопРеквизитыПредметаЗадачи` в scope | PARTIAL |
| ТЧ `ДополнительныеРеквизиты` | типовой БСП | — | FULL |
| Элементы формы 2.1 | комментарий про кнопку | guard vs фактические имена | ABSENT |
| `ПараметрыЗаписи` | имя из `&Перед` | не читается; аннотация выкл. | ABSENT |

Guard сам по себе не считался knowledge.

### Evaluation Checklist

| # | Вопрос | Ответ | Источник | Finding |
|---|--------|-------|----------|---------|
| 1 | Proportionality >3x / вложенность >2x? | yes | `pav_ФормаЗадачи…` 100 строк vs ~30; nest 6 vs 2 | DISPROPORTIONATE_COMPLEXITY |
| 2 | Один источник DIRECT и DEFENSIVE? | yes | задача: предметы DIRECT, исполнитель через `Ссылка` | CONTRACT_INCONSISTENCY |
| 3 | PARTIAL/ABSENT? | yes | тип задачи, ТабНЗ, элементы 2.1, ПараметрыЗаписи | KNOWLEDGE_DEFICIT |
| 4 | EXPLORATORY? | no | нет перебора путей к одному полю | — |
| 5 | Попытка как компенсация контракта? | no | блоков Попытка нет | — |
| 6 | Domain test fail на символах? | yes | `ТабНЗ`, `ОбПредмет`, `ЕстьИзм`, `СтрДР`, `КолвоНастроек` | CLARITY_DEFICIT + AP-031 |
| 7 | Authority substitution? | yes | дубль `УправлениеСвойствами`; `Выполнить` вместо API | AUTHORITY_MISPLACEMENT + AP-047 |

Completeness: 7/7.

## New identifiers (domain test)

Полный обзор символов модулей (diff нет — full review, fail-closed).

| Символ | Доменное значение одной фразой | Verdict |
|--------|--------------------------------|---------|
| `НавигационныеСсылкиИнтегрированногоОбъекта` | список e1c-ссылок объекта в интегрированных системах | OK |
| `ЗаписатьАдресИБИнтегрированнойСистемы` | сохранить адрес ИБ узла интеграции | OK |
| `ДобавитьСсылкиИнтегрированныхСистемНаФорму` | вывести ссылки ИС на форму | OK |
| `pav_ФормаЗадачиПередЗаписьюНаСервере` | обработка перед записью формы задачи | OK (prefix dismiss `code-prefix`) |
| `ПереходКДокументам21_ПриСозданииНаСервере` | кнопка перехода в Документооборот 2.1 при создании формы | OK (21 = продукт) |
| `pavВыводНастроекФормыНаСервере` | вывести сохранённые настройки формы | OK (`code-prefix`) |
| `ПередУдалением` | обработчик удаления | OK |
| `ИДВнешнегоОбъекта` | идентификатор объекта во внешней системе | OK (`protocol`/принятое ИД) |
| `ТабНЗ` | — | AP-031 |
| `НЗ` / `СтрНЗ` | — | AP-031 |
| `ОбПредмет` | объект предмета задачи | AP-031 (аббревиатура) |
| `ЕстьИзм` | предмет изменён | AP-031 |
| `СтрДР` / `ЗначениеДР` | строка/значение доп. реквизита | AP-031 |
| `ИзмЗадача` | задача изменена с формы | AP-031 + undeclared |
| `СвойстваЗадачи` | список свойств доп. реквизитов задачи | OK по смыслу, мёртвый |
| `КолвоНастроек` | число найденных настроек | AP-031 |
| `КнопкаПереход21` | сторожок/реквизит кнопки 2.1 | OK |
| `ОткрытьДокумент21` | команда открытия документа 2.1 | OK |
| `ЭтаФорма` (параметр) | форма | OK с оговоркой: лучше `Форма` |
| `Узел` | узел плана обмена (факт: ещё и массив) | OK имя, сломан контракт AP-007 |
| `МассивСсылок` / `ЧастиИдентификатора` | контейнеры с доменом | OK |
| `ГруппаСсылкиИС` / `pavОткрытьСвязиИС` | UI ссылок ИС | OK (`code-prefix`, ИС = домен) |
| `СвойстваЗадачи` cycle var `СтрСвойство` | строка описания свойства БСП | OK |

## Touched-scope identifiers (domain test)

Full review: все тела процедур считаются touched.

| Символ | Процедура | Scope | Доменное значение | Verdict |
|--------|-----------|-------|-------------------|---------|
| `ТабНЗ` | `pav_ФормаЗадачиПередЗаписьюНаСервере` | touched-procedure | — | AP-031 MUST_FIX |
| `ОбПредмет` | то же | touched-procedure | объект предмета | AP-031 MUST_FIX |
| `ЕстьИзм` | то же | new-line | признак изменения предмета | AP-031 MUST_FIX |
| `Узел` | `НавигационныеСсылкиИнтегрированногоОбъекта` | touched-procedure | узел ИС | OK имя / AP-007 использование |
| `КолвоНастроек` | `pavВыводНастроекФормыНаСервере` | touched-procedure | количество настроек | AP-031 MUST_FIX |
| `КнопкаПереход21` | `ПереходКДокументам21_ПриСозданииНаСервере` | touched-procedure | кнопка перехода 2.1 | OK |
| `ИДВнешнегоОбъекта` | `НавигационныеСсылкиИнтегрированногоОбъекта` | touched-procedure | внешний идентификатор | OK dismiss protocol |

### Phase 1 notes

- Linter unavailable — documented; JSDoc MUST_FIX вручную.
- Naming evidence skipped — domain-test выполнен, не PASS по «scan skipped».
- Comment hygiene skipped — семантический проход: AP-054 не найден (латиница в `//&Перед` — синтаксис аннотации; `e1cib` в строках кода). AP-040/044 — да.
- Whitelist controls пусты → marker-canon strict: `// + Листов` не exempt.

### Попытка Audit Table

| # | Procedure | Lines | Operations | Throwability | RootCause | External | Guard before | Log | Fallback=success | User feedback | Persistent | Re-raise | Downstream | Verdict |
|---|-----------|-------|------------|--------------|-----------|----------|--------------|-----|------------------|---------------|------------|----------|------------|---------|
| — | — | — | нет блоков Попытка в scope | — | — | — | — | — | — | — | — | — | — | N/A |

Completeness: 0 = 0.

### Defensive Checks Table

| Procedure | Line | Source | Field | Guard | Contract | verified? | Verdict |
|-----------|------|--------|-------|-------|----------|-----------|---------|
| `НавигационныеСсылки…` | 3 | `Узел` | (сам параметр) | `= Неопределено` | optional default | verified optional | OK (бизнес-default); overwrite = AP-007 отдельно |
| `pav_ФормаЗадачи…` | 7 | `Форма.Объект.Ссылка` | тип | `ТипЗнч` | фиксирован формой задачи | unverified → needs-resolution | AP-004 |
| `pav_ФормаЗадачи…` | 6 | `Форма.Объект.Ссылка` | заполненность | `ЗначениеЗаполнено` | новая vs существующая задача | verified business | OK |
| `ПереходКДокументам21…` | 9 | `ЭлементыФормы` | `КнопкаПереход21` | `ЕстьРеквизитИлиСвойствоОбъекта` | динамическая форма | needs-resolution | не AP-004 fixed; неверный ключ = logic HIGH |
| `pav_ФормаЗадачи…` | 41 | `НЗ` | `Количество` | повторный `> 0` | уже отфильтрован | verified | AP-006 |
| access only inside Попытка | — | — | — | — | — | — | нет |

Completeness: все non-DIRECT из Contract Map покрыты.

### Identity / Hardcode Audit Table

| # | Procedure | Line | Literal | Detector | Justification? | Contradiction? | Verdict | Evidence |
|---|-----------|------|---------|----------|----------------|----------------|---------|----------|
| — | — | — | нет `ИмяФормы` / `ОткрытьФорму("…")` / allow-list метаданных в хуке | — | — | — | N/A | `Тип("ЗадачаСсылка.ЗадачаИсполнителя")` — не identity-filter форм; UUID в комментарии pavОбщийМодуль:79 — не runtime; ключ фичи `ИспЗаполнениеРеквПредметаВЗадаче` — не AP-055 |

Completeness: 0 = 0.

### Boundary coverage

Review Boundaries не переданы → **full** по четырём указанным поверхностям. Форма `ФормаЗадачиИсполнителя` и `УправлениеСвойствами` читались только как callers/similar (Phase 3), без MUST_FIX по их телам. BOUNDARY_EXCEPTION: вызов формы с 5 аргументами и `Отказ = Ложь` в форме (std-686 сброс отказа) объясняет поломку контракта ревьюируемого метода, finding записан на сигнатуру `РаботаСБизнесПроцессами`.

### Phase 3

- Similar: `УправлениеСвойствами.pav_ФормаЗадачиПередЗаписьюНаСервере` — канон; `ПолнаяНавигационнаяСсылка21` — тот же byte-swap UUID для e1cib.
- Metadata MCP / RLM: NOT_CONNECTED.
- Prior findings: нет RLM; дубль алгоритма = recurrent candidate (confidence поднят на AP-047 не до 0.9 из-за одного соседнего модуля, не истории ревью).
- Callers: интеграционные методы pavОбщийМодуль живые; `ПереходКДокументам21_ПриСозданииНаСервере` — нет; `pavВыводНастроекФормыНаСервере` — команда карточек.

### Phase 3.5 self-review

- У всех findings: severity/kind из каталога или явная security/vendor категория; risk_score по формуле; QualityFlag/Disposition; Evidence; Counterfactual.
- Subjective AP-036/037: confidence 0.5.
- Silent VERIFIED_OK не ставился.
- design ≠ PASS: антипример std-06 — weak/needs-confirm, не закрыт.
- Q1b → есть REFACTOR AP-036.
- AP-046: не применён (нет активного `&Перед`/`Возврат` в первых операторах; тело обёрнуто в `Если`, аннотация закомментирована).
- AP-008..032: 0 Попытка.
- AP-022: `&Перед` не на Функции.
- AP-011/015/023/026/033/034/035/039/041/042/045/048/049/051/052/053/054/055: не сработали на in-scope коде (AP-048: сборка e1cib из уже строкового ИД, не `УникальныйИдентификатор()` в JSON-обмене).
- `&ИзменениеИКонтроль`: нет в scope; base cf не требовался.

Prompt contract: **4** (агент 4.1, чек-листы в `reviewer-checks.md`).
