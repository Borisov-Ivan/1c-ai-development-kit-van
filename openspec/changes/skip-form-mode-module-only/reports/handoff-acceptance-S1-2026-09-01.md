## Срез S1 — передача на приёмку: skip-form-mode-module-only

**Change:** skip-form-mode-module-only
**Schema:** spec-driven
**Прогресс:** 8/9 задач [x] (приёмка среза S1.accept: [ ])

### 1. Что реализовано

- [x] S1.1 — классификатор «только модуль / разметка / неясно» в правиле поставки формы: при доказанном «только модуль» в карточке сразу программно, форму из списка не выкидывать.
- [x] S1.2 — поясняющая строка «записываю поставку программно»: не вопрос выбора; отсутствие строки не дефект.
- [x] S1.3 — цикл `/opsx:new` на design: классификатор до вопроса; смесь форм в одном ходе; kit без форм по-прежнему «не применимо».
- [x] S1.4 — FAQ: вопрос задаётся не всегда; пустой ответ — только на заданный вопрос.
- [x] S1.5 — быстрый старт: убрана формулировка «new всегда спросит режим формы».
- [x] S1.6–S1.8 — сверка: политика макета и чтение устаревшего единого поля режима не менялись.

### Карта правок (перед тестом)

См. секцию среза S1 в `reports/code-map.md`.

1. Классификатор перед вопросом поставки формы — [`.cursor/rules/forms-mxl-mode-gate.mdc`](.cursor/rules/forms-mxl-mode-gate.mdc):37-65
2. Цикл создания ЗНИ: skip без меню, вопрос только при разметке или неясности — [`.cursor/skills/openspec-new-change/SKILL.md`](.cursor/skills/openspec-new-change/SKILL.md):266-286
3. Справка: вопрос не всегда — [`.cursor/docs/faq-kit.md`](.cursor/docs/faq-kit.md):22-32
4. Быстрый старт §5 — [`.cursor/docs/quick-start.md`](.cursor/docs/quick-start.md):78-80

### 2. Что проверить СЕЙЧАС

**Primary acceptance:** создать учебную ЗНИ с постановкой «только модуль панели, разметку не трогаем» → выбора вручную/автоматически/программно нет → в карточке поставка программно.

Опционально (не блокирует приёмку): поясняющая строка не требует ответа; постановка с разметкой по-прежнему даёт вопрос из трёх; две формы (модуль + разметка) получают разные режимы. Остальные — см. `tasks.md`.

### 3. Следующие задачи

| Задача | Действие | Тип | Исполнитель | Зависит от | Статус |
|--------|----------|-----|-------------|------------|--------|
| `S1.accept` | Принять срез: на постановке «только модуль» нет выбора из трёх, в карточке программно | Ручной тест | пользователь | `S1.1`–`S1.8` | [ ] |

### 4. Как вернуться

`/opsx:apply skip-form-mode-module-only` — новая сессия начнётся с запроса вердикта (принят / не принят / дефект в предыдущем срезе). Если нужно изменить scope/design/tasks — `/opsx:extend skip-form-mode-module-only`; затем снова `/opsx:apply skip-form-mode-module-only`.

### 5. Blockers

Нет.

### 7. Short-cut

Если уже проверено и принято — напишите обычной фразой («принято», «срез S1 принят»).
