---
report_type: architecture
generated_at: 2026-08-28
agent: onec-code-architect
mode: task-readiness
scope:
  change: scenario-map-canvas
  files:
    - .cursor/skills/scenario-map-canvas/SKILL.md
    - .cursor/skills/scenario-map-canvas/fixtures/canvas-shell.md
    - .cursor/skills/scenario-map-canvas/fixtures/map-good-causal.md
    - .cursor/skills/scenario-map-canvas/fixtures/map-bad-accordion.md
    - .cursor/agents/onec-scenario-map-designer.md
    - .cursor/skills/1c-agent-patterns/scenario-map-designer.md
    - .cursor/skills/openspec-explain/SKILL.md
    - .cursor/skills/openspec-explain/templates/inventory-card.md
    - .cursor/skills/openspec-explain/templates/exit-card.md
    - .cursor/skills/openspec-explain/templates/explain-report.md
    - .cursor/skills/openspec-explore/SKILL.md
    - .cursor/rules/gate-dispatcher.mdc
    - .cursor/rules/model-selection.mdc
    - .cursor/rules/tool-name-guard.mdc
    - .cursor/rules/1c-agent-delegation.mdc
    - .cursor/docs/chat-lexicon.md
    - openspec/glossary.md
    - AGENTS.md
verdict: READY
confidence: high
open_questions_count: 0
---

# Task Readiness — scenario-map-canvas (post-apply)

## KB references

Блок `## Existing Knowledge` во входном промпте: Discovery выполнен, совпадений нет (kit, таксономии KB в репозитории нет; подтверждено `design.md:7` «Таксономия KB в kit отсутствует — Discovery пропущен»). KB-фактов, применимых к этой ЗНИ, нет; секция `## Knowledge conflicts` не требуется.

## Task

Оценить, можно ли было по `proposal.md` / `design.md` / `specs/scenario-map-canvas/spec.md` / `tasks.md` реализовать ЗНИ без возвратов. Post-apply: все `S<N>.<M>` и все три `S<N>.accept` — `[x]`. Предмет — markdown кита (скиллы, шаблоны, правила, агент), не прикладной BSL.

## Complexity

Medium — 18 файлов кита в трёх слоях (контракт скилла, точки вызова в двух командах, маршрутизация роли), без метаданных 1С и без форм.

## Simplicity Check

`Only one viable option` — режим `task-readiness` не выбирает техническое решение, а проверяет уже зафиксированный план на исполнимость. Альтернативной архитектуры аудита нет; выбор решения сделан в `design.md` § Implementation Options (Option B) и отдельно не пересматривается в этом отчёте.

## Итоговый вердикт

**READY** — GAP по пяти критериям промпта и по критерию 8 (Precedent Coherence) не обнаружено. Каждая задача называет файл, содержание и D-ссылку; каждый затронутый файл существует и содержит предписанное; каждое требование дельты имеет реализующую задачу; пользовательских runtime-шагов внутри `S<N>.<M>` нет.

Ниже — сверка по критериям и четыре наблюдения без статуса блокера.

## Критерий 1 — Реализуемость задач кита

**OK.**

Проверка сплошная: для каждой задачи `S<N>.<M>` сверено, что целевой файл существует и содержит предписанный текст.

| Слой | Задачи | Подтверждение в коде кита |
|---|---|---|
| Контракт панели | S1.1–S1.7, S2.1, S2.1a, S2.1b, S2.4, S2.4b, S2.5, S2.7, S3.1 | `.cursor/skills/scenario-map-canvas/SKILL.md`: Entry Protocol (25–29), порядок действий 1–8 (35–48), шапка/узел/связь/виды (60–94), закрытый словарь с `follows` (102–108), таблица «Молчание и пороги» (116–125), проверка топологии (131–143), отложенная постройка (149), текстовый резерв с тремя причинами (190–202) |
| Эталоны и шаблон файла | S2.2, S2.3, S2.3a, S2.13 | `fixtures/map-good-causal.md`, `fixtures/map-bad-accordion.md` (ссылки — SKILL.md:216–218); `fixtures/canvas-shell.md` — каркас с `openFile` (70–82) и запретом `newComposerChat` (10) |
| Картограф | S2.8, S2.8a, S2.9–S2.12 | `.cursor/agents/onec-scenario-map-designer.md`: `model: inherit` (2), OUTPUT без `file_path` (33–48), guardrails (50–58); `model-selection.mdc:94` строка `*(без model)*`; `tool-name-guard.mdc:46`; `1c-agent-delegation.mdc:29`; `1c-agent-patterns/SKILL.md:44`; `AGENTS.md:19`; `agents-CHANGELOG.md:7–9`; шаблон промпта `1c-agent-patterns/scenario-map-designer.md` (манифест, `check`/`repair_round`, «поля file_path нет») |
| Точки вызова разбора | S1.9, S1.10, S1.10a, S2.5, S2.6, S3.2, S3.3, S3.4 | `openspec-explain/SKILL.md:135–142, 154, 176`; `inventory-card.md:14, 24` (вариант «сразу карту»); `exit-card.md:25, 57, 81` (обе строки «Следующий шаг», условия замеров нет); `explain-report.md:108, 130` (секция «Схема (текстовый резерв)» и оговорка о сохранении); `gate-dispatcher.mdc:28` |
| Точка вызова исследования | S3.5 | `openspec-explore/SKILL.md:217` (правило приоритета «Дальше», запрет схемы и разбора вместе, отсечка при блоке постановки), `229`/`231` (интерпретация согласия и «без карты») |
| Словарь и указатели | S1.11, S1.12, S3.6 | `chat-lexicon.md:69`; `openspec/glossary.md:20–22` (три имени разведены); `AGENTS.md:19` |
| Статические сверки | S1.13, S2.12, S3.7 | Формулировки самодостаточны: перечислены файлы и признак прохождения; отдельной команды карты в `.cursor/commands/` нет |

Формулировки задач исполнимы буквально: глагол + путь + что записать + ссылка на решение. Ни одна задача не требует изобретать контракт, отсутствующий в `design.md`.

## Критерий 2 — Реализуемость форм и метаданных

**n/a** — как и ожидалось. `proposal.md:23` — `form_mode: n/a`; `proposal.md:48` — «Продуктовый BSL и конфигурация 1С не меняются». В `tasks.md` нет ни одной задачи на `src/**`, `Form.xml`, `Template.xml` или XML метаданных. Mode Gate форм и XML-write-guard в этой ЗНИ не задействованы.

## Критерий 3 — Разрешённость решений

**OK.**

- Все D-ссылки из `tasks.md` (D1–D10) резолвятся в `design.md`; висячих ссылок нет.
- Спорные оси зафиксированы явно, а не оставлены на исполнителя: «просьба сильнее предложения» и порог после двух отсевов (D3, § «Решения verify»); `parent_side_registration` — файл пишет родитель, картограф отдаёт манифест (D8, D10); порядок прохода подписывается только `follows` (D3, § «Происхождение связи»); отложенная постройка после «сразу карту» с публикацией в том же сообщении, где карточка впервые дала порог (D1 п.2).
- Пять открытых вопросов (`design.md:228–232`) — ни один не блокер: 1 и 3 — MAY после живой эксплуатации; 2 — вне scope (отдельная ЗНИ); 4 — follow-up без чекбокса; 5 — закрыт формулировкой.
- Развилки, которые могли бы остановить исполнителя (что делать при линейной цепочке; что при неподтвердившейся топологии; кто пишет файл; чем считать успех публикации), закрыты однозначными предписаниями, а не «на усмотрение».

## Критерий 4 — Полнота покрытия requirements

**OK.** Десять требований дельты, у каждого — реализующие задачи, не только приёмочные пункты.

| Requirement | Реализующие задачи |
|---|---|
| Silence unless asked or hinted | S1.3, S1.8, S2.4, S2.6 |
| Direct request draws the scenario map | S2.1, S2.4, S2.4b, S2.7, S2.12; согласие — S3.4 |
| Technical fallback is not a map | S1.6, S1.10a, S2.5 |
| Node contract forbids empty or code-primary nodes | S1.4, S2.1, S2.1a, S2.1b, S2.3 |
| Causal map has layers or branches | S2.1a, S2.2, S3.1 |
| Offer by topology not by topic | S3.1, S3.2, S3.3, S3.4, S3.5 |
| No dedicated map command | S1.13, S3.7 |
| Two map names stay distinct | S1.11, S3.6; эталон — S2.2, S2.3, S2.3a |
| Hint only on an existing decision line | S2.6, S3.3, S3.4, S3.5 |
| Map outside walkthrough uses named source | S1.5, S2.7 |

Обратная проверка: задач без требования нет — каждая `S<N>.<M>` ложится либо на требование дельты, либо на статическую сверку инварианта D9 (S1.13, S2.12, S3.7). Покрытие 40 Scenarios подтверждено независимо в `reports/quality-control-2026-08-28-7.md`; расхождений с моей сверкой нет.

Инвариант U (D9) исполним и проверяем: предикаты предложения в `SKILL.md:131–139`, `exit-card.md:57/81`, `inventory-card.md:24`, `openspec-explore/SKILL.md:217` сформулированы через форму связей; предметных имён нет. Предметный сюжет остался только в эталоне `map-good-causal.md` — ровно одна из двух разрешённых ролей.

## Критерий 5 — User Task Contract

**OK.** Нарушений вида «user runtime-spike в `S<N>.<M>`» нет.

- Все 41 реализующая задача — работа агента над markdown: «Создать», «Добавить», «Переписать», «Дописать», «Сузить», «Описать», «Развести», «Убрать», «Верифицировать по коду кита», «Проверить по скиллу». Ни одна не просит пользователя открыть Конфигуратор, поднять ИБ, запустить консоль или дождаться стенда.
- Сессионные проверки живут только в `S1.accept` / `S2.accept` / `S3.accept` — это приёмка среза, не нарушение контракта. Метка «без ИБ продукта» в метаданных приёмки подтверждает граничный характер шага.
- `## Follow-up` (kill-критерии) вынесен за срезы и **не имеет чекбокса** — незакрытой работы внутри слайс-гейта нет.

## Критерий 8 — Precedent Coherence

**OK.** `design.md` содержит `## Blast Radius` (186–204) с колонками контракт / архивный источник / бизнес-эффект / альтернативы / обоснование; отменяемые предикаты среза S1 перечислены построчно (намёк при замерах, запрет намёка в «Дальше», предикат доступности панели, ссылка как критерий успеха, картограф пишет файл, плоский контракт узла, эталон-список). ADR-0001, ADR-0002, ADR-0006 отмечены как соблюдённые, `Supersedes: нет`. Капабилити одна, вторая активная дельта на неё не заводилась (Option «новая ЗНИ» отклонён). Молчаливой отмены прецедента нет.

## Наблюдения (не GAP, не блокируют)

### O1. Шаблон панели фиксирует внешний API, у которого нет якоря версии

`fixtures/canvas-shell.md` жёстко называет поверхность носителя: импорт из `cursor/canvas`, `useCanvasAction`, `useCanvasState`, действие `openFile`, упоминание `computeDAGLayout`. Источник этих имён — глобальный скилл среды, который D7 намеренно **не** копирует в kit. Для реализации этого хватило (источник назван), но у шаблона нет ни версии носителя, ни строки «при расхождении — сверить с глобальным скиллом». Риск отложенный: смена API среды сделает fixture молча устаревшим, а обнаружится это только на живой публикации.

Рекомендация (не для этой ЗНИ): при первом расхождении добавить в fixture одну строку-указатель на глобальный скилл как на источник имён API.

### O2. Один возврат внутри S2 был вызван фактом, невыводимым из артефактов

`S2.4a` вытеснена `S2.4b` / `S2.8a`: путь «картограф пишет файл» заменён на «картограф отдаёт манифест, файл регистрирует родитель». Основание — разовый опыт 2026-08-28 (`design.md:130`): запись субагента панель не открыла, полная запись родителя открыла. Этот факт нельзя было получить из `proposal.md` / `design.md` до попытки — то есть возврат был не следствием слабой постановки, а следствием непроверяемого заранее поведения среды. Сейчас факт зафиксирован как решение (D10) и как приёмка (`slice-acceptance-S2`, круг ремонта 1 → чистая проверка).

Остаточная точка того же типа: допущение «каталог панелей резолвится по путям сессии и правилам глобального скилла Canvas» (`design.md:176`) не имеет в ките процедуры проверки — на неё же опираются две из трёх причин текстового резерва. Для будущего исполнителя это единственное место, где он зависит от внешних, не закреплённых в репозитории правил.

### O3. Порядок блоков решений в design.md

D10 стоит перед D9 (`design.md:126` и `134`). Косметика: все ссылки из `tasks.md` резолвятся, двусмысленности нет. При правке design стоит переставить, чтобы чтение шло по номерам.

### O4. Косметика из QC подтверждена, на готовность не влияет

Обе SUGGESTION из `quality-control-2026-08-28-7.md` воспроизводятся: заголовок S1 всё ещё называет намёк на выходе, которым владеет S3; Scenario «Карта во время прохода из уже пройденных точек» назван в `S2.accept`, но не процитирован в `**Связь со spec:**` ни одного среза. Покрытие при этом полное (S1.5 + optional-пункт приёмки). Ни то, ни другое не могло вызвать возврат при реализации.

## Ответ на вопрос промпта

По этим артефактам ЗНИ была реализуема без возвратов **по постановке**: контракт карты, порог, происхождение связей, разделение «картограф — манифест / родитель — файл», критерий успеха и обе точки предложения заданы однозначно, а спорные оси закрыты в § «Решения verify». Единственный фактический возврат внутри apply (S2.4a → S2.4b) вызван поведением среды, которое артефакты не могли предсказать; после его фиксации остаточных незакрытых развилок в плане нет.

## Next Steps

1. Наблюдения O1–O4 — не блокеры; O3 и O4 закрываются косметической правкой при следующем касании `design.md` / `tasks.md`.
2. Отдельного архитектурного действия перед архивированием не требуется.
