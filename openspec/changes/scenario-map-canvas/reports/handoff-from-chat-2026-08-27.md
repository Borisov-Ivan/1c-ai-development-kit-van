# Постановка из чата — scenario-map-canvas

Дата: 2026-08-27  
Роль: primary source для `/opsx:new` (блок `## Постановка ЗНИ` в сессии).  
Не является отчётом исследователя: файлы `temp/reports/exploration-2026-08-27-*.md` в этот каталог не копировались.

## Симптом

Панель рядом с чатом рисуется вне правил кита. Первый макет стал навигатором по модулю.

## Корневая причина

[verified] по постановке: ценность узкая (≥5 шагов + замеры/ветвление); журнал во время прохода ещё не записан.

## Что менять

Один скилл, жёсткий контракт узлов, без отдельной команды, намёк в существующей строке «Следующий шаг», молчание по умолчанию, панель статична, код в чате/журнале.

## Файлы (из постановки)

- скилл `.cursor/skills/<имя>/SKILL.md`
- строка в диспетчере гейтов
- вариант в шаблоне выхода explain
- условие в explore
- строка в `AGENTS.md`

## Architect / verify

not-required для прикладной конфигурации 1С (kit-only).

## Срезы (черновик постановки → design)

S1 контракт + молчание; S2 намёк; эталон узлов влит в S1 (живой файл панели вне git).
