## Срез S2 — передача на приёмку: visual-explanation-composition

**Change:** visual-explanation-composition
**Schema:** spec-driven
**Прогресс:** 9/11 задач [x] (приёмка среза S2.accept: [ ])

### 1. Что реализовано

- [x] S2.1 — в навыке сначала называется одна работа над уже сказанным текстом, затем язык картинки; скелет только для слоёв; голая просьба показать схему не выбирает поток из цепочек отчёта, если классы уже сказаны. файл: `.cursor/skills/visual-explanation/SKILL.md`
- [x] S2.2 — шаблон стал библиотекой рецептов: классификация рядом без обязательных кнопок; копия без поля формы не рисует скелет; пустые связи не ставят стрелки следования. файл: `.cursor/skills/visual-explanation/fixtures/panel-shell.md`
- [x] S2.3 — в опорном решении перечень форм — подсказка рецепта, не закрытый мир; умолчания скелета нет. файл: `openspec/adrs/ADR-0010-visual-explanation-panel.md`
- [x] S2.4 — сверка по файлам kit совпала с дельтой среза.

### Карта правок (перед тестом)

См. секцию «Срез S2» в `reports/code-map.md`.

## Explain scope (handoff)

- source: apply
- change: visual-explanation-composition
- focus: slice-S2
- files:
  - path: .cursor/skills/visual-explanation/SKILL.md
  - path: .cursor/skills/visual-explanation/fixtures/panel-shell.md
  - path: openspec/adrs/ADR-0010-visual-explanation-panel.md
- report: openspec/changes/visual-explanation-composition/reports/code-map.md

### 2. Что проверить СЕЙЧАС

1. В чате уже есть сопоставление одноимённых эффектов (классы) и просьба дать схему, отдельно отметив отличия; журнала разбора может не быть.
2. Открыть панель по просьбе (без подсказки «три колонки»).
3. Одно полотно класса сопоставление: уточнённый пункт — элемент класса, не ветка процесса; пометки одного ранга; интерактив не обязателен; если якорь доказательства есть — один или точечный, не пачка кнопок в один файл; в чате нет пути к файлу.

Опционально: остальные сценарии — см. `tasks.md`, задача `S2.accept`.

### 3. Следующие задачи

| Задача | Действие | Тип | Исполнитель | Зависит от | Статус |
|--------|----------|-----|-------------|------------|--------|
| `S2.accept` | Принять срез — классы уже сказанного сопоставления без конвейера и без пачки кнопок | Ручной тест | пользователь | `S2.1`–`S2.4` | [ ] |

### 4. Как вернуться

`/opsx:apply visual-explanation-composition` — новая сессия начнётся с запроса вердикта (принят / не принят / дефект в предыдущем срезе).

### 5. Blockers

Нет.

### 7. Short-cut

Если уже проверено и принято — напишите обычной фразой («принято», «срез S2 принят»), отмечу без повторной простыни в чате.
