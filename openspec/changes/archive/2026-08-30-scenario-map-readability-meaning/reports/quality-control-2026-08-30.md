# Quality Control — scenario-map-readability-meaning

Date: 2026-08-30  
Report: `quality-control-2026-08-30.md`  
Mode: **slice** (`# Срез S1` in `tasks.md`)  
Scope: criteria 1–6, 8, 8b, 9–11 + task readability  
Prior reports: `quality-control-2026-08-29.md` … `quality-control-2026-08-29-4.md` — **не перезаписывались**. Этот прогон — после петли приёмки и пересборки постановки (главный вид отвечает на вопрос шапки; два средства; бюджет разборчивости).  
Out of scope: исполнимость приёмки «прямо сейчас» на живой панели / в проекте Документооборота; тестовые данные; эталоны ИБ (отсутствие `map-good-table.md` — результат открытой задачи, не дыра среза).

Context: kit-only change (скилл, шаблон панели, эталоны, роль сборщика, дельта spec, ADR-0009). Продуктовый BSL/XML не требуется. `form_mode: n/a`. **Режим apply:** mechanical. В spec **17** `#### Scenario:`. Приёмочный чекбокс среза открыт. Закрытые `S1.21`–`S1.22` — блокер доставки, в переработку не входят; как слой «кнопка среды» они остаются силами этого же среза. Приёмка живой панели — только на границе среза (`S1.accept`), не внутри рабочих задач.

Sources: `tasks.md`, `design.md` (`## Slices`), `proposal.md`, `specs/scenario-map-canvas/spec.md` (17 `#### Scenario:`), `.cursor/rules/vertical-slices.mdc`, `.cursor/rules/task-readability.mdc`.

Mechanical pre-check (prompt): чекбоксы на месте; ровно один `S1.accept`; закрывающий `<!-- slice-gate -->` есть; `<!-- phase-gate -->` нет; пустых буллетов нет; ID задач с префиксом среза. Нумерация рабочих задач: 1–20, затем 23–25 (новые), 21–22 закрытый блокер доставки — намеренно. User Task Contract DENY-grep по строкам `^- \[[ x]\] S\d+\.\d+` (не accept): none. Маркеров ручной конфигурации нет.

Repository (prompt): `canvas-shell.md`, `SKILL.md`, `map-good-causal.md`, `map-bad-no-insight.md`, `onec-scenario-map-designer.md`, `ADR-0009` — на месте; `map-good-table.md` отсутствует (явный результат открытой `S1.25`).

## Verdict

`OK`

Один срез, одна приёмка, один маркер границы. Все семнадцать сценариев спецификации покрыты. Обязательный осмотр — **один**: попросить карту по теме с несколькими отчётами → открыть панель штатной кнопкой → скрыть шапку → назвать ответ и ловушку; в том же взгляде имена читаются без сжатия всего полотна и стартово выбран носитель исхода. Это не четыре отдельных обязательных прохода. Скрыть шапку, бюджет ширины, стартовый узел и ловушка достижимы задачами **этого же** среза; второго среза нет и не требуется.

## Slice Summary

| Slice | Scenario | Tasks | Acceptance | Dependencies | Gate |
|---|---|---|---|---|---|
| S1: Читаемая карта со смыслом | Разработчик просит карту по теме с несколькими отчётами и открывает панель штатной кнопкой среды | 25 агентских (`S1.1`–`S1.25`) + `S1.accept`; группы `## 1`–`## 8`. Открытая переработка: `S1.1`, `S1.2`, `S1.4`, `S1.9`–`S1.11`, `S1.13`–`S1.15`, `S1.19`–`S1.20`, `S1.23`–`S1.25`. Закрыты и не в переработке: `S1.3`, `S1.5`–`S1.8`, `S1.12`, `S1.16`–`S1.18`, `S1.21`–`S1.22` | `S1.accept` (17/17 имён: 6 в Primary, 11 optional) | нет | да (`<!-- slice-gate -->`) |

Notes:

- Порог: 25 кодовых задач — Full (≥16). Второй срез **не** требуется: один самостоятельный пользовательский исход (на открытой панели при скрытой шапке читатель называет ответ и ловушку; имена читаются; стартово выбран носитель исхода). Согласовано с `design.md` `## Slices` (черновик из трёх срезов и отдельный «контракт средства» отклонены).
- `map-good-table.md` в репозитории ещё нет — результат `S1.25`, не structural unreachability Primary (таблица — optional; Primary закрывается графом с ловушкой).
- `map-bad-no-insight.md` существует; задача «создать или обновить» остаётся открытой (предикат скрытой шапки) — доработка эталона, не дыра слоя.
- Дубль имени «Аннотации не добирают порог» в поле `**Связь со spec:**` снят: имя один раз, у требования про контракт узла. В spec сценарий живёт под требованием про слои; MUST «аннотации не в пороге» есть и в тексте контракта узла. Уникальных имён — 17.
- Нумерация `S1.23`–`S1.25` после `S1.20`, закрытый блок `S1.21`–`S1.22` ниже — намеренный разрыв ID, не дубль приёмки и не второй срез.

## Scenario Coverage

17 `#### Scenario:` в `openspec/changes/scenario-map-readability-meaning/specs/scenario-map-canvas/spec.md`. Поле `**Связь со spec:**` перечисляет все семнадцать уникальных имён. Имена в ёлочках чеклиста и задач совпадают с заголовками spec буквально.

| Scenario | Covered by | Status |
|---|---|---|
| Ответ читается при скрытой шапке | Primary `S1.accept` + `S1.4`, `S1.9` | OK |
| Текст полотна читается в ширине панели | Primary `S1.accept` + `S1.23`, `S1.9` | OK |
| Стартово выбран носитель исхода | Primary `S1.accept` + `S1.3` (закрыта), `S1.10`, `S1.11` | OK |
| Главный вывод виден без клика по узлу | Primary `S1.accept` + `S1.3` (закрыта), `S1.10` | OK |
| Находка, меняющая действие, на полотне | Primary `S1.accept` + `S1.5` (закрыта), `S1.11` | OK |
| Ловушка одной подписи видна на полотне | Primary `S1.accept` + `S1.15`, `S1.17` (закрыта), `S1.19` | OK |
| Слои или ветвление видны на панели | optional `S1.accept` (если средство — граф) + `S1.1` | OK |
| Направление связей и легенда видны без клика | optional `S1.accept` (если средство — граф) + `S1.2` | OK |
| С узла открывается доказательство, новый чат с панели не запускается | optional `S1.accept` + `S1.3`, `S1.7` (закрыты) | OK |
| Вывод только в шапке не публикуется | optional `S1.accept` + `S1.5` (закрыта), `S1.13` | OK |
| Плашка без доказательства не публикуется | optional `S1.accept` + `S1.13`, `S1.17` (закрыта) | OK |
| Аннотации не добирают порог | optional `S1.accept` + `S1.16` (закрыта), `S1.20` | OK |
| Переключатель вида не прячет рёбра | optional `S1.accept` (если средство — граф) + `S1.2` | OK |
| Связь без видимого отношения не выдумывается | optional `S1.accept` + `S1.12` (закрыта), `S1.18` (закрыта) | OK |
| Провал смысла не уходит в текстовый резерв | optional `S1.accept` (сверка скилла) + `S1.8` (закрыта), `S1.20` | OK |
| Эталоны показывают средство, отвечающее на вопрос | optional `S1.accept` (сверка) + `S1.14`, `S1.25` | OK |
| Таблица со смысловыми колонками отвечает на вопрос | optional `S1.accept` + `S1.24`, `S1.25` | OK |

Покрытие только задачей агента больше не используется: «Провал смысла…» и «Эталоны…» имеют optional-буллеты. `accept-bullets-missing-scenario` **не** эмитируется.

Implementation-only путь (сверка скилла / эталона markdown) сохранён рядом с optional: user IB/runtime spike в `S1.<M>` нет.

## Dependency Graph

```mermaid
flowchart LR
  S1[S1 Читаемая карта со смыслом]
```

- Cycles: none.
- Forward acceptance dependencies: none (единственный срез; критерий 8b не срабатывает механикой пары S1/S2).
- Undeclared slice predecessors: none (`**Зависимости:** нет`).
- Intra-slice (порядок файла): шаблон (`S1.1`–`S1.4`) → скилл (`S1.5`–`S1.10`) → роль/бриф (`S1.11`–`S1.12`) → эталон «полотно без смысла» (`S1.13`–`S1.14`) → аннотации (`S1.15`–`S1.19`) → сверка текстов (`S1.20`) → бюджет и второе средство (`S1.23`–`S1.25`) → закрытый блокер доставки (`S1.21`–`S1.22`) → `S1.accept`. Цикла нет. Явная межсрезовая зависимость не нужна.

## Criterion 8b — Primary достижим силами этого среза

Пары соседних срезов нет. Семантика: каждый элемент обязательного осмотра имеет включающий слой в `S1.<M>` (открытые или уже закрытые задачи того же среза).

| Элемент Primary | Задачи того же среза | Достижимость |
|---|---|---|
| Попросить карту по теме с несколькими отчётами | `S1.11` (INPUT: несколько путей и расхождения) | да, открыта |
| Открыть панель штатной кнопкой среды | `S1.21`, `S1.22` (закрытый блокер доставки; в переработку не входят) | да, слой уже в срезе |
| Скрыть шапку | `S1.4` (переключатель в шаблоне панели) | да, открыта |
| Назвать ответ на вопрос шапки при скрытой шапке | `S1.5` (закрыта, проверка смысла), `S1.9` (предикат `6a` при скрытой шапке) | да |
| Назвать ловушку | `S1.15` (рисунок аннотации у якоря), `S1.17`/`S1.18` (закрыты), `S1.19` (ловушка на хорошем эталоне графа) | да |
| Имена читаются без сжатия всего полотна | `S1.23` (запрет масштабировать полотно целиком), `S1.9` (бюджет в `8a`) | да, открыты |
| Стартово выбран носитель исхода | `S1.3` (закрыта: стартовый выбор `header.focus_node`), `S1.10`/`S1.11` (носитель исхода, не первый шаг) | да |

Наблюдаемый исход не заимствован у более позднего среза. Таблица как второе средство **не** входит в mandatory Then: optional `S1.24`/`S1.25`. Отсутствие `map-good-table.md` до `S1.25` — результат задачи, не forward-зависимость приёмки.

Формулировка `S1.4` («закрепить в обязательных пунктах … переключатель») указывает на тот же файл шаблона панели, что и остальные правки полотна. Слой для When «скрыть шапку» в срезе есть. Уточнение для apply (не дефект 8b): в шаблоне должен появиться сам переключатель (шапка и абзац вывода скрыты, полотно и детали узла остаются — Behavior Contract п.12), а не только строка чек-листа.

`slice-accept-not-self-achievable` **не** эмитируется. Объединение срезов не требуется.

## Criterion 10 — один обязательный осмотр или несколько?

В теле `S1.accept` **один** mandatory-буллет без пометки «опционально». Остальные одиннадцать помечены «(опционально)».

Сплетение «ответ + ловушка + читаемость имён + стартовый узел» — **один осмотр одной опубликованной панели**, не четыре обязательных прохода:

1. Given: карта по теме с несколькими отчётами (чтобы на полотне была находка/ловушка).
2. When: открыть штатной кнопкой → скрыть шапку.
3. Then (тот же кадр): назвать ответ и ловушку; имена читаются без сжатия всего полотна; в панели деталей выбран носитель исхода (детали узла остаются при скрытой шапке, Behavior Contract п.12).

Это не несколько journey: нет второй публикации, нет другого экрана, нет второго When «пересобрать карту». Ловушка — аннотация на том же полотне, что несёт ответ. Ширина — свойство того же полотна. Стартовый выбор виден в деталях на том же кадре. `design.md` `## Slices` прямо отклоняет разделение «читаемость» и «смысл»: проверяются одним взглядом на ту же панель.

Шесть имён Scenario в скобках одного Then — покрытие связанных сценариев одним mandatory journey (как в каноническом примере «открыть → включить флаг → сохранить → переоткрыть»). Разные WHEN в spec («только что открыта» vs «скрыл шапку») здесь — шаги одной последовательности, не два blocking-буллета.

`acceptance-simplicity-overload` **не** эмитируется. Разворачивать шесть имён в шесть обязательных буллетов **запрещено** remediation критерия 10.

## Checklist Results

| # | Criterion | Result |
|---|---|---|
| 1 | Scenario Coverage | **Pass** — 17/17. Primary / optional / смежная задача агента. User runtime только на границе `S1.accept` |
| 2 | Slice Independence | **Pass** — принимать S1 можно без несуществующего следующего среза |
| 3 | Slice Completeness | **Pass** — слои kit для Primary: шаблон (`S1.1`–`S1.4`, `S1.15`, `S1.23`), скилл (`S1.5`–`S1.10`, `S1.16`–`S1.17`), роль (`S1.11`–`S1.12`, `S1.18`), эталоны (`S1.13`–`S1.14`, `S1.19`), сверка (`S1.20`), кнопка среды (`S1.21`–`S1.22`, закрыты). Таблица (`S1.24`–`S1.25`) — слой optional-сценария, не дыра Primary. Метаданных/форм 1С нет и не требуется |
| 4 | Slice Dependency Graph | **Pass** — S1 → нет |
| 5 | Slice Gate Integrity | **Pass** — ровно один `S1.accept`, ровно один `<!-- slice-gate -->`. Legacy `S1.T<M>` нет. Фаз / `phase-gate` нет |
| 5b | Acceptance Checklist Coverage | **Pass** — есть `**Primary acceptance:**` в metadata и `**Primary (обязательно):**` в теле accept. Чеклист не пуст. Чужих сценариев нет (один срез). Все Scenario spec где-то покрыты |
| 6 | Rework Risk | **Pass** — нет опоры на непринятый предыдущий срез; сценарии не дублируются между срезами. Повторное открытие части `[x]` после петли — inside-slice завершение того же среза (приёмка не подписана), не ложная граница. `S1.21`–`S1.22` закрыты и исключены из переработки намеренно |
| 8 | Slice Verticality | **Pass** — mandatory Primary: попросить карту → открыть штатной кнопкой → скрыть шапку → **назвать** ответ и ловушку; имена читаются; выбран носитель исхода. Black-box (панель рядом с чатом), не вызов API и не код-ревью. Programmatic (сверка скилла, эталон markdown) в optional / `S1.<M>` |
| 8b | Self-Achievable Acceptance | **Pass** — см. секцию выше. Нет пары S1/S2. Все включающие слои Primary лежат в задачах этого же среза |
| 9 | Foundation slice with gate | **Pass** — нет foundation-среза с programmatic accept и зависимого UX-среза. Подготовка шаблона, таблицы и аннотаций внутри того же S1 |
| 10 | Acceptance Simplicity | **Pass** — **один** mandatory black-box journey. Ответ+ловушка+читаемость+стартовый узел = один осмотр (см. секцию критерия 10). Не четыре обязательных буллета |
| 11 | User Task Contract | **Pass** — DENY-grep по `S1.<M>` пуст. Семантика: рабочие задачи — правки markdown / сверка по тексту (агент). Осмотр живой панели, скрытие шапки, клик, негатив «панель не появилась» — только в `S1.accept`. Условных цепочек «после verify / после стенда» нет. «После чистой проверки панели» в `S1.6` (закрыта) — место вставки чек-листа в скилл, не обязанность пользователя на стенде. Assumptions design п.4 (пересборка в Документообороте) = граница приёмки |

## Task Readability

Применяется ко всем `S1.1`–`S1.25` кроме `S1.accept`. `S1.accept` — по исключению (бизнес-результат в заголовке + чеклист).

| Check | Result |
|---|---|
| Глагол + файл/объект + зачем + опора (D/ADR/Scenario) | **Pass** — зачины «В `<путь>`: …» / «Создать `<файл>`» / «Сверить по тексту …»; опоры D1–D11, ADR-0008, ADR-0009, Behavior Contract, имена Scenario |
| Антипаттерн «голый идентификатор решения» | нет |
| Антипаттерн «глагол без объекта» | нет |
| Описание < 8 слов | нет |
| `S1.accept` заголовок с бизнес-результатом | **Pass** — «карта отвечает на вопрос шапки и читается глазами» |
| Имена Scenario в ёлочках | **Pass** — буквальное совпадение с `#### Scenario:` spec; 17/17 имён в чеклисте accept |
| Recipe vs outcome | **Pass** — детали раскладки, бюджета (`maxWidth`), двух средств и аннотаций — принятые решения design (D1, D2, D9–D11, ADR-0009), не самовольный рецепт вместо наблюдаемого Then |

Алерты читаемости не эмитируются. `S1.7`, `S1.12`, `S1.18`, `S1.24` трогают два файла одной строкой — формулировки самодостаточны, дробить не требуется.

## Alerts

Нет.

**Не эмитировано:** `no-slices`; `deprecated-phase-gate`; `legacy-acceptance-format`; `primary-acceptance-missing`; `accept-checklist-empty`; `accept-bullets-missing-scenario`; `accept-bullet-foreign-scenario`; `slice-not-vertical`; `slice-accept-not-self-achievable`; `slice-foundation-with-gate`; `acceptance-simplicity-overload`; `user-task-contract-violation`; `task-opaque-title`; `task-too-short`; `task-opaque-acceptance`.

## Recommendations

**Automatic fix**

Нет.

**Decision required**

Нет. Объединение срезов / переписывание Primary не требуется.

**Do not**

- Не вводить второй срез «шаблон», «таблица», «бюджет» или «смысл отдельно».
- Не разворачивать шесть имён в Primary в шесть обязательных буллетов.
- Не переносить осмотр живой панели / скрытие шапки / клик / негатив «панель не появилась» в `S1.<M>`.
- Не откладывать подпись `S1.accept` «пока не будет следующего среза».
- Не требовать уже созданного `map-good-table.md` / тестовых данных / эталона ИБ как условие когерентности среза (out of scope QC / результат задачи).
- Не открывать заново `S1.21`–`S1.22` ради переработки представления: слой кнопки среды уже в срезе.

**Apply note (не алерт):** при `S1.4` в шаблоне панели нужен живой переключатель «скрыть шапку» (Behavior Contract п.12), чтобы When обязательного осмотра был выполним после закрытия задач среза.
