## Срез S1 — передача на приёмку: last-slice-review-or-archive

**Change:** last-slice-review-or-archive
**Schema:** spec-driven
**Прогресс:** 10/10 рабочих задач [x]; приёмка среза S1.accept: `[ ]` (ручная приёмка)

### 1. Что реализовано

После «принято» на последнем срезе ЗНИ с кодом расширения чат задаёт один вопрос с тремя словами: `ревью`, `архив`, `стоп`. Слово `ревью` печатает команду предрелиза с именем ЗНИ и не запускает ревью в этой сессии. Слова `архив` и `стоп` ведут себя как раньше. Без модулей расширения третьего слова нет. Тот же вопрос — при возврате в сессию с «принят», при входе в полностью принятую ЗНИ и в карточке завершения.

- [x] S1.1 — единственная формулировка развилки в скилле реализации
- [x] S1.2 — условие показа `ревью` по признаку кода расширения
- [x] S1.3 — разбор `ревью` / `архив` / `стоп`
- [x] S1.4 — возврат в сессию на последнем срезе ведёт на ту же развилку
- [x] S1.5 — вход «все задачи закрыты» не печатает свой финал
- [x] S1.6 — карточка завершения: чат = развилка, файл — перечень команд
- [x] S1.7 — памятка оформления финала согласована
- [x] S1.8 — строка в памятке ревью
- [x] S1.9 — слот проверки постановки после реализации не менялся
- [x] S1.10 — сверка единства формулировки

### Карта правок (перед тестом)

См. `reports/code-map.md`.

1. После «принято» на последнем срезе — один вопрос с тремя словами, постановку ещё можно дописать. [`422:425:.cursor/skills/openspec-apply-change/SKILL.md`](.cursor/skills/openspec-apply-change/SKILL.md)
2. На `ревью` в чат уходит одна команда `/release-review <имя>`; архив и стоп как раньше. [`426:429:.cursor/skills/openspec-apply-change/SKILL.md`](.cursor/skills/openspec-apply-change/SKILL.md)
3. Возврат в сессию с «принят» на последнем срезе — та же развилка, не отдельная строка архива. [`165:165:.cursor/skills/openspec-apply-change/SKILL.md`](.cursor/skills/openspec-apply-change/SKILL.md)
4. Полностью принятая ЗНИ и карточка завершения печатают тот же вопрос; в файле handoff — список команд без формы вопроса. [`69:69:.cursor/skills/openspec-apply-change/SKILL.md`](.cursor/skills/openspec-apply-change/SKILL.md)
5. Памятка ревью: после приёмки последнего среза вызывать `/release-review <change>`. [`29:29:.cursor/docs/review-guide.md`](.cursor/docs/review-guide.md)

Полная карта: `openspec/changes/last-slice-review-or-archive/reports/code-map.md`

### 2. Что проверить СЕЙЧАС

**Primary acceptance:** ЗНИ с кодом расширения → принять последний срез фразой «принято» → в чате три слова, ничего не заархивировано → ответить `ревью` → одна команда `/release-review <имя>`, каталог ЗНИ на месте.

1. Возьмите ЗНИ, в задачах которой есть модули расширения (не эту kit-ЗНИ).
2. Примите последний срез фразой «принято».
3. В чате должны быть три слова `ревью` / `архив` / `стоп`; каталог ЗНИ ещё в `openspec/changes/`.
4. Ответьте `ревью` — в чате одна команда `/release-review <имя>`, предрелиз в этой сессии не стартует.

Опционально: `архив` и `стоп` как раньше; kit-only ЗНИ — без слова `ревью`; возврат в сессию и карточка завершения — та же развилка. Остальные — см. `tasks.md`.

### 3. Следующие задачи

| Задача | Действие | Тип | Исполнитель | Зависит от | Статус |
|--------|----------|-----|-------------|------------|--------|
| `S1.accept` | Принять срез — после последнего среза чат предлагает ревью или архив одним вопросом | Ручной тест | пользователь | S1.1–S1.10 | [ ] |

### 4. Как вернуться

`/opsx:apply last-slice-review-or-archive` — новая сессия начнётся с запроса вердикта (принят / не принят / дефект в предыдущем срезе). Если нужно изменить scope/design/tasks — `/opsx:extend last-slice-review-or-archive`; затем снова `/opsx:apply last-slice-review-or-archive`. Пока вы проверяете — оркестратор ничего не делает.

### 5. Blockers

Нет.

### 7. Short-cut

Если уже проверено и принято — напишите обычной фразой («принято», «срез S1 принят»), отмечу без полного handoff.
