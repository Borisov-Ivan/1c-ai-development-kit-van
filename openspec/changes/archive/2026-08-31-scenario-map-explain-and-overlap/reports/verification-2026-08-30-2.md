---
verify_mode: pre-apply
change: scenario-map-explain-and-overlap
date: 2026-08-30
verdict: GO
layer_status:
  layer_1_hygiene: PASS
  layer_2_internal_coherence: PASS
  layer_2_5_loop_detection: PASS
  layer_3_problem_solution: PASS
  layer_4_independent_challenge: SKIPPED
  layer_5_implementation_readiness: PASS
snapshot:
  acceptance_loop_max: 3
  repair_attempt: 1
  accepted_tasks: []
  closed_decisions: []
  open_decision_id: null
  decision_round: 0
  decision_round_max: 2
  verify_depth: slice
  slice: S1
  assumptions_accepted: []
  open_known_questions: []
---

## Резюме

Узкая сверка на границе среза S1 после рабочих задач: постановка не разъехалась с шаблоном и скиллом. Приёмка среза ещё открыта.

### Слои

- **Layer 1:** рабочие задачи S1.1–S1.10 `[x]`; `S1.accept` `[ ]`; маркер границы среза на месте.
- **Layer 2:** один срез, Primary самодостижим задачами шаблона и скилла; живой осмотр — только приёмка.
- **Layer 2.5:** первая запись ожидания приёмки; петли нет.
- **Layer 3:** Why (наложение + «как работает») закрыт шаблоном и пунктами 6a/8a.
- **Layer 4:** повторного независимого разбора постановки нет — design не менялся в этом ходе.
- **Layer 5:** якоря шаблона на месте: `fitLines`, `placeLabelBox`, `wrapLayout` с `originX`, `RANK_GAP = Math.max`, `expandCanvas`, `data-canvas-budget`.

### Регресс двенадцати унаследованных сценариев (S1.8, по тексту)

| Сценарий | Где держится |
|---|---|
| Слои или ветвление видны на панели | полосы по полю `layer`, не по рангу |
| Семь линейных шагов не обязаны давать карту | порог — не число шагов; без топологии предложение не строится |
| Направление связей и легенда видны без клика | маркер на ребре, легенда под графом |
| Главный вывод виден без клика по узлу | стартовый `focus_node`, эффект в деталях, ход на полотне |
| Вывод только в шапке не публикуется | пункт 6a |
| Находка, меняющая действие, на полотне | пункт 6b |
| Ловушка одной подписи видна на полотне | пункт 6c, карточка у якоря |
| Плашка без доказательства не публикуется | пункт 6d |
| Аннотации не добирают порог | аннотации не входят в порог четырёх |
| Переключатель вида не прячет рёбра | вид меняет раскладку, состав рёбер тот же |
| Стартово выбран носитель исхода | `resolveFocus` / `header.focus_node` |
| Таблица со смысловыми колонками отвечает на вопрос | `medium: table`, эталон `map-good-table.md` |

### Дельта spec (S1.9)

Пункты 1–8 контракта поведения на месте: текст не перекрыт; ход с полотна; зазор не сжимать; два средства вида; координаты считает панель.

**Следующий шаг:** ручная приёмка `S1.accept`.
