# Приёмка S2 — Карта показывает причинность

**Change:** scenario-map-canvas
**Дата:** 2026-08-28
**Песочница:** `temp/sandbox-scenario-map-2026-08-28`

## Primary

Родительский агент записал файл панели в каталог текущей области. Проверка панели: без ошибок. Схема открывается штатной кнопкой среды. Ссылка в чате критерием не была. Файл вне git.

Живой файл сессии — managed-каталог панелей (не в репозитории). Предметный эталон связей — `fixtures/map-good-causal.md`.

## Опционально

- Список без связей — отрицательный эталон `map-bad-accordion.md`, публикация запрещена.
- Порядок прохода — `follows`, не причинность.
- Клик узла — `openFile`; `newComposerChat` в шаблоне панели запрещён.
- Намеренная ошибка типов → строка проверки с ошибкой → повторная запись родителя → проверка чистая (один круг ремонта).
- Ниже порога и вне источника — отказы одной строкой в скилле.

## Вердикт

Срез принят.
