## ADDED Requirements

### Requirement: MAY профиля не меняет язык /opsx:* и не отменяет канон лимита

Профиль модели чата MAY настраивать длину и тон в пределах лимитов чата. Профиль MUST NOT разрешать английский progress в команде `/opsx:*` и MUST NOT отменять канон после лимита («дорогие модели недоступны — дальше на модели чата»).

#### Scenario: Профиль не меняет язык

- **WHEN** сессию ведёт Grok 4 с разрешением прямой речи, и оркестратор пишет progress `/opsx:*`
- **THEN** строка остаётся русской; канон после лимита не заменяется английским абзацем про лимит или «chat model»
