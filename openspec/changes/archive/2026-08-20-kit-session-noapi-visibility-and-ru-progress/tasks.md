# Срез S1: Сигнал лимита

**Сценарий:** после лимита в чате канон первой строкой; токен не печатается; дальше модель чата.
**Primary acceptance:** В бюджете чата §5 есть дословный канон и триггер; канон — первая строка хода; оркестратор не печатает `-noapi`; в verify канон не ждёт финальной карточки.
**Приёмка:** чтение правил kit и FAQ; без информационной базы 1С.
**Связь со spec:** Requirement «Видимость режима без API — канон в том же ходе, не токен» — Scenario «Канон в том же ходе», Scenario «Токен не печатается», Scenario «Фон не отменяется»; Requirement «Токен пользователя не подменяется каноном лимита» — Scenario «Токен не требует канона лимита»; Requirement «FAQ отличает ключ от памяти после лимита» — Scenario «FAQ токен и память»; Requirement «Fable только как закрытая эскалация архитектора» — Scenario «Нет слага сильной модели — строка про Opus 5», Scenario «Независимый разбор постановки идёт на Fable».
**Зависимости:** нет.
**Режим apply:** mechanical

## 1. Правило выбора моделей и триггер в бюджете чата

- [x] S1.1 Добавить в `.cursor/rules/model-selection.mdc` подсекцию видимости: один режим «без API»; оркестратор не пишет `-noapi` и не говорит «включился noapi»; канон «дорогие модели недоступны — дальше на модели чата» в том же ходе, в том числе пока другой вызов в фоне; текст платформы не протокол и не замена канона; текст канона не менять (D1, D2)
- [x] S1.14 Добавить в `.cursor/rules/chat-output-budget.mdc` и `chat-output-budget-full.mdc` §5 (разбор сбоя субагента) дословный канон «дорогие модели недоступны — дальше на модели чата» и триггер: как только вызов вернул липкий сбой (лимит / недоступность / ошибка выбора модели) — **первая строка** в этом же ходе = этот канон; не откладывать до финала команды (D2)
- [x] S1.16 Записать в `.cursor/rules/chat-output-budget-full.mdc` §1 (строка verify) и §5a, в `.cursor/rules/chat-output-budget.mdc` §5 (verify: одно финальное), в `.cursor/rules/verify-user-communication.mdc` (пункт про 0 промежуточных сообщений и пункт самопроверки «одно финальное сообщение») и в `.cursor/skills/openspec-verify-change/SKILL.md` плюс `templates/chat-summary.md`: канон лимита и «Модель архитектора: Opus 5» — разрешённое исключение из «одно финальное сообщение» / «ноль промежуточных»; они не ждут карточки вердикта; «Дописываю постановку…» по-прежнему progress repair (D2, D5, D7)
- [x] S1.17 Добавить в `.cursor/rules/chat-output-budget.mdc` и `chat-output-budget-full.mdc` §1b пункт «канон»: после липкого сбоя в этом ходе канон присутствует и стоит первой строкой; этот пункт — зона среза сигнала, не смешивать с пунктом «язык» среза progress; в полном теле обновить счёт пунктов в заголовке §1b (D2)
- [x] S1.2 Записать в `.cursor/rules/model-selection.mdc`, что уже ушедший вызов с платной моделью не отменяется; после фона с тем же лимитом канон не повторяется; отдельный абзац «ещё летит» не нужен (D3)
- [x] S1.3 Записать в `.cursor/rules/model-selection.mdc`, что явный `-noapi` включает режим как сейчас и не требует канона лимита, если памяти ещё не было (D4)
- [x] S1.4 Записать в `.cursor/rules/model-selection.mdc` рядом с каноном лимита отдельный сигнал: если для независимого разбора постановки или для явной просьбы о самой сильной модели в enum нет слага — в чат «Модель архитектора: Opus 5»; эта строка не заменяет канон лимита и наоборот; канон лимита без имени модели; эта строка — семейство Opus 5 без слага `claude-opus-5-thinking-high`; ту же фразу — в `.cursor/skills/1c-agent-patterns/architect.md` вместо «дорогая эскалация недоступна»; ту же замену — в `.cursor/skills/openspec-verify-change/SKILL.md` (шаг вызова независимого разбора постановки): вместо «одна строка, что дорогая эскалация недоступна» — «одна строка в чат: Модель архитектора: Opus 5» (D7)

## 2. Cue в соседних правилах

- [x] S1.5 Добавить в `.cursor/rules/tool-name-guard.mdc` в пункт чеклиста про `model`: после липкого сбоя режим = без API, даже если пользователь не писал `-noapi`; таблицу ролей не копировать (D1)
- [x] S1.6 Добавить в `.cursor/rules/session-discipline.mdc` cue: после липкого сбоя память = без API, сигнал = канон, не токен; `command-session-persistence.mdc` не трогать (D10)

## 3. FAQ

- [x] S1.7 Добавить в `.cursor/docs/faq-kit.md` два абзаца: чем ключ в чате отличается от памяти после лимита; почему после лимита оркестратор не печатает `-noapi` (D9)

## 4. Статическая сверка

- [x] S1.8 Верифицировать по тексту `.cursor/rules/chat-output-budget.mdc` §5 и `.cursor/rules/model-selection.mdc`: канон обязан быть **первой строкой** того же хода, фраза дословно в бюджете, триггер в разборе сбоя субагента, без откладывания до финала команды (Scenario «Канон в том же ходе», D2)
- [x] S1.9 Верифицировать по тексту `.cursor/rules/model-selection.mdc`: оркестратор не печатает `-noapi` и не говорит «включился noapi» (Scenario «Токен не печатается», D1)
- [x] S1.10 Верифицировать по тексту `.cursor/rules/model-selection.mdc`: фоновый вызов не отменяется; канон после фона не повторяется (Scenario «Фон не отменяется», D3)
- [x] S1.11 Верифицировать по тексту `.cursor/rules/model-selection.mdc`: явный `-noapi` без предшествующего лимита не требует канона про дорогие модели (Scenario «Токен не требует канона лимита», D4)
- [x] S1.12 Верифицировать по тексту `.cursor/docs/faq-kit.md`: есть отличие ключа от памяти и объяснение, почему после лимита нет `-noapi` (Scenario «FAQ токен и память», D9)
- [x] S1.13 Сверить в `.cursor/rules/model-selection.mdc` таблицу «Роли и Task.model»: строки Primary совпадают с текстом до правок среза
- [x] S1.15 Верифицировать по тексту `.cursor/rules/model-selection.mdc`, `.cursor/skills/1c-agent-patterns/architect.md` и `.cursor/skills/openspec-verify-change/SKILL.md`: при отсутствии слага сильной модели канон в чат — «Модель архитектора: Opus 5»; нет «недоступна» / «эскалация» / «сборка» ни в одном из трёх файлов; канон лимита без имени модели (Scenario «Нет слага сильной модели — строка про Opus 5», D7)
- [x] S1.18 Верифицировать по тексту `.cursor/rules/chat-output-budget-full.mdc` §1/§5a, стаба §5, `.cursor/rules/verify-user-communication.mdc` и `.cursor/skills/openspec-verify-change/SKILL.md`: канон лимита и «Модель архитектора: Opus 5» названы исключением из «одно финальное сообщение» / «ноль промежуточных» во всех этих местах; §1b содержит отдельный пункт «канон» (первая строка) (Scenario «Канон в том же ходе», D2)

## 5. Приёмка

- [x] S1.accept Принять срез S1 «Сигнал лимита» — после лимита в чате виден канон первой строкой, токен оркестратор не печатает:
  - **Primary (обязательно):** В бюджете чата §5 есть дословный канон и триггер; канон — первая строка хода; оркестратор не печатает `-noapi`; в verify канон не ждёт финальной карточки.
  - Scenario «Фон не отменяется» (опционально): по тексту правил уже ушедший вызов не отменяется, канон после фона не повторяется; в verify канон уходит до финальной карточки
  - Scenario «Токен не требует канона лимита» (опционально): по тексту правил явный `-noapi` без лимита не требует строки про дорогие модели
  - Scenario «FAQ токен и память» (опционально): в FAQ kit видно, чем ключ отличается от памяти и почему после лимита нет `-noapi`
  - Scenario «Нет слага сильной модели — строка про Opus 5» (опционально): в правиле выбора моделей и в промпте архитектора канон «Модель архитектора: Opus 5», без «недоступна» / «эскалация» / «сборка»
  - Scenario «Независимый разбор постановки идёт на Fable» (опционально): в правилах при отсутствии слага вызов на Opus 5 и в чате «Модель архитектора: Opus 5»

<!-- slice-gate: в бюджете чата §5 дословный канон и триггер; первая строка хода; без печати -noapi; в verify канон не ждёт финала -->

# Срез S2: Русский progress

**Сценарий:** речь `/opsx:*` по-русски; progress — частный случай; профиль не отменяет язык.
**Primary acceptance:** В бюджете чата progress и вводная речь `/opsx:*` только русские; в профиле Grok — запрет менять язык команды.
**Приёмка:** чтение бюджета чата, стиля `/opsx:*` и профиля Grok; без информационной базы 1С.
**Связь со spec:** Requirement «Progress команд /opsx:* только на русском» — Scenario «Progress на русском», Scenario «Verify без английского progress»; Requirement «MAY профиля не меняет язык /opsx:* и не отменяет канон лимита» — Scenario «Профиль не меняет язык».
**Зависимости:** нет.
**Режим apply:** mechanical

## 1. Бюджет чата

- [x] S2.1 Добавить в `.cursor/rules/chat-output-budget.mdc` §6 требование: progress в сессии `/opsx:*` только русский, одна короткая строка; язык команды важнее Communication; английские каркасы вроде `I'll` / `Let's` / `Starting` / `Switched to` / `chat model` — примеры провала и для вводной речи, не новые пункты запретного списка; язык progress не ослабляется профилем модели (D5, D6)
- [x] S2.2 Добавить в `.cursor/rules/chat-output-budget.mdc` §1b пункт «язык»: progress `/opsx:*` и вводная речь оркестратора на русском; не смешивать с пунктом «канон» среза сигнала (D5, D6)
- [x] S2.3 Дописать то же в `.cursor/rules/chat-output-budget-full.mdc` §6 и §1b пункт «язык», чтобы stub и полное тело не разъехались; счёт пунктов §1b в заголовке полного тела согласовать со stub (включая пункт «канон» из среза сигнала) (D5)

## 2. Стиль и профиль

- [x] S2.4 Добавить в `.cursor/docs/opsx-output-style.md` §2 одно предложение-отсылку: runtime-норма языка `/opsx:*` — бюджет чата, даже если Communication или профиль модели толкает английский; этот гайд не единственный дом нормы (D6)
- [x] S2.5 Добавить в `.cursor/rules/model-grok4.mdc` узкий MUST NOT: MAY прямой речи не меняет язык `/opsx:*` и не отменяет канон лимита (D6)
- [x] S2.6 Добавить в `.cursor/rules/model-adaptation.mdc` в Precedence: профиль не разрешает английский progress и не отменяет канон лимита (D6)
- [x] S2.7 Добавить в `.cursor/skills/openspec-verify-change/SKILL.md` короткую отсылку: английский progress не изобретать; допустимые русские строки — из бюджета чата и `model-selection.mdc` (D5, D7)

## 3. Статическая сверка

- [x] S2.8 Верифицировать по тексту `.cursor/rules/chat-output-budget.mdc` и `chat-output-budget-full.mdc`: §6 требует русский progress; §1b пункт «язык» требует русскую вводную речь; список пунктов §1b и счёт в заголовке полного тела совпадают между stub и полным телом (Scenario «Progress на русском», D5, D6)
- [x] S2.9 Верифицировать по тексту `.cursor/skills/openspec-verify-change/SKILL.md` и `.cursor/rules/chat-output-budget-full.mdc`: в verify нет английского «I'll rerun»; допустимы канон лимита, «Модель архитектора: Opus 5», «Дописываю постановку…» — в том числе до финальной карточки (Scenario «Verify без английского progress», D5, D7)
- [x] S2.10 Верифицировать по тексту `.cursor/rules/model-grok4.mdc` и `.cursor/docs/opsx-output-style.md` §2: MAY прямой речи не покрывает смену языка `/opsx:*` (Scenario «Профиль не меняет язык», D6)

## 4. Приёмка

- [x] S2.accept Принять срез S2 «Русский progress» — progress команд `/opsx:*` по правилам только русский:
  - **Primary (обязательно):** В бюджете чата progress и вводная речь `/opsx:*` только русские; в профиле Grok — запрет менять язык команды.
  - Scenario «Verify без английского progress» (опционально): в скилле verify и полном бюджете чата нет английского каркаса повторного вызова; допустимы канон лимита, «Модель архитектора: Opus 5», «Дописываю постановку…»
  - Scenario «Профиль не меняет язык» (опционально): в профиле Grok и стиле `/opsx:*` явно, что MAY тона не меняет язык команды

<!-- slice-gate: в бюджете чата progress и вводная речь /opsx:* только русские; профиль Grok не меняет язык команды -->

# Срез S3: Маркер только при BSL

**Сценарий:** `/opsx:new` без BSL не спрашивает маркер; с BSL — спрашивает как сейчас.
**Primary acceptance:** В протоколе `/opsx:new` вопрос маркера пропускается только при доказанном kit-only; ЗНИ без `.bsl` из правил kit не спрашивает маркер.
**Приёмка:** чтение скилла `/opsx:new` и карточки брифа; без информационной базы 1С.
**Связь со spec:** Requirement «Вопрос маркера автора только если будет BSL» — Scenario «Нет BSL — нет вопроса маркера», Scenario «Есть BSL — вопрос маркера как сейчас», Scenario «Деловая постановка без .bsl — вопрос маркера», Scenario «Поздний BSL — вопрос на apply».
**Зависимости:** нет.
**Режим apply:** mechanical

## 1. Протокол new

- [x] S3.1 Дописать в `.cursor/skills/openspec-new-change/SKILL.md` шаг Metadata Gate: спрашивать маркер автора, если постановка / блок «Постановка ЗНИ» / текст запроса (поля «Что менять», «Файлы») содержит `.bsl` или модули в `src/` / расширении — не по ещё не созданному Impact; **пропуск только при доказанном kit-only** (только `.cursor/**`, `openspec/**`, документы kit); деловая постановка без литерала `.bsl` и без доказанного kit-only — спросить; иначе записать `developer: n/a`, пустой `comment_suffix`, `marker_style: minimal` без карточки выбора; пропуск закрывает гейт (`n/a` не плейсхолдер «пропущен молча»); согласовать guardrail шага 1.5 и анти-паттерн «MUST NOT be silently skipped»: молчаливый пропуск при BSL в постановке по-прежнему запрещён; resume с заполненным `developer` (включая `n/a`) не переспрашивать (D8)
- [x] S3.2 Дописать в `.cursor/docs/templates/brief-card.md` в секцию Metadata Gate то же условие: вопрос описания маркера пропускается только при доказанном kit-only; деловой язык без `.bsl` — спросить (D8)
- [x] S3.6 Дописать в `.cursor/skills/openspec-apply-change/SKILL.md` Metadata Prep: значение `n/a` считать плейсхолдером автора; если `marker_scope` не пуст — если разработчик по умолчанию из карточки проекта задан, взять его без вопроса; если пусто — один вопрос про ФИО до правки BSL; в маркер кода `n/a` не писать (D8, Scenario «Поздний BSL — вопрос на apply»)

## 2. Статическая сверка

- [x] S3.3 Верифицировать по тексту `.cursor/skills/openspec-new-change/SKILL.md`: ЗНИ только из `.cursor/**` / `openspec/**` без `.bsl` не требует вопроса маркера; `n/a` закрывает гейт без WARNING плейсхолдера (Scenario «Нет BSL — нет вопроса маркера», D8)
- [x] S3.4 Верифицировать по тексту `.cursor/skills/openspec-new-change/SKILL.md` и `.cursor/docs/templates/brief-card.md`: при `.bsl` в постановке вопрос маркера остаётся, без вопроса про форму в том же сообщении (Scenario «Есть BSL — вопрос маркера как сейчас», D8)
- [x] S3.7 Верифицировать по тексту `.cursor/skills/openspec-new-change/SKILL.md`: деловая постановка без литерала `.bsl` и без доказанного kit-only требует вопроса маркера (Scenario «Деловая постановка без .bsl — вопрос маркера», D8)
- [x] S3.5 Верифицировать по тексту `.cursor/skills/openspec-apply-change/SKILL.md`: при `developer: n/a` и непустом `marker_scope` сначала разработчик по умолчанию из карточки проекта, иначе вопрос до правки BSL; в маркер не попадает `n/a` (Scenario «Поздний BSL — вопрос на apply», D8)

## 3. Приёмка

- [x] S3.accept Принять срез S3 «Маркер только при BSL» — на ЗНИ без кода 1С вопрос маркера не задаётся:
  - **Primary (обязательно):** В протоколе `/opsx:new` вопрос маркера пропускается только при доказанном kit-only; ЗНИ без `.bsl` из правил kit не спрашивает маркер.
  - Scenario «Есть BSL — вопрос маркера как сейчас» (опционально): при `.bsl` в постановке вопрос маркера остаётся отдельным ходом, без вопроса про форму в том же сообщении
  - Scenario «Деловая постановка без .bsl — вопрос маркера» (опционально): деловой язык без литерала `.bsl` и без доказанного kit-only — вопрос маркера задаётся
  - Scenario «Поздний BSL — вопрос на apply» (опционально): при `n/a` в proposal и пути к `.bsl` в задачах apply не пишет `n/a` в маркер кода

<!-- slice-gate: в протоколе /opsx:new вопрос маркера пропускается только при доказанном kit-only -->
