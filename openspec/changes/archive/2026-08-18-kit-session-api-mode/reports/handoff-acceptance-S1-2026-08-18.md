## Срез S1 — передача на приёмку: kit-session-api-mode

**Change:** kit-session-api-mode
**Schema:** spec-driven
**Прогресс:** 13/14 задач среза S1 [x] (приёмка `S1.accept`: [ ])

### 1. Что реализовано

- [x] S1.1 — в правило выбора моделей добавлен разбор ключей как целых слов после пробелов; `--api-key` не токен; дешёвые команды тоже переключают режим; таблица ролей не менялась; файл: `.cursor/rules/model-selection.mdc`
- [x] S1.2 — пропуск шага 1 только на новых вызовах; уже ушедший вызов обязан сделать шаг 2 без параметра модели; файл: `.cursor/rules/model-selection.mdc`
- [x] S1.3 — липкая память только для лимита / недоступности / ошибки выбора модели; таймаут не липнет; одна строка без повтора; файл: `.cursor/rules/model-selection.mdc`
- [x] S1.4 — в режиме без API нет Primary и самой дорогой эскалации, кроме разового слага; порядок слоёв и правило «слаг + `-noapi`»; файл: `.cursor/rules/model-selection.mdc`
- [x] S1.5 — `-noapi` не заменяет пропуск архитектора и не пишет `.gate-override.yaml`; файл: `.cursor/rules/model-selection.mdc`
- [x] S1.6 — в чеклисте вызова: в режиме без API не передавать платную модель; файл: `.cursor/rules/tool-name-guard.mdc`
- [x] S1.7 — cue на каждом ходе: токены, затем память, затем таблица ролей; файл: `.cursor/rules/session-discipline.mdc`
- [x] S1.8–S1.13 — статическая сверка текста правил (оба токена, ложное слово, целостность первого сбоя, таблица ролей, токен на дешёвой команде, разовый слаг)

### Карта правок (перед тестом)

См. `reports/code-map.md`, секция «Срез S1 — Режим сессии».

- **S1.1** · правило выбора моделей · секция «Токены в сообщении» (created) — разбор `-noapi` / `-api` как целых слов, `--api-key` не токен, дешёвые команды тоже переключают режим. [`.cursor/rules/model-selection.mdc`](.cursor/rules/model-selection.mdc):22-30
- **S1.2** · правило выбора моделей · секция «Пропуск шага 1 цепочки» (created) — на новых вызовах сразу модель чата; уже ушедший вызов обязан сделать шаг 2. [`.cursor/rules/model-selection.mdc`](.cursor/rules/model-selection.mdc):43-47
- **S1.3** · правило выбора моделей · секция «Два множества сбоев» (created) — липнет только лимит / недоступность / ошибка выбора модели; таймаут не липнет; одна строка без повтора. [`.cursor/rules/model-selection.mdc`](.cursor/rules/model-selection.mdc):49-61
- **S1.4** · правило выбора моделей · секция «Порядок перед вызовом» (created) — слаг → токены → память → таблица ролей; разовый слаг с `-noapi` на этот вызов. [`.cursor/rules/model-selection.mdc`](.cursor/rules/model-selection.mdc):32-41
- **S1.5** · правило выбора моделей · секция «Не путать с `--skip-architect`» (created) — разбор постановки остаётся, `.gate-override.yaml` не создаётся. [`.cursor/rules/model-selection.mdc`](.cursor/rules/model-selection.mdc):63-65
- **S1.6** · чеклист вызова · пункт про `model` (modified) — в режиме без API не передавать платную модель, кроме разового override. [`.cursor/rules/tool-name-guard.mdc`](.cursor/rules/tool-name-guard.mdc):24
- **S1.7** · дисциплина сессии · cue режима (created) — на каждом ходе: токены, память, таблица ролей; follow-up не сбрасывает. [`.cursor/rules/session-discipline.mdc`](.cursor/rules/session-discipline.mdc):27

Кода 1С в срезе нет — секция Explain scope не требуется.

### 2. Что проверить СЕЙЧАС

1. Написать в чате `-noapi` или `--noapi`. Следующий вызов с конкретной платной моделью должен идти сразу на модели чата, без попытки платной. Таблица «Роли и Task.model» в правиле выбора моделей на месте.

Опционально: остальные сценарии — см. `tasks.md`, задача `S1.accept` («принять срез Режим сессии»).

### 3. Следующие задачи

| Задача | Действие | Тип | Исполнитель | Зависит от | Статус |
|--------|----------|-----|-------------|------------|--------|
| `S1.accept` | Принять срез «Режим сессии» — ключ направляет дорогой вызов на модель чата | Ручной тест | пользователь | `S1.1`–`S1.13` | [ ] |
| `S2.1` | Добавить в FAQ kit, как включить и выключить режим ключом | Проверка | оркестратор | `S1.accept` (невыполнима до приёмки) | [ ] |
| `S2.2` | Одна строка в описании `/opsx:new`: ключ пишется в чате, не флаг команды | Проверка | оркестратор | `S1.accept` | [ ] |
| `S2.3` | Та же строка в описании `/opsx:verify` | Проверка | оркестратор | `S1.accept` | [ ] |
| `S2.4` | Та же строка в описании `/opsx:apply` | Проверка | оркестратор | `S1.accept` | [ ] |

### 4. Как вернуться

`/opsx:apply kit-session-api-mode` — новая сессия начнётся с запроса вердикта (принят / не принят / дефект в предыдущем срезе).

### 5. Blockers

Нет.

### 7. Short-cut

Если уже проверено и принято — напишите обычной фразой («принято», «срез S1 принят»), отмечу без повторной простыни в чате.
