---
report_type: task-readiness
generated_at: 2026-08-31
agent: onec-code-architect
mode: task-readiness
scope:
  change: universal-visual-explanation
  slices: [S1]
  files:
    - .cursor/skills/visual-explanation/SKILL.md
    - .cursor/skills/visual-explanation/fixtures/panel-shell.md
    - .cursor/skills/scenario-map-canvas/
    - .cursor/agents/onec-scenario-map-designer.md
    - .cursor/skills/1c-agent-patterns/scenario-map-designer.md
    - .cursor/rules/gate-dispatcher.mdc
    - .cursor/rules/1c-agent-delegation.mdc
    - .cursor/rules/model-selection.mdc
    - .cursor/rules/tool-name-guard.mdc
    - .cursor/docs/glossary.md
    - .cursor/docs/chat-lexicon.md
    - .cursor/docs/agents-CHANGELOG.md
    - AGENTS.md
    - openspec/glossary.md
    - openspec/knowledge/_taxonomy.yaml
    - openspec/adrs/ADR-0010-visual-explanation-panel.md
    - openspec/changes/scenario-map-explain-and-overlap/debug.md
  capabilities:
    - visual-explanation
    - scenario-map-canvas
    - always-apply-context-budget
    - delegation-safeguards
related_reports:
  - reports/quality-control-2026-08-31.md
  - reports/architecture-task-readiness-2026-08-30.md
  - reports/architecture-slice-decomposition-2026-08-30.md
  - reports/design-challenge-2026-08-31.md
confidence: high
open_questions_count: 0
gaps_count: 2
supersedes: reports/architecture-task-readiness-2026-08-30.md
superseded_by: null
verdict: READY_WITH_NOTES
verdict_ru: ГОТОВО С ЗАМЕЧАНИЯМИ
---

# Оценка готовности к реализации — universal-visual-explanation (прогон 2026-08-31)

## Вердикт

**ГОТОВО С ЗАМЕЧАНИЯМИ** — по `proposal.md`, `design.md`, `specs/**` и `tasks.md` срез S1 реализуем силами агента (правки markdown в `.cursor/**` и `openspec/**`, apply mechanical) и принимается пользователем открытием панели в Cursor, без Конфигуратора и без стенда 1С. Два замечания — не пересмотр подхода и не вопрос к заказчику: в списке файлов миграции пропущены два живых файла репозитория, которые после удаления старого скилла останутся указывать на удалённый продукт (`openspec/glossary.md`) и называть его в таксономии базы знаний (`openspec/knowledge/_taxonomy.yaml`). Оба закрываются одной строкой в `tasks.md` и одной строкой в сверке по файлам; готовые сниппеты — в разделе «Пробелы».

Дельта к прогону 2026-08-30: закрыт бывший неоднозначный пункт про штатный расчёт раскладки — `design.md` D4 и задачи `S1.2` / `S1.11` теперь прямо запрещают `computeDAGLayout` в этой поставке, разночтения «MAY при наличии связей» больше нет. Добавленные после уточнения сценарии (прямая просьба на проверке постановки и ревью; двусмысленная «покажи схему») закрыты `S1.1`, `S1.11` и чеклистом приёмки.

## KB references

Во входном блоке `## Existing Knowledge` совпадений нет; фактов KB не использовано и не выдумано. Файлов фактов в `openspec/knowledge/` нет (есть только `_taxonomy.yaml`) — конфликтов с `active` KB быть не может. Опора отчёта: `proposal.md`, `design.md`, `specs/**/spec.md`, `tasks.md`, главные spec `openspec/specs/{scenario-map-canvas,always-apply-context-budget,delegation-safeguards}/spec.md` и фактические файлы kit.

## Precedent Coherence

`design.md` § Blast Radius заполнен построчно: явные отмены (молчание, порог четырёх сущностей, закрытый словарь, «граф или таблица» как единственные средства, экзамен со скрытой шапкой, отдельный сборщик манифеста, обязательный отчёт как источник) и явные сохранения (кнопка среды, запрет голого списка без вывода, ширина текста без масштабирования полотна, стартовый элемент-исход, файл не в git, «без схемы» до выхода сессии, код и SQL не главное содержимое, рисунок в чате не заменяет панель, этапы поставки не источник полотна). Замена несущих решений ADR-0008 / ADR-0009 на ADR-0010 заявлена в design (D5) и закрывается задачей `S1.9`. Соседняя открытая ЗНИ `scenario-map-explain-and-overlap` — Non-Goal «не продолжать», закрытие — `S1.10` (одна пометка в её `debug.md`, без переписывания её `tasks.md` и spec). Молчаливой отмены архивных контрактов нет.

Сверка дельты REMOVED с главным spec: в `openspec/specs/scenario-map-canvas/spec.md` ровно 11 требований, и все 11 перечислены в `specs/scenario-map-canvas/spec.md` (REMOVED) с `Reason` и `Migration`. Требования `Map cue in dispatcher is a pointer` (MODIFIED) и `Map designer does not full-read the canvas skill` (REMOVED) существуют в главных spec под теми же именами. Осиротевших требований при архивации не остаётся.

## Оценка по критериям

| # | Критерий | Вердикт | Обоснование |
|---|----------|---------|-------------|
| 1 | Реализуемость кодовых задач | OK | У каждой из `S1.1`–`S1.11` есть путь файла, наблюдаемый результат и опора на решения D1–D7 / имена Scenario. ЧТО и ГДЕ читается без догадок: создать скилл и шаблон панели (каталога `.cursor/skills/visual-explanation/` в репозитории нет — это «создать»); заменить строки-указатели в названных правилах и скиллах (все они существуют и действительно содержат старые упоминания); удалить существующий комбайн; записать ADR-0010 и проставить статус «Superseded» в ADR-0008/0009 и в `openspec/adrs/README.md`; одна пометка в существующем `debug.md` соседней ЗНИ; сверка по файлам kit. Модель данных панели (D3) и стартовый набор форм заданы полностью, имена констант не изобретаются исполнителем. |
| 2 | Реализуемость форм и метаданных | OK | Форм 1С, объектов метаданных и `Form.xml` в ЗНИ нет: `form_mode: n/a`, маркеров ручной конфигурации в `tasks.md` не найдено, скрытых `Template.xml` / MXL в задачах нет. Шаблон панели — markdown-фикстура `fixtures/panel-shell.md` с рендером на примитивах среды (`Stack`, `Table`, `Text`), тот же класс артефакта, что и существующая `fixtures/canvas-shell.md` у снимаемого скилла. Конфигуратор не требуется. |
| 3 | Разрешённость решений | OK | Путь реализации один (вариант с заполнением данных родителем сессии и одним тонким шаблоном), альтернативы в design закрыты. Спорный ранее пункт про штатный расчёт раскладки разрешён в запрет: D4 «`computeDAGLayout` в v1 запрещён», `S1.2` — «не вызывать», `S1.11` — проверка отсутствия. Решение про проверку постановки и ревью закрыто одинаково в трёх местах: design § «Решения verify», spec (Scenario «Проверка постановки без автопанели» и «Просьба на проверке постановки»), задачи `S1.1` / `S1.11` — авто нет, прямая просьба открывает панель, в чате одна строка эффекта. Двусмысленная «покажи схему» разрешена однозначно: одна строка выбора, панели нет до ответа. Перечисления «поток / сравнение / иерархия / карточка» и «`confidence` или без стрелки» — правила поведения во время работы, а не незакрытая развилка постановки. Блокирующих открытых вопросов в design нет. |
| 4 | Полнота покрытия | GAP | Все 8 ADDED-требований `visual-explanation` и MODIFIED «Map cue in dispatcher is a pointer» покрыты задачами и приёмкой; все 24 заголовка `#### Scenario:` закрыты обязательной проверкой, опциональными пунктами приёмки или задачами `S1.<M>` (совпадает со сводкой контроля согласованности от 2026-08-31). Требования REMOVED без сценариев закрываются `S1.4`, `S1.5`, `S1.8`, `S1.11`. **Пробел:** снятие требования «Technical fallback is not a map» и удаление скилла не доведены до `openspec/glossary.md` — там живут статьи «Карта сценария» (со ссылкой на `.cursor/skills/scenario-map-canvas/SKILL.md`) и «Текстовый резерв»; файл не назван ни в `tasks.md`, ни в `## Migration Plan`, ни в сверке `S1.11`. После `S1.8` ссылка станет битой. См. Пробел 1. |
| 5 | Согласованность | GAP | Противоречий `tasks.md` ↔ `design.md` ↔ spec не найдено: состав файлов среза, обязательная проверка и имена сценариев совпадают дословно; формулировки «создать» соответствуют состоянию репозитория (скилла, шаблона панели и ADR-0010 нет), формулировки «удалить» и «заменить» — тоже (комбайн, агент-сборщик, шаблон промпта и все перечисленные указатели на месте). Расхождение `## Migration Plan` (нет `AGENTS.md` и журнала агентов) с `tasks.md` не мешает: источник работ для apply — `tasks.md`. **Пробел:** в `openspec/knowledge/_taxonomy.yaml` поддомен базы знаний называется `scenario-map`; после снятия capability имя указывает на несуществующий продукт, а прошлый прогон готовности утверждал, что таксономии в kit нет. Фактов KB нет, миграция не нужна — но артефакт остаётся рассогласованным. См. Пробел 2. |
| 6 | Связность кода и порядок задач | OK | Порядок непротиворечив: скилл (`S1.1`) → шаблон панели (`S1.2`) и указатели (`S1.3`–`S1.7`) → удаление комбайна (`S1.8`, зависит от всех указателей) → сверка по файлам (`S1.11`, зависит от `S1.2`, `S1.8`, `S1.9`) → приёмка. Висячих ссылок «удалили скилл, а правила ещё зовут его» порядок не создаёт. `S1.9` (несущее решение) и `S1.10` (пометка в соседней ЗНИ) независимы и параллельны — это не дыра приёмки. Наблюдение без пробела: `S1.2` формально не связана с `S1.8`, поэтому шаблон можно писать уже после удаления старой фикстуры; на исполнимость это не влияет — модель данных и запреты изложены в тексте задачи, а копировать старую раскладку прямо запрещено. |
| 7 | Архитектурная эстетика (Design Smells) | OK | Переусложнения нет: один срез, один новый скилл, один тонкий шаблон, остальное — замена указателей и удаление. Старый чертёжный слой не переносится (запрет на расчёт раскладки, на поиск места для подписи и на фиксированную коробку как носитель смысла зафиксирован и в задаче, и в проверке). Отдельная роль сборщика манифеста снимается, `Task` к ней не вызывается — задачи `S1.4`, `S1.5`, `S1.8` это закрывают. Отдельной команды для панели не заводится. Единственное стилевое замечание (без алерта): `S1.1` — одна крупная задача на весь протокол скилла; дробить её нельзя, объект правки один. |
| 8 | User Task Contract | OK | В строках `S1.1`–`S1.11` пользовательских проверок на живом стенде нет: все они — запись и правка файлов kit, а `S1.11` — сверка по файлам глазами агента (статическая проверка текста и отсутствия каталогов), не запуск панели. Приёмка «открыть панель рядом с чатом и прочитать объяснение» стоит только в `S1.accept`, там же опциональные пункты про проверку постановки и ревью — это граница среза. `design.md` § Assumptions подтверждает: приёмка без стенда 1С. Структурного пользовательского прогона внутри `S<N>.<M>` нет. |

## Покрытие сценариев (сводка)

24 заголовка `#### Scenario:` в дельтах (23 в `visual-explanation`, 1 в `always-apply-context-budget`; REMOVED-дельты сценариев не содержат). Обязательная проверка приёмки сворачивает четыре сценария одного осмотра («Просьба на обычный ответ в чате», «Среда есть — не подменять панелью рисунок в чате», «Вывод виден без каталога сущностей», «Первая схема в проекте»). Шестнадцать имён вынесены в опциональные пункты приёмки. Четыре охранных сценария («Вывод только в шапке не публикуется», «Неизвестный жанр не даёт отказ», «Нет среды панели», «Cue «покажи схему»») закрыты текстом скилла и сверкой по файлам (`S1.1`, `S1.3`, `S1.6`, `S1.11`). Имена в `**Связь со spec:**`, в чеклисте приёмки и в spec совпадают дословно.

## Состояние репо vs формулировки задач

| Задача | Формулировка | Фактическое состояние | Совпадение |
|---|---|---|---|
| S1.1, S1.2 | создать | `.cursor/skills/visual-explanation/` отсутствует | да |
| S1.3 | заменить строку указателя | `.cursor/rules/gate-dispatcher.mdc` содержит строку про скилл карты | да |
| S1.4 | убрать роль и шаблон картографа | `.cursor/rules/1c-agent-delegation.mdc` содержит блок про картографа | да |
| S1.5 | убрать строки сборщика | `.cursor/rules/model-selection.mdc`, `.cursor/rules/tool-name-guard.mdc` содержат упоминания | да |
| S1.6 | заменить слоты и убрать «текстовый резерв» | `openspec-explore/SKILL.md`, `openspec-explain/SKILL.md` и его шаблоны (`explain-report.md`, `exit-card.md`, `inventory-card.md`), `openspec-overview/SKILL.md` содержат упоминания | да |
| S1.7 | заменить статьи индекса kit | `.cursor/docs/glossary.md`, `.cursor/docs/chat-lexicon.md`, `.cursor/skills/1c-agent-patterns/SKILL.md`, `.cursor/docs/agents-CHANGELOG.md`, `AGENTS.md` содержат упоминания | да |
| — | (не назван) | `openspec/glossary.md` содержит статьи «Карта сценария» и «Текстовый резерв» со ссылкой на удаляемый скилл | **нет** → Пробел 1 |
| — | (не назван) | `openspec/knowledge/_taxonomy.yaml` содержит поддомен `scenario-map` | **нет** → Пробел 2 |
| S1.8 | удалить | каталог скилла, `.cursor/agents/onec-scenario-map-designer.md`, `.cursor/skills/1c-agent-patterns/scenario-map-designer.md` на месте | да |
| S1.9 | записать ADR-0010, пометить 0008/0009 | ADR-0010 отсутствует; ADR-0008, ADR-0009 и `openspec/adrs/README.md` существуют | да |
| S1.10 | одна пометка | `openspec/changes/scenario-map-explain-and-overlap/debug.md` существует | да |

## Замечания без возврата

1. Ссылки на ADR-0008/0009 остаются в артефактах соседней открытой ЗНИ (`scenario-map-explain-and-overlap`) и в её отчётах. Это соответствует решению «не продолжать и не переписывать»; закрытие — пометка `S1.10`.
2. Архивные отчёты и архивные дельты продолжают упоминать карту сценария — переписывать их запрещено проектным решением, сверка `S1.11` явно исключает `openspec/changes/archive/`.
3. `## Migration Plan` в design перечисляет не все файлы из `tasks.md` (нет `AGENTS.md`, журнала агентов). Для apply источник работ — `tasks.md`, возврата на уточнение не требует; при желании абзац можно выровнять тем же сниппетом из Пробела 1.

## Пробелы

### Пробел 1 — глоссарий OpenSpec не переключён на визуальное объяснение

- **Задача / артефакт:** `tasks.md` `S1.7` и `S1.11`; `design.md` § Migration Plan (шаг 2).
- **Что отсутствует:** файл `openspec/glossary.md` не назван ни в одной задаче. В нём есть статья «**Карта сценария**» с прямой ссылкой на `.cursor/skills/scenario-map-canvas/SKILL.md` (строка 21) и статья «**Текстовый резерв**» (строка 22) — понятие, которое дельта требований снимает как замену панели. После `S1.8` ссылка становится битой, а словарь продолжает описывать снятый продукт как действующий контракт.
- **Рекомендация:** добавить путь в задачу правки индекса kit и в сверку по файлам; тем же сниппетом дополнить шаг 2 плана миграции в design.

Готовая замена строки `S1.7` в `tasks.md`:

```markdown
- [ ] S1.7 Заменить в `.cursor/docs/glossary.md`, `openspec/glossary.md`, `.cursor/docs/chat-lexicon.md`, `.cursor/skills/1c-agent-patterns/SKILL.md`, `.cursor/docs/agents-CHANGELOG.md` и `AGENTS.md` статьи «карта сценария» / «текстовый резерв» / картограф: указать визуальное объяснение и `.cursor/skills/visual-explanation/SKILL.md`, убрать ссылки на `scenario-map-canvas` и `onec-scenario-map-designer`, чтобы индекс kit не звал снятый продукт (D6, D7). Зависимости: S1.1
```

Готовая вставка в проверку `S1.11` (добавить в перечень сверки):

```markdown
в `.cursor/docs/glossary.md` и `openspec/glossary.md` нет статьи «Карта сценария» со ссылкой на `.cursor/skills/scenario-map-canvas/SKILL.md` и нет «текстового резерва» как замены панели;
```

Готовая замена шага 2 в `design.md` § Migration Plan (добавить путь в перечень):

```markdown
`.cursor/docs/glossary.md`, `openspec/glossary.md`, `.cursor/docs/chat-lexicon.md`, `.cursor/docs/agents-CHANGELOG.md`, `.cursor/skills/1c-agent-patterns/SKILL.md`, `AGENTS.md`
```

### Пробел 2 — таксономия базы знаний называет снятый продукт

- **Задача / артефакт:** `tasks.md` (задачи нет); `design.md` § Migration Plan.
- **Что отсутствует:** в `openspec/knowledge/_taxonomy.yaml` домен `kit` содержит поддомен `scenario-map` (строка 15). Capability снимается целиком, значит рубрика указывает на несуществующий продукт; фактов KB в `openspec/knowledge/` нет, поэтому переименование не требует миграции записей. Прошлый прогон готовности исходил из того, что таксономии в kit нет — это неверно.
- **Рекомендация:** добавить отдельную короткую задачу переименования (предпочтительно) либо явно вынести таксономию в раздел «вне scope» в `proposal.md`.

Готовая новая строка в `tasks.md` (в группу «Указатели сессий», перед снятием конвейера):

```markdown
- [ ] S1.12 Переименовать в `openspec/knowledge/_taxonomy.yaml` поддомен домена `kit` с `scenario-map` на `visual-explanation` (файлов фактов нет, миграция записей не требуется), чтобы рубрикатор базы знаний не называл снятый продукт (D6). Зависимости: S1.1
```

Альтернатива, если переименование сознательно откладывается — строка в `proposal.md` § Scope, «Out of scope»:

```markdown
- Out of scope: … ; рубрика `kit/scenario-map` в `openspec/knowledge/_taxonomy.yaml` (фактов нет; переименование — отдельной ЗНИ вместе с ревизией таксономии).
```

## Next Steps

1. Внести сниппеты Пробела 1 (`S1.7`, `S1.11`, шаг 2 плана миграции) — это одна правка `tasks.md` и одна `design.md`.
2. Решить по Пробелу 2: короткая задача переименования или явный вынос за scope.
3. После правок ЗНИ реализуема as-is: возвратов на уточнение постановки не требуется.
