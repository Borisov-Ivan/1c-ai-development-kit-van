---
report_type: task-readiness
generated_at: 2026-09-23
agent: onec-code-architect
mode: task-readiness
scope:
  change: model-tier-by-step
  tasks: [S1.1, S1.2, S1.3, S1.4, S1.5, S1.6, S1.7, S1.8, S1.9, S1.10, S1.11, S2.1, S2.2, S2.3, S2.4, S2.5, S2.6, S2.8, S2.7]
  files:
    - .cursor/rules/model-selection.mdc
    - .cursor/skills/openspec-verify-change/SKILL.md
    - .cursor/rules/tool-name-guard.mdc
    - .cursor/rules/verified-cause-gate.mdc
    - .cursor/rules/architect-gate.mdc
    - .cursor/skills/1c-agent-patterns/architect.md
    - .cursor/skills/1c-agent-patterns/SKILL.md
    - AGENTS.md
confidence: high
open_questions_count: 0
---

# Готовность задач S1 и S2 — model-tier-by-step (прогон 2026-09-23)

## Вердикт

**ГОТОВО.**

Дыры нет: все рабочие задачи S1.1–S1.11 и S2.1–S2.8 можно выполнить как написано. Замечания прошлого прогона (легенда типа, перенос строки в чат, шаблон без перечня, одна катч-олл строка, фраза у исходов слоя) закрыты формулировками задач. Новые уточнения (заголовок раздела, режим без API, самосверка слага, S2.8) отражены в задачах и design; якоря в целевых файлах существуют. Маркеров ручной конфигурации нет.

## Как проверялось

Каждая задача сверена с текущим текстом целевого файла (якорь + предмет правки), с `design.md` (D1–D4) и с дельтой `specs/subagent-model-mapping/spec.md`. Прошлый отчёт готовности как вердикт не переиспользовался; из него взята только карта замечаний W1–W3 / I1–I2 для проверки закрытия.

## Закрытие замечаний прошлого прогона

| ID | Было | Сейчас в `tasks.md` | Статус |
|---|---|---|---|
| W1 | Легенда `readiness` не входила в задачу | S1.8: оговорка и в строке матрицы, и в легенде типа «оценка без блокировки этапа» | закрыто |
| W2 | Четыре колонки без переноса строки в чат | S1.1: «у уже написанных строк текст строки в чат переезжает в новую колонку и в ячейках моделей не остаётся» | закрыто |
| W3 | «Заменить перечень» у scope-coherence, где перечня нет | S1.9: «своего перечня нет — добавить отсылку, не заменяя отсутствующий текст» | закрыто |
| I1 | Две катч-олл строки | S1.1: единственная строка «шаг, не названный исключением» = бывшая «Все остальные режимы»; S1.3: «она и есть строка обычного шага, не вторая» | закрыто |
| I2 | Фраза о продолжении без отчёта не у маппинга | S1.6: «рядом с перечнем исходов слоя» — слой остаётся предупреждением, риск в принятые допущения | закрыто |

## Новые уточнения

| Уточнение | Где зафиксировано | Якорь в файле | Выполнимо |
|---|---|---|---|
| Заголовок раздела не переименовывается | S1.1, S2.8, design D1/D4 | `model-selection.mdc` § «Закрытая эскалация Fable» | да (S1.1 не трогает заголовок; S2.8 правит verify, заголовок чужого файла не меняет) |
| Режим без API не глушит строку без модели | S1.1, design D1 | правило под таблицей: «Пока сессия в режиме „без API“…» | да — сузить оговорку до строк с платной моделью |
| Самосверка читает слаг в строке обычного шага | S1.10 (проверка), design D3, spec | § «Самосверка enum» + после S1.1 слаг в катч-олл строке, не в ячейке роли | да; отдельной write-строки на § «Самосверка» нет — см. INFO ниже |
| S2.8 заменяет пересказ лестницы отсылкой | S2.8, design D4 | Layer 4 «Запуск» п.1; Layer 2.5 алгоритм п.3 (пересказ Fable/Opus) | да |

## Проверка якорей правки

| Задача | Якорь | Существует | Комментарий |
|---|---|---|---|
| S1.1 | § «Закрытая эскалация Fable», таблица 3 кол.; строка роли `onec-code-architect`; оговорка «без API» под таблицей | да | колонки расширяются до 4; заголовок остаётся |
| S1.2 | § «Цепочка для архитектора», перечень с `task-readiness`, фраза «не часть этой цепочки» | да | |
| S1.3 | строка «Все остальные режимы (… `task-readiness` …)» | да | та же катч-олл, что в S1.1 |
| S1.4 | Layer 5, абзац делегирования `mode=task-readiness` | да | явной модели нет — «записать» |
| S1.5 | таблица «Запуск агентов verify», строка Layer 5, колонки Mode / Когда | да | |
| S1.6 | Layer 5: реюз отчёта + **Маппинг** (три исхода) | да | фраза — рядом с маппингом |
| S1.7 | tool-name-guard: чеклист п.2; § параметра `model`; п.4 ошибки enum | да | |
| S1.8 | абзац «Обычный вызов»; легенда типов; строка матрицы Layer 5 | да | |
| S1.9 | шаблоны task-readiness / design-challenge / deep-analysis / scope-coherence; два места имени отчёта | да | сейчас `task-readiness-review-*.md` → `architecture-task-readiness-*.md`; у scope-coherence перечня нет |
| S1.10 | тексты трёх файлов после правок | да | регрессия + самосверка слага |
| S1.11 | `AGENTS.md`: «архитектор Opus 5» | да | |
| S2.1 | та же таблица после S1 | да (после S1) | |
| S2.2 | «аудиты coherence» в катч-олл | да | |
| S2.3 | блок «Правила:» под таблицей | да | бюджет чата уже разрешает строку — менять не нужно |
| S2.4 | verified-cause-gate HALT 4 п.2, `mode=precedent-coherence-audit` | да | «отослать», не «заменить перечень» |
| S2.5 | «Обычный вызов»: перечень «независимый разбор; тяжёлый deep-analysis» | да | |
| S2.6 | SKILL.md «Кратко»: пересказ лестницы независимого разбора | да | |
| S2.8 | Layer 4 п.1 и Layer 2.5 п.3 — пересказ лестницы | да | overlap с S1 по файлу, но другие абзацы |
| S2.7 | текст `model-selection.mdc` после правок | да | |

## Оценка по критериям

| # | Критерий | Вердикт | Обоснование |
|---|---|---|---|
| 1 | Реализуемость задач | OK | Все 19 рабочих задач называют файл, раздел и предмет. Якоря найдены. Нет задачи «угадать структуру, которой нет» |
| 2 | Формы и метаданные | OK | `form_mode: n/a`. Маркеров ручной конфигурации в задачах нет (кроме «Конфигуратор не нужны» в приёмке) |
| 3 | Разрешённость решений | OK | Колонки, катч-олл, лестница, сбой единственного вызова, поведение без API — в D1/D2/D3/D4 и в задачах. «Или» без выбора нет |
| 4 | Полнота покрытия | OK | Сценарии S1 (чат-модель, сбой, регрессии) и S2 (лестница прецедента, регрессии) покрыты. S2.8 закрывает пересказ лестницы в verify (Layer 4 и петля приёмки), которого не было в прошлом прогоне |
| 5 | Согласованность | OK | Proposal Scope = 8 файлов = задачи. Design D4 и S2.8 согласованы; список файлов среза S2 в design ## Slices не перечисляет verify — см. INFO |
| 6 | Связность и порядок | OK | S2 зависит от S1. S2.1/S2.2/S2.5 требуют не откатывать S1. S2.8 правит другие абзацы того же SKILL, что S1.4–S1.6. S1.10 перед S1.11 — scope регрессии не включает AGENTS |
| 7 | Архитектурная эстетика | OK | Одна таблица, отсылки, без новых ролей/статусов |
| 8 | User Task Contract | OK | Нет ИБ/консоли/отладчика/эмуляции API в рабочих задачах |
| 9 | Precedent Coherence | OK | `## Blast Radius` заполнен; критерий 8 режима task-readiness (precedent) закрыт |

## Simplicity Check

- **Viable alternatives:** (1) несколько пояснений рядом с абзацами; (2) одна расширенная таблица шагов + отсылки; (3) вторая отдельная таблица.
- **Selected simplest viable design:** (2) — уже Chosen в design; задачи не вводят второй структуры.
- **Why not simpler:** (1) отвергнут empirically (одиночное чтение соседней фразы возвращает шаг на тяжёлую). (3) запрещён задачами и spec.
- **Complexity budget:** 8 файлов; 0 новых таблиц; +1 колонка и +2 строки в существующей; 0 новых ролей/статусов.

## Замечания (не дыры)

### INFO-1. Список файлов среза S2 в design ## Slices не называет `openspec-verify-change/SKILL.md`

S2.8 и design D4 явно правят этот файл; в proposal ## Scope он есть. В блоке **Файлы:** среза S2 в design перечислены только model-selection, verified-cause-gate, architect-gate, SKILL навигатора. На выполнение S2.8 не влияет; при желании — одна правка design ## Slices для симметрии.

### INFO-2. Самосверка: write опирается на design D3, не на отдельную строку S1.1

S1.1 убирает слаг из ячейки роли и кладёт его в катч-олл; design D3 и spec требуют читать слаг в строке обычного шага; S1.10 это проверяет. Отдельной фразы «в § Самосверка enum записать…» в S1.1 нет. Исполнитель с design в руках сделает правку; риск — только если apply идёт строго «по одной строке задачи без design». Не мешает старту.

## Пробелы

Критических пробелов нет. Задачи S1.1–S1.11 и S2.1–S2.8 можно выполнить как написано.

## Источники

- `openspec/changes/model-tier-by-step/tasks.md`
- `openspec/changes/model-tier-by-step/design.md`
- `openspec/changes/model-tier-by-step/proposal.md`
- `openspec/changes/model-tier-by-step/specs/subagent-model-mapping/spec.md`
- `openspec/changes/model-tier-by-step/reports/architecture-task-readiness-2026-09-22-3.md` (карта прошлых замечаний)
- `.cursor/rules/model-selection.mdc`
- `.cursor/skills/openspec-verify-change/SKILL.md`
- `.cursor/rules/tool-name-guard.mdc`
- `.cursor/rules/architect-gate.mdc`
- `.cursor/rules/verified-cause-gate.mdc`
- `.cursor/skills/1c-agent-patterns/architect.md`
- `.cursor/skills/1c-agent-patterns/SKILL.md`
- `.cursor/rules/chat-output-budget-full.mdc` (строка «Разбор постановки» уже разрешена — S2.3)
- `AGENTS.md`
