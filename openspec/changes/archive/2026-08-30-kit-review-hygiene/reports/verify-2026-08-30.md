# Verify — kit-review-hygiene — 2026-08-30

Режим: kit-only, mechanical. Продуктовый BSL/XML не затронуты. Соседние ЗНИ не принимались и не архивировались.

## Вердикт

Постановка срезов S1–S5 закрыта по тексту скиллов, правил и Grep. Приёмочные чекбоксы `S<N>.accept` ждут фразы «принято».

## Сверка требований

### kit-project-neutrality

- Фильтр охвата: `.cursor/skills/openspec-apply-change/SKILL.md` — сегменты `/cf/` `/cfe/`, нормализация `\`, словарь без ярлыка выгрузки, `openspec/project.md` не источник класса. То же в status и glossary. Чеклист поставки п. 15 — фильтр без путей заказчика.
- Last-slice kit-only / пустой / `cf` / не определён: вопрос архива без слова `ревью`. `cfe`/`mixed` — три слова. Then `ревью`/`архив`/`стоп` не переписывались. Слот post-apply verify — `/opsx:archive`.
- Список утечки в `.cursor/**`: 0 совпадений (Павлик, ДО21_Павлик, pavЭтоИБПавлик, ЭДО ПАО, ЭДО и ЭА, ДО3 Демо, КонтурДиадок, do2-pavlik, nastroyka-knopok-processov). Ярлык `cf-ea` в `.cursor/**` = 0.
- Allow-list AP-031 (`pav_` / `lvv_` / `пр_`) на месте.
- Эталоны overview/explain после замены имён сохраняют урок «хорошо / плохо».

### scenario-map-canvas (описание)

- Overview: порог ≥4 после отсева выбранного средства; таблица не требует инцидентной связи. Фразы «после поставки соседней ЗНИ» нет. Согласие — тот же один отчёт.
- Слоты исследования и разбора этой ЗНИ не менялись (кроме explain: «оба отсева» остаётся слотом разбора, не overview).

### always-apply-context-budget

- `session-discipline.mdc`: Read скилла только на первом ходе; follow-up без повторного Read; имена `executive-summary` / `info-section` / `layer-1-hygiene-table`.
- Диспетчер: cue «покажи карту сценария» / «покажи схему» без тела протокола.
- Бюджет always-apply + `AGENTS.md` = 23001 байт ≤ 34 КБ.

### delegation-safeguards

- `openspec-new-change/SKILL.md` → `.cursor/skills/1c-agent-patterns/quality-controller.md` (файл есть).
- `context-strategy`: ordinary `explore`, нет `explore/fast`.
- Картограф: контракт из брифа, не полный Read скилла карты.
- Стайл-гайд: stub бюджета и шапка `opsx-output-style.md` — читать процитированный раздел.
- Fallback Grep/Glob: explorer, architect, writer, trace-analyst, скиллы БСП.
- Журнал агентов: SSOT моделей — `model-selection.mdc`.

## Границы, не дефекты этой сверки

- Allow-list префиксов overlay в антипаттерне именования — механизм, не учебный проект.
- `openspec/changes/archive/**` не входил в замену.
- Порог «оба отсева» в скилле разбора (`openspec-explain`) — вне среза описания.

## Следующий шаг постановки

Приёмка срезов S1–S5 фразой «принято». Эта ЗНИ kit-only: после последнего «принято» — развилка архива без слова «ревью».
