# Срез S1: Охват маркеров без чужих путей

**Сценарий:** в чужом проекте после последнего среза развилка смотрит на расширение в задачах этой ЗНИ, а не на имена выгрузки ЭДО.
**Primary acceptance:** kit-only ЗНИ (в задачах нет `.bsl`) → фраза «принято» на последнем срезе → по тексту скилла реализации вопрос с ответами `архив` и `стоп`, слова `ревью` нет.
**Приёмка:** сверка текста скиллов и Grep фильтра; не прогон чата на чужой базе.
**Связь со spec:** Requirement «Filter without customer dump paths» — Scenario «Фильтр без путей заказчика», Scenario «Kit-only — архив без ревью», Scenario «Карточка проекта не подменяет охват ЗНИ»; Requirement «Last-slice acceptance offers prerelease or archive» — Scenario «Последний срез — развилка из трёх слов», Scenario «Ответ ревью даёт команду предрелиза», Scenario «Ответ архив без изменений», Scenario «Ответ стоп без изменений», Scenario «Без кода расширения слова ревью нет», Scenario «Возврат в сессию — та же развилка», Scenario «Карточка завершения — та же развилка», Scenario «Проверка постановки после apply не меняется», Scenario «Детектор смотрит сегмент пути, не имя выгрузки».
**Зависимости:** нет.
**Режим apply:** mechanical

## 1. Классификатор охвата

- [x] S1.1 Заменить в `.cursor/skills/openspec-apply-change/SKILL.md` классификатор `marker_scope`: после нормализации `\` → `/` сегменты `/cf/` и `/cfe/` в путях `src/**/*.bsl` из `tasks.md`/`design.md` (есть `/cfe/` без `/cf/` → `cfe`; только `/cf/` → `cf`; оба → `mixed`; нет путей → не определён); словарь без `cf-ea`; литералы `ЭДО и ЭА` / `ЭДО ПАО` / `ДО3 Демо` убрать; ярлык шаблона маркеров и веток last-slice `cf-ea` → `cf`; формулировку трёх слов и разбор `ревью`/`архив`/`стоп` не переписывать; `openspec/project.md` не источник `marker_scope` (BC1, Decisions п. 1)
- [x] S1.2 Заменить в `.cursor/skills/openspec-status/SKILL.md` ключ `marker_scope`: тот же алгоритм сегментов и словарь `cf` | `cfe` | `mixed` | не определён без `cf-ea` и без путей ЭДО/ДО3, чтобы снимок статуса совпадал с глоссарием (BC1, EM5)

## 2. Глоссарий и чеклист поставки

- [x] S1.3 Сверить по тексту `.cursor/docs/glossary.md` словарём `marker_scope`: значения уже `cf` | `cfe` | `mixed` | не определён — не возвращать `cf-ea` и не ломать статью (EM5)
- [x] S1.4 Добавить в `.cursor/docs/delivery-integrity.md` пункт чеклиста про фильтр охвата по сегментам `cf`/`cfe` без путей заказчика, не про список утечки имён, чтобы поставка ловила чужой классификатор (S1 SHALL)

## 3. Сверка last-slice и карточки проекта

- [x] S1.5 Сверить по тексту `.cursor/skills/openspec-apply-change/SKILL.md` и `.cursor/skills/openspec-verify-change/templates/chat-summary.md` развилку last-slice после смены ярлыка: три слова при `cfe`/`mixed`; разбор `ревью` / `архив` / `стоп` с прежним Then; возврат в сессию и карточка завершения — та же развилка; слот проверки постановки после apply — команда архива, без развилки; соседние ЗНИ не принимать и не архивировать; `openspec/changes/archive/**` не править (Decisions п. 1, Scenario «Ответ ревью даёт команду предрелиза», Scenario «Ответ архив без изменений», Scenario «Ответ стоп без изменений», Scenario «Возврат в сессию — та же развилка», Scenario «Карточка завершения — та же развилка», Scenario «Проверка постановки после apply не меняется»). Зависимости: S1.1
- [x] S1.6 Сверить по тексту `.cursor/skills/openspec-apply-change/SKILL.md` и `.cursor/skills/openspec-status/SKILL.md` границу карточки: наличие корня расширения в `openspec/project.md` не делает kit-only ЗНИ `mixed` и не добавляет слово `ревью` (BC1, Scenario «Карточка проекта не подменяет охват ЗНИ»). Зависимости: S1.1, S1.2

## 4. Приёмка

- [x] S1.accept Принять срез S1 «Охват маркеров без чужих путей» — kit-only после «принято» даёт архив без слова «ревью»:
  - **Primary (обязательно):** по тексту `.cursor/skills/openspec-apply-change/SKILL.md` — kit-only ЗНИ (в задачах нет `.bsl`) после «принято» на последнем срезе: вопрос с ответами `архив` и `стоп`, слова `ревью` нет (Scenario «Kit-only — архив без ревью»)
  - Scenario «Без кода расширения слова ревью нет» (опционально): тот же Then при охвате `cf` или «не определён»
  - Scenario «Фильтр без путей заказчика» (опционально): в скилле реализации нет путей ЭДО/ДО3 как фильтра; класс — сегменты `/cf/` `/cfe/` (S1.1)
  - Scenario «Карточка проекта не подменяет охват ЗНИ» (опционально): карточка с корнем cfe при отсутствии `.bsl` в задачах текущей ЗНИ не даёт `mixed` и не предлагает `ревью` (S1.6)
  - Scenario «Последний срез — развилка из трёх слов» (опционально): по тексту скилла при `cfe`/`mixed` в путях задач — три слова `ревью` / `архив` / `стоп`
  - Scenario «Детектор смотрит сегмент пути, не имя выгрузки» (опционально): модуль в `/cfe/` без путей ЭДО → три слова, включая `ревью`
  - Scenario «Ответ ревью даёт команду предрелиза» (опционально): по тексту скилла Then ответа `ревью` не изменён (S1.5)
  - Scenario «Ответ архив без изменений» (опционально): по тексту скилла Then ответа `архив` не изменён (S1.5)
  - Scenario «Ответ стоп без изменений» (опционально): по тексту скилла Then ответа `стоп` не изменён (S1.5)
  - Scenario «Возврат в сессию — та же развилка» (опционально): по тексту скилла возврат ведёт на ту же развилку (S1.5)
  - Scenario «Карточка завершения — та же развилка» (опционально): по тексту скилла карточка `all_done` печатает ту же развилку (S1.5)
  - Scenario «Проверка постановки после apply не меняется» (опционально): в `chat-summary.md` слот следующего шага — команда архива (S1.5)

<!-- slice-gate: kit-only после «принято» — вопрос архива без слова «ревью»; фильтр охвата без путей заказчика -->

# Срез S2: Порог схемы после описания

**Сценарий:** после описания ЗНИ намёк схемы считает порог по правилам выбранного средства карты, а не «оба отсева».
**Primary acceptance:** выбранное средство — таблица; на одном отчёте после отсева без доказательства остаются четыре строки с доказательством, инцидентных связей нет, есть вывод и признак формы связей → в строке следующего шага есть вариант схемы.
**Приёмка:** сверка текста скилла описания и скилла карты.
**Связь со spec:** Requirement «Offer by topology not by topic» — Scenario «Исследование предлагает схему при топологии без замеров», Scenario «Разбор предлагает схему на подтверждении списка без порога публикации», Scenario «Разбор предлагает схему на выходе без замеров времени», Scenario «На линейных двух шагах схему не предлагают», Scenario «Предложение не зависит от темы механизма», Scenario «Описание предлагает схему при топологии в отчётах», Scenario «Описание с таблицей без рёбер», Scenario «Описание без отчётов схему не предлагает», Scenario «Плоский набор без формы связей в описании не даёт намёка», Scenario «Отказ без карты в описании не повторяется», Scenario «Согласие описания отдаёт тот же один отчёт».
**Зависимости:** нет.
**Режим apply:** mechanical

## 1. Порог и согласие

- [x] S2.1 Заменить в `.cursor/skills/openspec-overview/SKILL.md` порог «после обоих отсевов»: считать не меньше четырёх публикуемых по правилам выбранного средства (граф: без доказательства и без инцидентной связи; таблица: без доказательства, связь не нужна); условия «конкретный вывод» и «топологический признак» не менять (BC3, ADR-0009)
- [x] S2.2 Убрать в `.cursor/skills/openspec-overview/SKILL.md` фразу «после поставки соседней ЗНИ»: согласие вызывает сборщика с тем же одним отчётом, без подмешивания «всех путей»; путь control-run без slug `nastroyka-knopok-processov` (BC3)

## 2. Сверка слотов и регресса описания

- [x] S2.3 Сверить по тексту `.cursor/skills/scenario-map-canvas/SKILL.md` и `.cursor/skills/openspec-overview/SKILL.md` слоты исследования и разбора: «Дальше» при топологии без замеров; подтверждение списка без порога публикации; выход разбора без замеров времени; линейные два шага без намёка; независимость от темы механизма — этой ЗНИ не меняются (Scenario «Исследование предлагает схему при топологии без замеров», Scenario «Разбор предлагает схему на подтверждении списка без порога публикации», Scenario «Разбор предлагает схему на выходе без замеров времени», Scenario «На линейных двух шагах схему не предлагают», Scenario «Предложение не зависит от темы механизма»)

## 3. Приёмка

- [x] S2.accept Принять срез S2 «Порог схемы после описания» — таблица из четырёх доказанных строк без рёбер даёт намёк схемы:
  - **Primary (обязательно):** по тексту `.cursor/skills/openspec-overview/SKILL.md` — средство таблица, один отчёт, после отсева без доказательства четыре строки с доказательством, инцидентных связей нет, вывод и признак на месте → в строке шага есть вариант схемы (Scenario «Описание с таблицей без рёбер»)
  - Scenario «Описание предлагает схему при топологии в отчётах» (опционально): порог ≥4 после отсева выбранного средства на том же одном отчёте (S2.1)
  - Scenario «Согласие описания отдаёт тот же один отчёт» (опционально): фразы про соседнюю ЗНИ нет; в сборщика уходит тот же путь (S2.2)
  - Scenario «Описание без отчётов схему не предлагает» (опционально): по тексту скилла без отчётов разбора варианта схемы нет, причины в чат нет
  - Scenario «Плоский набор без формы связей в описании не даёт намёка» (опционально): по тексту скилла без топологического признака намёка нет
  - Scenario «Отказ без карты в описании не повторяется» (опционально): по тексту скилла после «без карты» намёк до конца сессии не повторяется
  - Scenario «Исследование предлагает схему при топологии без замеров» (опционально): слот «Дальше» не менялся этой ЗНИ (S2.3)
  - Scenario «Разбор предлагает схему на подтверждении списка без порога публикации» (опционально): слот подтверждения списка не менялся (S2.3)
  - Scenario «Разбор предлагает схему на выходе без замеров времени» (опционально): слот выхода разбора не менялся (S2.3)
  - Scenario «На линейных двух шагах схему не предлагают» (опционально): линейные два шага без намёка (S2.3)
  - Scenario «Предложение не зависит от темы механизма» (опционально): отдельного условия по теме нет (S2.3)

<!-- slice-gate: после описания порог схемы считается по выбранному средству; таблица без рёбер намёк не снимает -->

# Срез S3: Скилл на входе сессии

**Сценарий:** follow-up в той же команде не требует повторного чтения всего скилла; «покажи схему» в диспетчере остаётся строкой-указателем.
**Primary acceptance:** по тексту правила сессии — на первом ходе команды единственный вызов батча 1 = Read скилла; следующее сообщение той же команды скилл повторно не читает.
**Приёмка:** сверка текста правила сессии и диспетчера; замер бюджета — задача агента.
**Связь со spec:** Requirement «Skill file is read on session entry» — Scenario «Скилл читается на входе», Scenario «Follow-up без повторного чтения скилла»; Requirement «Map cue in dispatcher is a pointer» — Scenario «Cue «покажи схему»».
**Зависимости:** нет.
**Режим apply:** mechanical

## 1. Вход сессии и диспетчер

- [x] S3.1 Заменить в `.cursor/rules/session-discipline.mdc` правило D5: Read `SKILL.md` только на первом ходе команды, не на каждом; протокол на follow-up без повторного Read; страховка обрезки — 3–6 строк с именами карточек `executive-summary`, `info-section`, `layer-1-hygiene-table`, без копирования тела `.cursor/rules/command-skill-gate.mdc` (BC4, ADR-0001)
- [x] S3.2 Сузить в `.cursor/rules/gate-dispatcher.mdc` строку карты: cue прямой просьбы «покажи схему» / «покажи карту сценария» ведёт к скиллу карты, без тела протокола молчания и намёка (BC4)

## 2. Бюджет always-apply

- [x] S3.3 Замерить сумму always-apply `.cursor/rules/*.mdc` плюс `AGENTS.md`: размер ≤ 34 КБ после S3.1, чтобы правка сессии не раздула бюджет поставки (Risks п. 2, delivery-integrity п. 7). Зависимости: S3.1

## 3. Приёмка

- [x] S3.accept Принять срез S3 «Скилл на входе сессии» — полный скилл читается только на входе команды:
  - **Primary (обязательно):** по тексту `.cursor/rules/session-discipline.mdc` — первый ход: единственный вызов батча 1 = Read скилла; следующее сообщение той же команды скилл повторно не читает (Scenario «Скилл читается на входе», Scenario «Follow-up без повторного чтения скилла»)
  - Scenario «Cue «покажи схему»» (опционально): в диспетчере короткая строка-указатель без копипасты молчания и намёка (S3.2)

<!-- slice-gate: Read скилла только на первом ходе команды; cue «покажи схему» без тела протокола -->

# Срез S4: Ссылки и запасные инструменты

**Сценарий:** контролёр срезов находится по рабочей ссылке; смысловой поиск при отсутствии инструмента не стопит сессию.
**Primary acceptance:** в скилле постановки ЗНИ путь шаблона указывает на `.cursor/skills/1c-agent-patterns/quality-controller.md`, и этот файл существует.
**Приёмка:** сверка текста ссылок и Grep `explore/fast`.
**Связь со spec:** Requirement «Quality controller template path is resolvable» — Scenario «Ссылка контролёра открывается»; Requirement «Search fallback without semantic tool» — Scenario «Fallback поиска»; Requirement «Generic explore has no fast mode» — Scenario «План анализа без режима fast»; Requirement «Map designer does not full-read the canvas skill» — Scenario «Картограф без полного чтения скилла карты»; Requirement «Style guide is read by cited section» — Scenario «Стайл-гайд по цитате».
**Зависимости:** нет.
**Режим apply:** mechanical

## 1. Постановка ЗНИ и план анализа

- [x] S4.1 Заменить в `.cursor/skills/openspec-new-change/SKILL.md` ссылку контролёра на `.cursor/skills/1c-agent-patterns/quality-controller.md`, чтобы относительное `openspec-quality-controller.md` без каталога не оставалось рабочей ссылкой (S4 SHALL)
- [x] S4.2 Заменить в `.cursor/skills/openspec-new-change/SKILL.md` ярлык preview `cf-ea` на `cf`, чтобы словарь постановки совпадал с глоссарием (Decisions п. 1)
- [x] S4.3 Заменить в `.cursor/skills/context-strategy/SKILL.md` вызов `explore/fast` обычным `explore`, чтобы план анализа не ссылался на несуществующий режим (S4 SHALL)

## 2. Картограф, журнал и чтение по цитате

- [x] S4.4 Убрать в `.cursor/agents/onec-scenario-map-designer.md` полный Read скилла карты: средство, порог и запрет выдуманных рёбер приходят в брифе или коротком фрагменте, достаточном для манифеста (S4 SHALL)
- [x] S4.5 Добавить в шапку `.cursor/docs/agents-CHANGELOG.md` указатель: SSOT моделей субагентов — `.cursor/rules/model-selection.mdc`, чтобы журнал не дублировал таблицу ролей
- [x] S4.6 Записать в `.cursor/rules/chat-output-budget.mdc` и `.cursor/docs/opsx-output-style.md`: оркестратор читает только процитированный § стайл-гайда (как §5.2 в скилле реализации), не весь файл на каждое сообщение (Scenario «Стайл-гайд по цитате»)
- [x] S4.7 Добавить в `.cursor/agents/onec-code-explorer.md` (и architect/writer / скиллы БСП, где SemanticSearch указан без запасного пути): при отсутствии смыслового поиска — Grep и Glob, сессия не стоп (Scenario «Fallback поиска»)

## 3. Приёмка

- [x] S4.accept Принять срез S4 «Ссылки и запасные инструменты» — ссылка контролёра срезов открывается:
  - **Primary (обязательно):** в `.cursor/skills/openspec-new-change/SKILL.md` путь шаблона — `.cursor/skills/1c-agent-patterns/quality-controller.md`, файл на месте (Scenario «Ссылка контролёра открывается»)
  - Scenario «План анализа без режима fast» (опционально): в `.cursor/skills/context-strategy/SKILL.md` нет `explore/fast` (S4.3)
  - Scenario «Fallback поиска» (опционально): отсутствие смыслового поиска не стопит протокол — Grep/Glob (S4.7)
  - Scenario «Картограф без полного чтения скилла карты» (опционально): агент не читает скилл карты целиком (S4.4)
  - Scenario «Стайл-гайд по цитате» (опционально): читается процитированный §, не весь стайл-гайд (S4.6)

<!-- slice-gate: ссылка контролёра открывается; в плане анализа нет режима fast -->

# Срез S5: Нейтральные примеры

**Сценарий:** скопированный каталог `.cursor/` не содержит имён чужих проектов в эталонах, командах и шаблонах.
**Primary acceptance:** Grep по списку утечки в `.cursor/**` даёт 0 совпадений.
**Приёмка:** Grep списка утечки; выборочная сверка «хорошо / плохо» после замены имён.
**Связь со spec:** Requirement «Examples without real project names» — Scenario «Примеры без имён проектов», Scenario «Смысл эталона сохраняется».
**Зависимости:** нет.
**Режим apply:** mechanical

## 1. Команды и эталоны описания

- [x] S5.1 Заменить в `.cursor/commands/opsx-extend.md`, `.cursor/commands/opsx-knowledge-add.md` и `.cursor/commands/opsx-knowledge-audit.md` литералы списка утечки нейтральным словарём (`sample-change`, `src/Конфигурация/cf/`, `src/Конфигурация/cfe/ДемоРасширение/`, префикс `ext_`), смысл примеров вызова сохранить (BC5)
- [x] S5.2 Заменить в `.cursor/skills/openspec-overview/fixtures/` и `.cursor/skills/openspec-overview/templates/` (в т.ч. `control-run.md`, `golden-88781-overview.md`, `voice-good-stage.md`, `customer-overview.md`, `style-checklist.md`) имена чужих проектов нейтральным словарём, голос «хорошо / плохо» не ломать (BC5, Scenario «Смысл эталона сохраняется»)

## 2. Разбор, пауза, verify

- [x] S5.3 Заменить в `.cursor/skills/openspec-explain/fixtures/voice-good-point.md` и `.cursor/skills/openspec-explain/fixtures/voice-good-report-snippet.md` литералы `Павлик` / `pavЭтоИБПавлик` нейтральным словарём (`ext_`), урок эталона сохранить (BC5)
- [x] S5.4 Заменить в `.cursor/skills/openspec-verify-change/templates/card-decision.md` slug `do2-pavlik` и пути `ДО21_Павлик` нейтральными `sample-change` / `ДемоРасширение`, структуру развилки карточки не менять (BC5)
- [x] S5.5 Заменить в `.cursor/skills/openspec-apply-change/templates/pause-wait-chat.md` имя расширения «Павлик» нейтральным учебным именем, список «что создать» сохранить (BC5)

## 3. Документы, шаблоны агентов, антипаттерны

- [x] S5.6 Заменить в `.cursor/commands/opsx-knowledge-init.md`, `.cursor/skills/openspec-knowledge-init/SKILL.md`, `.cursor/rules/knowledge-format.mdc` и `.cursor/docs/architect-report-schema.md` литералы списка утечки (в т.ч. `КонтурДиадок`) нейтральным словарём; если совпадений нет — оставить файл без правки (BC5)
- [x] S5.7 Заменить в `.cursor/skills/1c-agent-patterns/architect.md` литералы `ДО3 Демо` / `ЭДО ПАО` нейтральными именами конфигурации, смысл пункта про прототип сохранить (BC5)
- [x] S5.8 Заменить в `.cursor/docs/opsx-output-style.md`, `.cursor/docs/review-guide.md`, `.cursor/docs/templates/brief-card.md` и `.cursor/docs/ux-acceptance-isolated-chat.md` литералы `Павлик` / `ДО21_Павлик` / `КонтурДиадок` / `do2-pavlik` нейтральным словарём, структуру абзацев не трогать (BC5)
- [x] S5.9 Заменить в `.cursor/docs/antipatterns/bsl-antipatterns.md` учебные литералы списка утечки (в т.ч. «ЭДО ПАО» в комментарии-примере) нейтральным словарём; allow-list AP-031 (`pav_` / `lvv_` / `пр_`) не менять (Decisions п. 3)

## 4. Дочистка и доказательство Grep

- [x] S5.10 Заменить в `.cursor/**` остаток литералов списка утечки нейтральным словарём: Павлик, ДО21_Павлик, pavЭтоИБПавлик, ЭДО ПАО, ЭДО и ЭА, ДО3 Демо, КонтурДиадок, do2-pavlik, nastroyka-knopok-processov; `openspec/changes/archive/**` и allow-list AP-031 не трогать (BC5)
- [x] S5.11 Сверить Grep по `.cursor/**` на список утечки: Павлик, ДО21_Павлик, pavЭтоИБПавлик, ЭДО ПАО, ЭДО и ЭА, ДО3 Демо, КонтурДиадок, do2-pavlik, nastroyka-knopok-processov — 0 совпадений; каталог `openspec/changes/archive/**` в поиск не включать; совпадения allow-list AP-031 (`pav_` без имён из списка) не считать утечкой (BC5, S5 SHALL). Зависимости: S5.1–S5.10

## 5. Приёмка

- [x] S5.accept Принять срез S5 «Нейтральные примеры» — в копируемом `.cursor/` нет имён чужих проектов:
  - **Primary (обязательно):** Grep списка утечки (Павлик, ДО21_Павлик, pavЭтоИБПавлик, ЭДО ПАО, ЭДО и ЭА, ДО3 Демо, КонтурДиадок, do2-pavlik, nastroyka-knopok-processov) по `.cursor/**` = 0 (Scenario «Примеры без имён проектов»)
  - Scenario «Смысл эталона сохраняется» (опционально): в эталонах overview/explain после замены имён тот же урок «хорошо / плохо» (S5.2, S5.3)

<!-- slice-gate: Grep списка утечки в `.cursor/**` даёт 0 совпадений -->
