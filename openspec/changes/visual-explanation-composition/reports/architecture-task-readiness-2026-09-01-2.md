---
report_type: task-readiness
generated_at: 2026-09-01
agent: onec-code-architect
mode: task-readiness
scope:
  change: visual-explanation-composition
  slices: [S1, S2]
  files:
    - .cursor/skills/visual-explanation/SKILL.md
    - .cursor/skills/visual-explanation/fixtures/panel-shell.md
    - openspec/adrs/ADR-0010-visual-explanation-panel.md
  capabilities: [visual-explanation]
related_reports:
  - reports/quality-control-2026-09-01-3.md
  - reports/architecture-task-readiness-2026-09-01.md
  - reports/design-challenge-2026-09-01-2.md
  - reports/architecture-extend-coherence-2026-09-01.md
  - reports/architecture-2026-09-01-canvas-companion.md
confidence: high
open_questions_count: 0
superseded_by: null
---

# Готовность задач к реализации — visual-explanation-composition (после repair)

## Вердикт

**ГОТОВО** — открытые рабочие задачи S2.1–S2.4 можно выполнить as-is по design + spec + тексту задачи, без вопроса человеку. Три дописки прошлого прогона (якоря в навыке, хвост копии шаблона / пара `Grid`+`Callout` / не пятое значение формы, пункт «Решение» 3 в ADR) стоят в телах задач. Целевые файлы существуют и содержат дословные фразы, которые S2 снимает. Возврат на уточнение не требуется. Пробелов нет.

## KB references

Блок `## Existing Knowledge` во входе пуст по существу: Discovery выполнен, совпадений нет, таксономии KB в kit нет. Верифицированных фактов, на которые опирался бы разбор, нет; конфликтов нет.

## Closed decisions (вход)

- `hint-slots-explain-overview` — намёк в пошаговом разборе и обзоре вне поставки; Follow-up в `tasks.md` не поднят в рабочие задачи.
- `two-pictures-or-one-signoff` — две отдельные приёмки, срезы не сливать. В `tasks.md` ровно один `S1.accept` и один `S2.accept`.

## Оценка по критериям

| # | Критерий | Вердикт | Обоснование |
|---|----------|---------|-------------|
| 1 | Реализуемость кодовых задач | OK | Kit-change: markdown/шаблон, не `.bsl`. Каждая открытая задача называет файл и наблюдаемый исход. S2.1: якоря в файле навыка названы (шаг 2 `presentation.form` — подсказка; шаг 3 «Форма по содержанию» — снять «ближайшая из четырёх» как мир; шаг 5 «Смысл» — сузить экзамен вывода; после шага 7 — интерактив по D9); смешанный источник закрыт фразой «голая „покажи схему“ не выбирает поток из-за цепочек в отчёте». В файле навыка эти места есть дословно (`Неизвестный жанр → ближайшая из четырёх`; обязательный союз формы; абзац `openFile`). S2.2: библиотека рецептов; хвост копии MUST NOT оставлять «всё остальное → поток со скелетом»; классификация — фрагмент или чистый лист, не новое значение формы; запрет `Grid`/`Callout` как продукта снять оба. В шаблоне есть умолчание скелета, `Main()` иначе → `SkeletonChrome`+`FlowView`, `ItemButton`, `TableView` с колонками «Элемент / Пояснение / Связь», запрет пары виджетов. S2.3: пункт «Решение» 3 — подсказка рецепта, не мир; пункт 4 — тот же инвариант формы, что Protects-invariants; в ADR-0010 пункт 3 всё ещё читается как союз `flow \| table \| hierarchy \| card`. S2.4: сверка тех же трёх файлов после S2.1–S2.3, включая хвост копии, пустой список связей и пункт 3 ADR. Правило S1.1 «ближайшая из четырёх» при `[x]` — план переписи, не дыра. |
| 2 | Реализуемость форм и метаданных | OK / n/a | `form_mode: n/a`. Form.xml, метаданные 1С и Конфигуратор вне scope. Маркеров ручной конфигурации 1С нет. Носитель — markdown навыка и шаблон панели kit. |
| 3 | Разрешённость решений | OK | Закрытые решения verify/extend зафиксированы и не оставляют вилки в рабочих задачах. «Доказательство — одно на полотно или точечная цитата» и «рецепт или чистый лист» — два допустимых поведения одного контракта, не два пути на выбор. `description MAY` — необязательная формулировка шапки, не развилка реализации. Open Question про фикстуру слепого прогона помечен как не блокирующий; Chosen — сценарий в постановке. Условных задач (`Если` / `Иначе` / два apply-пути) нет. Repair не открыл ось: D8/E1 на месте. |
| 4 | Полнота покрытия | OK | Семь требований дельты имеют носителя. Авто/намёк и слот исследования — закрыты S1.1/S1.3 (S2.1 их не меняет). Форма за работой над текстом, неизвестный жанр, таблица без обязательных кнопок — S2.1/S2.2/S2.4. Одна сцена на скелете и порог шести частей — S1; S2 сужает «плакат» и добавляет классификацию без степпера. Язык = отношение в тексте и смешанный источник (в том числе голая просьба) — S2.1 + optional Scenario S2. Компоненты среды и запрет метатекста — уже в навыке/шаблоне; S2.2 сохраняет запреты HTML/водяного текста/координат. Умолчание копии шаблона — S2.2/S2.4. Пункт ADR «Решение» 3 — S2.3/S2.4. Follow-up explain/overview — закрытое решение, не пробел покрытия. QC: 22 Scenario покрыты. |
| 5 | Согласованность | OK | Намеренное расхождение S1.1/S1.2/S1.4/S1.5 `[x]` (ближайшая из четырёх; скелет по умолчанию; запрет `Grid` как продукта; сверка S1.5) с S2 — план переписи тех же файлов, объявлен в design (граф «S2 → нет», E-D1, S2.4: галочки S1.5 не закрывают спутник). S2.1 сохраняет рецепт скелета для работы «слои / последовательность» и не трогает авто — не ломает Then S1. Spec «четыре значения — рецепты, не мир» совпадает с D8/S2.1. Closed `hint-slots-explain-overview` совпадает с Follow-up и запретом сверки слота *как* рисовать. Repair design/spec/tasks согласован: хвост копии, голая просьба, пункт 3 ADR — в трёх местах. |
| 6 | Связность кода и порядок задач | OK | S2.1, S2.2, S2.3 независимы (три разных файла). S2.4 явно зависит от S2.1–S2.3. Затем `S2.accept`. Цикла нет. Ровно один accept и один `<!-- slice-gate -->` на срез. `**Зависимости:**` у обоих срезов — `нет`. `S1.accept` остаётся `[ ]` по E-D1 и closed `two-pictures-or-one-signoff` — не дыра порядка apply S2: рабочие S1.1–S1.5 `[x]`, приёмка механизма слоями не требует задач S2. |
| 7 | Архитектурная эстетика (Design Smells) | OK | Подход не пересматривается. Один навык + один шаблон-библиотека + уточнение ADR-0010 in-place — без нового объекта, без пятой формы, без второго файла шаблона. Over-engineering / invasiveness / reinventing — нет. Замечания прошлого прогона (безымянные якоря, только `Grid`, риск пятого значения формы, пункт 3 ADR не назван) закрыты repair в телах S2.1–S2.3. Длина S2.1/S2.4 остаётся, но исполнитель не останавливается: якоря и чеклист сверки достаточны. |
| 8 | User Task Contract + прецедент | OK | В строках `S2.1`–`S2.4` нет runtime-spike (ИБ, консоль, отладчик, эмуляция API). S2.4 — сверка по файлам kit (агент). «Слепой прогон без хардкода сюжета» стоит только в поле `**Приёмка:**` среза S2, не в рабочей задаче `S<N>.<M>`. DENY-маркеров нет. Прецедент: `## Blast Radius` в design заполнен; ADR-0010 уточняется in-place без замены несущего решения и без переписи секции 0008/0009; граф/родитель/отдельная команда/подавление на проверке сохраняются. Молчаливой отмены архива нет. KB-совпадений нет. |

## Пробелы

Нет. Repair закрыл три дописки `reports/architecture-task-readiness-2026-09-01.md` вставкой в существующие S2.1–S2.3/S2.4. Новых задач, файлов и решений не требуется. Сниппетов для авто-вставки нет.

## Simplicity Check

- **Viable alternatives:**
  1. Apply S2.1–S2.4 as-is после repair (три файла: навык, шаблон, ADR-0010).
  2. Разбить S2.1 надвое (работа/язык vs хром/носитель) и дублировать сверку — те же файлы, длиннее чеклист.
  3. Два файла шаблона (механизм / сопоставление) вместо хвоста `Main()` — лишняя точка правки и N+1 при новой работе.
- **Selected simplest viable design:** вариант 1. Постановка после repair называет хвост копии, смешанный источник и пункт «Решение» 3; исполнитель правит уже существующие файлы.
- **Why not simpler:** пропустить S2.2 (оставить `Main()` иначе → поток/скелет) или S2.3 (оставить союз формы в пункте 3) — класс ошибки «не та картина» / закрытый мир остаётся в копируемом примере и в несущем решении. Вариант 2 не даёт нового наблюдаемого поведения. Вариант 3 хуже по числу файлов (Hardcode Justification: новая работа не плодит виджет в enum и не плодит файл).
- **Complexity budget:**
  - Files touched: 3 (навык, шаблон, ADR-0010)
  - Hooks/intercepts: 0
  - New procedures/functions: 0 (kit markdown; в шаблоне — рецепт-фрагмент на уже импортируемых примитивах)
  - Conditional branches / feature flags: 0 (`description MAY` не флаг поставки)

## Источники

- `proposal.md` — Why, What Changes п. 3, Decisions 5–6, Acceptance 3
- `design.md` — D2, D5, D8, D9, E-D1…E-D6, Behavior 1, 3–4, 7–9, Existing Mechanisms 1, Slices, Blast Radius, Open Questions, «Решения verify (зафиксировано)»
- `tasks.md` — S2.1–S2.4 (после repair: якоря, хвост копии, пункт «Решение» 3, смешанный источник), `S2.accept`, Follow-up
- `specs/visual-explanation/spec.md` — 7 требований, 22 Scenario, в т.ч. «Смешанный источник не даёт конвейер» (WHEN с голой просьбой)
- `.cursor/skills/visual-explanation/SKILL.md` — шаги 2–3, 5, абзац после шага 7 (`ближайшая из четырёх`, союз формы, `openFile`)
- `.cursor/skills/visual-explanation/fixtures/panel-shell.md` — запрет Grid/Callout, `ItemButton`, `TableView`, умолчание скелета, `Main()` иначе → `FlowView`
- `openspec/adrs/ADR-0010-visual-explanation-panel.md` — Protects-invariants, Решение 3–4, Blast Radius 0008/0009
- `debug.md` — closed_decisions `hint-slots-explain-overview`, `two-pictures-or-one-signoff`
- `reports/quality-control-2026-09-01-3.md` — Verdict OK; UTC без DENY; 8b/9 не сработали
- `reports/architecture-task-readiness-2026-09-01.md` — предыдущие три дописки, закрытые repair
