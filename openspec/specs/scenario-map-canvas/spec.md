# scenario-map-canvas

## Purpose

Capability снята. Панель рядом с чатом — `visual-explanation` (`openspec/specs/visual-explanation/spec.md`). Карта сценария как отдельный продукт не публикуется.

## Requirements

Все требования удалены ЗНИ `universal-visual-explanation`. Миграция — capability `visual-explanation`.
