# Test-case: explore-bug — Symptom Relevance (signature verify) — Ultra-Lite

Регрессионный сценарий для `/opsx:explore` Ultra-Lite. Основан на инциденте `2026-05-20-signature-verify-double-binary` (исторически — сессия в `openspec/sessions/`, читать только как legacy-контекст).

## Вход

- **Профиль:** `explore-bug`
- **Симптом пользователя (слот **Вопрос** / `user-goal`):** валидная подпись Kontur, типовая проверка показывает «недействительна»; в отладчике два элемента в `ВариантыДополнительныхПроверок`.
- **Трасса:** `temp/Проверка подписи_TRACE_FULL.txt`
- **Архив (пользовательская ссылка, не причина по умолчанию):** `openspec/changes/archive/2026-05-18-kontur-signing-contract-regression`
- **Ожидаемая открытая гипотеза в архиве:** `H-sign-task` в `reports/exploration-verify-org-variants-2026-05-18.md`

## Контрольные точки (7)

| # | Шаг | Ожидание | Ожидаемый итог |
|---|-----|----------|----------------|
| 1 | Entry-бриф B3 в чате | 8–12 строк (escape до 14), слоты **От вас / Вопрос / План / На выходе / Подтвердить?**, заголовок `Бриф:`, без `target`/`prev`/имён агентов; **подтверждение текстом**, не `AskQuestion` | PASS |
| 2 | После «да» — файл сессии не создан | `Glob openspec/sessions/<новый-slug>/` ничего не возвращает; бриф не сохранён в файл (`brief.md` нет) | PASS |
| 3 | После `Task` → `onec-trace-analyst` — полный отчёт в `temp/reports/` | `temp/reports/trace-analysis-YYYY-MM-DD-signature-verify-double-binary.md` записан агентом / оркестратором | PASS |
| 4 | Превью в чате (8–15 строк) + Symptom Relevance Check | Превью с якорями + путь к `temp/reports/...`; перед «Вердиктом» проверено: «двойное получение байтов **не** объясняет invalid подписи» → `не объясняет` | PASS |
| 5 | Блок «Итог / Вердикт / Дальше» | **Вердикт** не «штатно» / «допустимое поведение»; **Symptom Lock** не позволяет такого вердикта при неснятом якоре; кандидат: рассогласование sign/verify вариант [0] / `H-sign-task` | PASS |
| 6 | «Дальше» — конкретный шаг | Гипотеза устранения симптома (например, «второй `Task` к `onec-code-explorer` на verify-org-variants») **или** «достаточно, формирую блок `## Для /opsx:ff`»; нет бинарных «хватит / копаем дальше» без конкретики | PASS (`profiles/bug.md` HALT для «Дальше») |
| 7 | Финал bug — блок `## Для /opsx:ff` **в чате** | Полный блок с полями Симптом, Корневая причина с маркером `[verified]` / `[hypothesis: план]`, Что менять, Файлы, Приёмка, **Связь с архивом** (`extends kontur-signing-contract-regression`), Architect/verify; handoff-файл создаётся **только** если пользователь словами просил «сохрани» | PASS |

## Эталонный entry-бриф B3 (≈11 строк)

```markdown
Бриф: проверка подписи показывает «недействительна»

**От вас** — Подпись Kontur валидна, проверка в ДО показывает «недействительна»; в отладчике два варианта проверки.
**Вопрос** — Что расходится между подписанием и проверкой; чинить или это штатное поведение.
**План**
1. Разобрать трассу — почему два варианта.
2. Сверить с архивным ЗНИ про подпись Kontur (если относится).
3. При связи — найти точку расхождения в расширении.
**На выходе** — ответ в чате с выводом «баг / не баг» и при необходимости блок `## Для /opsx:ff` для создания ЗНИ.
**Артефакты** — `temp/Проверка подписи_TRACE_FULL.txt`; архив `kontur-signing-contract-regression`.
**Подтвердить?** — «да» или уточните (например, только сценарий «из задачи»).
```

## Эталонный фрагмент `## Для заказчика` в отчёте trace-analyst

В полном отчёте `temp/reports/trace-analysis-YYYY-MM-DD-signature-verify-double-binary.md`, после технического раздела:

1. **Что наблюдаешь:** подпись валидна, проверка в ДО — недействительна.
2. **Почему так устроено:** типовая ДО строит два канона (организация заполнена/пустая); в отладчике два варианта.
3. **Связано ли:** `не объясняет, симптом из другой ветки` (двойное получение ≠ invalid).
4. **Что чинить:** `[hypothesis: byte-diff sign vs verify вариант [0]]` — рассогласование `ДанныеПодписанта` / `Стороны.*` при подписании из задачи (`H-sign-task`, exploration-verify-org-variants).

## Эталонный блок `## Для /opsx:ff` в чате (финал)

```markdown
## Для /opsx:ff

**Симптом:** Подпись Kontur валидна, проверка в ДО показывает «недействительна»; два варианта проверки в отладчике.
**Корневая причина:** `cfe/ргОблачныеПодписиKonturCryptoAPI_ЭДОПАО:???` — рассогласование `ДанныеПодписанта` / `Стороны.*` между sign и verify для варианта [0] [hypothesis: byte-diff verify-time canonical mismatch — explorer на verify-org-variants vs sign-task]
**Что менять:** выровнять контракт канона на sign и verify (вариант [0] = «организация заполнена»)
**Файлы:** `cfe/ргОблачныеПодписиKonturCryptoAPI_ЭДОПАО/` + типовой модуль построения вариантов проверки
**Приёмка:** для тестового документа из инцидента проверка возвращает «действительна» обеими вариантами; регресс по «организация пустая» не воспроизводится.
**Связь с архивом:** extends `kontur-signing-contract-regression` (закрывает `H-sign-task`)
**Architect / verify:** да (UX-значимый фикс отображения статуса подписи)
```

## Прогон

1. Запустить `/opsx:explore` с тем же входом (вход в чате + путь к трассе; legacy-сессия в `openspec/sessions/2026-05-20-...` не реанимируется — только читается, если пользователь явно сошлётся).
2. Пройти таблицу контрольных точек 1–7.
3. При FAIL — править `profiles/bug.md` (Symptom Lock, HALT для «Дальше»), `SKILL.md` (Symptom Relevance Check, лимиты `Task`, формат финального блока), `onec-trace-analyst.md` (требование `## Для заказчика`); **не ослаблять** HALT.
