---
name: prerelease-review
description: Pre-release code review for 1C extensions (full-extension or change-scoped by OpenSpec change name). Delegates to onec-code-reviewer with prerelease mode (severity escalation), produces a structured report, then offers openspec follow-up. Use when user requests "/opsx:prerelease-review", "/prerelease-review", "предрелизное ревью", "проверить перед релизом", "review before release".
license: MIT
compatibility: Requires openspec CLI. Delegates to onec-code-reviewer.
metadata:
  author: project
  version: "1.4"
---

Провести предрелизное ревью расширения 1С. Результат — структурированный отчёт с замечаниями по уровням. После — предложение создать openspec change на устранение (или дополнение существующего ЗНИ в режиме change-scoped).

**Input**:

- **Без аргументов** — определить расширение из контекста или спросить; режим **`full-extension`** (как раньше).
- **Один аргумент** — имя расширения (папка в `src/*/cfe/`); режим **`full-extension`**.
- **Два аргумента** — `<расширение> <change-name>`; режим **`change-scoped`**: Tier 1 и механика 1.7/1.7b/1.7c только по файлам ЗНИ; Tier 2 (explorer) — по всему расширению.

---

## Шаг 0. Определение scope и режима (`full-extension` / `change-scoped`)

### 0.1 Режим

| Ввод пользователя | `review_mode` | Действие |
|-------------------|---------------|----------|
| Только расширение или без аргументов (после уточнения) | `full-extension` | Поведение как до версии 1.3: все шаги без условий change-scoped. |
| Расширение + имя каталога change | `change-scoped` | Выполнить шаг 1.3a; валидацию change (ниже). |

**Имя change (`change-name`):** полное имя папки в `openspec/changes/<name>/` **или** `openspec/changes/archive/<name>/`. Порядок поиска: сначала `openspec/changes/<name>/`, затем `openspec/changes/archive/<name>/`.

При `change-scoped`:

1. Убедиться, что существует `.../<name>/tasks.md`. Иначе — **СТОП**: сообщить ошибку; перечислить доступные каталоги в `openspec/changes/` и `openspec/changes/archive/` (Glob `**/tasks.md` или `dir` по двум корням).
2. Зафиксировать путь к change: `change_dir = openspec/changes/<name>/` или `.../archive/<name>/`.
3. Зафиксировать `change_in_archive = true`, если путь содержит `/archive/`.

### 0.2 Расширение

Если пользователь указал конкретное расширение (имя папки в `src/*/cfe/`) — использовать. Пути к cf/cfe — из [openspec/project.md](../../../openspec/project.md), секция «Структура репозитория».

Иначе — найти доступные расширения:

```bash
# Найти расширения в src/
dir src\*\cfe\ -Directory
```

Использовать **AskUserQuestion tool** для выбора. Показать список расширений (например, `pav_ИсключениеУчастниковПоУсловию`).

Также проверить активные openspec changes:

```bash
openspec list --json
```

При `full-extension`, если есть активный change — уточнить, является ли ревью частью его workflow (опционально).

**ВАЖНО**: Не начинать ревью до получения чёткого scope (расширение + при `change-scoped` — валидный `change_dir`).

---

## Шаг 1. Сбор контекста

### 1.1 Прочитать стандарты проекта

- `.cursor/rules/.cursor/docs/1c-coding-standards.md` — стандарты BSL проекта.
- `.cursor/agents/onec-code-reviewer.md` — категории и правила ревью (в т.ч. отсылка к вендорским стандартам).
- **Вендорские стандарты 1С** (для полноты предрелизной проверки): `.cursor/skills/1c-vendor-standards/SKILL.md` — чеклисты по доменам; навигация — `.cursor/docs/standard/1c-standards-navigator.md`; полный текст по доменам — `.cursor/docs/standard/std-*.md` (метаданные, запросы, транзакции, формы и т.д.). Согласовано с AGENTS.md и onec-code-reviewer.

### 1.1a Соглашения по комментариям BSL (openspec/project.md)

**До шагов 1.7, 1.7b и 1.7c** прочитать [openspec/project.md](../../../openspec/project.md), секцию **«Форматы и соглашения по комментариям BSL»**. Если секции нет — whitelist и обязательный контроль **пусты**.

Извлечь **две** таблицы (если есть подзаголовки **Whitelist предрелиза** и **Обязательный контроль**):

**Whitelist**

- для каждой строки с заполненным **Префикс** — исключать комментарии, у которых после `//` (и пробелов) текст начинается с этого префикса;
- для каждой строки с заполненным **Regex** (на всю строку `//…`) — исключать комментарии, если **вся** строка комментария (от `//` до конца строки) удовлетворяет regex;
- строки-заглушки вроде «*(пока нет)*» не считать активными правилами.

**Обязательный контроль**

- перенести в бриф (шаг 1.5) все активные строки: **ID**, «Где проверять», «Требование / пример», **Regex допустимой строки**, **Уровень**, **kind**;
- если в таблице только пример (например EX-001), который команда не приняла — пользователь должен удалить строку из `project.md`, иначе предрелиз будет требовать соблюдения.

Кратко в брифе (шаг 1.5):

- **Whitelist форматов комментариев (project.md):** имена и правила (префикс/regex) или «нет»;
- **Обязательный контроль комментариев (project.md):** список ID и требований или «нет».

Памятка по колонкам (без дублирования таблиц): [.cursor/docs/bsl-comment-formats-project.md](../../docs/bsl-comment-formats-project.md).

### 1.2 Прочитать спецификацию расширения (если есть)

Искать в `openspec/docs/` файл со спецификацией расширения. Извлечь:

- Префикс расширения (например, `pavIU_`).
- Точки перехвата (список `&Вместо`/`&Перед`/`&После`).
- Префикс журнала регистрации.

Если спецификация не найдена — попросить у пользователя префикс расширения.

### 1.3a Извлечение файлов ЗНИ (только `review_mode = change-scoped`)

Цель — список **`change_files`**: уникальные пути к `.bsl`, относящиеся к **выполненным** задачам (`[x]`).

#### Основной путь: `tasks.md` в `change_dir`

1. Прочитать `change_dir/tasks.md`.
2. Разбить текст на блоки задач: строки вида `- [x] ...` (выполненные). Задачи с `[ ]` **не** участвуют в извлечении путей.
3. Для **каждой** задачи с `[x]` в её теле (до следующей задачи того же уровня или до заголовка `##`):
   - Из строк `**Файл:**` / `**Файлы:**` извлечь пути, заключённые в обратные кавычки `` `...` ``, содержащие `src/` и оканчивающиеся на `.bsl`.
   - Дополнительно найти в теле задачи все вхождения по regex: `` `src/[^`]*\.bsl` `` или `src/[^\s`]+\.bsl` (пути без кавычек в скобках — по согласованности с текстом задачи).
   - Если в задаче явно указано **«тот же»** / **«тот же файл»** / **«тот же Module.bsl»** (регистр не важен) и новый путь не извлечён — **наследовать** последний известный путь `.bsl` из **той же секции** `## N.` (последний заголовок `##` выше по файлу).
4. Нормализовать пути относительно корня репозитория; дедупликация; отсортировать.
5. **Проверка принадлежности расширению:** путь должен находиться под каталогом выбранного расширения `src/*/cfe/<название>/` **или** (если ЗНИ трогает базовую cf) — под соответствующим `cf/` из [openspec/project.md](../../../openspec/project.md). Собрать множество **`files_outside_extension`**: `.bsl` из `change_files`, которые **не** лежат под `src/*/cfe/<название>/`. Если `files_outside_extension` не пусто — **AskUserQuestion**: включить эти файлы в scope ревью (cf/cfe по ЗНИ) или ограничиться только модулями под `cfe/<название>/`. По ответу пользователя скорректировать `change_files`.

#### Подсчёт выполненных задач

**`completed_task_count`** — число строк `- [x]` в `tasks.md` (грубая метрика для отчёта). **`files_from_tasks_count`** — число уникальных `.bsl`, извлечённых из тел задач с `[x]` до шага fallback.

#### Fallback: git (если после шага выше `change_files` пуст)

1. Из `change-name` взять **slug** для grep: последний сегмент после даты, если имя вида `YYYY-MM-DD-<slug>` (например `2026-03-25-edo-ea-batch-signing-rs-elektronnye-podpisi` → кандидаты: полное имя и `edo-ea-batch-signing-rs-elektronnye-podpisi`); иначе использовать `<change-name>` целиком.
2. Выполнить: `git log --all --oneline --grep="<slug>"` (при необходимости повторить с укороченным slug).
3. Для каждого найденного коммита (сверху вниз, пока не наберутся `.bsl`): `git diff --name-only <commit>~1 <commit>` (или `git show --name-only --pretty="" <commit>`), отобрать строки, оканчивающиеся на `.bsl`, пересечь с деревом репозитория.
4. Отфильтровать по scope расширения: оставить только пути под `src/*/cfe/<название>/` **или** (по согласованию с п.5 выше) cf из project.md, если пользователь согласился на cf.
5. Если список не пуст — **обязательно** сообщить пользователю: «Пути из `tasks.md` для `[x]` не извлечены; использован git-fallback; файлы: … Продолжить?» При отказе — СТОП или переключение на `full-extension` по выбору пользователя.

#### Граничные случаи `change-scoped`

- В `tasks.md` **нет** ни одной строки `- [x]`: предупредить «Нет выполненных задач — нет файлов в scope для Tier 1». Предложить **`full-extension`** или СТОП.
- После tasks + git **`change_files` всё ещё пуст**: предупредить; предложить **`full-extension`** или СТОП.

### 1.3b Review Boundaries и diff (только `review_mode = change-scoped`)

После формирования **`change_files`** (шаг 1.3a), **до** батчевого ревью (шаг 2.1):

1. **Режим Tier 1:** ревьювер получает **`diff-focused`** по умолчанию: замечания functional / style / release-hygiene только в изменённых процедурах/функциях и `[module-level]` участках. **Tier 2** (шаг 2.5) — по-прежнему **всё** расширение (`extension_all_bsl`).
2. **Baseline и `git diff`:** применить логику [.cursor/skills/review/SKILL.md](../review/SKILL.md) **шаг 1.5.1** для набора файлов `change_files` (ветка ≠ основной → `merge-base` с `main` / `master` / `origin/main`; на основной ветке + ЗНИ → коммиты по slug из шага 1.3a fallback; при неудаче — fallback с `git log` и AskQuestion или **`Focus: full`** для затронутых файлов с предупреждением). При необходимости учесть незакоммиченные правки: объединить с `git diff HEAD -- <file>` (как в review шаг 1.5.1 п.1).
3. **Извлечение строк и маппинг на процедуры:** как review **шаги 1.5.3–1.5.4** (строки с префиксом `+`, границы `Процедура`/`Функция` … `КонецПроцедуры`/`КонецФункции`, иначе `[module-level]`).
4. **Новый файл** в diff → для этого файла в `## Review Boundaries` указать `Focus: full`.
5. **Пустой diff** по файлу при наличии в `change_files` → предупредить в сводке/отчёте; для файла выставить **`Focus: full`** или уточнить baseline у пользователя.
6. **Сборка `## Review Boundaries`:** как review **шаг 1.5.5**, отдельно для каждого **батча** (только файлы батча). Сохранить блоки для вставки в промпты шага 2.1.

**Артефакт:** для каждого пути из `change_files` — набор диапазонов строк «в фокусе» (процедуры + module-level) для **фильтрации** механик 1.7 и 1.7b.

### 1.3 Собрать список .bsl файлов

- **`review_mode = full-extension`:** Glob по директории расширения (`src/*/cfe/<название>/`). Перечислить все `.bsl` с путями. Этот список используется для Tier 1, механики 1.7–1.7c и Tier 2.
- **`review_mode = change-scoped`:** список для Tier 1 и механики 1.7–1.7c = **`change_files`** из шага 1.3a. **Дополнительно** (только для Tier 2, шаг 2.5): выполнить Glob по всему каталогу расширения и сохранить как **`extension_all_bsl`** — полный перечень `.bsl` расширения; не подменять им шаг 1.6 для классификации Tier 1.

### 1.4 Загрузить артефакты change

- **`review_mode = full-extension`:** если ревью в контексте активного change (по желанию пользователя или `openspec list`):

```bash
openspec instructions apply --change "<name>" --json
```

Прочитать `design.md`, `tasks.md` — для понимания контрактов и ожидаемого поведения.

- **`review_mode = change-scoped`:** шаг выполняется **всегда**. Прочитать из `change_dir`: `tasks.md`, `design.md`, при наличии — `proposal.md`. Опционально: `openspec instructions apply --change "<name>" --json`, если CLI знает change (для активных change); иначе достаточно чтения файлов из `change_dir`.

### 1.5 Сформировать контекст для ревьювера

Подготовить структурированный бриф. Использовать его в промптах батчей (шаг 2.1). Шаблон:

- **Режим scope:** `full-extension` или `change-scoped (<change-name>)`.
- При **`change-scoped`:** кратко: цель ЗНИ из `proposal.md`/`design.md`; **N** файлов в scope Tier 1 из **M** выполненных задач (`[x]`); перечень путей `change_files` (или ссылка «см. список в промпте»).
- **Расширение:** название, префикс, префикс журнала (если есть).
- **Файлы в scope (Tier 1):** по батчу — список .bsl с путями (из 1.6: группы A/B/C).
- **Директория расширения (полный путь):** `src/*/cfe/<название>/` — для Grep по вызовам процедур/функций при проверке **unused code** (категория 15). **Всегда** передавать явно: область поиска вызовов — **вся** директория расширения. При **`change-scoped`** с шагом 1.3b ревьювер в промпте должен выполнять Grep по имени **только для процедур/функций, перечисленных в `## Review Boundaries` для данного файла**, а не для всех процедур модуля (см. дополнение к шагу 2.1).
- **Review Boundaries (`change-scoped`):** в каждый промпт батча (шаг 2.1) вставить блок `## Review Boundaries` для файлов батча; протокол — `.cursor/agents/onec-code-reviewer.md` (Review Boundaries). **Полный** Tier 1 без границ — только если для всех файлов батча `Focus: full` (fallback 1.3b); тогда секцию можно не дублировать или указать пофайлово `Focus: full`.
- **Стандарты:** .cursor/docs/1c-coding-standards.md, 1c-vendor-standards/SKILL.md, конфигурация onec-code-reviewer.md.
- **Whitelist форматов комментариев (project.md):** из шага 1.1a — перечень имён и правил (префикс/regex) или «нет».
- **Обязательный контроль комментариев (project.md):** из шага 1.1a — ID, где проверять, пример допустимой строки, regex (если есть), уровень/kind; или «нет».
- **Режим ревью:** mode=prerelease; для каждого замечания указывать kind (functional / style / release-hygiene).
- **Naming quality:** AP-031 — доменный тест + тест уровня абстракции + контрастный тест (см. onec-code-reviewer.md Phase 0 Q6 и bsl-antipatterns AP-031). Применять ко всем идентификаторам в ревьюируемом scope.

### 1.6 Классификация модулей

Автоматически разбить **список .bsl для Tier 1** (при `full-extension` — все файлы расширения; при `change-scoped` — только `change_files`) на группы:

- **Группа A (глубокий ревью):** собственные модули расширения — путь содержит префикс расширения (например `рг` в имени папки/модуля: `ргСервисКриптографии*`, `ргЭкземпляры*` и т.д.). Содержат оригинальный код проекта.
- **Группа B (стандартный ревью):** перехваты типовых модулей — общие/объектные модули с аннотациями &Вместо/&Перед/&После/&ИзменениеИКонтроль, но не в группе A. Проверка: аннотации, корректность структуры #Вставка/#Удаление (директивы не удалять — это синтаксис переопределения), минимальные паттерны.
- **Группа C (лёгкий ревью):** модули форм — пути содержат `\Forms\` и `Form\Module.bsl` или `Form.xml`. Структура, обработчики, базовые паттерны.

Указать в контексте: количество файлов и суммарное число строк (если доступно) в каждой группе. Если группа A содержит >10 файлов — разбить её на 2 батча (A1 и A2 по 5–8 файлов), иначе ревьювер не осилит глубокий проход.

### 1.7 Механическая проверка: ссылки на артефакты разработки

До батчевого ревью — запустить Grep для обнаружения процессных ссылок в комментариях (rule 17, категория 9 reviewer). Совпадения автоматически включаются в отчёт как HIGH [release-hygiene] без ожидания от ревьювера.

**Scope файлов для `rg`:**

- **`full-extension`:** `<dir>` = каталог расширения `src/*/cfe/<название>/` с `--glob "*.bsl"` (как раньше).
- **`change-scoped`:** передать **конкретные пути** из `change_files` (без `--glob` по каталогу): `rg "<pattern>" путь1 путь2 ...` (в PowerShell пути в кавычках). Если один файл — один аргумент пути. Если список длинный — разбить на несколько вызовов `rg` по подмножествам путей.

```bash
# Kebab-case идентификаторы в комментариях (имена changes: fix-signing-result и т.п.)
rg "//.*[a-z]+-[a-z]+-[a-z]+" --glob "*.bsl" <dir>   # full-extension
# change-scoped: без --glob, в конце перечислить пути change_files

# Англоязычные процессные термины в комментариях
rg -i "//.*\b(design|decision|proposal|architecture|exploration)\b" --glob "*.bsl" <dir>

# Нумерованные решения / ссылки на задачи
rg "//.*Decision\s+\d+" --glob "*.bsl" <dir>
```

Допустимые исключения (не флагуются):
- Общепринятые технические англицизмы: HTTP, XML, API, XDTO, COM, base64, hex, JSON, UUID.
- Ссылки на внешние стандарты: RFC, ИТС, v8std.
- Директивы расширения: #Вставка, #КонецВставки, #Удаление, #КонецУдаления.
- TODO/FIXME (технический долг, не процессный маркер).
- **Строки комментариев, попадающие под whitelist** из шага 1.1a (префикс после `//` или совпадение regex на всю строку комментария).

Перед добавлением в отчёт: для каждого совпадения grep проверить whitelist из [openspec/project.md](../../../openspec/project.md) (секция whitelist, шаг 1.1a) — если комментарий подпадает под префикс/regex whitelist, **не включать** в сводку.

**Фильтр по границам ревью (только `change-scoped` после успешного шага 1.3b):** если для файла заданы диапазоны «в фокусе» (изменённые процедуры + `[module-level]`), **отбросить** совпадения `rg`, у которых номер строки **не** попадает ни в один такой диапазон. Если для файла применён fallback `Focus: full` — этот фильтр **не** применять.

При обнаружении (после фильтра whitelist и фильтра границ) — добавить в сводку (шаг 2.2) с `kind=release-hygiene`, уровень HIGH. Формат замечания: `файл:строка — [release-hygiene] Ссылка на артефакт разработки в комментарии. Рекомендация: переписать комментарий в доменных терминах.`

### 1.7b Механическая проверка: англоязычный жаргон в комментариях

До батчевого ревью — запустить Grep на **англоязычный технический жаргон** в однострочных комментариях `//` (не в строковых литералах `НСтр` и многострочных блоках — возможны ложные срабатывания; при сомнении сверить строку вручную). **Scope:** как в шаге 1.7 (`full-extension` — каталог + `--glob "*.bsl"`; `change-scoped` — список путей `change_files`).

```bash
# Жаргон и устойчивые англ. термины в комментариях (расширяемый набор)
rg -i "//.*\b(fail-fast|feature.toggle|workaround|fallback|downstream|re-raise|first.wins|last.wins|accumulate|per-action|pending|signal)\b" --glob "*.bsl" <dir>
```

Дополнительно (по смыслу проекта) можно прогнать термины из колонки «Запрещено» [.cursor/docs/tz-lexicon-dictionary.md](../../docs/tz-lexicon-dictionary.md), если они уместны для комментариев кода — только в комментариях `//`.

Исключения (не флагуются): те же, что в шаге 1.7 (HTTP, XML, API, XDTO, COM, base64, hex, JSON, UUID, RFC, ИТС, v8std, TODO/FIXME, директивы расширения) **плюс** явные ссылки на платформенные типы и методы в комментарии (`COMОбъект`, `HTTPСоединение` и т.п.) **плюс** **whitelist** из шага 1.1a (префикс или regex на всю строку комментария).

Перед добавлением в отчёт — фильтр whitelist, как в 1.7.

**Фильтр по границам ревью** — как в шаге 1.7 (только `change-scoped` + шаг 1.3b с диапазонами).

Совпадения после фильтра whitelist и фильтра границ — **HIGH [release-hygiene]** в сводку (шаг 2.2), `kind=release-hygiene`. Формат: `файл:строка — [release-hygiene] Англоязычный жаргон в комментарии. Рекомендация: переписать на русском (допустимы аббревиатуры HTTP, XML, API, XDTO, COM, JSON, UUID, MD5, RFC и ссылки на типы платформы).`

### 1.7c Механическая проверка: обязательные форматы комментариев (project.md)

Если в [openspec/project.md](../../../openspec/project.md) в таблице **«Обязательный контроль»** есть активные строки с непустым **Regex допустимой строки** — выполнить проверку. **Scope:** при `full-extension` — по каталогу расширения `<dir>`; при `change-scoped` — только по файлам из `change_files` (для шага 1: `rg "#Вставка" -n` с перечислением путей или фильтрация результатов grep по каталогу до путей из `change_files`). При **нескольких** правилах с разными условиями — выполнить проверку **отдельно для каждого ID** по колонке «Где проверять» (ниже — базовая механика для правил вида «первый `//` после `#Вставка`»).

1. `rg "#Вставка" --glob "*.bsl" -n <dir>` — список вхождений (**`change-scoped`:** те же пути, что в 1.7 — без glob по всему каталогу, только `change_files`).
2. Для каждого вхождения: открыть файл, идти **вниз** от строки с `#Вставка`, пропуская пустые строки; первую строку, начинающуюся с `//` (после возможных пробелов в начале строки), сравнить с **Regex допустимой строки** из соответствующего правила (для правила EX-001 и аналогов — «первый `//` после `#Вставка`»; если в `project.md` для другого ID указано иное «Где проверять» — следовать ему, даже если механика сложнее одного прохода — тогда зафиксировать остаток проверки ревьюеру).
3. Если первый значимый однострочный комментарий `//` после блока не совпадает с regex допустимой строки — добавить в сводку (шаг 2.2) замечание с **ID правила** из таблицы, **уровнем** и **kind** из таблицы, `файл:строка`, краткое описание несоответствия и ссылку на колонку «Требование / пример».

Если regex в таблице для правила **пуст** — механика 1.7c для этого правила не выполняется; проверка только у ревьюера (батчи 1b, 2).

Если таблица обязательного контроля пуста или все строки — заглушки — шаг 1.7c пропустить.

**Важно:** в `project.md` должны оставаться только правила, которые команда реально требует; иначе возможны массовые замечания.

---

## Шаг 2. Батчевый ревью параллельными субагентами

### 2.0 Входные данные

Использовать группы A/B/C из шага 1.6 и общий контекст из 1.5. Имя расширения, префикс, стандарты, режим mode=prerelease — едины для всех батчей. Набор файлов батчей = **Tier 1 scope** из шага 1.3 (при `change-scoped` — только `change_files`). При **`change-scoped`** подготовить из шага **1.3b** блок `## Review Boundaries` **на каждый батч** (подмножество файлов батча).

### 2.1 Параллельные вызовы onec-code-reviewer (до 4 одновременно)

> Перед вызовом Task — **Task Pre-call Checklist** из `.cursor/rules/tool-name-guard.mdc` (subagent_type из списка 1С-агентов; `model` не передавать).

Вызвать **Task** с `subagent_type: onec-code-reviewer` для каждого батча. Запускать батчи параллельно, где возможно. Если группа **A** пуста — батчи **1a** и **1b** не вызывать. Если группа **B** или **C** пуста — батчи **2** или **3** не вызывать.

**Префикс промпта при `review_mode = change-scoped`** (вставить **в начало** каждого промпта батчей 1a, 1b, 2, 3, перед основным текстом):

> «ЗНИ (change-scoped): `<change-name>`. Файлы в этом батче — доработки по этому ЗНИ. Цель и контракты — из `design.md` / `proposal.md` (кратко перескажи в ходе ревью из прочитанных артефактов). Проверяй код в контексте ожидаемого поведения из design. **Сразу после этого абзаца** оркестратор вставляет блок `## Review Boundaries` (шаг 1.3b) для файлов **этого** батча. Соблюдай Review Boundaries Protocol в `onec-code-reviewer.md`: читай каждый файл целиком для контекста; замечания (включая Phase 0, Phase 2.5, prerelease kind) — **только** внутри границ; не «полируй» неизменённый код. **Неиспользуемый код / пустые процедуры / пустые вставки:** Grep по имени процедуры выполняй по **всей** директории расширения, но **только для процедур/функций, перечисленных в Review Boundaries** для соответствующего файла (не перебирай все процедуры модуля). Исключения: обработчики событий, БСП-команды, callback — как в тексте батча ниже.»

**Батч 1a — Группа A, логика и безопасность:**
- Файлы: список модулей группы A (или A1, если A разбита).
- Промпт: «Провести **глубокий** предрелизный ревью собственных модулей расширения — фокус на логике и безопасности. Прочитай КАЖДЫЙ указанный файл целиком. Проверь: (1) логику и потоки данных, обработку ошибок, (2) Performance — запросы в циклах, индексы, (3) Security — инъекции, доступ, (4) **транзакционные паттерны**: НачатьТранзакцию без Попытки/Исключения, НачатьТранзакцию без ЗафиксироватьТранзакцию/ОтменитьТранзакцию, отсутствие ОтменитьТранзакцию в Исключении, пользовательское взаимодействие внутри транзакции, (5) **утечка ресурсов**: COM-объекты без Попытки/Исключения с освобождением, HTTP/FTP-соединения без обработки ошибок, файловые операции без закрытия в пути ошибки, (6) заглушки — функции, возвращающие пустые/невалидные значения, (7) Попытка/Исключение без логирования при проглатывании ошибки, (8) **неиспользуемый и устаревший код** (std-01 п.2.2, std-06 п.2, std-10 п.3.1): для каждой Процедуры/Функции в ревьюируемых файлах выполни Grep по имени во ВСЕЙ директории расширения (не только в файлах батча) — если вызовов нет, это неиспользуемый код. Исключения: обработчики событий, команды БСП (ExternalDataProcessorInfo), callback-процедуры (переданные строкой в ОписаниеОповещения). Невызываемая экспортная функция/процедура = HIGH [unused-code] kind=functional; невызываемая служебная = MEDIUM [unused-code] kind=style. Найди также процедуры/функции с комментарием «Устарела:» / «Deprecated» или расположенные в #Область УстаревшиеПроцедурыИФункции — MEDIUM [obsolete] kind=style. Если устаревшая процедура ещё вызывается из неустаревшего кода — HIGH [obsolete] kind=functional. (9) **пустые процедуры и функции**: найди все процедуры/функции с пустым телом (тело не содержит исполняемых операторов — только пустые строки, комментарии или сразу КонецПроцедуры/КонецФункции). Для каждой выполни Grep по имени во ВСЕЙ директории расширения. Если вызовов нет (исключения: обработчики событий, БСП-команды, callback по строке) → empty-unused, HIGH [release-hygiene], рекомендация: удалить. Если пустая, но вызывается → empty-body, MEDIUM [style], рекомендация: реализовать или удалить. (10) **Язык комментариев (предрелиз):** комментарии к процедурам, функциям и блокам — на русском; англоязычный технический жаргон (fail-fast, workaround, fallback, downstream, feature toggle, pending, per-action и т.п.) — HIGH [release-hygiene] kind=release-hygiene. Исключения: аббревиатуры HTTP, XML, API, XDTO, COM, JSON, UUID, MD5, RFC; ссылки на типы платформы; директивы расширения (#Вставка и т.п.). **Комментарии в whitelist** из [openspec/project.md](../../../openspec/project.md) (переданы в брифе шага 1.5) — не флаговать жаргоном/мусором и не предлагать удалять или переписывать только из-за шаблонной лексики, если строка укладывается в формат whitelist. (11) **Качество именования (AP-031):** применить доменный тест, тест уровня абстракции (убрать реализационное слово — остаётся ли доменный смысл?) и контрастный тест (доменные данные в безликом контейнере) ко всем идентификаторам (переменные, параметры, процедуры) в ревьюируемом scope. Имя, описывающее роль в коде без доменного квалификатора — MEDIUM [style]. Экспортные — HIGH [style]. В finding обязательно предложить доменный синоним. Для каждого замечания укажи файл:строка, уровень, kind, категорию (empty-unused / empty-body / и т.д.), описание, рекомендацию. РЕЖИМ: mode=prerelease.»

**Батч 1b — Группа A, структура и гигиена:**
- Файлы: те же модули группы A (или A1, если A разбита).
- Промпт: «Провести предрелизный ревью собственных модулей расширения — фокус на структуре, документации и релизной гигиене. Прочитай КАЖДЫЙ указанный файл целиком. Проверь: (1) БСП compliance, (2) аннотации расширений, (3) структуру модуля (#Область/#КонецОбласти — дублирование), (4) документацию методов, (5) Specific 1C Patterns (ТекущаяДата/ТекущаяДатаСеанса, Сообщить, Свойство, НСтр), (6) ЭтаФорма вместо ЭтотОбъект в оповещениях, (7) Экспорт в секции СлужебныеПроцедурыИФункции, (8) имя метода противоречит директиве компиляции, (9) опечатки и смешение кириллицы/латиницы в пользовательских строках, (10) **релизная чистота комментариев**: маркеры в комментариях (автор, дата, заявка, строки // НАЧАЛО/КОНЕЦ, +++/---), временные инструкции, закомментированный старый код — такие замечания = HIGH [release-hygiene]. Директивы #Вставка/#Удаление не удалять и не помечать как мусор — это синтаксис переопределения расширения. (11) **пустые процедуры и функции**: те же правила, что в батче 1a — пустое тело (нет исполняемого кода), Grep по имени в расширении; empty-unused = HIGH [release-hygiene], empty-body = MEDIUM [style]. (12) **Язык комментариев (предрелиз):** как в батче 1a, п. (10) — русский язык; жаргон — HIGH [release-hygiene]; аббревиатуры и типы платформы — исключения; директивы #Вставка/#Удаление не трогать; **whitelist из project.md (бриф)** — не считать мусором и не требовать устранения только из-за шаблонной лексики. (13) **Обязательный контроль (project.md):** по списку из брифа проверить соблюдение форматов там, где указано «Где проверять»; несоответствие — уровень/kind из таблицы project.md (дополняет механику 1.7c). Если механика 1.7c уже нашла нарушение по ID — не дублировать. (14) **Качество именования (AP-031):** как в батче 1a, п. (11) — доменный тест, тест уровня абстракции, контрастный тест; MEDIUM [style] (экспортные — HIGH [style]); в finding предложить доменный синоним. Для каждого замечания укажи файл:строка, уровень, kind, категорию (empty-unused / empty-body), описание, рекомендацию. РЕЖИМ: mode=prerelease.»

**Батч 2 — Группа B (стандартный ревью):**
- Файлы: список модулей группы B.
- Промпт: «Провести предрелизный ревью перехватов типовых модулей. Фокус: аннотации &Вместо/&Перед/&После/&ИзменениеИКонтроль, корректность структуры #Вставка/#Удаление (директивы не удалять), префикс перехватов, Module Structure, Method Documentation, Code Cleanliness, Specific 1C Patterns. Отдельно проверь релизную чистоту комментариев (маркеры в комментариях, временные инструкции, закомментированный старый код) — HIGH [release-hygiene]. Директивы #Вставка/#Удаление не удалять. Проверь: дублирование #Область/#КонецОбласти, опечатки/кодировка в строках, заглушки (проверяется всегда, не только в prerelease), ЭтаФорма вместо ЭтотОбъект, Попытка/Исключение без логирования, Экспорт в СлужебныеПроцедурыИФункции, имя метода vs директива, **транзакционные паттерны**: НачатьТранзакцию без Попытки, без ЗафиксироватьТранзакцию/ОтменитьТранзакцию, пользовательское взаимодействие внутри транзакции. Дополнительно проверь **неиспользуемые вставки**: для каждой служебной процедуры/функции внутри блоков #Вставка выполни Grep по имени во ВСЕЙ директории расширения — если вызовов нет, это неиспользуемый код: MEDIUM [unused-code] kind=style. Найди также блоки #Вставка, которые полностью закомментированы или содержат только заглушки без реального кода — MEDIUM [unused-code] kind=style. Проверь **пустые процедуры и функции**: тело без исполняемых операторов (только пустые строки, комментарии или сразу КонецПроцедуры/КонецФункции); для каждой Grep по имени во ВСЕЙ директории расширения — если вызовов нет → empty-unused, HIGH [release-hygiene], рекомендация удалить; если пустая, но вызывается → empty-body, MEDIUM [style]. **Язык комментариев (предрелиз):** как в батче 1a, п. (10) — русский; жаргон в комментариях — HIGH [release-hygiene]; whitelist из project.md (бриф) — не помечать как мусор и не требовать удаления, если комментарий в формате whitelist. **Обязательный контроль (project.md):** как в батче 1b, п. (13) — проверить все ID из брифа по колонке «Где проверять»; не дублировать замечания 1.7c. **Качество именования (AP-031):** применить доменный тест, тест уровня абстракции и контрастный тест к идентификаторам в блоках #Вставка; MEDIUM [style] (экспортные — HIGH [style]); в finding предложить доменный синоним. Для каждого замечания: файл:строка, уровень, kind (functional/style/release-hygiene), категория (в т.ч. empty-unused / empty-body), описание, рекомендация. РЕЖИМ: mode=prerelease.»

**Батч 3 — Группа C (лёгкий ревью):**
- Файлы: список модулей группы C (модули форм).
- Промпт: «Провести предрелизный ревью модулей форм расширения. Фокус: структура, обработчики событий, аннотации перехватов в формах, базовые паттерны (ТекущаяДата, Сообщить, НСтр). Отдельно проверь релизную чистоту комментариев (маркеры в комментариях; не трогать #Вставка/#Удаление) — HIGH [release-hygiene]. Проверь: дублирование #Область/#КонецОбласти, опечатки/кодировка в пользовательских строках, ЭтаФорма вместо ЭтотОбъект, имя процедуры vs директива (&НаСервере/НаКлиенте). Дополнительно проверь **обработчики без привязки**: обработчики событий формы (процедуры в модуле формы), которые не привязаны ни к одному элементу формы и не вызываются из кода — признак остатков после удаления элемента; отметь как MEDIUM [unused-code] kind=style с рекомендацией проверить привязку или удалить. Проверь **пустые обработчики событий**: процедура с &НаКлиенте/&НаСервере и пустым телом (нет исполняемого кода). Если обработчик не привязан к элементу формы или привязан, но не используется по смыслу → empty-handler, HIGH [release-hygiene]. Рекомендация: удалить процедуру из Module.bsl; для привязки в форме — указать необходимость удаления обработчика события у элемента (инструкция ручного конфигурирования Form.xml). **Язык комментариев (предрелиз):** как в батче 1a, п. (10); whitelist из project.md (бриф) — не флаговать и не предлагать убирать как артефакт разработки, если строка в whitelist. **Обязательный контроль (project.md):** при наличии правил в брифе — проверить соблюдение в модулях форм, если «Где проверять» это охватывает; иначе отметить «вне scope батча 3» и не дублировать 1.7c. **Качество именования (AP-031):** применить доменный тест, тест уровня абстракции (убрать реализационное слово — остаётся ли доменный смысл?) и контрастный тест (доменные данные в безликом контейнере) ко всем идентификаторам (переменные, параметры, процедуры) в ревьюируемом scope. Имя, описывающее роль в коде без доменного квалификатора — MEDIUM [style]. Экспортные — HIGH [style]. В finding обязательно предложить доменный синоним. Для каждого замечания: файл:строка, уровень, kind, категорию (empty-handler / empty-unused / empty-body и т.д.), описание, рекомендацию. РЕЖИМ: mode=prerelease.»

Если группа A разбита на A1 и A2 — выполнить два отдельных вызова для A1 и A2 с тем же «глубоким» промптом (батчи 1a и 1b для каждого подмножества); батчи B и C можно запускать параллельно с A.

### 2.5 Архитектурный анализ (Tier 2, параллельно с шагом 2.1)

> Перед вызовом Task — **Task Pre-call Checklist** из `.cursor/rules/tool-name-guard.mdc` (subagent_type из списка 1С-агентов; `model` не передавать).

Запустить **Task** с `subagent_type: onec-code-explorer` **параллельно** с батчами reviewer (шаг 2.1). Explorer читает ВСЕ .bsl файлы расширения разом (не батчами) и выполняет кросс-модульные проверки, недоступные пофайловому reviewer.

**Входные данные:** **всегда** полный список `.bsl` каталога расширения: при `full-extension` — из Glob шага 1.3; при `change-scoped` — список **`extension_all_bsl`** из шага 1.3 (не подменять списком `change_files`). Контекст из шага 1.5 (имя расширения, префикс).

**Промпт explorer:**

«Провести архитектурный анализ расширения [имя] для предрелизного ревью. Прочитай ВСЕ указанные .bsl файлы. Выполни 5 кросс-модульных проверок:

1. **Error strategy consistency:** Найди все блоки Попытка/Исключение во всех модулях. Для каждого классифицируй стратегию: (a) лог + ВызватьИсключение, (b) лог + Возврат fallback, (c) лог + Продолжить цикл, (d) без лога (silent). Сгруппируй по типу ошибки (получение объекта, запись, внешний вызов и т.д.). Если один тип ошибки обрабатывается 2+ разными стратегиями в разных модулях — флаг: inconsistent error strategy. Уровень: MEDIUM.

2. **Implicit contracts (ДополнительныеСвойства):** Найди все обращения к ДополнительныеСвойства: `.Вставить("ключ", ...)` и `.Свойство("ключ", ...)`. Построй матрицу: свойство → модуль-writer → модуль-reader. Если writer-модуль ≠ reader-модуль — это скрытый межмодульный контракт. Если >3 таких свойств без единого документированного реестра (комментария в шапке модуля или спецификации) — флаг. Уровень: MEDIUM.

3. **Repeated type-dispatch:** Найди все блоки `Если ТипЗнч(X) = Тип("...")`. Сгруппируй по набору проверяемых типов (например, ДеревоЗначений / ДанныеФормыДерево / ДанныеФормыКоллекцияЭлементовДерева). Если один и тот же набор типов проверяется в 3+ процедурах — флаг: кандидат на утилитную функцию. Уровень: MEDIUM.

4. **Semantic duplication:** Для каждой процедуры/функции извлеки: (a) входные сущности (типы параметров, обращения к реквизитам), (b) ключевые операции (вызовы БСП-методов, записи объектов, обращения к регистрам), (c) выходные действия (возврат, установка свойств). Найди пары процедур в разных модулях с >70% совпадением по этим признакам — кандидаты на рефакторинг в общую процедуру. Уровень: MEDIUM.

5. **Scope creep в обработчиках расширений:** Для каждого перехвата (&Перед/&После/&Вместо) определи, какую функциональность расширения он реализует (по именам вызываемых процедур с префиксом расширения). Если обработчик содержит вызовы, не связанные ни с одной функциональностью расширения (другой префикс, другой домен) — флаг: scope creep. Уровень: MEDIUM.

**Если сессия change-scoped (ЗНИ указан):** расширение анализируется **целиком**; доработки по ЗНИ затронули файлы: [перечислить `change_files`]. Замечания, относящиеся **только** к этим файлам и напрямую к доработке ЗНИ, помечай в описании тегом **`[change-scope]`** в начале строки описания (для приоритизации устранения).

Для каждого замечания укажи: файлы (все затронутые), уровень, kind=architecture, категорию (error-strategy / implicit-contract / type-dispatch / semantic-duplication / scope-creep), описание, рекомендацию. РЕЖИМ: mode=prerelease.»

(При `full-extension` абзац про `[change-scope]` из промпта **опустить**.)

### 2.2 Сведение отчётов

- Объединить выводы всех батчей Tier 1 (1a, 1b, 2, 3), **механические находки шагов 1.7, 1.7b, 1.7c** и **результат архитектурного анализа Tier 2 (шаг 2.5)** в один список замечаний.
- Дедуплицировать по ключу «файл:строка:категория» (одно и то же место — одно замечание). Батчи 1a и 1b пересекаются по файлам — дедупликация особенно важна.
- Сохранить атрибут **kind** (functional / style / release-hygiene / architecture) у каждого замечания для шага 3 и 4.
- Сохранять категории **empty-unused**, **empty-body**, **empty-handler** у замечаний — они используются в секции отчёта «Пустые процедуры и функции» и при формировании задач (шаг 4).
- Замечания из Tier 2 маркировать `kind=architecture`.

---

## Шаг 3. Формирование предрелизного отчёта

На основе сводного списка замечаний (шаг 2.2) составить итоговый отчёт. Указывать **kind** (functional / style / release-hygiene / architecture) у каждого замечания; в сводке — разбивка по kind (**functional**, **release-hygiene** и **style** обязательны до релиза — код передаётся заказчику). Замечания **empty-unused** и **empty-handler** учитывать в блоке «Обязательны до релиза (HIGH release-hygiene)».

```markdown
## Предрелизное ревью: [название расширения]

**Дата:** [дата]
**Scope:** [full-extension: «N файлов (A: n, B: n, C: n), M процедур/функций» | change-scoped: «change-scoped (<change-name>), Tier 1: K файлов из V задач [x]; Review Boundaries (diff-focused по процедурам, шаг 1.3b); A/B/C по этим файлам; Tier 2: всё расширение»]
**Режим:** предрелизный (Tier 1: code review + Tier 2: architecture review; эскалация: LOW→MEDIUM, MEDIUM→HIGH для kind=functional, release-hygiene и style — код заказчику)
**Стандарты:** .cursor/docs/1c-coding-standards.md (rules 1-22), вендорские (1c-vendor-standards, .cursor/docs/standard/ при необходимости), [спецификация расширения]

### Файлы в scope

_При `full-extension` эту секцию не включать._ При `change-scoped`:

- Перечень путей `change_files` (Tier 1 и механика 1.7–1.7c).
- Примечание: Tier 2 выполнялся по полному каталогу расширения (`extension_all_bsl`).

### Сводка

| Уровень  | functional | release-hygiene | architecture | style |
|----------|------------|----------------|--------------|-------|
| CRITICAL | N          | —              | —            | —     |
| HIGH     | N          | N              | N            | N     |
| MEDIUM   | N          | N              | N            | N     |
| LOW      | N          | N              | N            | N     |

(Эскалация: LOW→MEDIUM, MEDIUM→HIGH для kind=functional, release-hygiene и style — код предназначен заказчику. kind=style помечается тегом [style] и эскалируется так же, как functional/release-hygiene. kind=architecture — кросс-модульные замечания Tier 2; уровни по отчёту explorer, в эскалацию style/functional не включаются.)

### CRITICAL — блокеры релиза

- **`файл.bsl:строка`** — [Категория]  
  Проблема: описание.  
  Рекомендация: что сделать.

### HIGH — исправить до релиза

- **functional:** ...
- **release-hygiene:** ...
- **[style]:** ...

### MEDIUM — желательно исправить

- ...

### LOW — технический долг

- ...

### Неиспользуемый и устаревший код

| # | Тип | Файл:строка | Имя | Уровень | kind | Рекомендация |
|---|-----|-------------|-----|---------|------|--------------|
| 1 | unused-export | путь:123 | ИмяПроцедуры | HIGH | functional | Удалить или обосновать сохранение |
| 2 | unused-private | путь:456 | ИмяФункции | MEDIUM | style | Удалить |
| 3 | obsolete | путь:789 | УстаревшаяФункция | MEDIUM | style | Задокументировать замену (std-10) или удалить |
| 4 | obsolete-caller | путь:101 | ВызывающаяПроцедура | HIGH | functional | Мигрировать на новый API |

**Итого:** N неиспользуемых, M устаревших. Требуют действий до релиза: K.

### Пустые процедуры и функции

| # | Тип | Файл:строка | Имя | Уровень | Рекомендация |
|---|-----|-------------|-----|---------|--------------|
| 1 | empty-unused | путь:123 | ИмяПроцедуры | HIGH | Удалить процедуру/функцию |
| 2 | empty-handler | путь:456 | ОбработчикНажатия | HIGH | Удалить процедуру из Module.bsl; удалить привязку обработчика в форме (инструкция — в задачах change) |
| 3 | empty-body | путь:789 | Заглушка | MEDIUM | Реализовать или удалить |

Для empty-handler в рекомендации явно указывать: «Удалить процедуру из Module.bsl; удалить привязку обработчика в форме (инструкция — в задачах change)».

**Итого пустых:** N. Требуют удаления до релиза (empty-unused, empty-handler): K.

### Архитектурные замечания (Tier 2)

Кросс-модульные проблемы, обнаруженные архитектурным анализом (шаг 2.5). Не дублируются с Tier 1.

| # | Категория | Затронутые файлы | Уровень | Описание | Рекомендация |
|---|-----------|-----------------|---------|----------|--------------|
| 1 | error-strategy | модуль1, модуль2 | MEDIUM | Описание несогласованности | Унифицировать стратегию |
| 2 | implicit-contract | модуль1 → модуль2 | MEDIUM | Свойство X пишется в A, читается в B | Документировать контракт |
| 3 | type-dispatch | модуль1, модуль2, модуль3 | MEDIUM | Один набор ТипЗнч-проверок в 3+ местах | Извлечь утилитную функцию |
| 4 | semantic-duplication | модуль1, модуль2 | MEDIUM | Алгоритм X дублирован | Вынести в общую процедуру |
| 5 | scope-creep | модуль1 | MEDIUM | Логика Y не связана с расширением | Вынести в отдельное расширение или удалить |

**Итого архитектурных:** N замечаний. Рекомендуемы к устранению до релиза: K.

### Итог

[Готов к релизу / Не готов к релизу]

Блокеры (CRITICAL): [список или «нет»]
Обязательны до релиза (HIGH functional): [список или «нет»]
Обязательны до релиза (HIGH release-hygiene): [список или «нет»]
Архитектурные (MEDIUM architecture): [список или «рекомендательно»]
Обязательны до релиза (HIGH style): [список или «нет»]
```

---

## Шаг 4. Предложение openspec change / дополнение ЗНИ

Если обнаружены замечания уровня **CRITICAL**, **HIGH** или **MEDIUM** (включая kind=style, release-hygiene, architecture Tier 2):

**Маршрутизация:** если **`review_mode = change-scoped`** и **`change_in_archive = false`** — выполнить **только** раздел **4A**. Во всех остальных случаях (`full-extension` или change-scoped с ЗНИ в `archive/`) — раздел **4B** (создание `prerelease-fix-*` при согласии пользователя).

### 4A. `review_mode = change-scoped` и change **не** в `archive/`

**Не** предлагать создание нового `prerelease-fix-*` по умолчанию — ЗНИ уже существует.

Использовать **AskUserQuestion tool**:

> «Обнаружено N замечаний уровня CRITICAL/HIGH/MEDIUM. Добавить задачи устранения в `tasks.md` существующего change `<change-name>`? (Соблюдать [.cursor/rules/vertical-slices.mdc](../../rules/vertical-slices.mdc): если ЗНИ в slice mode — каждая группа замечаний оформляется как fix-срез `S<N+1>: Исправление prerelease-замечаний (<категория>)` либо вставляется в подходящий непринятый срез; если legacy — в подходящую секцию.)»

**Варианты:**
- Да, дополнить tasks.md
- Нет, только отчёт

**Если «Да»:** отредактировать `change_dir/tasks.md` по правилам группировки из блока **«3. Сформировать tasks.md по правилам»** (ниже), без создания нового каталога change. Сохранить отчёт в `change_dir/reports/prerelease-review-ГГГГ-ММ-ДД.md` (если каталога `reports/` нет — создать).

**Если «Нет, только отчёт»:** сохранить отчёт в `change_dir/reports/prerelease-review-ГГГГ-ММ-ДД.md` или в `temp/reports/prerelease-review-<расширение>-<change-slug>-ГГГГ-ММ-ДД.md` по выбору пользователя.

### 4B. `review_mode = full-extension` **или** `change-scoped` с **`change_in_archive = true`**

Использовать **AskUserQuestion tool**:

> «Обнаружено N замечаний уровня CRITICAL/HIGH/MEDIUM (в т.ч. стиль и гигиена комментариев). Создать openspec change `prerelease-fix-<расширение-kebab>` для их устранения? MEDIUM войдут в tasks.md и proposal кратким списком.»

(Для **архивного** ЗНИ причина: change уже заархивирован — новые задачи вносятся в отдельный change.)

**Варианты:**
- Да, создать change
- Нет, только отчёт

### Если пользователь выбрал «Да» (новый prerelease-fix change)

1. Создать новый change (имя в kebab-case, например `prerelease-fix-rg-kontur-cryptoapi`):

```bash
openspec new change "prerelease-fix-<расширение-kebab>" --schema prerelease-fix
```

Если схема `prerelease-fix` недоступна — использовать `--schema spec-driven` и создать обязательный артефакт specs по инструкции схемы.

2. Сформировать proposal (описание задачи) на основе отчёта:
   - Суть: устранение замечаний предрелизного ревью расширения `<название>`.
   - Список замечаний **CRITICAL** и **HIGH** (с указанием kind: functional / style / release-hygiene / architecture).
   - **Краткий список MEDIUM** (по категориям или перечислением) — обязателен, если такие замечания есть.
   - Ссылка на отчёт: `reports/prerelease-review-ГГГГ-ММ-ДД.md`.

3. Сформировать tasks.md по правилам (те же правила при **дополнении** существующего change в п. 4A):
   - **CRITICAL** = отдельная задача на каждое замечание (блокер).
   - **HIGH functional** = отдельная задача на каждое замечание.
   - **HIGH style** и **HIGH release-hygiene** = отдельная задача на каждое замечание (как functional) — стиль и гигиена до релиза обязательны для кода заказчику.
   - **HIGH style / HIGH release-hygiene / MEDIUM (массовые):** если замечание одного типа затрагивает >3 файлов — одна задача с подзадачами по файлам (чекбоксы-подпункты). Пример:
     ```markdown
     - [ ] **2.2** [HIGH/style] Разметка #Область
       - [ ] CommonModules\РаботаСЭП\Ext\Module.bsl
       - [ ] CommonModules\РаботаСКартинками\Ext\Module.bsl
       - [ ] ...
     ```
   - **Все остальные MEDIUM** (включая kind=architecture из Tier 2) — каждая находка в **tasks.md** (отдельная задача или группировка по категории, 1 задача на категорию при множестве однотипных).
   - **LOW** — одна групповая задача «Технический долг».
   - **Неиспользуемый и устаревший код (группировка):**
     - `unused-code` (>3 процедур/функций): одна задача «Удалить неиспользуемый код» с подзадачами по файлам.
     - `obsolete` (>3 процедур/функций): одна задача «Вычистить устаревший код» с подзадачами.
     - Единичные unused/obsolete — отдельными задачами по обычным правилам уровня HIGH/MEDIUM.
   - **Пустые процедуры и функции (группировка):**
     - `empty-unused` (>3 штук): одна задача «Удалить пустые неиспользуемые процедуры и функции» с подзадачами по файлам (чекбоксы по каждому файлу или по каждой процедуре).
     - `empty-handler`: для каждого — отдельная задача «Удалить пустую процедуру <Имя> из <путь к Module.bsl>». Если у обработчика есть привязка в форме — добавить к задаче или в отдельный артефакт **инструкцию ручного конфигурирования** по шаблону из `.cursor/rules/1c-xml-write-guard.mdc`: **Форма:** <полное имя формы>, **Конфигурация/расширение:** <имя>. **Действия в Конфигураторе:** 1) Открыть форму; 2) Найти элемент/событие, к которому привязан обработчик <Имя>; 3) Удалить привязку обработчика события. **После выгрузки:** в Form.xml исчезнет соответствующий атрибут события у элемента.
     - Единичные empty-unused / empty-body — отдельными задачами по уровню HIGH/MEDIUM.
   - Удаление кода в .bsl выполняется при `/opsx:apply` через onec-code-writer; Form.xml не править — только инструкция пользователю.

   Формат задачи (одиночное замечание):
   ```markdown
   - [ ] [CRITICAL] Файл:строка — Категория: Описание. Рекомендация.
   - [ ] [HIGH] [functional] Файл:строка — ...
   - [ ] [HIGH] [style] Файл:строка — ...
   - [ ] [HIGH] [release-hygiene] Файл:строка — ...
   - [ ] [MEDIUM] [architecture] Описание (Tier 2) — ...
   ```

4. Сохранить отчёт ревью в `openspec/changes/prerelease-fix-<расширение>/reports/prerelease-review-ГГГГ-ММ-ДД.md`.

5. Сообщить пользователю:
   - Название созданного change.
   - Количество задач в tasks.md.
   - Следующий шаг: `/opsx:apply prerelease-fix-<расширение>`.

---

## Граничные случаи

- **Замечаний нет**: сообщить «Замечаний, блокирующих релиз, не найдено. Расширение готово к релизу.»
- **Только LOW**: предложить создать задачу технического долга или пропустить.
- **Нет спецификации расширения**: выполнить ревью с общими правилами; в отчёте отметить, что проверка соответствия спецификации не выполнена.
- **Нет активного change** (`full-extension`): ревью выполняется в standalone-режиме; отчёт сохранить в `temp/reports/prerelease-review-<расширение>-ГГГГ-ММ-ДД.md`.
- **Change-scoped: каталог change не найден** после поиска в `openspec/changes/` и `openspec/changes/archive/`: СТОП; вывести список доступных имён (по подкаталогам с `tasks.md`).
- **Change-scoped: в `tasks.md` нет `[x]`**: предупредить; предложить **`full-extension`** или СТОП (см. шаг 1.3a).
- **Change-scoped: после tasks + git список файлов пуст**: предупредить; предложить **`full-extension`** или СТОП.
- **Change-scoped: пути из задач не под `cfe/<расширение>/`**: шаг 1.3a — AskUserQuestion об включении cf/cfe в scope; зафиксировать решение в отчёте.
- **Замечаний нет при `change-scoped`**: шаг 4 не применять; отчёт сохранить в `change_dir/reports/` или `temp/reports/` по согласованию с пользователем.

---

## Интеграция

- **Режимы:** `full-extension` (по умолчанию, обратная совместимость) и **`change-scoped`** (второй аргумент — имя каталога ЗНИ): Tier 1 и механика 1.7–1.7c по `change_files` + **Review Boundaries** (шаг 1.3b, diff-focused по изменённым процедурам); фильтр строк в 1.7/1.7b по границам; Tier 2 по полному каталогу расширения (`extension_all_bsl`); шаг 4A/4B — см. выше.
- **Tier 1 (Code Review):** `onec-code-reviewer` v1.6 с поддержкой `mode=prerelease`, категориями 1-15 и новыми проверками (parameter mutation, magic constants, mixed responsibilities, prefix consistency, excessive logging).
- **Tier 2 (Architecture Review):** `onec-code-explorer` — кросс-модульный анализ (error strategy, implicit contracts, type-dispatch, semantic duplication, scope creep). Запускается параллельно с Tier 1. Только в prerelease.
- Стандарты кода: `.cursor/docs/1c-coding-standards.md` v1.4 (rules 1-22, включая rule 21 Parameter Integrity и rule 22 No Duplicated Literals).
- Стандарты вендора и навигация: `AGENTS.md` → секция «Стандарты вендора 1С»; `.cursor/docs/standard/` (1c-standards-navigator.md, std-*.md) и `.cursor/skills/1c-vendor-standards/SKILL.md` — единый набор для предрелизной проверки.
- Если после ревью обнаружены архитектурные замечания (новые объекты, изменение API, структуры хранения) — не создавать задачи writer, остановиться и показать пользователю.
- Отчёт сохраняется согласно `preserve-subagent-reports.mdc`.
- **Соглашения по комментариям BSL:** [openspec/project.md](../../../openspec/project.md) — whitelist (шаг 1.1a; механика 1.7 / 1.7b не флагует совпадения whitelist) и **обязательный контроль** (механика 1.7c + ревьюер в батчах 1b/2). Памятка по колонкам: `.cursor/docs/bsl-comment-formats-project.md`.
