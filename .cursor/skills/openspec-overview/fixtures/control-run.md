# Control run: overview #88781

Вход для изолированного прогона `/opsx:overview` (сверка с `golden-88781-overview.md`).

## Changes

1. `nastroyka-knopok-processov` — active: `openspec/changes/nastroyka-knopok-processov`
2. `podpisanie-cherez-soglasovanie` — active: `openspec/changes/podpisanie-cherez-soglasovanie`

## TZ

`ТЗ/Направление по Процессу подписания на основе согласования [ID#88781]/Направление по Процессу подписания на основе согласования [ID#88781].md`

## Ось этапов (обязательна; не имена ЗНИ)

1. **Этап 1 — Система настроек** — регистр сведений правил, вкладка в доп. функциях, выключатель; кнопки на форме задачи ещё не появляются.
2. **Этап 2 — Кнопки и применение настроек** — показ кнопок по правилам, старт процессов, что вне первой поставки.

Запрещено: заголовки этапов = `nastroyka-knopok-processov` / `podpisanie-cherez-soglasovanie` / «ЗНИ 1» / «ЗНИ 2».

## Сверка после компиляции

### Ситуация ≠ механизм

В заголовках строк `## Этап` **не** должно быть:

- автомат, алгоритм, серверн*, ядро, триггер, перехват, «вызов при», актуализац*

Ориентир: `fixtures/voice-good-stage.md`.  
Чеклист: `templates/style-checklist.md` § Ситуация ≠ механизм.

### Laconic / anti-tutor — fail при наличии в теле этапов

- мета: «по сути то, о чём…», «так достигается цель исходного ТЗ»
- педагогика: «Итого этап», «не одна „магическая“…», CRUD «добавляют, правят, сохраняют»
- уговоры: «привычное место», «ломать не нужно»
- совет стенда: «логична группа вроде…», «как у синей кнопки»

Чеклист: `templates/style-checklist.md` § Laconic / anti-tutor.

## Audience

фа (функциональный архитектор)

## Output path

`openspec/changes/nastroyka-knopok-processov/overview-control-88781.md`

## Brief override (как будто пользователь подтвердил)

Тема: направление по процессу подписания на основе согласования (ЭДО 2.1).  
Ось этапов: как выше.  
Подтвердить: да (для control — без паузы END TURN).
