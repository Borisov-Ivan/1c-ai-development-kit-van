---
description: Стандарты кода 1С/BSL — обязательны при любой работе с кодом 1С
globs: "**/*.bsl"
---

# 1C Coding Standards

Стандарты написания кода на платформе 1С:Предприятие (BSL).

**Приоритет**: 150 (высокий)  
**Применяется**: Всегда при работе с кодом 1С

Глубокие стандарты вендора: `.cursor/docs/standard/1c-standards-navigator.md` (индекс доменных файлов `std-NN-<domain>.md`). Для architect и reviewer: чеклисты — `.cursor/skills/1c-vendor-standards/SKILL.md`; детали — Read соответствующий доменный файл из `.cursor/docs/standard/`. Платформенная документация: `.cursor/docs/platform/Оглавление-1С-документации.md`.

---

## ОБЩИЕ ПРИНЦИПЫ

### Комментарии

```yaml
Правило: Избегай комментариев, которые повторяют очевидное

Комментарии допускаются только когда добавляют ценность:
  - Мотивация решения
  - Нетривиальный алгоритм
  - Ограничения и граничные случаи
  - Контракты (параметры, возвращаемое значение)
  - Технический долг (TODO, FIXME)

Плохо:
  // Получаем клиента
  Клиент = ПолучитьКлиента();

Хорошо:
  // Используем кеш, т.к. данные клиента не меняются в рамках сессии
  Клиент = ПолучитьКлиентаИзКеша();
```

```yaml
Принцип релизной чистоты комментариев:
  Комментарии описывают код (контракт, намерение, неочевидное решение).
  Комментарии НЕ фиксируют процесс (кто, когда, в какой заявке менял, по какому решению).
  История и решения ведутся в Git и артефактах проекта, не в комментариях.

  В релизном коде не допускаются:
    - Маркеры изменений в комментариях (автор, дата, заявка, строки вида // НАЧАЛО/КОНЕЦ Изменения, +++/---)
    - Временные инструкции ("добавить в xml...", "перенести после...")
    - Закомментированный старый код с пометками "оригинальный/новый код"
    - Избыточные пустые разделители // (допустимы только внутри JSDoc-блоков)
    - Ссылки на процесс и артефакты разработки: внутренние документы
      (design, decision, proposal, architecture, exploration, ADR, ЗНИ),
      имена изменений (fix-signing-result, add-feature-X),
      номера задач из планов (п. 3.1, задача 2.2).
      Плохо:  // По design Decision 6 (fix-signing-result): при записи ...
      Хорошо: // При записи результата «Подписано» с пустыми участниками ...
  Не являются маркерами и не удаляются: директивы переопределения расширения #Вставка, #КонецВставки, #Удаление, #КонецУдаления.
```

### Структура модулей

```yaml
Порядок областей в общем модуле:

1. #Область ПрограммныйИнтерфейс
   - Экспортные процедуры и функции
   - Доступны всем
   - Документированы (JSDoc-style)

2. #Область СлужебныйПрограммныйИнтерфейс
   - Экспортные процедуры и функции
   - Доступны только внутри подсистемы
   - Документированы

3. #Область СлужебныеПроцедурыИФункции
   - Неэкспортные процедуры и функции
   - Доступны только внутри модуля
   - Краткие комментарии (опционально)

Пример:
  #Область ПрограммныйИнтерфейс
  
  // Получает данные клиента
  //
  // Параметры:
  //   Клиент - СправочникСсылка.Клиенты - ссылка на клиента
  //
  // Возвращаемое значение:
  //   Структура - данные клиента с полями:
  //     * Наименование - Строка
  //     * ИНН - Строка
  //
  Функция ПолучитьДанныеКлиента(Клиент) Экспорт
      // ...
  КонецФункции
  
  #КонецОбласти
  
  #Область СлужебныеПроцедурыИФункции
  
  Функция ПолучитьДанныеКлиентаВнутренний(Клиент)
      // ...
  КонецФункции
  
  #КонецОбласти
```

### Переиспользование кода

```yaml
Правило: Перед написанием кода проверяй существующие методы

Где искать:
  1. Общие модули (ОбщегоНазначения, ОбщегоНазначенияКлиент)
  2. Модули менеджеров объектов
  3. БСП (Библиотека стандартных подсистем)
  4. MCP серверы (метаданные, код, справка)

Инструменты:
  - user-PROJECT-codemetadata (project-specific MCP)-codesearch: Поиск по коду
  - user-PROJECT-graph (project-specific MCP)-search_metadata: Поиск по метаданным
  - user-1c-help-docsearch: Справка по платформе
  - user-1c-ssl-ssl_search: Поиск в БСП
```

---

## ИМЕНОВАНИЕ

### Переменные

```yaml
Правило: Не используй венгерскую нотацию

Плохо:
  МассивКонтрагентов
  СтрокаИНН
  ЧислоКоличество

Хорошо:
  Контрагенты
  ИНН
  Количество

Исключение: Типы коллекций допустимы, если улучшают читаемость
  ТаблицаКлиентов  // ТаблицаЗначений
  СписокДокументов // СписокЗначений
```

### Глобальный контекст

```yaml
Правило: Не используй в именах переменных имена из глобального контекста 1С

Запрещено использовать:
  - Документы
  - Справочники
  - Регистры (РегистрыСведений, РегистрыНакопления, РегистрыБухгалтерии)
  - Метаданные
  - Константы
  - Пользователи
  - ПараметрыСеанса
  - РольДоступна
  - И другие глобальные объекты

Плохо:
  Документы = Новый Массив;
  Справочники = ПолучитьСправочники();

Хорошо:
  СписокДокументов = Новый Массив;
  ДанныеСправочников = ПолучитьСправочники();

Проверка: Используй MCP-справку для проверки коллизий
  user-1c-help-docsearch("имя переменной")
```

### Процедуры и функции

```yaml
Правило: Используй глаголы для процедур, вопросительную форму для предикатов

Процедуры (действие):
  ЗаполнитьТаблицу()
  ОбновитьДанные()
  СоздатьДокумент()
  УдалитьЗапись()

Функции-предикаты (проверка):
  ЭтоНовый()
  ЕстьОшибки()
  МожноИзменить()
  ДоступноПользователю()

Функции (получение):
  ПолучитьДанные()
  НайтиКлиента()
  ВычислитьСумму()
  СформироватьОтчет()
```

### Доменная релевантность

```yaml
Правило: Имя идентификатора должно быть понятно в терминах предметной области без чтения ЗНИ

Первичный тест (доменный): коллега, знающая предметную область, но не читавшая постановку, может одной фразой сказать, что обозначает имя. Если нет — имя слишком абстрактное, «мета» (из языка задачи) или пустышка.

Вторичный тест (уровень абстракции): имя описывает роль данных в реализации (контейнер, промежуточное, массив для добавления), а не доменную сущность. Операция: мысленно убрать реализационное слово (Массив, Новый, Добавляемые, Временный) — если остаток не несёт доменного смысла, имя плохое.

Контрастный тест: в одном scope есть доменно названные данные и безликий контейнер для них — контейнер провалил тест.

Плохо (мета-имена, пустышки, постановка):
  МеханикаВключена
  ДействиеДляГейта
  GateAction
  Результат
  Данные
  Шаг1
  РежимЗадачи

Плохо (implementation-role — роль в коде вместо домена):
  МассивДобавляемыхРеквизитов
  НовыйРеквизит
  МассивРеквизитовМеханики

Хорошо (доменные, уточнённые):
  ИсключениеУчастниковВключено
  РеакцияНаНарушениеУсловия
  ПараметрыОтправки
  ДанныеПервичнойПроверки
  РежимОтправкиДокумента

Хорошо (домен в контейнере):
  РеквизитыПризнакаИсключения
  РеквизитВключенностиИсключения

Исключение: термин является устоявшимся в предметке
  ФазаПроизводства
  ШагМаршрута
  РежимНалогообложения

См. также: AP-031 в .cursor/rules/bsl-antipatterns.mdc (reviewer-only)
```

---

## ЗАПРОСЫ

### Форматирование

```yaml
Правило: Текст запроса без отступа на новой строке после =

Правильно:
  Запрос.Текст = 
  "ВЫБРАТЬ
  |    Таблица.Поле КАК Поле,
  |    Таблица.Поле2 КАК Поле2
  |ИЗ
  |    Справочник.Таблица КАК Таблица
  |ГДЕ
  |    Таблица.Поле = &Параметр";

Неправильно:
  Запрос.Текст = "ВЫБРАТЬ Таблица.Поле ИЗ Справочник.Таблица";
  
  Запрос.Текст = 
      "ВЫБРАТЬ
      |    Таблица.Поле
      |ИЗ
      |    Справочник.Таблица";  // Отступ после =
```

### Промежуточная переменная

```yaml
Правило: Для результата запроса используй промежуточную переменную

Плохо:
  Выборка = Запрос.Выполнить().Выбрать();
  Таблица = Запрос.Выполнить().Выгрузить();

Хорошо:
  РезультатЗапроса = Запрос.Выполнить();
  Выборка = РезультатЗапроса.Выбрать();
  
  РезультатЗапроса = Запрос.Выполнить();
  Таблица = РезультатЗапроса.Выгрузить();

Причина: Улучшает читаемость и отладку
```

### Псевдонимы

```yaml
Правило: Всегда давай псевдонимы полям через КАК

Плохо:
  ВЫБРАТЬ
      Контрагенты.ИНН,
      Контрагенты.Наименование
  ИЗ
      Справочник.Контрагенты

Хорошо:
  ВЫБРАТЬ
      Контрагенты.ИНН КАК ИНН,
      Контрагенты.Наименование КАК Наименование
  ИЗ
      Справочник.Контрагенты КАК Контрагенты

Причина: Явные имена полей в результате
```

### Проверка метаданных

```yaml
Правило: Перед написанием запроса проверяй метаданные

Проверяй:
  - Существование объекта метаданных
  - Имена реквизитов (точное написание)
  - Типы реквизитов
  - Индексы (для производительности)

Инструменты:
  user-PROJECT-codemetadata-metadatasearch (project-specific MCP)("Справочники.Клиенты.Реквизиты")  // поиск объектов и реквизитов метаданных
  user-PROJECT-codemetadata-codesearch (project-specific MCP)("ПолучитьКлиента")                   // поиск по BSL-коду (функции, вызовы)
  user-PROJECT-graph (project-specific MCP)-search_metadata("Справочник Клиенты")                  // Neo4j-поиск связей
```

### Обработка NULL и составных типов

```yaml
Правило: Используй ЕСТЬNULL() для защиты от NULL в соединениях

Плохо:
  ВЫБРАТЬ
      Документы.Ссылка КАК Документ,
      Данные.Сумма КАК Сумма  // NULL если нет строки в Данные
  ИЗ ...
  ЛЕВОЕ СОЕДИНЕНИЕ ...

Хорошо:
  ВЫБРАТЬ
      Документы.Ссылка КАК Документ,
      ЕСТЬNULL(Данные.Сумма, 0) КАК Сумма
  ИЗ ...
  ЛЕВОЕ СОЕДИНЕНИЕ ...

Правило: При обращении к реквизиту составного типа через точку используй ВЫРАЗИТЬ()

Плохо:
  ГДЕ Регистр.Регистратор.Дата = &Дата  // ошибка для составного типа

Хорошо:
  ГДЕ ВЫРАЗИТЬ(Регистр.Регистратор КАК Документ.РеализацияТоваровУслуг).Дата = &Дата

Подробности: .cursor/skills/1c-query-optimization/SKILL.md, std-04-queries.md
```

### Виртуальные таблицы

```yaml
Правило: Передавай параметры виртуальным таблицам, а не в WHERE

Плохо:
  ВЫБРАТЬ ... ИЗ РегистрНакопления.Товары.Остатки КАК Остатки
  ГДЕ Остатки.Период = &Дата  // период не фильтрует виртуальную таблицу в WHERE

Хорошо:
  ВЫБРАТЬ ... ИЗ РегистрНакопления.Товары.Остатки(&Дата, ) КАК Остатки

Правило: Для временных таблиц указывай ИНДЕКСИРОВАТЬ ПО при последующих соединениях

Хорошо:
  ВЫБРАТЬ Ссылка ПОМЕСТИТЬ ВТ_Документы ИЗ ...
  ИНДЕКСИРОВАТЬ ПО Ссылка;
  ...
  ЛЕВОЕ СОЕДИНЕНИЕ ВТ_Документы ПО ...  // соединение по индексированному полю

Подробности: .cursor/skills/1c-query-optimization/SKILL.md, std-04-queries.md
```

---

## ПРОИЗВОДИТЕЛЬНОСТЬ

### Запросы в цикле

```yaml
Правило: Не используй запросы внутри цикла

Плохо:
  Выборка = Запрос.Выполнить().Выбрать();
  Пока Выборка.Следующий() Цикл
      ДанныеКлиента = ПолучитьДанныеКлиента(Выборка.Клиент); // Запрос!
      // ...
  КонецЦикла;

Хорошо (вариант 1 - JOIN):
  Запрос.Текст = 
  "ВЫБРАТЬ
  |    Клиенты.Ссылка КАК Клиент,
  |    Данные.Поле1 КАК Поле1,
  |    Данные.Поле2 КАК Поле2
  |ИЗ
  |    Справочник.Клиенты КАК Клиенты
  |    ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДанныеКлиентов КАК Данные
  |    ПО Клиенты.Ссылка = Данные.Клиент";

Хорошо (вариант 2 - временная таблица):
  Запрос.Текст = 
  "ВЫБРАТЬ
  |    Клиенты.Ссылка КАК Клиент
  |ПОМЕСТИТЬ ВТ_Клиенты
  |ИЗ
  |    Справочник.Клиенты КАК Клиенты;
  |
  |ВЫБРАТЬ
  |    ВТ_Клиенты.Клиент,
  |    Данные.Поле1,
  |    Данные.Поле2
  |ИЗ
  |    ВТ_Клиенты КАК ВТ_Клиенты
  |    ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДанныеКлиентов КАК Данные
  |    ПО ВТ_Клиенты.Клиент = Данные.Клиент";

Причина: Запросы в цикле = N+1 проблема (10x медленнее)
```

### Реквизиты из ссылки

```yaml
Правило: Не получай реквизиты из ссылки через точку

Плохо:
  ИНН = Клиент.ИНН;  // Загружает весь объект!
  Наименование = Клиент.Наименование;  // Ещё раз загружает!

Хорошо (1 реквизит):
  ИНН = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Клиент, "ИНН");

Хорошо (несколько реквизитов от одного объекта):
  Реквизиты = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(
      Клиент, 
      "ИНН, Наименование, КПП"
  );
  ИНН = Реквизиты.ИНН;
  Наименование = Реквизиты.Наименование;

Хорошо (1 реквизит от нескольких объектов):
  МассивКлиентов = Новый Массив;
  МассивКлиентов.Добавить(Клиент1);
  МассивКлиентов.Добавить(Клиент2);
  
  ИННы = ОбщегоНазначения.ЗначениеРеквизитаОбъектов(МассивКлиентов, "ИНН");

Хорошо (несколько реквизитов от нескольких объектов):
  Реквизиты = ОбщегоНазначения.ЗначенияРеквизитовОбъектов(
      МассивКлиентов,
      "ИНН, Наименование"
  );

Причина: Обращение через точку загружает весь объект из БД
```

### Контракт источника данных и защитные проверки

**Принцип «Доверяй контракту»:** если функция/метод имеет документированный тип возврата или фиксированную структуру, НЕ добавлять ни ТипЗнч(), ни Свойство(), ни ЗначениеЗаполнено() как «подушку безопасности». Все защитные проверки должны быть обоснованы неопределённостью контракта.

**Принцип «Знание вместо проверки»:** защитная проверка — НЕ замена знанию типа. Не знаешь тип переменной — СТОП: прочитай метаданные (XML в `src/`), документацию вызываемой функции или вызывающий код. Не можешь определить — спроси пользователя. Добавление проверки вместо выяснения типа — антипаттерн «компенсация незнания».

**Принцип «Уточни, не защищайся»:** при неуверенности в структуре контракта — сначала установить контракт (прочитать код функции, метаданные XML, документацию, спросить пользователя). Свойство()/ТипЗнч()/ЗначениеЗаполнено() добавлять как защиту **только** если после уточнения подтверждено, что поле/тип может отсутствовать (опциональный ключ Структуры, внешний API с динамической схемой, обобщённый код для разных типов). «Не знаю контракт» без попытки выяснить = «компенсация незнания».

**HALT: перед добавлением ЛЮБОЙ защитной проверки (ТипЗнч, Свойство, ЕстьРеквизит, ЗначениеЗаполнено, Колонки.Найти) — ОБЯЗАТЕЛЬНО установить контракт источника данных и выбрать правильный метод проверки.**

```yaml
Правило: Свойство() — ИСКЛЮЧИТЕЛЬНО Структура / ФиксированнаяСтруктура

Метод Свойство() существует ТОЛЬКО у двух типов:
  - Структура
  - ФиксированнаяСтруктура
Вызов .Свойство() на ЛЮБОМ другом типе — runtime-ошибка «Метод объекта не обнаружен».
Ниже — примеры типов без .Свойство() (список НЕ исчерпывающий):
  - Ссылочные типы (СправочникСсылка, ДокументСсылка, ПеречислениеСсылка и т.д.)
  - Объекты (ДокументОбъект, СправочникОбъект и т.д.)
  - Строка дерева/таблицы значений
  - Форма (УправляемаяФорма)
  - ОбъектXDTO
Простое правило: если тип — не Структура и не ФиксированнаяСтруктура → .Свойство() запрещён.

Алгоритм (HALT перед добавлением проверки):
  1. Определить источник строки/объекта:
     - ТЧ этого объекта (Исполнители, Товары, ...) → структура задана метаданными → ПРОВЕРКА НЕ НУЖНА
     - Результат запроса с явным списком полей → структура задана текстом запроса → ПРОВЕРКА НЕ НУЖНА
     - Возврат функции с документированным типом (Структура, ФиксированнаяСтруктура) → тип и ключи заданы контрактом → ТипЗнч() и Свойство() НЕ НУЖНЫ
     - Параметр функции с документированным типом → структура задана контрактом → ПРОВЕРКА НЕ НУЖНА
     - Структура (опциональные ключи, ДополнительныеСвойства) → ПРОВЕРКА НУЖНА → Свойство("Имя")
     - Произвольный объект/форма/строка ТЗ (тип может варьироваться) → ПРОВЕРКА НУЖНА → ЕстьРеквизитИлиСвойствоОбъекта()
     - Обобщённый код для разных типов БП/документов → ПРОВЕРКА НУЖНА → выбрать метод по типу
  2. ТипЗнч(): если контракт гарантирует тип возврата (документация, типовой API) — НЕ добавлять проверку типа.
  3. ЗначениеЗаполнено(): если поле гарантированно заполнено по контракту/метаданным — НЕ добавлять как защиту; допустимо только как бизнес-проверка (например, «не сопоставлено с внешней системой»).
  4. Если контракт фиксирован (ТЧ, запрос, документированный параметр/возврат):
     → НЕ добавлять проверку, обращаться к полю напрямую.
  5. Если контракт неопределён:
     → Сначала попытаться установить: прочитать тело вызываемой функции,
       метаданные XML в src/, документацию. Не удалось — спросить пользователя.
     → Если после уточнения контракт стал фиксированным — ПРОВЕРКА НЕ НУЖНА (п. 4).
     → Если подтверждено, что поле/тип МОЖЕТ отсутствовать (опциональный ключ,
       внешний API, обобщённый код) — добавить проверку ПРАВИЛЬНЫМ методом
       (см. дерево решений ниже). В комментарии указать ПОЧЕМУ поле опционально.
     → Добавлять проверку «на всякий случай» без подтверждения опциональности —
       ЗАПРЕЩЕНО (антипаттерн «компенсация незнания», AP-004).

Проверка наличия поля — дерево решений (только при неопределённом контракте):
  Структура/ФиксированнаяСтруктура? → Свойство("Имя")
  Форма / строка ТЗ / объект? → ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Объект, "Имя")
  По метаданным конфигурации? → ОбщегоНазначения.ЕстьРеквизитОбъекта("Имя", Метаданные)

Антипаттерн «Defensive cake» — стек из 2+ защитных проверок на одном значении, где хотя бы одна проверка избыточна (поглощается другой или контракт фиксирован). Применяется при ЛЮБОМ типе контракта (фиксированный и динамический).

  Тип 1 — Fixed-contract cake (все проверки избыточны, контракт гарантирует структуру):
  ПЛОХО:
    ОписаниеОрганизации = Ядро.Организации_ЗаполненныеРеквизиты(Идентификаторы);
    Если ТипЗнч(ОписаниеОрганизации) <> Тип("Структура")      // контракт гарантирует Структура
        Или Не ОписаниеОрганизации.Свойство("BoxID")            // контракт гарантирует ключ
        Или Не ЗначениеЗаполнено(ОписаниеОрганизации.BoxID) Тогда  // маскировка
        Результат.ТекстОшибки = "Организация не сопоставлена с Диадок";
        Возврат Результат;
    КонецЕсли;
  ХОРОШО (доверяй контракту, проверяй только бизнес-смысл):
    ОписаниеОрганизации = Ядро.Организации_ЗаполненныеРеквизиты(Идентификаторы);
    Если Не ЗначениеЗаполнено(ОписаниеОрганизации.BoxID) Тогда
        Результат.ТекстОшибки = "Организация не сопоставлена с Диадок";
        Возврат Результат;
    КонецЕсли;

Полная таксономия и детектирование: AP-004, AP-005, AP-006, AP-028 в реестре антипаттернов (`.cursor/rules/bsl-antipatterns.mdc`, `.cursor/docs/antipatterns/bsl-antipatterns.md`).
```

### Кеширование

```yaml
Правило: При повторяющихся вычислениях в цикле используй кеширование

Плохо:
  Для Каждого Строка Из Таблица Цикл
      Коэффициент = ПолучитьКоэффициент(Строка.Дата);  // Повторные вызовы!
      Строка.Сумма = Строка.Сумма * Коэффициент;
  КонецЦикла;

Хорошо:
  КешКоэффициентов = Новый Соответствие;
  
  Для Каждого Строка Из Таблица Цикл
      Коэффициент = КешКоэффициентов[Строка.Дата];
      
      Если Коэффициент = Неопределено Тогда
          Коэффициент = ПолучитьКоэффициент(Строка.Дата);
          КешКоэффициентов.Вставить(Строка.Дата, Коэффициент);
      КонецЕсли;
      
      Строка.Сумма = Строка.Сумма * Коэффициент;
  КонецЦикла;

Причина: Избегаем повторных вычислений
```

---

## КЛИЕНТ-СЕРВЕР

### Директивы компиляции

```yaml
Правило: В модулях форм предпочитай &НаСервереБезКонтекста вместо &НаСервере

Плохо:
  &НаСервере
  Функция ПолучитьДанные()
      // Имеет доступ к реквизитам формы (контекст)
      // Сериализация контекста = overhead
  КонецФункции

Хорошо:
  &НаСервереБезКонтекста
  Функция ПолучитьДанные(Параметр1, Параметр2)
      // Не имеет доступа к реквизитам формы
      // Нет сериализации контекста
      // Быстрее
  КонецФункции

Исключение: Используй &НаСервере когда действительно нужен доступ к реквизитам формы
```

### Минимизация переходов

```yaml
Правило: Минимизируй переходы клиент-сервер

Плохо:
  &НаКлиенте
  Процедура ОбработатьДанные()
      Для Каждого Строка Из Таблица Цикл
          Строка.Сумма = ВычислитьСуммуНаСервере(Строка.Количество);  // N переходов!
      КонецЦикла;
  КонецПроцедуры

Хорошо:
  &НаКлиенте
  Процедура ОбработатьДанные()
      Таблица = ВычислитьСуммыНаСервере(Таблица);  // 1 переход
  КонецПроцедуры
  
  &НаСервереБезКонтекста
  Функция ВычислитьСуммыНаСервере(Таблица)
      Для Каждого Строка Из Таблица Цикл
          Строка.Сумма = ВычислитьСумму(Строка.Количество);
      КонецЦикла;
      Возврат Таблица;
  КонецФункции

Причина: Каждый переход клиент-сервер = сериализация + сеть
```

### Асинхронные методы

```yaml
Правило: Вместо ОписаниеОповещения используй Асинх-методы где возможно

Старый подход (ОписаниеОповещения):
  Оповещение = Новый ОписаниеОповещения("ПослеВопроса", ЭтотОбъект);
  ПоказатьВопрос(Оповещение, "Продолжить?", РежимДиалогаВопрос.ДаНет);
  
  Процедура ПослеВопроса(Результат, ДополнительныеПараметры) Экспорт
      Если Результат = КодВозвратаДиалога.Да Тогда
          // ...
      КонецЕсли;
  КонецПроцедуры

Новый подход (Асинх):
  Асинх Процедура СпроситьПользователя()
      Результат = Ждать ВопросАсинх("Продолжить?", РежимДиалогаВопрос.ДаНет);
      
      Если Результат = КодВозвратаДиалога.Да Тогда
          // ...
      КонецЕсли;
  КонецПроцедуры

Причина: Асинх-методы проще читать и отлаживать (линейный код)
```

---

## СООБЩЕНИЯ И ОШИБКИ

### Вывод сообщений

```yaml
Правило: Не используй Сообщить()

Плохо:
  Сообщить("Ошибка!");  // Не контролируется, не локализуется

Хорошо (на сервере):
  ОбщегоНазначения.СообщитьПользователю(
      "Ошибка при обработке данных",
      ,
      "Объект.Реквизит",
      ,
      Отказ
  );

Хорошо (на клиенте):
  ОбщегоНазначенияКлиент.СообщитьПользователю(
      "Ошибка при обработке данных"
  );

Причина: БСП-методы поддерживают:
  - Привязку к полю формы
  - Локализацию
  - Логирование
  - Отмену действия (Отказ)
```

### Обработка исключений

```yaml
Правило: Не глотай исключения молча

Плохо:
  Попытка
      // Опасная операция
  Исключение
      // Молчим
  КонецПопытки;

Плохо:
  Попытка
      // Опасная операция
  Исключение
      Сообщить("Ошибка");  // Недостаточно информации
  КонецПопытки;

Хорошо:
  Попытка
      // Опасная операция
  Исключение
      ТекстОшибки = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
      
      ЗаписьЖурналаРегистрации(
          "Модуль.Функция",
          УровеньЖурналаРегистрации.Ошибка,
          ,
          ,
          ТекстОшибки
      );
      
      ВызватьИсключение;  // Пробрасываем дальше, если нужно
  КонецПопытки;

Правило: Обрабатывай только ожидаемые исключения
  - Если знаешь, что может произойти - обработай
  - Если не знаешь - не глотай, пусть всплывёт
```

### Когда использовать Попытка/Исключение

**Главный принцип:** Попытка ставится только там, где при корректно написанном коде может возникнуть ошибка (внешние факторы: объект удалён другим пользователем, свойство отсутствует в иной конфигурации, таймаут внешнего вызова). Во всех остальных случаях — правильный чистый код: явные проверки (Свойство() — только для Структуры; ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта — для формы/объекта/строки; ТипЗнч(), проверка колонки, ЗначениеЗаполнено()); если исключение всё же возникло — это баг, он должен всплыть, а не ловиться.

```yaml
Плохо (маскировка + лишний лог):
  Попытка
      Идентификатор = СтрокаДерева.Идентификатор;
  Исключение
      Идентификатор = Неопределено;  // Тихий возврат — баг скрыт
  КонецПопытки;

Хорошо (явная проверка, без Попытки):
  Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(СтрокаДерева, "Идентификатор")
      И ЗначениеЗаполнено(СтрокаДерева.Идентификатор) Тогда
      Идентификатор = СтрокаДерева.Идентификатор;
  Иначе
      Идентификатор = Неопределено;
  КонецЕсли;

Хорошо (ожидаемый сбой, осмысленный fallback):
  Попытка
      ДействиеОбъект = Действие.ПолучитьОбъект();  // Мог быть удалён в фоне
  Исключение
      ЗаписьЖурналаРегистрации(..., ОписаниеОшибки());
      Возврат;  // Контракт: при сбое — пропустить итерацию
  КонецПопытки;
```

### Fail-fast вместо тихого пропуска

Правило: если код проверяет структурное предусловие (тип, наличие свойства, размер коллекции, ожидаемый формат) и предусловие не выполняется — код **обязан явно сигнализировать об ошибке** (ВызватьИсключение с понятным текстом). Молча продолжать работу запрещено: ни Продолжить в цикле, ни тихий Возврат из функции, ни пустая ветка Если без Иначе.

Цель: структурный баг проявляется сразу, а не скрывается молчаливым пропуском.

```yaml
Плохо (цикл — молчаливый пропуск элемента):
  Если НЕ ЭлементРезультата.Свойство("Данные") Тогда Продолжить;
  Если ТипЗнч(Элемент) <> Тип("Структура") Тогда Продолжить;

Плохо (функция — тихий возврат при несоответствии предусловия):
  Если НЕ ДанныеКорректны(Входные) Тогда Возврат; КонецЕсли;
  // ← вызывающий код не узнает, что обработка не произошла

Плохо (ветка — пустой пропуск):
  Если ТипЗнч(Значение) = Тип("Структура") Тогда
      ОбработатьСтруктуру(Значение);
  КонецЕсли;
  // ← если не Структура — молча ничего не произошло; баг скрыт

Хорошо (прямой доступ — fail-fast):
  Результат = Элемент.Данные;  // Нет свойства -> runtime-ошибка

Хорошо (валидация предусловия с ВызватьИсключение):
  Если Набор1.Количество() <> Набор2.Количество() Тогда
      ВызватьИсключение "Несовпадение количеств элементов набора";
  КонецЕсли;

Хорошо (явная ошибка в ветке Иначе):
  Если ТипЗнч(Значение) = Тип("Структура") Тогда
      ОбработатьСтруктуру(Значение);
  Иначе
      ВызватьИсключение "Ожидалась Структура, получен " + ТипЗнч(Значение);
  КонецЕсли;

Исключение: пропуск по бизнес-признаку (фильтрация по Статусу, Типу документа
и т.п.) — допустим; это не маскировка структурной ошибки.
```

### Условное действие vs инвертированный ранний выход

Правило: когда вся оставшаяся логика процедуры — одно действие или компактный блок, привязывай условие к **действию**, а не к **выходу**.

```yaml
Плохо (инвертированный ранний выход — условие привязано к выходу, а не к действию):
  РезультатЗапроса = Запрос.Выполнить();
  Если РезультатЗапроса.Пустой() Тогда
      Возврат;
  КонецЕсли;
  УстановитьРезультат(Действие, Результат);

Хорошо (условное действие — условие привязано к действию):
  РезультатЗапроса = Запрос.Выполнить();
  Если НЕ РезультатЗапроса.Пустой() Тогда
      УстановитьРезультат(Действие, Результат);
  КонецЕсли;

Когда допустим ранний выход (guard clause):
  // Предусловия в НАЧАЛЕ процедуры — допустимы:
  Если ТипЗнч(ДействиеСсылка) <> Тип("СправочникСсылка.ДействияПодписания") Тогда
      Возврат;
  КонецЕсли;

  Если ПараметрыУчастника <> Неопределено Тогда
      Возврат;
  КонецЕсли;

  // ... основная логика процедуры (много строк) ...

Критерий различия:
  - Guard clause (допустимо): проверка предусловий В НАЧАЛЕ процедуры;
    после guard clauses идёт основная логика из нескольких действий.
  - Инвертированный ранний выход (запрещено): проверка НЕПОСРЕДСТВЕННО
    перед единственным оставшимся действием; Возврат используется как
    инверсия условия для этого действия.
```

### Попытка Justification Gate

**HALT перед добавлением Попытка/Исключение** — обязательная идентификация внешнего фактора. Аналог rule 14 (defensive checks gate): как проверка Свойство()/ТипЗнч() требует обоснования контрактом, так и Попытка требует обоснования внешним фактором. Flow-centric: AP-027 (guard-then-catch) — если guard уже валидирует значение, используемое в Попытка, код противоречит сам себе.

```yaml
Алгоритм (HALT перед добавлением Попытка/Исключение):
  1. Определить, есть ли внешний фактор — причина сбоя при корректном коде:
     - Сеть/HTTP/FTP (таймаут, разрыв соединения) → ЕСТЬ
     - Файловая система (файл удалён, нет доступа) → ЕСТЬ
     - Конкурентный доступ к данным (объект удалён другим пользователем, блокировка) → ЕСТЬ
     - COM-объект (внешний процесс, нестабильный) → ЕСТЬ
     - Чужая конфигурация (свойство/метод может отсутствовать при встраивании) → ЕСТЬ
     - Конвертация строк (hex, base64, числа, даты) → НЕТ, валидировать вход
     - Арифметика, математические операции → НЕТ
     - Доступ к метаданным текущей конфигурации → НЕТ
     - Обращение к полю/методу с фиксированным контрактом → НЕТ (см. rule 14)
  2. Нет внешнего фактора → НЕ ДОБАВЛЯТЬ Попытку; валидировать входные данные
  3. Есть внешний фактор → проверить последствия подавления:
     a. Fallback некорректен (вызывающий код получает значение или состояние, неотличимое от успеха,
        но ведущее к ложноотрицательному результату ниже по стеку) → re-raise после логирования
     b. Fallback корректен (вызывающий код получает правильный результат) → проверить п. c
     c. Операция внутри Попытка имеет персистентные побочные эффекты (запись в БД, ФС, внешнюю систему)
        И исключение не пробрасывается? Если ДА → проверить: зависит ли последующий код (после КонецПопытки,
        после цикла, или у caller) от успешного завершения этой записи? Зависимость есть → re-raise после логирования
        ИЛИ накопить ошибки и передать caller/пользователю с блокировкой downstream-логики (AP-032).
        Зависимости нет (самодостаточная изолированная операция) → перейти к п. d
     d. Пользователь получает обратную связь? (сообщение на форме, маркер «обработано с ошибками»,
        визуальная индикация, всплывающее исключение) → если НЕТ — скрытый частичный результат (AP-030), НЕДОПУСТИМО.
        Частичный результат (продолжение работы после ошибки) допустим ТОЛЬКО при явном требовании в логике приложения
        (ТЗ, design, спецификация описывают сценарий «продолжить при ошибке и сообщить»).
        Обоснование должно быть зафиксировано в **design.md** (секция Error Handling Policy), не в комментарии к коду.
        Ревьювер обязан проверить наличие ссылки на design/spec. Без документированного обоснования — CRITICAL (AP-030/AP-032).
  4. В блоке Исключение обязательно одно из:
     - ЗаписьЖурналаРегистрации + ВызватьИсключение (пробросить)
     - ЗаписьЖурналаРегистрации + осмысленный fallback (документированный)
     - Исключение без лога и без re-raise → ЗАПРЕЩЕНО (бесследное подавление)

Дерево решений:
  Есть внешний фактор? ──НЕТ──→ НЕ ДОБАВЛЯТЬ Попытку; валидировать вход
          │
         ДА
          │
  Fallback корректен? ──НЕТ──→ В Исключение: лог + ВызватьИсключение
          │
         ДА
          │
  Персистентные побочные эффекты? ──ДА──→ Downstream зависит от записи?
          │                                      │
         НЕТ                                 ДА → НЕДОПУСТИМО (AP-032):
          │                                      re-raise или accumulate+signal
          │                                 НЕТ ↓
          │←────────────────────────────────────┘
          │
  Пользователь узнает об ошибке? ──НЕТ──→ НЕДОПУСТИМО (скрытый частичный результат, AP-030)
          │
         ДА (сообщение на форме / явный маркер в результате / исключение всплывает)
          │
  → Попытка допустима; в Исключение: лог + осмысленный fallback
```

```yaml
Антипаттерн 1 — Попытка вокруг детерминированной операции:
  ПЛОХО:
    Попытка
        ДвоичныеДанные = Новый ДвоичныеДанные(ОтпечатокHex);
        Результат = Base64Строка(ДвоичныеДанные);
        Возврат Результат;
    Исключение
        Возврат ОтпечатокHex;
    КонецПопытки;
    // Конвертация hex → base64 — детерминированная операция.
    // Если вход валиден — ошибка невозможна. Если невалиден — это баг, не исключительная ситуация.
  ХОРОШО:
    Если НЕ ЭтоВалидныйHex(ОтпечатокHex) Тогда
        ВызватьИсключение "Некорректный hex-формат отпечатка";
    КонецЕсли;
    ДвоичныеДанные = ПолучитьДвоичныеДанныеИзHexСтроки(ОтпечатокHex);
    Возврат Base64Строка(ДвоичныеДанные);

Остальные антипаттерны (Попытка вместо валидации, silent degradation, бесследное подавление, layered Попытка): AP-008, AP-009, AP-010, AP-006 (cross-boundary) в реестре антипаттернов (`.cursor/rules/bsl-antipatterns.mdc`, `.cursor/docs/antipatterns/bsl-antipatterns.md`).
```

---

## ДАТЫ И ВРЕМЯ

### Текущая дата

```yaml
Правило: Для текущей даты на сервере используй ТекущаяДатаСеанса()

Плохо:
  ТекущаяДата = ТекущаяДата();  // Дата сервера (UTC или локальная)

Хорошо:
  ТекущаяДата = ТекущаяДатаСеанса();  // Дата сеанса пользователя

Причина: 
  - ТекущаяДата() зависит от часового пояса сервера
  - ТекущаяДатаСеанса() учитывает часовой пояс пользователя
  - Важно для многопользовательских систем
```

---

## КОНКУРЕНТНОСТЬ

### Транзакции

```yaml
Правило: НачатьТранзакцию() — всегда в связке с Попытка/Исключение; ОтменитьТранзакцию() обязателен в Исключении

Обязательный паттерн:
  НачатьТранзакцию();
  Попытка
      // Бизнес-логика в транзакции
      ЗафиксироватьТранзакцию();
  Исключение
      ОтменитьТранзакцию();
      ВызватьИсключение;
  КонецПопытки;

Запрещено:
  - НачатьТранзакцию() без ЗафиксироватьТранзакцию()/ОтменитьТранзакцию() в том же scope
  - НачатьТранзакцию() без обёртки Попытка/Исключение
  - ПоказатьВопрос(), Предупреждение(), Сообщить() внутри транзакции — блокирует поток на стороне сервера

Плохо:
  НачатьТранзакцию();
  ЗаписатьОбъект(Объект);  // Если здесь ошибка — транзакция зависает

Хорошо:
  НачатьТранзакцию();
  Попытка
      ЗаписатьОбъект(Объект);
      ЗафиксироватьТранзакцию();
  Исключение
      ОтменитьТранзакцию();
      ВызватьИсключение;
  КонецПопытки;
```

### Блокировки

```yaml
Правило: Проверяй код на конкурентность и необходимость блокировок

Когда нужны блокировки:
  - Изменение данных (запись объектов)
  - Чтение с последующей записью
  - Работа с последовательностями/автонумерацией
  - Критические секции

Управляемые блокировки (единственный актуальный способ):
  НачатьТранзакцию();
  Попытка
      Блокировка = Новый БлокировкаДанных;
      ЭлементБлокировки = Блокировка.Добавить("Справочник.Клиенты");
      ЭлементБлокировки.УстановитьЗначение("Ссылка", СсылкаКлиента);
      Блокировка.Заблокировать();  // снимается при завершении транзакции
      
      // Работа с данными
      
      ЗафиксироватьТранзакцию();
  Исключение
      ОтменитьТранзакцию();
      ВызватьИсключение;
  КонецПопытки;

Учитывай внешние транзакции:
  - Обработчики событий объектов (ПередЗаписью, ПриЗаписи)
  - Выполняются в транзакции записи объекта
  - Блокировки не нужны (уже заблокировано)
```

---

## ОБРАБОТЧИКИ СОБЫТИЙ ОБЪЕКТОВ

```yaml
Правило: ПередЗаписью — только проверки, ПриЗаписи — движения и побочные эффекты

ПередЗаписью:
  - Проверка заполнения реквизитов
  - Установка вычисляемых полей
  - НЕ делать тяжёлые запросы
  - НЕ изменять данные вне записываемого объекта

ПриЗаписи:
  - Формировать движения регистров
  - Отправлять уведомления
  - Побочные эффекты после записи

Правило: При загрузке обмена — пропускать лишние проверки и движения

Плохо:
  Процедура ПередЗаписью(Отказ)
      // без проверки ОбменДанными.Загрузка
      ПровестиВалидацию(Отказ);
  КонецПроцедуры

Хорошо:
  Процедура ПередЗаписью(Отказ)
      Если ОбменДанными.Загрузка Тогда Возврат; КонецЕсли;
      ПровестиВалидацию(Отказ);
  КонецПроцедуры

Правило: ОбработкаПроверкиЗаполнения — только бизнес-проверки обязательности

  Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
      // Добавлять реквизиты в ПроверяемыеРеквизиты через Добавить()
      // или исключать через Удалить() при условных реквизитах
  КонецПроцедуры

Подробности: std-03-event-handlers.md
```

## ВРЕМЕННОЕ ХРАНИЛИЩЕ И ФОНОВЫЕ ЗАДАНИЯ

```yaml
Правило: Для передачи больших данных между клиентом и сервером используй временное хранилище

Правило: Если данные во временном хранилище должны сохраняться между несколькими серверными вызовами одной формы, передавать вторым параметром идентификатор формы (ЭтаФорма.УникальныйИдентификатор). Тогда платформа привязывает данные к сроку жизни формы: они не удаляются при первом/втором и последующих серверных вызовах и автоматически удаляются при закрытии формы. Наиболее безопасный вариант (источник: 1cmanager.ru).

Плохо (передача объекта через параметр):
  &НаКлиенте
  Процедура ЗагрузитьФайл()
      // Нельзя передать ДвоичныеДанные напрямую на сервер без хранилища
  КонецПроцедуры

Хорошо:
  &НаКлиенте
  Процедура ЗагрузитьФайл()
      // второй параметр — привязка к сроку жизни формы
      АдресДанных = ПоместитьВоВременноеХранилище(ДанныеФайла, ЭтаФорма.УникальныйИдентификатор);
      ОбработатьФайлНаСервере(АдресДанных);
  КонецПроцедуры
  
  &НаСервереБезКонтекста
  Процедура ОбработатьФайлНаСервере(АдресДанных)
      ДанныеФайла = ПолучитьИзВременногоХранилища(АдресДанных);
      // работа с данными
      УдалитьИзВременногоХранилища(АдресДанных);
  КонецПроцедуры

Правило: Длительные операции — через ДлительныеОперации (БСП)

  // Запуск фонового задания
  ПараметрыЗадания = ДлительныеОперации.ПараметрыВыполненияВФоне(...);
  Задание = ДлительныеОперации.ВыполнитьВФоне(
      "МойОбщийМодуль.МоёДлительноеДействие",
      ПараметрыЗадания,
      ПараметрыВыполнения
  );
  // Результат получать через ДлительныеОперации.ПолучитьРезультат

Подробности: std-09-forms-ui.md, 1c-bsp/SKILL.md
```

---

## АННОТАЦИИ РАСШИРЕНИЙ

```yaml
КРИТИЧЕСКОЕ ОГРАНИЧЕНИЕ:
  &Перед и &После — ТОЛЬКО для процедур (Процедура).
  Для функций (Функция) — &Вместо (с ПродолжитьВызов()) или &ИзменениеИКонтроль.

Правило: Предпочитай &Перед/&После (для процедур), прибегай к &ИзменениеИКонтроль только при необходимости

Приоритет аннотаций (от предпочтительного к крайнему):
  1. &Перед / &После — минимальная связанность с типовым кодом
  2. &ИзменениеИКонтроль — точечная правка внутри метода
  3. &Вместо — полная замена (ломается при обновлениях)

При использовании &ИзменениеИКонтроль:
  - Код вне директив #Вставка/#Удаление побитово совпадает с типовым
  - Не менять форматирование в неизменённых участках
  - Бизнес-логику выносить в отдельные процедуры расширения
  - Подробности: .cursor/skills/1c-extensions/SKILL.md
```

---

## ФОРМАТИРОВАНИЕ

### Ограничение строки

```yaml
Правило: Ограничение строки — 120 символов

Если строка длиннее:
  - Перенеси на новую строку
  - Выровняй отступы
  - Сохрани читаемость

Плохо:
  Результат = ОбщегоНазначения.ЗначенияРеквизитовОбъектов(МассивОбъектов, "Наименование, ИНН, КПП, ЮридическийАдрес, ФактическийАдрес");

Хорошо:
  Результат = ОбщегоНазначения.ЗначенияРеквизитовОбъектов(
      МассивОбъектов, 
      "Наименование, ИНН, КПП, ЮридическийАдрес, ФактическийАдрес"
  );

Или:
  РеквизитыДляПолучения = "Наименование, ИНН, КПП, "
      + "ЮридическийАдрес, ФактическийАдрес";
  
  Результат = ОбщегоНазначения.ЗначенияРеквизитовОбъектов(
      МассивОбъектов, 
      РеквизитыДляПолучения
  );
```

### Тернарный оператор

```yaml
Правило: Не используй тернарный оператор ?()

Плохо:
  Результат = ?(Условие, Значение1, Значение2);

Хорошо:
  Если Условие Тогда
      Результат = Значение1;
  Иначе
      Результат = Значение2;
  КонецЕсли;

Причина: 
  - Тернарный оператор менее читаем
  - Сложнее отлаживать
  - Не соответствует стилю 1С
```

---

## САМОКОНТРОЛЬ

### Ревью после редактирования

```yaml
Правило: После редактирования кода выполняй ревью

Проверяй:
  1. Стиль:
     - Соответствие стандартам
     - Именование
     - Форматирование
     - Комментарии
  
  2. Читаемость:
     - Понятен ли код?
     - Можно ли упростить?
     - Нет ли дублирования?
  
  3. Корректность:
     - Логика верна?
     - Обработаны ошибки?
     - Учтены граничные случаи?
  
  4. Безопасность:
     - Нет SQL injection?
     - Проверены права доступа?
     - Нет hardcoded секретов?
  
  5. Конкурентность:
     - Нужны ли блокировки?
     - Учтены гонки?
     - Нет deadlock'ов?

Инструменты:
  - BSL LSP Bridge: bsl_lsp_diagnostics(file_path)
  - Syntax checker: user-1c-syntax-checker-syntaxcheck(code)
  - Code checker: user-1c-code-checker-check_1c_code(code, "logic")
  - onec-code-reviewer: Полное ревью
```

---

## ИНТЕГРАЦИЯ С ИНСТРУМЕНТАМИ

### BSL LSP Bridge (когда подключен)

```yaml
status: NOT_CONNECTED
fallback:
  - user-1c-syntax-checker-syntaxcheck(code) — синтаксис
  - user-1c-code-checker-check_1c_code(code, "logic") — логика
when_available:
  bsl_lsp_diagnostics(file_path): Получить все проблемы
  bsl_lsp_format(file_path): Форматировать код
  bsl_lsp_symbols(file_path): Получить список функций
```

### MCP Серверы

```yaml
Справка по платформе:
  user-1c-help-docsearch("метод или функция")

БСП (Библиотека стандартных подсистем):
  user-1c-ssl-ssl_search("функциональность")

Шаблоны кода:
  user-1c-templates-templatesearch("описание задачи")

Проверка синтаксиса:
  user-1c-syntax-checker-syntaxcheck(code)

Проверка логики:
  user-1c-code-checker-check_1c_code(code, "logic")

Метаданные:
  user-PROJECT-codemetadata-metadatasearch (project-specific MCP)("Справочники.Клиенты")
  user-PROJECT-graph (project-specific MCP)-search_metadata("Справочник Клиенты")

Код:
  user-PROJECT-codemetadata (project-specific MCP)-codesearch("функция или паттерн")
```

---

## КЛЮЧЕВЫЕ ВЕНДОРСКИЕ ПРИНЦИПЫ

Вендорский стандарт (ИТС v8std):

1. **ОбменДанными.Загрузка** — в ПередЗаписью/ПриЗаписи проверять: при загрузке обмена не выполнять лишние проверки и движения.
2. **НСтр()** — все строки для пользователя через НСтр("ru = '...'").
3. **Проведение** — движения формировать в ПриЗаписи (не в ПередЗаписью).
4. **Модуль объекта vs менеджера** — бизнес-логика записи в модуле объекта; получение данных для внешнего кода — в модуле менеджера.
5. **Функциональные опции** — не использовать для видимости элементов одной формы.

Подробно: `.cursor/skills/1c-vendor-standards/SKILL.md`.

---

## КРИТИЧЕСКИЕ ПРАВИЛА

**Эти правила ОБЯЗАТЕЛЬНЫ и не могут быть нарушены:**

1. ✅ **Проверяй метаданные** перед написанием запросов
2. ✅ **Не используй запросы в цикле** (N+1 проблема)
3. ✅ **Не получай реквизиты через точку** (используй БСП)
4. ✅ **Логируй исключения** (не глотай молча)
5. ✅ **Используй ТекущаяДатаСеанса()** (не ТекущаяДата())
6. ✅ **Проверяй конкурентность** (блокировки где нужно)
7. ✅ **Не используй Сообщить()** (используй БСП)
8. ✅ **Не используй имена из глобального контекста** (коллизии)
9. ✅ **Проводи самоконтроль** (ревью после написания)
10. ✅ **Используй инструменты** (BSL LSP, MCP, RLM)
11. ✅ **Попытка только там, где при корректном коде ошибка возможна** (внешние факторы); иначе — чистый код без обёртки, исключение = баг, пусть всплывает. HALT перед добавлением — см. rule 20 (Попытка Justification Gate). Flow-centric: AP-027 (guard-then-catch) — если guard уже валидирует значение, Попытка противоречит.
12. ✅ **Не маскируй ошибки** тихим Возвратом или присвоением без лога
13. ✅ **Если ловишь исключение — логируй только когда сбой ожидаем и лог нужен для диагностики**; не засоряй ЖР
14. ✅ **Доверяй контракту, знай тип:** ТипЗнч(), Свойство(), ЗначениеЗаполнено() — не добавлять, если контракт фиксирован (документированный тип возврата, ТЧ, запрос, параметр). Свойство() — ИСКЛЮЧИТЕЛЬНО Структура/ФиксированнаяСтруктура; на любом другом типе — runtime-ошибка. Для формы/объекта/строки ТЗ — ЕстьРеквизитИлиСвойствоОбъекта. HALT перед любой защитной проверкой: установить контракт; фиксирован → не проверять; неопределён → проверять правильным методом. Defensive cake (стек проверок на одном значении) = антипаттерн. **Компенсация незнания проверкой — антипаттерн:** не знаешь тип → выясни по метаданным/документации, не добавляй проверку. Flow-centric: AP-028 (check-after-establish) — проверка свойства после установления типа в коде избыточна. Процессный gate перед bug fix: `verified-cause-gate.mdc` (HALT 1-3).
15. ✅ **Аннотации расширений**: &Перед/&После предпочтительнее &ИзменениеИКонтроль, НО &Перед/&После применимы только к процедурам; для функций — &Вместо или &ИзменениеИКонтроль; при &ИзменениеИКонтроль — код вне директив побитово совпадает с типовым; бизнес-логику выносить в отдельные процедуры
16. ✅ **Fail-fast при структурных проверках**: если предусловие (тип, свойство, размер коллекции, формат) не выполняется — ВызватьИсключение; не пропускать молча (ни Продолжить, ни тихий Возврат, ни пустая ветка)
17. ✅ **Комментарии — только о коде**: маркеры в комментариях (автор, дата, заявка, // НАЧАЛО/КОНЕЦ, +++/---), закомментированный старый код, временные инструкции и ссылки на артефакты разработки (design, decision, proposal, change name, ЗНИ, ADR, номера задач) запрещены в релизе. Комментарий описывает код в доменных терминах, а не процесс принятия решений. Директивы #Вставка/#Удаление — синтаксис расширения, не трогать.
18. ✅ **Транзакционный паттерн**: `НачатьТранзакцию()` — всегда обёрнут в `Попытка/Исключение`; в блоке `Исключение` — обязательно `ОтменитьТранзакцию()` и `ВызватьИсключение`; пользовательское взаимодействие (вопросы, предупреждения) внутри транзакции запрещено.
19. ✅ **Оповестить() только на клиенте**: `Оповестить()`, `ОповеститьОбИзменении()` — клиентские методы; в серверном коде (`&НаСервере`, `&НаСервереБезКонтекста`, серверный общий модуль) не вызывать; выносить в клиентский обработчик после серверного вызова.
20. ✅ **Попытка Justification Gate**: HALT перед добавлением Попытка/Исключение — идентифицировать внешний фактор (сеть, ФС, конкурентный доступ, COM, чужая конфигурация). Нет внешнего фактора → НЕ ДОБАВЛЯТЬ, валидировать вход. Fallback неотличим от успеха (silent degradation) → re-raise после лога. Персистентная запись + подавление + downstream-зависимость → AP-032 (см. rule 24). Исключение без лога и без re-raise → запрещено. Процессный gate перед bug fix: `verified-cause-gate.mdc` (HALT 1-3).
21. ✅ **Parameter Integrity**: см. секцию PARAMETER INTEGRITY выше.
22. ✅ **No Duplicated Literals**: числовой или строковый литерал, используемый 2+ раз в модуле — извлечь в именованную переменную или параметр. Исключения: 0, 1, -1, Истина, Ложь; текст запросов; ключи структур; имена метаданных; литерал, присвоенный именованной переменной непосредственно перед использованием.
23. ✅ **Условное действие, не инвертированный ранний выход**: если вся оставшаяся логика — одно действие или компактный блок, оборачивай его в `Если <условие> Тогда ... КонецЕсли`, а не пиши `Если НЕ <условие> Тогда Возврат; КонецЕсли; <действие>`. Условие привязано к **действию**, не к **выходу**. Ранний выход допустим только как guard clause (предусловие в начале процедуры, после которого идёт основная логика из нескольких действий).
24. ✅ **Явный сбой вместо скрытого частичного результата (Partial Execution Gate)**: по умолчанию ошибка должна быть видна пользователю (fail-fast или явное сообщение). Попытка + лог + Продолжить/тихий Возврат без обратной связи пользователю = скрытый частичный результат (AP-030). Попытка + персистентная запись + подавление без re-raise + downstream-зависимость от успешности записи = рассогласование персистентного состояния (AP-032). Допустимо ТОЛЬКО при одновременном выполнении ВСЕХ условий: (1) Логика приложения явно предусматривает частичный результат (не «так проще реализовать»). (2) В design.md (или спецификации) зафиксирована секция **Error Handling Policy** с обоснованием: почему fail-fast неприемлем, каковы последствия частичного выполнения, как пользователь узнает о сбое. (3) Ревьювер верифицирует ссылку на design/spec; комментарий в коде **не** является достаточным обоснованием. При отсутствии п. 2 — CRITICAL (AP-030); при наличии персистентных side effects — CRITICAL (AP-032) независимо от наличия design.

---

## PARAMETER INTEGRITY

```yaml
Правило: Параметр функции/процедуры — входной контракт, не переменная

Плохо (parameter overwrite — значение от вызывающего выброшено):
  Процедура Обработать(Данные, Источник = Неопределено)
      Если ЗначениеЗаполнено(Источник) Тогда
          Данные = ПолучитьДанныеОтИсточника(Источник);
      КонецЕсли;
  КонецПроцедуры

Хорошо (раздельные процедуры с чёткими контрактами):
  Процедура ОбработатьДанные(Данные)
      // Данные используются как есть
  КонецПроцедуры

  Процедура ОбработатьДанныеОтИсточника(Источник)
      Данные = ПолучитьДанныеОтИсточника(Источник);
      ОбработатьДанные(Данные);
  КонецПроцедуры

Плохо (collection mutation без контракта out):
  Процедура ДополнитьСписок(Элементы, Источник)
      Элементы.Очистить();  // caller потерял свои данные
      // ... заполнение заново
  КонецПроцедуры

Хорошо (документированный output-параметр):
  // Параметры:
  //   Элементы - Массив - [out] очищается и заполняется заново
  Процедура ДополнитьСписок(Элементы, Источник)
      Элементы.Очистить();
      // ... заполнение заново
  КонецПроцедуры

Исключения (стандартные 1С-паттерны):
  - Отказ = Истина  // стандартный контракт обработчиков
  - ПроверяемыеРеквизиты.Добавить() / .Удалить()  // контракт ОбработкаПроверкиЗаполнения
  - Параметр с пометкой [out] в JSDoc-заголовке
```

---

## NO DUPLICATED LITERALS

```yaml
Правило: Литерал, используемый 2+ раз в модуле — извлечь в именованную переменную

Плохо (дублирование числового литерала):
  Функция РекурсияА(Глубина)
      МаксимальнаяГлубина = 100;
      Если Глубина > МаксимальнаяГлубина Тогда Возврат; КонецЕсли;
      // ...
  КонецФункции

  Функция РекурсияБ(Глубина)
      МаксимальнаяГлубина = 100;  // дубль
      Если Глубина > МаксимальнаяГлубина Тогда Возврат; КонецЕсли;
      // ...
  КонецФункции

Хорошо (общая переменная или параметр):
  Функция МаксимальнаяГлубинаРекурсии()
      Возврат 100;
  КонецФункции

  Функция РекурсияА(Глубина)
      Если Глубина > МаксимальнаяГлубинаРекурсии() Тогда Возврат; КонецЕсли;
  КонецФункции

Плохо (дублирование строкового литерала):
  Представление = СтрЗаменить(Представление, "(Будет удален по условию настроек)", "");
  // ... в другой процедуре:
  Представление = СтрЗаменить(Представление, "(Будет удален по условию настроек)", "");

Исключения (дублирование допустимо):
  - 0, 1, -1, Истина, Ложь
  - Текст запросов (Запрос.Текст = ...)
  - Ключи структуры ("Имя", "Значение")
  - Имена метаданных ("Справочник.Клиенты")
  - Литерал присвоен именованной переменной непосредственно перед использованием
```

---

## ПРИМЕРЫ

### Пример 1: Правильная структура модуля

```bsl
#Область ПрограммныйИнтерфейс

// Получает данные клиента
//
// Параметры:
//   Клиент - СправочникСсылка.Клиенты - ссылка на клиента
//
// Возвращаемое значение:
//   Структура - данные клиента с полями:
//     * Наименование - Строка - наименование клиента
//     * ИНН - Строка - ИНН клиента
//     * КПП - Строка - КПП клиента
//
Функция ПолучитьДанныеКлиента(Клиент) Экспорт
    
    Реквизиты = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(
        Клиент,
        "Наименование, ИНН, КПП"
    );
    
    Возврат Реквизиты;
    
КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Функция ПолучитьКешКлиентов()
    
    Если КешКлиентов = Неопределено Тогда
        КешКлиентов = Новый Соответствие;
    КонецЕсли;
    
    Возврат КешКлиентов;
    
КонецФункции

#КонецОбласти
```

### Пример 2: Оптимизированный запрос

```bsl
Функция ПолучитьДанныеКлиентов(МассивКлиентов) Экспорт
    
    Запрос = Новый Запрос;
    Запрос.Текст = 
    "ВЫБРАТЬ
    |    Клиенты.Ссылка КАК Клиент,
    |    Клиенты.Наименование КАК Наименование,
    |    Клиенты.ИНН КАК ИНН,
    |    Данные.Адрес КАК Адрес,
    |    Данные.Телефон КАК Телефон
    |ИЗ
    |    Справочник.Клиенты КАК Клиенты
    |    ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДанныеКлиентов КАК Данные
    |    ПО Клиенты.Ссылка = Данные.Клиент
    |ГДЕ
    |    Клиенты.Ссылка В (&МассивКлиентов)";
    
    Запрос.УстановитьПараметр("МассивКлиентов", МассивКлиентов);
    
    РезультатЗапроса = Запрос.Выполнить();
    
    Возврат РезультатЗапроса.Выгрузить();
    
КонецФункции
```

### Пример 3: Правильная обработка ошибок

```bsl
Процедура ОбработатьДанные(Данные) Экспорт
    
    Попытка
        // Опасная операция
        ВыполнитьОбработку(Данные);
    Исключение
        ТекстОшибки = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
        
        ЗаписьЖурналаРегистрации(
            "ОбщийМодуль.ОбработатьДанные",
            УровеньЖурналаРегистрации.Ошибка,
            ,
            ,
            ТекстОшибки
        );
        
        ОбщегоНазначения.СообщитьПользователю(
            "Ошибка при обработке данных. Подробности в журнале регистрации."
        );
        
        ВызватьИсключение;
    КонецПопытки;
    
КонецПроцедуры
```

---

---

## ТИПИЧНЫЕ ОШИБКИ AI В BSL

### Ключевое слово Перем
`Перем МояПеременная;` — НЕ `Переменная МояПеременная;`

### Общие модули: нет переменных модуля
Серверные общие модули не хранят состояние между вызовами. `Перем КешДанных;` — бессмысленно. Альтернативы: параметры процедур, ДополнительныеСвойства, временное хранилище.

### ЭтотОбъект в расширениях
В модуле объекта расширения реквизиты требуют `ЭтотОбъект.Реквизит` (НЕ просто `Реквизит`).

### &Вместо — полная копия
При `&Вместо` скопировать ВСЁ тело базового метода, заменить реквизиты на `ЭтотОбъект.`, добавить изменения. Предпочитать `&Перед`/`&После`.

### Контекст выполнения
- Серверная процедура не может вызвать клиентскую
- Клиентская не обращается к БД напрямую
- `&НаСервереБезКонтекста` не использует `ЭтаФорма`
- `Оповестить()`, `ОповеститьОбИзменении()` — только `&НаКлиенте`; на сервере вызов невозможен, выносить в клиентский обработчик после серверного вызова

### Избыточная проверка полей (контракт)
См. rule 14 (Контракт источника данных и защитные проверки) выше.

### Валидация имён метаданных (XML-first)
**Никогда не выдумывать имена метаданных и типов.** Любое упоминание метаданных в BSL (объекты, типы, значения перечислений, реквизиты, измерения/ресурсы) должно быть **проверено по XML-выгрузке в `src/`** до использования.
- **Validate**: перед `Перечисления.X.Y`, `РегистрСведений.X`, `Тип("...X")`, `Метаданные.<Kind>.<Name>` и т.п. найти соответствующий XML **по `src/` рекурсивно** и убедиться, что имя/значение существуют.
- **If not found**: поискать альтернативы (похожие имена в соседних XML и в `src/**/Configuration.xml`), показать пользователю доступные варианты, затем **STOP** (не писать код на непроверенных именах).
- **If multiple configurations/extensions**: явно уточнить, к какой относится проверка.
Скиллы openspec-apply-change и openspec-debug обязаны валидировать метаданные перед использованием в BSL.

---

**Last updated**: 2026-03-11 | **Version**: 1.6 | **Changes**: Merged 1c-metadata-validation.mdc into this file  
**Source**: AndreevED/1c-ai-feature-dev-workflow + улучшения + 1c-common-mistakes
