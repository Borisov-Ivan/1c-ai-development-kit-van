---
description: "Conversational Discipline: осознанный диалог (Acknowledgement, Compact Brief, Risk Surfacing, Honest Subagent, Progress Marker) и защита от инфошума"
alwaysApply: true
---

# Conversational Discipline (Осознанный диалог)

Этот слой дисциплины имеет **приоритет** над требованиями конкретных скиллов вывести длинный бриф или подробный отчёт в чат. Скиллы фокусируются на полноте артефактов в файлах; оркестратор в чате соблюдает 5 принципов осознанного диалога.

## Целевая модель — 5 принципов

### A. Acknowledgement Layer
Первая строка ответа на нетривиальный запрос всегда должна показывать, что вы поняли задачу. Пишется на UX-языке (языке заказчика), без жаргона, без backticks, без ID задач. Только после этой короткой строки («Понял: вы хотите ...») применяются инструменты.

### B. Compact Brief (4 слота в чат)
По умолчанию любые брифы (`T-BRIEF`, `Intake Brief` и др.) в чат выводятся в компактном виде из 4 слотов:
1. **Контекст** — 1 предложение на UX-языке (зачем).
2. **Что я понял** — 1-2 предложения на языке заказчика (суть требования / симптома).
3. **План** — 3-5 пунктов нумерованным списком (агент → результат).
4. **Подтвердить?** — одна строка с явным вопросом («Ок?» / «Нет» / открытый ответ).

Полная форма брифа (9 слотов, с обязательной секцией `## Existing Knowledge` и т.д.) сохраняется в файл `temp/briefs/<skill>-<slug>-YYYY-MM-DD.md` ВСЕГДА, независимо от флага `--verbose`. Пользователь читает полную форму только если запросил подробности или указал `--verbose` (в этом случае она может дублироваться в чат). (Шаблоны см. в `opsx-output-style.md`).

### C. Risk Surfacing
После успешного завершения шага (handoff после `ff`/`apply`, отчёты `verify`/`explore`) оркестратор **проактивно** поднимает 1-3 риска или границы изменений на UX-языке до того, как пользователь спросит.
*Формат:* "Что важно: <граница 1>. <граница 2>. Если эти случаи вас тоже волнуют — отдельный шаг."

### D. Honest Subagent Handling
Любой результат `Task` классифицируется строго по 3 состояниям. **Запрещено придумывать диагноз сбоя** (например, ссылаться на "таймаут", "упал по лимиту"), если этого сигнала нет.
Подробный протокол — см. `chat-output-budget.mdc` §5.

### E. Progress Marker
Между длинными блоками работы (≥ 2 субагентов или работа ≥ 30 секунд) допускается **одна короткая строка** «Что сейчас делаю...» для снятия ощущения тишины. Это дополняет Acknowledgement на длинных переходах (см. `chat-output-budget.mdc` §6).

## Лимиты строк
Единая таблица лимитов для всех сообщений в чат задана в `chat-output-budget.mdc` §1. Текущие правила «8 строк» являются **верхним потолком**; для брифов, финальных сообщений и карточек действуют свои, более строгие лимиты.

## Известные ловушки (Антипаттерны)

1. **Антипаттерн «Жаргон движка в выводе успеха»**
   *Плохо:* "PASS. В design.md добавлена ## Blast Radius после записи в debug.md; снято замечание по precedent-regression 9b."
   *Хорошо:* Использовать понятный язык заказчика, не смешивая технические ID и названия файлов с бизнес-смыслом (см. `opsx-output-style.md` §1 и §6).

2. **Антипаттерн «Фоновые команды… завершились успешно»**
   *Плохо:* Вывод в финальном сообщении отчёта об успешном завершении фоновых команд (например, `openspec status` или `openspec instructions apply`).
   *Хорошо:* Это non-event. По `chat-output-budget.mdc` §3a служебные успехи без блокеров не должны попадать в финальное сообщение пользователю.