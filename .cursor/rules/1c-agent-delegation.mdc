---
description: "HALT+DELEGATION: диспетчер 1С — проверять ДО любого действия, делегировать по триггерам"
alwaysApply: true
priority: 999
---

# 1C Code Gate — Dispatch & Delegation

## HALT CONDITIONS (проверять ДО любого действия)

Триггеры проверяются **сверху вниз**; более специфичный триггер (Light Mode, Mechanical Mode) имеет приоритет над общим («Правка .bsl файла»).

| Триггер | Действие | Детали |
|---------|----------|--------|
| Реализация ЗНИ без /opsx:apply (change существует в `openspec/changes/`, но сессия не в режиме apply) | **СТОП.** Правка .bsl, вызов writer/reviewer запрещены. **Исключение:** Light Mode и Mechanical Mode — работают без /opsx:apply (см. секции ниже). Предложить: «Для начала реализации: /opsx:apply <name>» | см. APPLY GATE ниже |
| Режим explore без активного change (сессия в `/opsx:explore`, в `openspec/changes/` нет каталога change кроме `_template` и `archive`) | **СТОП.** Реализация (writer, reviewer) запрещена. Предложить пользователю: «Для реализации создайте ЗНИ: `/opsx:new <name>` или `/opsx:ff <name>`» | `.cursor/skills/openspec-explore/SKILL.md` (Guardrails, Architect Gate) |
| Задача требует нового объекта метаданных (форма, модуль, справочник, регистр, роль, XML) | **СТОП. Никогда не создавать.** Выдать блокер с описанием. WAIT | `.cursor/rules/1c-no-metadata-creation.mdc` |
| Файл трассы (`.pff`, `*_TRACE_*.txt`) или стек ошибки 1С | СТОП. Подготовить бриф. Делегировать **onec-trace-analyst**. По результату — onec-code-explorer, onec-code-architect. **В explore:** бриф из пользовательского ввода (без чтения трасс/артефактов), вывести по шаблону openspec-explore/SKILL.md (шаг 2), END TURN; делегировать trace-analyst только после подтверждения пользователя | `.cursor/rules/1c-error-analysis.mdc` |
| Вызов `Task(subagent_type="explore"\|"generalPurpose")` для 1С-контента (BSL, XML из `src/**`, трассы `.pff`/TRACE, расширения `cfe/*`, отладка/ревью/архитектура) | **СТОП.** Повторить с корректным 1С-агентом: обследование 3+ модулей → **onec-code-explorer**; трасса / стек → **onec-trace-analyst**; архитектура → **onec-code-architect**. Перед повторным вызовом — пройти **Task Pre-call Checklist** (`tool-name-guard.mdc` → `## Task Pre-call Checklist`): `subagent_type` из списка 1С-агентов, `model` **не передавать** | `.cursor/rules/tool-name-guard.mdc` (Task Pre-call Checklist + NEGATIVE GUARD), `.cursor/rules/model-selection.mdc` |
| Точечная правка (Light Mode): 2–10 строк, 1 файл, без архитектуры | Делегировать **onec-code-writer** с контекстом, затем **onec-code-reviewer**. Без артефактов OpenSpec | см. LIGHT MODE ниже |
| Шаблонная правка (Mechanical Mode): find-replace, #Область, удаление комментариев | Прямая правка + Grep-верификация + **onec-code-reviewer** после. Без writer | см. MECHANICAL MODE ниже |
| Правка `.bsl` файла | СТОП. Загрузить скилл. — Light Mode (1 файл, ≤10 строк, без архитектуры) → **onec-code-writer** + **onec-code-reviewer**, без артефактов. — Иначе → проверить триггеры `architect-gate.mdc`; при срабатывании — **onec-code-architect** до реализации, затем **onec-code-writer** + **onec-code-reviewer**. Обязательно **onec-code-reviewer** после | см. ТАБЛИЦА ДЕЛЕГИРОВАНИЯ ниже |
| Исследование кода из 3+ модулей | Делегировать **onec-code-explorer** (не читать модули вручную) | см. DELEGATION GATE ниже |
| Исправление ошибки (bug fix). Маркеры: «исправь», «не работает», «падает», «ошибка», «баг», «fix», «crash», «добавь проверку», «Неопределено», «NPE», «null», «не заполнено», «пустое значение», «не находит», «вылетает», «сломалось», «регресс», «дефект», «поведение неверное» | СТОП. Проверить root cause + impact assessment по `verified-cause-gate.mdc` (HALT 1, 1b, 2, 3). При системном масштабе — **onec-code-architect**. При Light Mode bug fix — обязательный root cause блок в промпте writer (см. LIGHT MODE ниже) | `.cursor/rules/verified-cause-gate.mdc` |

## ПРИНЦИП

   Инварианты загружены соседними always-apply правилами:
- BSL write guard → `bsl-write-guard.mdc`
- Tool name (использовать **Task**) → `tool-name-guard.mdc`
- Project paths (cf/cfe) → `project-paths.mdc`

**Explore-mode guard:** В режиме `/opsx:explore` без активного change — onec-code-writer и onec-code-reviewer НЕ вызываются. Сначала ЗНИ (`/opsx:new` или `/opsx:ff`).

## APPLY GATE

Реализация (onec-code-writer, onec-code-reviewer, прямая правка .bsl) разрешена **только** в контексте активной команды `/opsx:apply`.

Создание и правка артефактов OpenSpec (proposal.md, design.md, tasks.md, specs/) — разрешено без /opsx:apply. Это планирование, не реализация.

Если change существует, но /opsx:apply не был вызван:
1. **СТОП.** Не вызывать writer/reviewer, не править .bsl.
2. Предложить: «Артефакты готовы. Для начала реализации: `/opsx:apply <name>`»

Исключения: Light Mode и Mechanical Mode — работают без артефактов OpenSpec (см. соответствующие секции). **Команда `/review`** — ревью кода + устранение замечаний по отчёту: reviewer разрешён без ограничений; writer — только после подтверждения пользователя и только для устранения замечаний из отчёта reviewer (см. `.cursor/skills/review/SKILL.md`).

## ПОЛНОМОЧИЯ АГЕНТОВ (BSL-ONLY)

Агенты-исполнители (writer): **только правка .bsl в существующих файлах**. Утилитарные агенты (simplifier и др.) — см. `1c-utility-agents.mdc`.
**ЗАПРЕЩЕНО** создавать, копировать или модифицировать XML-файлы метаданных, создавать директории метаданных, создавать новые .bsl в несуществующих директориях.
Нет нужного объекта → СТОП, блокер пользователю в формате из 1c-no-metadata-creation.mdc.
Architect может рекомендовать новые объекты в design.md.

## SKILLS-FIRST

Перед правкой BSL проверить `.cursor/skills/1c-*/SKILL.md` — covers task?

## ТАБЛИЦА ДЕЛЕГИРОВАНИЯ

| Тип задачи | Агент | Обязательно |
|------------|-------|-------------|
| Написание/правка BSL | **onec-code-writer** | Да (Требования к промпту writer — `.cursor/agents/onec-code-writer.md → INPUT CONTRACT`) |
| Ревью после правок | **onec-code-reviewer** | Да, задача НЕ завершена до ревью |
| Архитектура (не точечная правка) | **onec-code-architect** (primary) / **onec-code-architect-2nd** (fallback) | Да, до реализации. Триггеры — `architect-gate.mdc` |
| Исследование 3+ модулей | **onec-code-explorer** | Рекомендуется |
| Ошибка с трассой/стеком | **onec-trace-analyst** | Да, см. `1c-error-analysis.mdc` |

Шаблоны промптов, оценка сложности, интеграция со скиллами: `.cursor/skills/1c-agent-patterns/SKILL.md`.

## PROJECT PATHS

Перед вызовом 1С-агентов включать в их промпт блок путей из `openspec/project.md` (секция «Структура репозитория»). Протокол извлечения и формат блока — в `.cursor/rules/project-paths.mdc`.

## LIGHT MODE (точечная правка)

1 файл, 2–10 строк, без архитектуры → writer + reviewer. Без артефактов OpenSpec. За рамками → полный workflow (architect → writer → reviewer).

**Bug fix в Light Mode** (задача = исправление ошибки, а не новая функциональность):

1. Перед вызовом writer — проверить `verified-cause-gate.mdc` (HALT 1, 1b, 2).
2. Промпт writer обязан содержать **root cause блок** (см. шаблон «Writer — bug fix» в `1c-agent-patterns/SKILL.md`):
   - Симптом
   - Корневая причина (verified или hypothesis + follow-up)
   - Доказательство (ссылка на код/трассу/лог)
   - Подход к фиксу (почему устраняет причину, а не симптом)
3. Если root cause не установлен — **СТОП**, запросить у пользователя (описание ошибки, трассу, шаги воспроизведения).
4. Reviewer получает root cause контекст для валидации (см. шаблон «Reviewer — bug fix»).
5. После завершения (writer + reviewer): сохранить краткий **root cause brief** в `temp/reports/bugfix-lightmode-ГГГГ-ММ-ДД-<описание>.md`:
   - Симптом
   - Корневая причина
   - Что исправлено (файл, строки)
   - Verified / hypothesis
   Цель: root cause не теряется между сессиями. Без OpenSpec-артефактов это единственная фиксация контекста исправления.

## MECHANICAL MODE (шаблонная правка)

Критерии (все должны выполняться):
- Замена идентична во всех файлах (find-and-replace)
- Не меняется логика (только разметка, переименование, удаление комментариев)
- Паттерн проверяем через Grep до и после

Примеры: добавление `#Область`, переименование процедуры через replace_all, удаление маркеров чейнджлога в комментариях (не директив #Вставка/#Удаление), удаление закомментированного кода.

Workflow: прямая правка (StrReplace) + Grep-верификация + **onec-code-reviewer** после. Без writer.

## АВТО-ИСПРАВЛЕНИЕ РЕВЬЮ

Все кодовые замечания (critical/high/medium/low) → автоматически writer с замечаниями → повторный reviewer. Макс. 2 итерации — согласовано с Phase 7 агента writer.
Архитектурные замечания (новый объект, API, структура хранения, RLS) → СТОП, к пользователю.

**Без прямого StrReplace.** Все кодовые замечания — через writer. Оркестратор НЕ исправляет замечания ревью напрямую (StrReplace), даже если правка кажется «однострочной». Spot-check corrections после writer — тоже через writer.

Принцип: каждое замечание ревьювера = обязательное к исправлению. Если замечание не стоит исправления, ревьювер не должен его выдавать.

**После получения отчёта аналитического агента** (architect/explorer/trace-analyst) — сохранить полный отчёт: см. `preserve-subagent-reports.mdc`.

## DELEGATION GATE

**До вызова агента допустимо:** артефакты OpenSpec + до 3 точечных обращений к .bsl (для обогащения промпта агенту, НЕ для самостоятельного анализа).
**>3 обращений к .bsl** → СТОП, вызвать агента.
**Задача = обследование** (трассировка вызовов, поиск зависимостей, анализ сценариев, верификация гипотез по коду) → порог **0 обращений к .bsl**. Делегировать сразу, не начинать самостоятельно.
Чтение тел процедур, поиск вызовов/зависимостей, анализ модулей — **работа агента**, не твоя.

## ПРОМПТ WRITER: ГРАНИЦА ОТВЕТСТВЕННОСТИ

Оркестратор в промпте writer описывает:

```yaml
ДОПУСТИМО (ЧТО):
  - Описание задачи и критерии приёмки
  - Контракты: сигнатуры функций, типы параметров, ключи структур
  - Пути к файлам, указание на конкретные процедуры
  - Бизнес-логика: при каких условиях какое поведение
  - Ссылки на design.md, specs, архитектурный отчёт

ЗАПРЕЩЕНО (КАК):
  - Паттерны обработки ошибок ("оберни в Попытку", "сделай fallback")
  - Конкретные проверки ("добавь ТипЗнч()", "проверь Свойство()")
  - Структура кода ("сначала проверь X, потом вызови Y")
  - Обоснование Попытки ("обоснована внешним фактором ХР")

Если оркестратор хочет выразить ожидание поведения при ошибке:
  ПЛОХО: "в Попытку — вызов ядра, при исключении — fallback"
  ХОРОШО: "при недоступности ядра проверка должна выполниться
           стандартным путём (fallback), а не заблокироваться"
```

## LINT GATE

**После каждой правки .bsl** (writer, Mechanical Mode, прямая правка) — **до вызова reviewer**:

1. Вызвать `ReadLints` на изменённых `.bsl` файлах.
2. Если есть ошибки уровня error («Переменная не определена», «Метод не найден», несоответствие контекста) — **исправить до вызова reviewer** (writer повторно; в apply/review — только writer, без прямой правки).
3. Вывод линтера (или «линтер не выявил ошибок») передать в промпт reviewer как параметр «Диагностики линтера».
4. Если линтер недоступен или не возвращает диагностик для .bsl — пропустить шаг, не блокировать.

Поток: `writer → ReadLints → [fix if errors] → API EXISTENCE CHECK → [AskQuestion if WARNING] → reviewer (+ lint output + API check results)`

## WRITER PIPELINE (детали)

API EXISTENCE CHECK, EXTENSION GATE + VERIFICATION, CONTRACT RESOLUTION (формат Resolved Contracts и investigation loop) — в on-demand правиле `.cursor/rules/1c-writer-pipeline.mdc` (подгружается по globs `**/*.bsl`).

Сквозной поток пайплайна:

```
writer → ReadLints → [fix if errors] → API EXISTENCE CHECK →
  [AskQuestion if WARNING] → EXTENSION VERIFICATION (если &ИзменениеИКонтроль) →
  reviewer (+ lint output + API check results + base path + verification result)
```

Investigation loop (резолв контрактов) запускается **только** по запросу ревьювера (секция `## Investigation Request` в его отчёте) — см. детали в `1c-writer-pipeline.mdc` и `review/SKILL.md` шаг 3.5.

## ИСКЛЮЧЕНИЯ

- Однострочные правки (опечатка, переименование)
- Только комментарии
- Пользователь явно: «без агентов» / «без ревью»
- Mechanical Mode (шаблонная правка) — прямая правка StrReplace разрешена; см. секцию MECHANICAL MODE выше

**Ограничение:** Исключения (однострочная правка, только комментарии) не действуют в контексте командных сессий /opsx:apply и /review. Mechanical Mode и «без агентов» в этих сессиях по-прежнему допускают прямую правку + reviewer.

---

**Last updated**: 2026-04-20 | **Version**: 3.11 | **Changes**: HALT-строка запрета `explore`/`generalPurpose` для 1С переформулирована с явной отсылкой к **Task Pre-call Checklist** (`tool-name-guard.mdc`) — единая точка истины для предвызывной проверки параметров Task. Добавлена сноска на INPUT CONTRACT и согласован iteration limit.
