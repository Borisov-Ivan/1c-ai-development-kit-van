---
description: "GUARD: для вызова субагентов использовать инструмент Task"
alwaysApply: true
---

# Tool Name Guard

## Как вызывать субагентов

**Используй инструмент `Task`** — это единственный способ запуска субагентов в этом проекте.

- Вызов: `Task(subagent_type="...", description="...", ...)`.
- Перед вызовом: убедиться, что в коде/промпте указано именно **Task**.

## При ошибке `Invalid enum value`

1. Проверить имя инструмента — должен быть **Task** (в других средах или в памяти иногда фигурируют иные имена — здесь корректно только Task).
2. Проверить `subagent_type` — допустимые: onec-code-writer, onec-code-reviewer, onec-code-architect, onec-code-architect-2nd, onec-code-explorer, onec-trace-analyst, onec-code-simplifier, openspec-quality-controller, openspec-doc-writer, generalPurpose, explore, shell, mcp-deploy, onec-admin и др. (см. описание инструмента).
3. Не переключаться на generalPurpose — это маскирует причину.
4. **Fallback-перехват:** Если вызов `onec-code-architect` падает с ошибкой недоступности модели или таймаутом, не останавливаться, а выполнить повторный вызов с `subagent_type="onec-code-architect-2nd"`.
5. Если retry не помог — СТОП, сообщить пользователю.

---

**Last updated**: 2026-03-14 | **Version**: 1.2 | **Changes**: позитивная формулировка — акцент на «используй Task», убрано частое перечисление несуществующих имён
