---
description: "Architect Gate + Design Review — триггеры архитектурного ревью и ревью постановки, проверять в explore/verify/apply"
alwaysApply: true
priority: 200
---

# Architect Gate — единые триггеры

Единый источник истины для определения необходимости архитектурного ревью (onec-code-architect). Ссылаются: explore, verify, apply, 1c-agent-delegation, verified-cause-gate.

## ТРИГГЕРЫ

Любой сработавший триггер из любой группы → Gate срабатывает.

### Объективные маркеры (проверяются механически)

Не зависят от оценки агента. Проверяются через Glob/Grep:

| Маркер | Как проверить |
|--------|---------------|
| В сессии вызывался trace-analyst | Glob `reports/trace-analysis-*.md` в change dir или `temp/reports/` |
| В сессии вызывался explorer | Glob `reports/exploration-*.md` в change dir или `temp/reports/` |
| Bug fix | см. маркеры bug fix в `1c-agent-delegation.mdc` |
| Перехват/компенсация базовой платформенной процедуры | Grep design.md: «базовая процедура», «платформа», «повторная запись», «перехват», «после вызова базы», «компенсация» |
| Новый объект метаданных в design | Grep design.md на паттерны: «новый регистр», «создать регистр», «новый документ», «создать документ», «новый справочник», «создать справочник», «новый БП», «создать БП» |

### Семантические триггеры

| Триггер | Пояснение |
|---------|-----------|
| Подмена типового метода | &Вместо, &После, &Перед — аннотации расширения к базовому коду |
| Несколько возможных точек реализации | В обсуждении/design упоминаются альтернативы, выбор неочевиден |
| Новые связи между модулями расширения | Добавляются вызовы между модулями, которые ранее не взаимодействовали |
| Экспортная функция с 3+ вызывающими | Изменяемая функция имеет 3+ вызовов в других модулях |
| Отклонение от паттерна проекта | Предлагаемый подход не встречается в существующем коде |
| Design без секции Existing Mechanisms при интеграции | **Обследование:** design описывает интеграцию с базовой конфигурацией, но не фиксирует какие штатные механизмы были рассмотрены и почему не подошли. См. `existing-mechanism-priority.mdc` |
| Design Rationale отсутствует при интеграции | **Обоснование выбора:** design описывает интеграцию с существующим кодом, но нет секции Design Rationale (почему выбрана эта точка реализации, контракты, паттерны) |
| Design Rationale без верификации | Rationale написан агентом без ссылки на architecture-*.md или подтверждение пользователя |
| Фикс меняет пользовательский сценарий | Что видит/делает пользователь: открытие/закрытие формы, показ/скрытие диалога, порядок шагов UX. Даже при изменении 1 процедуры — не «точечный» |

### Структурные триггеры

| Триггер |
|---------|
| >1 файла затрагивается |
| >10 строк изменений |
| Изменяется контракт / API / экспортная сигнатура |

## ADR DISCOVERY (при срабатывании Gate)

Перед вызовом архитектора — проверить существующие ADR:

1. Glob `openspec/adrs/ADR-*.md`
2. Grep по области задачи (домен, подсистема, ключевые модули)
3. Если релевантные ADR найдены — включить в промпт архитектора как контекст:
   `Существующие ADR по области: ADR-NNNN (заголовок) — [краткое резюме решения]`
4. Если архитектор рекомендует подход, **противоречащий** существующему ADR — пометить как Supersedes и предложить обновить статус ADR

Формат ADR: `.cursor/rules/adr-format.mdc`.

## ДЕЙСТВИЕ ПРИ СРАБАТЫВАНИИ

Зависит от контекста вызова (определяется в каждом скилле):

| Контекст | Действие |
|----------|----------|
| **explore** (Structured Investigation, шаг Decide) | Архитектор **обязателен**. AskQuestion пользователю: «Сработали триггеры [...]. Запустить архитектора? [Рекомендуется / Пропустить]». При пропуске — задокументировать в Explore Summary |
| **verify** (pre-apply check, шаг 9) | CRITICAL в отчёте verify. Предложение запустить архитектора через авто-устранение (шаг 17). Основной рубеж проверки |
| **apply** (pre-flight) | Soft redirect на `/opsx:verify`. Если `reports/verification-pre-*.md` не найден — предупреждение. Не дублирует проверку триггеров |
| **1c-agent-delegation** (правка .bsl вне OpenSpec) | При срабатывании → onec-code-architect до реализации |

## ИСКЛЮЧЕНИЯ (Gate НЕ срабатывает)

- Light Mode (см. 1c-agent-delegation.mdc) без семантических триггеров и без изменения UX-сценария
- Mechanical Mode (find-replace, #Область, удаление комментариев)
- Пользователь явно отклонил архитектора — задокументировать решение
- Архитектор уже вызывался по этой теме (файл `architecture-*.md` существует в reports/)
- Рефакторинг без изменения поведения
- Опечатки, переименования, комментарии

## ЗАКРЫТИЕ GATE

Gate считается пройденным, если выполняется **любое** из:
1. Файл `architecture-*.md` существует в `reports/` change или `temp/reports/`
2. Пользователь явно отклонил архитектора (задокументировано в Explore Summary или design.md)
3. Триггеры не сработали (ни один из трёх групп)

**Design Rationale ≠ закрытие Gate.** Самописный Rationale агента без внешней валидации не закрывает Gate. Закрывает только файл architecture-*.md или осознанное решение пользователя.

---

## DESIGN REVIEW ТРИГГЕРЫ

Отдельный набор триггеров для оценки **качества постановки** (артефактов), а не архитектурного подхода. Основной потребитель — **verify** (pre-apply, шаг 10). Apply проверяет наличие отчёта verify, не дублирует триггеры.

### Триггеры (ANY → рекомендация)

| Триггер | Как проверить |
|---------|---------------|
| Bug fix | Маркеры из `1c-agent-delegation.mdc` в proposal.md / design.md |
| 2+ аналитических отчёта | Glob `reports/trace-analysis-*.md` + `reports/exploration-*.md` — суммарно ≥ 2 файла |
| &ИзменениеИКонтроль | Grep design.md или proposal.md на `&ИзменениеИКонтроль` |
| Условные задачи | Grep tasks.md на паттерны ветвления: `При отрицательн`, `Если в п.`, `Альтернатив`, `workaround`, `Иначе →`, `Иначе —` |
| Hypothesis без маркера | Grep design.md на `вероятно`, `возможно`, `скорее всего`, `гипотеза` — и при этом отсутствует секция `## Hypotheses` |
| Design Completeness (ручная конфигурация) | Grep tasks.md на `создать в расширении`, `добавить форму`, `создать обработку`, `создать регистр`, `добавить реквизит`, `создать справочник` + проверка design.md на наличие исчерпывающих инструкций (имена, типы, элементы формы). Если tasks содержат ручную конфигурацию, а design не содержит полного описания → WARNING |

### Действие

**Мягкая рекомендация** (AskQuestion), не блокирующий gate. Пользователь решает.

| Контекст | Действие |
|----------|----------|
| **verify** (pre-apply, шаг 10) | WARNING в отчёте verify. Предложение запустить ревью постановки через авто-устранение (шаг 17). Основной рубеж |
| **apply** (pre-flight) | Soft redirect на `/opsx:verify`. Не дублирует проверку триггеров |

При запуске → onec-code-architect с фокусом на качество артефактов:
- Verified vs hypothesis — все ли решения обоснованы?
- Полнота критериев приёмки в tasks
- Покрытие сценариев в spec
- Корректность fallback / conditional paths
- Пропущенные риски

### Закрытие

Design Review считается пройденным, если выполняется **любое** из:
1. Файл `reports/design-review-*.md` существует в change dir
2. Пользователь явно отклонил ревью
3. Триггеры не сработали

### Исключения

- Light Mode, Mechanical Mode
- Change без аналитических отчётов И без bug fix маркеров И без conditional tasks
- Design Review уже проводилось (`reports/design-review-*.md` существует)

---

**Last updated**: 2026-03-12 | **Version**: 1.5 | **Changes**: Design Completeness trigger — tasks с ручной конфигурацией без исчерпывающих инструкций в design
