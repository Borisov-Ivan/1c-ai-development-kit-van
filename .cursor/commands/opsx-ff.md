---
name: /opsx-ff
id: opsx-ff
category: Workflow
description: Create a change and generate all artifacts needed for implementation in one go
---

Fast-forward through artifact creation - generate everything needed to start implementation.

**FIRST AND ONLY action**: Read `.cursor/skills/openspec-ff-change/SKILL.md`.
Do NOT read any other files, traces, or modules in the same tool call.
After reading the skill, follow its instructions step by step before taking any other action.

Input: argument after the command is the change name (kebab-case) or a description of what the user wants to build.
