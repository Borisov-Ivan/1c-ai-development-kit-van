---
name: /opsx:sync
id: opsx-sync
category: Workflow
description: Sync delta specs from a change to main specs
---

**FIRST AND ONLY action**: Read `.cursor/skills/openspec-sync-specs/SKILL.md`.
Do NOT read any other files in the same tool call.
After reading the skill, follow its instructions step by step before taking any other action.

**Input**:
- `/opsx:sync` — без аргументов: синхронизирует спеки активного (единственного) ЗНИ.
- `/opsx:sync <name>` — синхронизирует спеки указанного ЗНИ.

Input: $input
