---
name: ""
overview: ""
todos: []
isProject: false
---

# План: AP-031 мета-имена — полные цитаты правок

Цель: агент-исполнитель вносит изменения **строго по блокам ниже**, без упрощений.

---

## 1. `.cursor/rules/bsl-antipatterns.mdc`

### 1.1 Вставить новую строку таблицы **после** строки AP-030 (перед пустой строкой `---` перед футером)

**Текущий хвост (контекст):**

```markdown
| AP-030 | Скрытый частичный результат (Попытка + Продолжить/Возврат без обратной связи) | exception/ux | HIGH | Попытка с Продолжить/тихим fallback; в блоке Исключение нет ОбщегоНазначения.СообщитьПользователю / ВызватьИсключение; пользователь не информирован о сбое. При root cause = contract-uncertainty (Phase 2.5) ремедиация — Вариант 3 (проверка контракта), не Вариант 2 (добавить сообщение) |

---

**Last updated**: 2026-03-15 | **Version**: 2.8
```

**Вставить между AP-030 и `---`:**

```markdown
| AP-031 | Мета-имя из постановки/оркестрации | naming | MEDIUM | Первичный тест: коллега, знающая предметную область без ЗНИ, не может однозначно объяснить назначение идентификатора. Вспомогательные маркеры: термины постановки (Гейт, Механика, Задача, ДействиеДля…), RU/EN аналоги (Gate, Phase, Workflow без домена), пустышки (Результат, Данные, Состояние, Контекст без уточнения), нумерация этапов (Шаг1, Этап2). Исключение: доменные термины с квалификатором (ФазаПроизводства, РежимНалогообложения). Экспортные процедуры/функции — severity HIGH |
```

### 1.2 Обновить футер

**Было:**

```markdown
**Last updated**: 2026-03-15 | **Version**: 2.8
```

**Стало:**

```markdown
**Last updated**: 2026-03-20 | **Version**: 2.9
```

---

## 2. `.cursor/docs/antipatterns/bsl-antipatterns.md`

### 2.1 Вставить полную карточку **в конец файла** (после последней строки файла; сейчас файл заканчивается блоком про AP-030 и ссылками)

**Добавить целиком:**

```markdown

---

## AP-031: Мета-имя из постановки/оркестрации

**Категория:** naming  
**Severity:** MEDIUM (экспортные процедуры и функции — HIGH)  
**Дата добавления:** 2026-03-20  
**Источник:** ревью продуктивного кода; имена из языка ЗНИ/агента вместо домена

### Правило

Идентификаторы в продуктивном коде (переменные, параметры, процедуры, функции) должны отражать сущности и процессы **предметной области**, а не терминологию постановки задачи, оркестрации разработки или внутренних артефактов (tasks, phase gates, «механика» задачи).

**Первичный критерий (обязателен к проверке):** если разработчик, знакомый с предметной областью конфигурации, но **не** читавший ЗНИ/TЗ, не может одной фразой объяснить, что обозначает имя, — имя считается мета-именем или непрозрачным. Список маркеров ниже — **вспомогательный**, не исчерпывающий.

### Симптом

Код читается как перенос чеклиста или псевдокода постановки; смысл сущности в терминах конфигурации не угадывается из имени.

### Детектирование

1. Применить **доменный тест** (см. Правило) к каждому сомнительному идентификатору в изменённом/ревьюируемом scope.
2. **Вспомогательные маркеры** (подозрение, не автоматический запрет):
   - Термины оркестрации/ЗНИ: **Гейт**, **Gate**, **Механика**, **Mechanics**, **ДействиеДля…**, **Задача** как абстракция без домена.
   - **Фаза**, **Шаг**, **Этап**, **Phase**, **Step** — если не являются устоявшимся доменным термином (сравни: `ФазаПроизводства` — ок; `Фаза2` — подозрительно).
   - **Режим**, **РежимЗадачи** без уточнения предметного режима.
   - **Флаг** как часть имени, когда тип уже булев (`ФлагОбработки` вместо `ОбработкаВыполнена`).
   - **Пустышки:** `Результат`, `Данные`, `Состояние`, `Контекст`, `Параметры` без уточняющего существительного домена.
   - **Нумерация без домена:** `Шаг1`, `Этап2`, `Вариант3`.
   - Англоязычные аналоги: **Workflow**, **Pipeline**, **Orchestration** в именах, если не термин предметки.
3. **Не считать нарушением:** доменные термины с квалификатором (`РежимНалогообложения`, `ШагМаршрута`, `ФазаСогласования`), имена, совпадающие с реквизитами/перечислениями метаданных.

### Пример BAD

```bsl
МеханикаВключена = ПолучитьНастройку();
ДействиеДляГейта = ОпределитьДействие();
Результат = Новый Структура;
Шаг1 = ВыполнитьПервыйШаг();
```

### Пример GOOD

```bsl
ИсключениеУчастниковВключено = ПолучитьНастройку();
РеакцияНаНарушениеУсловия = ОпределитьРеакцию();
ПараметрыОтправки = Новый Структура;
ДанныеПервичнойПроверки = ВыполнитьПервичнуюПроверку();
```

### Remediation

В finding указать **конкретное** доменное имя-замену (не только «переименовать»). При сомнении — сверка с `design.md`/спецификацией изменения (терминология ЗНИ для сущностей).

### Ссылки

- `1c-coding-standards.mdc`, секция ИМЕНОВАНИЕ — «Доменная релевантность»
- `onec-code-reviewer.md`, Phase 0: CLARITY_DEFICIT (пересечение — мета-имя часто делает намерение блока непрозрачным); Supporting: AP-031

```

---

## 3. `.cursor/agents/onec-code-reviewer.md`

### 3.1 Evaluation Checklist — заменить таблицу вопросов 1–5 и completeness gate

**Было:**

```markdown
| # | Вопрос | Ответ | Источник/обоснование | Finding (если yes) |
|---|--------|-------|----------------------|--------------------|
| 1 | Proportionality: actual > 3x expected по строкам или > 2x по вложенности? | yes/no | | DISPROPORTIONATE_COMPLEXITY |
| 2 | Consistency: один источник — часть полей DIRECT, часть DEFENSIVE/EXPLORATORY? | yes/no | | CONTRACT_INCONSISTENCY |
| 3 | Knowledge: источники с verdict PARTIAL или ABSENT? | yes/no | | KNOWLEDGE_DEFICIT |
| 4 | Exploratory: поля с access=EXPLORATORY? | yes/no | | CONTRACT_INFERENCE |
| 5 | Попытка as contract compensation: источник PARTIAL/ABSENT + Попытка на его полях? | yes/no | | KNOWLEDGE_DEFICIT + contract-compensating-try |

Completeness gate: 5 строк в таблице. Меньше — Phase 0 не завершена.
```

**Стало:**

```markdown
| # | Вопрос | Ответ | Источник/обоснование | Finding (если yes) |
|---|--------|-------|----------------------|--------------------|
| 1 | Proportionality: actual > 3x expected по строкам или > 2x по вложенности? | yes/no | | DISPROPORTIONATE_COMPLEXITY |
| 2 | Consistency: один источник — часть полей DIRECT, часть DEFENSIVE/EXPLORATORY? | yes/no | | CONTRACT_INCONSISTENCY |
| 3 | Knowledge: источники с verdict PARTIAL или ABSENT? | yes/no | | KNOWLEDGE_DEFICIT |
| 4 | Exploratory: поля с access=EXPLORATORY? | yes/no | | CONTRACT_INFERENCE |
| 5 | Попытка as contract compensation: источник PARTIAL/ABSENT + Попытка на его полях? | yes/no | | KNOWLEDGE_DEFICIT + contract-compensating-try |
| 6 | Naming: есть идентификаторы (переменные, параметры, процедуры, функции), чьё имя отражает постановку/оркестрацию, а не предметную область, или не проходит доменный тест (см. AP-031)? | yes/no | | CLARITY_DEFICIT + Supporting AP-031 (или отдельное finding AP-031 без дублирования с CLARITY в том же месте) |

Completeness gate: 6 строк в таблице. Меньше — Phase 0 не завершена.
```

### 3.2 Medium — вставить после строки про CLARITY_DEFICIT

**Было (фрагмент):**

```yaml
- Phase 0: CLARITY_DEFICIT (намерение блока неочевидно из кода)
- Naming convention violations
```

**Стало:**

```yaml
- Phase 0: CLARITY_DEFICIT (намерение блока неочевидно из кода)
- AP-031: мета-имена из постановки/оркестрации (доменный тест + маркеры в карточке AP-031); экспортные процедуры/функции — HIGH; в finding обязательно предложить доменный синоним
- Naming convention violations
```

### 3.3 Phase 2 — блок «9. Code cleanliness» — добавить буллеты

**Было (фрагмент):**

```yaml
9. Code cleanliness:
   - Detect changelog markers (// +++ Author, // ---, date-author comments)
   - Detect design/process artifact references in comments: short-form (D11, F5, Design §3),
     natural-language (По design Decision N, fix-signing-result), process terms, task numbers
   - Dead code — see category 15 (Obsolete and Unused Code)
```

**Стало:**

```yaml
9. Code cleanliness:
   - Detect changelog markers (// +++ Author, // ---, date-author comments)
   - Detect design/process artifact references in comments: short-form (D11, F5, Design §3),
     natural-language (По design Decision N, fix-signing-result), process terms, task numbers
   - Meta-naming (AP-031): identifiers reflecting task/orchestration language or failing domain-clarity test; see full card in anti-pattern registry; export = HIGH, local/internal = MEDIUM; Remediation: propose domain synonym in finding
   - Dead code — see category 15 (Obsolete and Unused Code)
```

### 3.4 Phase 2 — пункт 10 «Specific 1C patterns» — расширить диапазон AP

**Было:**

```yaml
10. Specific 1C patterns: → see AP-001..AP-028 in anti-pattern registry (category 16)
```

**Стало:**

```yaml
10. Specific 1C patterns: → see AP-001..AP-031 in anti-pattern registry (category 16)
```

---

## 4. `.cursor/rules/1c-coding-standards.mdc`

### 4.1 Вставить подсекцию **после** блока «### Процедуры и функции»

**Где резать:** в `1c-coding-standards.mdc` сразу после закрывающей строки ` 

``` `(которая завершает yaml-блок с`СформироватьОтчет()`), **до** горизонтальной линии` ---`перед заголовком`## ЗАПРОСЫ`(сейчас это примерно строки 178–204 в файле — ориентироваться по якорю`СформироватьОтчет()`).

**Текст для вставки (вставить целиком между ` 

``` `после процедур/функций и существующим`---`):**

```
### Доменная релевантность
```

```yaml
    Правило: Имя идентификатора должно быть понятно в терминах предметной области без чтения ЗНИ
    
    Первичный тест: коллега, знающая предметную область, но не читавшая постановку, может одной фразой сказать, что обозначает имя. Если нет — имя слишком абстрактное, «мета» (из языка задачи) или пустышка.
    
    Плохо (мета-имена, пустышки, постановка):
      МеханикаВключена
      ДействиеДляГейта
      GateAction
      Результат
      Данные
      Шаг1
      РежимЗадачи
    
    Хорошо (доменные, уточнённые):
      ИсключениеУчастниковВключено
      РеакцияНаНарушениеУсловия
      ПараметрыОтправки
      ДанныеПервичнойПроверки
      РежимОтправкиДокумента
    
    Исключение: термин является устоявшимся в предметке
      ФазаПроизводства
      ШагМаршрута
      РежимНалогообложения
    
    См. также: AP-031 в .cursor/rules/bsl-antipatterns.mdc (reviewer-only)
    

```

(В реальном файле отступы у заголовка `###` — **без** лишних пробелов в начале строки; в плане выше блок с отступом только чтобы не ломать разметку MD плана.)

**Проверка:** порядок: `### Процедуры и функции` → yaml → `### Доменная релевантность` → yaml → **существующий** `---` → `## ЗАПРОСЫ`.

---

## 5. `.cursor/agents/CHANGELOG.md`

### 5.1 Добавить запись в начало (после заголовка файла), новая версия секция

**Вставить:**

```markdown
## [3.1] - 2026-03-20

### Changed (onec-code-reviewer, bsl-antipatterns)
- AP-031: мета-имена из постановки/оркестрации — доменный тест + эвристики; интеграция в Phase 0 Evaluation Checklist (вопрос 6), Medium severity, Phase 2 code cleanliness; диапазон ссылок на реестр AP-001..AP-031
- 1c-coding-standards.mdc: подсекция ИМЕНОВАНИЕ — «Доменная релевантность»
- bsl-antipatterns.mdc / bsl-antipatterns.md: полная карточка AP-031

```

---

## 6. `AGENTS.md` (опционально, рекомендуется)

### 6.1 Параграф «Code reviewer» — одно предложение про AP-031

**Было (фрагмент одной длинной строки):**

Найти подстроку: `Каталог антипаттернов (AP-NNN) — вспомогательный шаг.`

**Добавить сразу после неё (в той же строке или следующей, по стилю файла):**

```markdown
 Мета-имена (постановка вместо домена): AP-031.
```

Итоговая вставка читается как: «... вспомогательный шаг. Мета-имена (постановка вместо домена): AP-031. Отчёт: ...»

---

## Контрольный чеклист исполнителя

- `bsl-antipatterns.mdc`: строка AP-031 + Version 2.9 + Last updated 2026-03-20
- `bsl-antipatterns.md`: карточка AP-031 в конце файла
- `onec-code-reviewer.md`: вопрос 6, gate 6 строк, Medium AP-031, пункт 9 code cleanliness, `AP-001..AP-031`
- `1c-coding-standards.mdc`: «Доменная релевантность» между процедурами и ЗАПРОСЫ
- `CHANGELOG.md`: [3.1] 2026-03-20
- `AGENTS.md`: упоминание AP-031 (опционально)

